---
phase: 5
slug: web-dashboard
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-23
revised: 2026-04-23
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for the VFX Familiar web dashboard. Generated by gsd-ui-researcher, verified by gsd-ui-checker. Consumed by gsd-planner and gsd-executor as the visual source of truth.

**Scope:** Pre-built Preact SPA served as a static bundle from `packages/dashboard/dist/` by the existing Hono server on `/`. Reads via `/api/*` REST routes and live updates via a single SSE stream at `/api/events`. Mostly-read surface with two write affordances: `diff` and `reproduce`. 127.0.0.1 bind only. Aesthetic direction: **ComfyUI-native** — palette, typography, and iconography lifted from comfy.org + ComfyUI open-source web UI (D-WEBUI-15), own layouts (tree sidebar + detail pane), not a ComfyUI-app clone.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Preact + Tailwind v4, hand-rolled components) |
| Preset | not applicable (shadcn targets React/Next.js/Vite-React, not Preact) |
| Component library | none — hand-rolled primitives in `packages/dashboard/src/components/` per D-WEBUI-11 |
| Icon library | `lucide-preact` (^0.555.0) — official Preact port, tree-shakable, Preact-native, no wrapper |
| Font | Inter (UI) + Inter Tight (headings) via `@fontsource/inter` + `@fontsource/inter-tight` (offline-capable, no CDN) per D-WEBUI-20 |
| CSS engine | Tailwind CSS v4 via `@tailwindcss/vite` with CSS-native `@theme` config in `packages/dashboard/src/styles/theme.css` |
| Theme mode | Dark by default, light toggle via `[data-theme="dark\|light"]` attribute on `<html>`; persists to `localStorage["vfx-familiar:theme"]` (D-WEBUI-16) |

**Source pre-population:** Icon library and component approach chosen by researcher under CONTEXT.md "Claude's Discretion". All other system properties locked by CONTEXT.md D-WEBUI-10, D-WEBUI-11, D-WEBUI-16, D-WEBUI-20.

---

## Spacing Scale

Declared values (multiples of 4, 8-point grid):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gap, chip internal padding, dense table row y-padding |
| sm | 8px | Compact element spacing, version-card inner gaps, chip outer margin |
| md | 16px | Default element spacing, card inner padding, form-field gaps |
| lg | 24px | Section padding within detail panes, drawer tab content padding |
| xl | 32px | Layout gaps between major panes (sidebar ↔ detail pane separator) |
| 2xl | 48px | Major section breaks, home-view section spacing |
| 3xl | 64px | Page-level spacing only (home hero empty-state top margin) |

Tailwind v4 `@theme` token mapping:
```css
--spacing-xs: 0.25rem;  /* 4px  */
--spacing-sm: 0.5rem;   /* 8px  */
--spacing-md: 1rem;     /* 16px */
--spacing-lg: 1.5rem;   /* 24px */
--spacing-xl: 2rem;     /* 32px */
--spacing-2xl: 3rem;    /* 48px */
--spacing-3xl: 4rem;    /* 64px */
```

Exceptions:
- **Sidebar fixed width:** 280px (D-WEBUI-17).
- **Version detail drawer:** 560px (D-WEBUI-24, locked specific value).
- **Diff drawer:** 720px (D-WEBUI-25, wider for side-by-side diff layout).
- **Responsive breakpoints:** 1024px (sidebar collapses to drawer), 1440px (optimal desktop target). Below 1024px: overlay message "Dashboard requires a desktop browser" per D-WEBUI-17.

---

## Typography

Exactly 4 role-sizes and 2 weights (prescriptive ceiling — no exceptions):

| Role | Size | Weight | Line Height | Family | Treatment |
|------|------|--------|-------------|--------|-----------|
| Body | 14px | 400 (regular) | 1.5 | Inter | default |
| Label | 12px | 400 (regular) | 1.4 | Inter | `text-transform: uppercase; letter-spacing: 0.05em` for section labels — weight NOT used for role differentiation |
| Heading | 18px | 600 (semibold) | 1.25 | Inter Tight | default |
| Display | 24px | 600 (semibold) | 1.2 | Inter Tight | default |

**Numeric formatting (mandatory on every numeric surface per D-WEBUI-20):**
```css
.num, td.num, .version-label, .timestamp, .elapsed, .count-badge {
  font-variant-numeric: tabular-nums;
  font-feature-settings: "tnum";
}
```

Applied to: `v###` version labels, timestamps (relative + absolute), elapsed-time counters, metadata/tag count badges, `Active generations (N)` header, `total_count` in paginated queries.

**Label role CSS contract:**
```css
.label-uppercase {
  font-size: 12px;
  font-weight: 400;
  line-height: 1.4;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-fg-muted);
}
```

The Label role is differentiated from Body by **uppercase + letter-spacing + muted color**, NOT by font-weight. This keeps the contract at exactly 2 weights (400 regular + 600 semibold).

**Weights rule:** Exactly 2 weights — 400 regular (for Body and Label) and 600 semibold (for Heading and Display). No other weights permitted anywhere in the dashboard — no 300, no 500, no 700. If a component ever appears to need additional weight differentiation, use size, color, uppercase, or letter-spacing instead.

**Fallback stack:**
```css
--font-sans: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
--font-display: "Inter Tight", "Inter", system-ui, sans-serif;
--font-mono: ui-monospace, "SF Mono", Menlo, Consolas, monospace;  /* JSON blocks */
```

Fonts load via CSS `@import` in `theme.css` from `@fontsource/inter` + `@fontsource/inter-tight` packages inside `packages/dashboard/` (NOT CDN — offline-capable demo, predictable load). `@fontsource` packages must be imported with the `400` and `600` weight files only — do not import `500` weight assets (they would be dead weight in the bundle).

---

## Color

ComfyUI-native palette extracted from `docs.comfy.org/interface/appearance` (ComfyUI canonical dark theme reference). 60/30/10 split applied as:

### Dark theme (default — `[data-theme="dark"]`)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#202020` | Main canvas background (body, detail-pane surface) — `comfy bg-color` canonical |
| Secondary (30%) | `#353535` | Sidebar, cards, version-card background, drawer surfaces — `comfy-menu-bg` canonical |
| Accent (10%) | `#B39DDB` | ComfyUI MODEL-slot purple — signature "made by Comfy team" color |
| Destructive | `#FF4444` | ComfyUI `error-text` canonical — status pill "failed" + error toasts |
| Accent secondary | `#FF9CF9` | ComfyUI LATENT-slot magenta — reserved for lineage badges only (reproduce/iterate indicators) |

### Supporting neutrals (dark)

| Role | Value | Usage |
|------|-------|-------|
| Input background | `#222` | JSON block background, form-field background, code backgrounds |
| Border | `#4e4e4e` | Card borders, drawer dividers, table row separators |
| Border subtle | `#303030` | Sidebar tree item dividers, secondary separators |
| Foreground primary | `#FFF` | Body text, headings, high-contrast labels |
| Foreground muted | `#999` | Timestamps, descriptive text, label hints — `comfy descrip-text` canonical |
| Foreground dim | `#666` | Disabled text, placeholder text — `comfy disabled-text` canonical |

### Status pill colors (dark)

| Status | Color | Background | Usage |
|--------|-------|------------|-------|
| submitted | `#64B5F6` | `rgba(100,181,246,0.12)` | Status pill + submitted-state thumbnail card background |
| running | `#FFA931` | `rgba(255,169,49,0.12)` | Status pill + running-state thumbnail card background + subtle pulse |
| completed | `#6EE7B7` | `rgba(110,231,183,0.12)` | Status pill (no card override — image replaces placeholder) |
| failed | `#FF4444` | `rgba(255,68,68,0.12)` | Status pill + failed-state thumbnail card background |

(These map 1:1 to ComfyUI slot colors: IMAGE `#64B5F6`, CONDITIONING `#FFA931`, CONTROL_NET `#6EE7B7`, plus `error-text` for failed.)

### Light theme (`[data-theme="light"]`)

Light theme inverts the dominant/secondary relationship and adjusts accents for contrast:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FAFAFA` | Main canvas background |
| Secondary (30%) | `#FFFFFF` | Cards, sidebar, drawer surfaces |
| Accent (10%) | `#7B61C9` | Darkened MODEL-purple for WCAG AA contrast on white |
| Destructive | `#D73535` | Darkened error for contrast |
| Border | `#D4D4D4` | |
| Foreground primary | `#1A1A1A` | |
| Foreground muted | `#6B6B6B` | |

### Accent reserved for (explicit list — NEVER "all interactive elements")

The `#B39DDB` ComfyUI-purple accent is RESERVED for exactly these elements:

1. **Primary action buttons** — `Reproduce Version` button in version drawer header, `Compare Version` diff button
2. **Active sidebar selection** — left-border stripe + text color on currently-selected tree node
3. **Active drawer tab** — underline + text color on currently-active tab (Summary/Workflow/Prompt/Models/Raw)
4. **Links** — `parent_version_id` links in lineage badges, breadcrumb segment hover state
5. **Focus ring** — 2px outline on keyboard-focused interactive elements
6. **Brand mark** — the VFX Familiar logo mark in the top-left of the sidebar header

The `#FF9CF9` LATENT-magenta SECONDARY accent is RESERVED for exactly:

1. **Lineage badges** — the small icon on version cards that have `lineage_type in ('reproduce', 'iterate')`
2. **Parent version links** — the "↑ v002" backlink in the provenance drawer Summary tab

Accent is NEVER used for: hover states on non-primary elements, general text emphasis, borders, success indicators, status pills (those have dedicated status colors above), or decorative gradients.

### Tailwind v4 `@theme` token mapping

```css
@theme {
  --color-bg: #202020;
  --color-surface: #353535;
  --color-surface-alt: #222;
  --color-border: #4e4e4e;
  --color-border-subtle: #303030;
  --color-fg: #ffffff;
  --color-fg-muted: #999999;
  --color-fg-dim: #666666;
  --color-accent: #B39DDB;
  --color-accent-secondary: #FF9CF9;
  --color-destructive: #FF4444;
  --color-status-submitted: #64B5F6;
  --color-status-running: #FFA931;
  --color-status-completed: #6EE7B7;
  --color-status-failed: #FF4444;
}

[data-theme="light"] {
  --color-bg: #FAFAFA;
  --color-surface: #FFFFFF;
  --color-surface-alt: #F4F4F4;
  --color-border: #D4D4D4;
  --color-border-subtle: #E5E5E5;
  --color-fg: #1A1A1A;
  --color-fg-muted: #6B6B6B;
  --color-fg-dim: #A0A0A0;
  --color-accent: #7B61C9;
  --color-accent-secondary: #C94AC4;
  --color-destructive: #D73535;
}
```

---

## Visual Hierarchy & Layout

Every view in the dashboard has one designated primary focal point. Secondary and tertiary elements are explicitly subordinate to it — sized, colored, and positioned to support (not compete with) the anchor.

### Primary visual anchors per view

| View | Primary anchor | Secondary | Tertiary (persistent) |
|------|---------------|-----------|-----------------------|
| **Shot detail view** | The version-card thumbnail grid in the main pane draws the eye first. Cards are the largest interactive objects on screen, set against the dominant `#202020` canvas with `#353535` surfaces, arranged in a 3-column grid at optimal viewport. | The tree sidebar (left, 280px) and the Active Generations Panel (sidebar bottom) are secondary persistent fixtures — always visible but visually quieter (smaller type, muted foreground on secondary surface). | Breadcrumb at top of main pane; status pills inside cards (small, colored dots). |
| **Home view (root `/`)** | The home hero (tagline + body copy) draws the eye first on empty-state; on populated installs, the `Recent Versions` grid is the anchor. Hero uses Display 24px/600 in accent color. | Active Generations summary panel (top of home pane); Workspaces quick-jump cards (middle section). | Tree sidebar (left); Active Generations panel in sidebar bottom (same as shot view). |
| **Version detail drawer** | The active tab's primary content — JSON block (Workflow/Prompt/Raw), Summary metadata table, or Models list — fills the drawer body. The two primary action buttons (`Reproduce Version`, `Compare Version`) sit in the drawer header in accent color. | Tab strip across the top of the drawer (Summary/Workflow/Prompt/Models/Raw). | Close `×` button top-right; sidebar and main pane remain visible but dimmed by the 40% backdrop overlay. |
| **Diff drawer (2nd-level)** | The two-column structured diff (from `engine.diffVersions`) fills the drawer body — side-by-side params, models, seeds, workflow, metadata. Add/remove rows use status-running `#FFA931` for additions and destructive `#FF4444` for removals (small colored markers, not saturated backgrounds). | Diff summary header at the top of the drawer (one-line count of changes per section). | Version drawer behind (dimmed), sidebar and main pane further dimmed. |
| **Workspace / Project / Sequence detail views** | The immediate-children grid or list (projects under workspace, sequences under project, shots under sequence) is the primary anchor — each child is a clickable card with breadcrumb + count badges. | Breadcrumb at top of main pane. | Tree sidebar (left); Active Generations panel (sidebar bottom). |

### Hierarchy enforcement rules

- **One primary anchor per view.** Never two competing focal points at the same visual weight.
- **Size proportions:** primary anchor elements are at least 1.5x larger (in width, height, or overall presence) than secondary supporting elements.
- **Color proportions honor 60/30/10.** The accent color appears on the primary anchor's action affordance (buttons, links) and nowhere else in the view.
- **Persistent fixtures stay quiet.** Sidebar and Active Generations Panel use secondary surface `#353535` with muted foreground `#999` for body text — they do not compete with detail-pane content.
- **Breadcrumbs are navigation, not anchors.** Keep them at Body 14px/400 on muted foreground; they orient without drawing focus away from the anchor.

---

## Motion Contract

Per D-WEBUI-21: restrained, serious product posture. CSS-only animations, zero motion library deps.

| Trigger | Duration | Easing | Description |
|---------|----------|--------|-------------|
| State change (hover/focus) | 150ms | ease-out | Background/border color shift |
| Drawer open/close | 200ms | ease-out | Translate-X slide from right edge |
| Tab switch | 180ms | ease-out | Opacity cross-fade + 4px translate-Y |
| Status transition (flash) | 400ms | ease-in-out | Brief 12%-opacity tint overlay matching terminal status color, fades to normal |
| Running pulse | 1500ms | ease-in-out infinite | Sine-wave opacity 0.6 ↔ 1.0 on running status pill + amber card |
| Thumbnail fade-in | 200ms | ease-out | `<img>` opacity 0 → 1 on `onload` |
| Toast appearance | 180ms | ease-out | Translate-Y + opacity from bottom |
| Toast auto-dismiss | 5000ms hold, 180ms fade | ease-in | Matches D-WEBUI-22 |
| Skeleton shimmer | 1800ms | ease-in-out infinite | Subtle 20%-width gradient sweep on gray skeleton-thumb |

Explicitly **NOT** allowed in Phase 5:
- Springs, bounces, elastic curves
- Staggered list entrances
- Particle effects
- Page transitions beyond simple fade
- Any motion > 400ms (except infinite pulses/shimmers)

---

## Component Inventory

Hand-rolled primitives in `packages/dashboard/src/components/`. Executor implements each to this contract.

| Component | Purpose | Key states |
|-----------|---------|------------|
| `TreeSidebar` | Left collapsible hierarchy (ws → proj → seq → shot) | collapsed, expanded, selected, hover |
| `ActiveGenerationsPanel` | Bottom-pinned panel in sidebar showing running/submitted versions | empty, 1-5 rows, 5+ rows with scroll |
| `VersionCard` | Thumbnail + metadata card on shot detail grid | loading, submitted/running/completed/failed, with/without lineage |
| `StatusPill` | Inline status label with dot + text | 4 status variants |
| `LineageBadge` | Small icon + parent link for reproduce/iterate versions | reproduce, iterate (distinct icons) |
| `TagChip` | Read-only tag label | default, overflow (`+N more`) |
| `MetadataCount` | "3 metadata" label with keys tooltip | 0, 1, 2-N |
| `SkeletonThumbnail` | Gray shimmer placeholder before `<img>` load | loading |
| `JsonBlock` | Syntax-highlighted collapsible JSON with copy button | collapsed, expanded, copied |
| `VersionDrawer` | Right-side drawer, 5 tabs, close button | per tab |
| `DiffDrawer` | Second-level drawer for `engine.diffVersions` output | empty/loading, populated |
| `Toast` | Bottom-right notification (completion/failure) | success, error; auto-dismiss |
| `ThemeToggle` | Dark/light switcher in sidebar header | dark, light |
| `EmptyState` | Contextual message for empty hierarchy/shot/versions | with CTA link, without |
| `Breadcrumb` | `workspace / project / sequence / sh###` segments | compact, full |
| `Button` | Primary, secondary, ghost variants | default, hover, active, focus, disabled |
| `Tabs` | Generic tab strip (used in `VersionDrawer`) | active, inactive, focus-visible |
| `Drawer` | Generic slide-in container (base for Version/Diff) | closed, open, closing |

---

## Copywriting Contract

All copy strings locked. Executor references these verbatim; no copy invented at implementation time.

**CTA rule:** Every primary and secondary CTA is a verb + noun pair. Single-word verbs are only permitted when the button sits inline with contextual metadata in the same row (e.g., a `Copy` button directly adjacent to a JSON block which is itself the implicit noun).

### Primary interactive elements

| Element | Copy | Rationale |
|---------|------|-----------|
| Primary CTA (drawer header) | `Reproduce Version` | Verb + noun; button stands alone at top of drawer, needs explicit object. |
| Secondary CTA (drawer header) | `Compare Version` | Verb + noun; dropdown button that opens a sibling-version picker. After selection, the picker row shows `Compare with v002` as an inline confirmation. |
| Diff trigger (inside picker dropdown) | `Compare with v###` (e.g., `Compare with v002`) | Inline row — version id already present in the same row; verb-first form is acceptable because context is adjacent. |
| Copy-to-clipboard button | `Copy` → `Copied` (1.5s reset) | Inline with the JSON block it targets; the block is the noun. |
| Tab labels (drawer) | `Summary` / `Workflow` / `Prompt` / `Models` / `Raw` | Nouns (tab labels are section headers, not CTAs). |
| Theme toggle | Light mode shows `Dark` icon (moon); dark mode shows `Light` icon (sun). No text label. Aria-label: `Switch to {dark\|light} theme` | Icon-only with full verb+noun in aria-label. |
| Sidebar header | `VFX Familiar` (brand wordmark; Inter Tight 18px/600) | Brand mark, not a CTA. |
| Active generations header | `Active generations ({N})` — N is live count | Section header, not a CTA. |
| Cancel / close drawer button | `×` icon only. Aria-label: `Close drawer` | Icon-only with verb+noun in aria-label. |

### Empty states

| Scope | Heading | Body |
|-------|---------|------|
| Home — no workspaces | `No workspaces yet` | `Create your first workspace through an MCP agent. The dashboard will reflect hierarchy and generation activity in real time.` |
| Workspace detail — no projects | `No projects in this workspace` | `Agents create projects by calling workspace.create or project.create via MCP.` |
| Project detail — no sequences | `No sequences yet` | `Agents create sequences via MCP to organize shots by storyline beats.` |
| Sequence detail — no shots | `No shots in this sequence` | `Shots follow the VFX naming convention — sh010, sh020, etc.` |
| Shot detail — no versions | `No versions yet` | `Submit a generation via your MCP agent. The first version will appear here automatically.` |
| Home — no active generations | `No active generations` | *(single line, muted text; no secondary copy in compact panel)* |
| Asset query — no results | `No versions match these filters` | `Try broadening your tags or removing a date range.` |

### Loading states

| Scope | Copy |
|-------|------|
| Initial app boot | No copy — skeleton layout only (skeleton sidebar + skeleton detail pane) |
| Drawer opening mid-fetch | `Loading version details…` — muted, 14px, centered |
| Thumbnail pre-load | No copy — skeleton shimmer only |

### Error states

All error copy includes: problem description + actionable next step. Error codes come from the existing typed-error vocabulary (reused from Phase 1-4 — `VERSION_NOT_FOUND`, `INVALID_INPUT`, `INVALID_SCOPE`, `COMFYUI_CREDENTIALS_MISSING`, etc.) plus Phase 5's new `OUTPUT_UNAVAILABLE` (D-WEBUI-34).

| Error code | Heading | Body |
|-----------|---------|------|
| `VERSION_NOT_FOUND` | `Version not found` | `This version may have been removed or never existed. {hint or "Return to the shot's version list."}` |
| `OUTPUT_UNAVAILABLE` | `Preview unavailable` | `The output file is missing from disk. Provenance is still viewable. Run Reproduce Version to regenerate.` |
| `PROVENANCE_UNAVAILABLE` | `Cannot reproduce this version` | `Provenance data was not captured (likely a failed generation). Use Iterate from a later version instead.` |
| `COMFYUI_CREDENTIALS_MISSING` | `ComfyUI credentials required` | `Set COMFYUI_API_KEY in your .env file, then restart the server.` |
| `INVALID_INPUT` | `Request was invalid` | `{field-specific hint from the typed error, e.g. "limit must be between 1 and 100."}` |
| `INVALID_SCOPE` | `Query scope is invalid` | `Filter by exactly one of workspace_id, project_id, sequence_id, or shot_id — not multiple.` |
| Network error (fetch fail) | `Dashboard lost connection` | `The server may be restarting. Refresh this page in a moment.` |
| SSE disconnect | `Live updates paused` | `Reconnecting…` — muted banner at the top; auto-hides on reconnect |
| 500 unknown (HTTP status) | `Something went wrong` | `{error.message if present} — Check the server logs for details.` |

### Toast copy (bottom-right, auto-dismiss 5s per D-WEBUI-22)

| Trigger | Variant | Copy |
|---------|---------|------|
| `version.status_changed` → completed | success (green) | `v{N} in {shot_name} completed` |
| `version.status_changed` → failed | error (red) | `v{N} in {shot_name} failed — {error excerpt, first 60 chars}` |
| `POST /api/versions/:id/reproduce` → 202 | success (green) | `Reproduce submitted — tracking as v{N}` |
| `POST /api/versions/:id/reproduce` → error | error (red) | `Reproduce failed — {typed error message}` |
| JSON block copy | success (green, 1.5s) | `Copied to clipboard` |

Toast copy uses the bare verb "Reproduce" (no noun) intentionally — the toast is a system-status message, not a CTA; it references the action that was just taken, and the version number is already in the same line.

### Destructive actions in Phase 5

**None.** Per D-WEBUI-25, the dashboard exposes only `Reproduce Version` and `Compare Version` (diff). No delete, no remove-tag, no remove-metadata from the UI in Phase 5. Agents drive destructive operations through MCP.

No confirmation dialogs required in Phase 5 scope.

### Demo-audience polish copy (first impression)

| Scope | Copy |
|-------|------|
| Home hero tagline (if no activity) | `Your VFX pipeline, managed by your AI familiar.` (Inter Tight 24px/600, accent-colored) |
| Home hero body (fresh install) | `Connect your MCP agent and start creating. Everything you do here will appear as it happens.` |
| Dashboard fallback (dist/ missing) | `Dashboard not built` + `Run npm run build:dashboard from the repo root.` (pinned by D-WEBUI-12) |
| Below-1024px overlay | `Dashboard requires a desktop browser` + `Try on a device at least 1024px wide.` |

---

## Interaction Contracts

### SSE client-side filtering

Per D-WEBUI-03, single global stream. Client filters per current view:

| Current view | Events rendered | Events silently ingested (for cross-view state) |
|--------------|----------------|------------------------------------------------|
| Home | all | `version.status_changed`, `version.created`, `hierarchy.created` update aggregate counters |
| Shot detail | `version.*` where `shot_id === current_shot_id` | others mutate active-generations panel only |
| Version drawer open | `version.status_changed` / `tag.changed` / `metadata.changed` where `version_id === current_version_id` | others silent |
| Any view | `version.status_changed` → toast if version was previously in active panel and transitions to terminal | always |

### Active generations panel behavior

Per D-WEBUI-22 and SPECIFICS:

- **Sort order:** `created_at ASC` (oldest first — newer pushes down, queue mental model).
- **Row content:** breadcrumb (`project / sequence / sh###`) + `v###` + live elapsed-timer (ticks each 1s) + status pill.
- **Transition:** on `version.status_changed` → terminal status, row animates out (200ms ease-out translate-X + opacity) and toast fires.
- **No cancel button** in Phase 5 (deferred — MCP `generation.cancel` doesn't exist yet).
- **Pause-on-hover:** yes — hovering a row pauses the elapsed-timer flicker (visual polish, counter keeps incrementing).
- **Empty state:** single line `No active generations` in muted text, 14px, centered, md vertical padding.

### Drawer interaction

Per D-WEBUI-24:

- **Open:** click any `VersionCard`. Drawer slides in from right (200ms ease-out translate-X from +560px to 0).
- **Tabs:** Summary (default active), Workflow, Prompt, Models, Raw. Tab bar fixed at top; content below scrolls independently.
- **Close:** `×` button top-right corner, `Escape` key, click on backdrop (backdrop is a semi-transparent overlay at 40% opacity `#000`).
- **Diff drawer (second drawer):** opens on top of the primary drawer at 720px width. `Compare Version` button opens a dropdown; selecting a sibling version opens the diff drawer. `Escape` closes only the top drawer.

### Keyboard baseline (minimum)

- `Escape` — close active drawer (closes diff first, then version drawer)
- `Tab` / `Shift+Tab` — focus-cycle through interactive elements with visible focus ring (2px outline in accent color)
- `Enter` / `Space` — activate focused button
- Arrow keys on tab strip — move tab selection (no wrap)

Additional shortcuts (cmd-k search palette, j/k navigation) are deferred per CONTEXT.md Deferred Ideas — planner may add opportunistically, not contract-required.

### Breakpoints & responsive posture

Per D-WEBUI-17:

| Viewport | Behavior |
|----------|----------|
| >= 1440px | Optimal: full sidebar (280px), main pane, drawers overlay main pane without displacing sidebar |
| 1024-1439px | Graceful degrade: sidebar still fixed at 280px, main pane narrows, drawers cover main pane up to drawer width |
| < 1024px | Overlay shown, underlying UI hidden: `Dashboard requires a desktop browser` + `Try on a device at least 1024px wide.` — no mobile layout in Phase 5 |

---

## Registry Safety

Preact ecosystem — shadcn registry does not apply. No third-party component registries in Phase 5.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — Preact target, shadcn is React/Vue/Svelte-only |
| Third-party registries | none | not applicable |

### Dashboard npm dependencies (declared in `packages/dashboard/package.json`)

Non-registry deps (standard npm packages) — provenance per CONTEXT.md D-WEBUI-10/11/20 and researcher discretion:

| Package | Purpose | Provenance |
|---------|---------|------------|
| `preact` | Client framework | Locked by D-WEBUI-10 |
| `@preact/signals` | Fine-grained reactive state | Locked by D-WEBUI-10 |
| `typescript` | Language | Locked by D-WEBUI-10 |
| `vite` | Dev server + bundler | Locked by D-WEBUI-08/13 |
| `@preact/preset-vite` | Preact JSX + HMR | Standard Vite + Preact pairing |
| `tailwindcss@^4` | Styling | Locked by D-WEBUI-11 |
| `@tailwindcss/vite` | Tailwind v4 Vite plugin | Standard Tailwind v4 pairing |
| `@fontsource/inter` | Inter font (offline-capable; import weights 400 + 600 only) | Locked by D-WEBUI-20 (not CDN) |
| `@fontsource/inter-tight` | Inter Tight font (import weight 600 only) | Locked by D-WEBUI-20 (not CDN) |
| `lucide-preact` | Icon library (^0.555.0) | Researcher pick: Preact-native, tree-shakable, community-maintained fork of Feather Icons; avoids phosphor-icons (no official Preact support) and hand-drawn SVGs (time cost) |
| `vitest` | Component test runner | Locked by D-WEBUI-35 |
| `@testing-library/preact` | Component test utilities | Locked by D-WEBUI-35 |
| `jsdom` | Test DOM environment | Standard Vitest pairing |

**Bundle size posture:** Informal target < 200KB gzipped for the main bundle per CONTEXT.md Deferred Ideas. No formal budget enforcement in Phase 5. All deps above are tree-shakable; Lucide only ships icons actually imported. `@fontsource` imports explicitly restrict to weights 400 + 600 to honor the 2-weight typography ceiling and avoid shipping dead 500-weight assets.

**JSON syntax highlighter (`JsonBlock` component):** Researcher defers to planner per CONTEXT.md "Claude's Discretion". Three viable options at bundle-size ascending order:
1. Hand-rolled mini-tokenizer (~150 lines, zero dep, string/number/boolean/null coloring only) — **RECOMMENDED** for demo scope.
2. `shiki` tree-shaken with one theme — ~30KB additional, richer output.
3. `prismjs` with json language only — ~10KB, simpler API.

If planner chooses an external highlighter, register it here post-selection and apply the 4-token color contract:
- JSON key: `--color-fg` (default white/dark)
- JSON string: `--color-accent-secondary` (`#FF9CF9` magenta)
- JSON number: `--color-status-running` (`#FFA931` amber)
- JSON boolean / null: `--color-accent` (`#B39DDB` purple)

---

## Accessibility Baseline

Per standard product expectations for a public-demo deliverable:

- **Contrast:** All text on dominant/secondary backgrounds ≥ WCAG AA (4.5:1 for body, 3:1 for large text). Dark theme: white `#FFF` on `#202020` = 18.9:1; muted `#999` on `#202020` = 6.9:1 ✓. Accent `#B39DDB` on `#202020` = 7.8:1 for text use ✓. Light theme accents darkened specifically for contrast.
- **Focus rings:** 2px solid accent-colored outline, offset 2px, on every keyboard-focusable element (`:focus-visible` selector — never `:focus`, so mouse clicks don't show rings).
- **Semantic HTML:** `<nav>` for sidebar tree, `<main>` for detail pane, `<aside>` for active generations panel, `<dialog>` (or `role="dialog"` + `aria-modal="true"`) for drawers.
- **Keyboard traps:** drawers set focus on open to their close button; `Escape` closes; focus returns to the triggering element on close.
- **ARIA live regions:** toast container is `<div role="status" aria-live="polite" aria-atomic="true">` for screen-reader announcement of completion/failure events.
- **Reduced motion:** respect `prefers-reduced-motion: reduce` — disables pulse, shimmer, and cross-fade animations; instant state transitions instead.

---

## File Inventory (where the design contract lives in code)

Executor implementation map:

| File | Contract it implements |
|------|------------------------|
| `packages/dashboard/src/styles/theme.css` | Tailwind v4 `@theme` block with all color/spacing/typography tokens above |
| `packages/dashboard/src/styles/motion.css` | CSS keyframes for pulse, shimmer, fade; `@media (prefers-reduced-motion)` overrides |
| `packages/dashboard/src/components/*` | Component Inventory table above |
| `packages/dashboard/src/lib/copy.ts` | Copywriting Contract constants (every string from the copy tables, exported as named constants) |
| `packages/dashboard/src/lib/status.ts` | Status color + copy resolver (`statusColor(status): string`, `statusPillLabel(status): string`) |
| `packages/dashboard/index.html` | Light/dark theme script (reads `localStorage["vfx-familiar:theme"]`, sets `[data-theme]` on `<html>` BEFORE React-hydrate to avoid FOUC) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Pre-Population Provenance

| Contract area | Source |
|---------------|--------|
| Aesthetic direction (ComfyUI-native) | CONTEXT.md D-WEBUI-15 |
| Palette (hex anchors) | Researcher — extracted from docs.comfy.org/interface/appearance ComfyUI canonical theme values |
| Accent choice (MODEL-purple + LATENT-magenta split) | Researcher — within D-WEBUI-15 discretion; palette values are canonical ComfyUI slot colors |
| Font stack (Inter + Inter Tight + tabular-nums) | CONTEXT.md D-WEBUI-20 |
| Font loading (`@fontsource`, no CDN) | CONTEXT.md Specifics block |
| Theme mode (dark default, light toggle, `[data-theme]`) | CONTEXT.md D-WEBUI-16 |
| Component library choice (hand-rolled, no shadcn) | CONTEXT.md D-WEBUI-11 + Preact target |
| Icon library (`lucide-preact`) | Researcher — within CONTEXT.md "Claude's Discretion" |
| Spacing scale (4/8/16/24/32/48/64) | Standard 8-point grid; researcher default |
| Drawer widths (560px / 720px) | CONTEXT.md D-WEBUI-24 / D-WEBUI-25 + Specifics |
| Sidebar width (280px) | CONTEXT.md D-WEBUI-17 |
| Responsive breakpoints (1024 / 1440) | CONTEXT.md D-WEBUI-17 |
| Motion specs (restrained, 150-200ms, no springs) | CONTEXT.md D-WEBUI-21 |
| Component inventory (18 components) | Researcher — derived from CONTEXT.md view/behavior decisions |
| Copywriting — CTA labels (`Reproduce Version`, `Compare Version`) | CONTEXT.md D-WEBUI-25 + Specifics (revised 2026-04-23 to add explicit nouns per checker feedback) |
| Copywriting — tab labels | CONTEXT.md D-WEBUI-24 |
| Copywriting — empty states | Researcher — written for VFX-production context per REQUIREMENTS.md WEBUI-01..05 |
| Copywriting — error states | Researcher — error codes pulled from CONTEXT.md `<canonical_refs>` typed-error vocabulary + new `OUTPUT_UNAVAILABLE` from D-WEBUI-34 |
| Copywriting — toast copy | CONTEXT.md D-WEBUI-22 (template) + researcher specifics |
| Typography — 2-weight ceiling (400 + 600) | Researcher — revised 2026-04-23 after checker flagged 3-weight count. Label role now uses uppercase + letter-spacing for differentiation, not weight. |
| Visual Hierarchy & Layout section | Researcher — added 2026-04-23 per checker Dimension 2 feedback to declare primary focal point per view. |
| Registry safety (none — Preact target) | Design system detection: no `components.json`, Preact framework |
| Bundle size target (< 200KB gzipped) | CONTEXT.md Deferred Ideas |
| JSON syntax highlighter | Researcher deferred to planner per CONTEXT.md "Claude's Discretion"; 3-option matrix with recommendation |
| Accessibility baseline | Standard WCAG AA; researcher default for public-demo deliverable |
| localStorage keys | CONTEXT.md Specifics block |
| Active generations sort order (oldest first) | CONTEXT.md Specifics block |

**Questions not asked of the user:** All design contract areas were either locked by CONTEXT.md or filled by researcher within documented "Claude's Discretion" scope. Zero new decisions escalated — CONTEXT.md was dense enough that no follow-up user interaction was needed.

---

## Revision Log

### 2026-04-23 — checker-requested revision

**Dimension 4 (Typography) — BLOCKING fix applied:**
- Collapsed 3 font weights (400, 500, 600) to exactly 2 (400, 600).
- Label role changed from weight 500 → 400; differentiation now via `text-transform: uppercase` + `letter-spacing: 0.05em` + muted foreground color.
- Rewrote "Weights rule" prose — removed the "500 exception" framing; now states exactly 2 weights with no exceptions.
- Added explicit Label role CSS contract snippet.
- Noted `@fontsource` import discipline in the dependency table (import 400 + 600 weight files only; do not ship 500 weight as dead bundle weight).

**Dimension 1 (Copywriting) — recommended fix applied:**
- Primary CTA copy changed from `Reproduce` → `Reproduce Version` (verb + noun) in version drawer header.
- Secondary CTA copy changed from `Compare with v###` → `Compare Version` (verb + noun) at the dropdown button level; the `Compare with v###` form is retained for the inline dropdown row where context is adjacent.
- Added explicit CTA rule at the top of the Copywriting Contract: every primary/secondary CTA is verb + noun; single-word verbs only permitted with adjacent metadata context (`Copy` button next to JSON block).
- Audited every CTA in the document:
  - Tab labels (Summary/Workflow/Prompt/Models/Raw) — nouns, not CTAs; unchanged.
  - Theme toggle — icon-only with full verb+noun in aria-label; rationale added.
  - Close drawer — `×` icon-only with aria-label `Close drawer` (verb + noun); rationale added.
  - Copy button — inline with JSON block; rationale added.
  - Error + empty state copy — all contain actionable next steps with verbs and nouns; unchanged.
  - Toast copy — explanatory note added: toasts are system status messages not CTAs; bare `Reproduce` remains acceptable because the version number is adjacent.
  - Error body for `OUTPUT_UNAVAILABLE` updated to reference `Reproduce Version` by full name.

**Dimension 2 (Visuals) — recommended fix applied:**
- Added a new top-level section `Visual Hierarchy & Layout` after the Color section.
- Declared primary visual anchor for each view: shot detail (version-card thumbnail grid), home view (hero tagline on empty / recent versions grid when populated), version drawer (active tab content), diff drawer (two-column structured diff), workspace/project/sequence detail (immediate-children grid).
- For each view, also declared secondary and tertiary (persistent) elements so the 60/30/10 hierarchy is unambiguous.
- Added 5 hierarchy-enforcement rules: one anchor per view, 1.5x size proportions, color proportions, quiet persistent fixtures, breadcrumbs as navigation.

**Not changed:**
- Color palette (locked by CONTEXT.md)
- Spacing scale (locked; 8-point grid)
- Component inventory (18 components — unchanged)
- Motion contract (locked by D-WEBUI-21)
- All other copy strings (only the two CTAs + one error body updated)
- No new dependencies introduced.

---

*Phase: 05-web-dashboard*
*UI-SPEC drafted: 2026-04-23 by gsd-ui-researcher*
*UI-SPEC revised: 2026-04-23 by gsd-ui-researcher (checker feedback pass)*
