---
phase: 23-production-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/types/shot-grid.ts
  - packages/dashboard/src/styles/theme.css
  - src/store/shot-status-repo.ts
  - src/store/__tests__/shot-status-repo-stats.test.ts
  - src/store/__tests__/shot-status-repo-grid.test.ts
autonomous: true
requirements: [OVR-01, OVR-02]
must_haves:
  truths:
    - "D-01: Extends GET /api/sequences/:id/shot-grid response envelope with top-level `stats` field and per-row `is_stale: boolean` — zero new HTTP routes; repo layer adds getSequenceStats(sequenceId) alongside existing listShotGrid; engine composes both into one envelope returned by the existing thin Hono handler"
    - "D-02: getSequenceStats(db, sequenceId) returns { total, counts: Record<ShotStatus, number>, stale_count } with all 5 status keys initialized to 0 even when GROUP BY emits sparse rows; SequenceStats envelope shape (total, approved_pct, counts, pending_review_backlog, stale_count) is LOCKED — pending_review_backlog is emitted explicitly even though it duplicates counts['pending-review']"
    - "D-03: listShotsForGrid returns a per-row is_stale flag (0|1 from SQLite CASE) for every shot in the paginated buffer; ShotGridRow.is_stale: boolean is computed server-side using STALE_SHOT_DAYS; dashboard reads is_stale (for initial border render) AND latest_completed_version.completed_at (for SSE re-derive)"
    - "D-08: New theme token `--color-shot-stale` is DISTINCT from `--color-shot-status-pending-review` (Phase 21 amber `#fbbf24`) — using the same hex would visually collapse two semantic states; LOCKED hex values (UI-SPEC A5 escalation): dark `#f97316` (orange-500, 5.41:1) and light `#b45309` (amber-700, 4.62:1) SUPERSEDE CONTEXT D-08 recommendations of `#fbbf24`/`#f59e0b` which fail contrast"
    - "EXPLAIN QUERY PLAN for getSequenceStats stale-count query uses idx_shots_status (no full SCAN shots) AND no SCAN versions full table scan"
    - "D-15: Zero-completed-version shots are NOT marked stale (grace period — EXISTS clause falls out); pragmatic interpretation of OVR-02 — only shots with a real prior completed version >14d old qualify as stale"
    - "STALE_SHOT_DAYS = 14 constant at src/store/shot-status-repo.ts:54 is unchanged (single source of truth)"
    - "Phase 21 listShotsForGrid CORRELATED-on-ranked-CTE invariant still holds (no regression in pre-existing EXPLAIN tests)"
    - "Dashboard types/shot-grid.ts exports SequenceStats interface with snake_case fields (approved_pct, pending_review_backlog, stale_count) and ShotGridRow gains is_stale: boolean, ShotGridResponse gains stats: SequenceStats"
    - "D-16: theme.css contains --color-shot-stale: #f97316 in @theme dark block AND --color-shot-stale: #b45309 in [data-theme=\"light\"] light block (Pitfall 6 — both themes patched; both blocks MUST contain the token)"
    - "D-17: SSE payload extension NOT required — existing `{ shotId, fromStatus, toStatus, changedBy, note?, sequenceId }` carries enough info; latest_completed_at read from shotGrid.value.shots[idx], not from SSE payload"
  artifacts:
    - path: "packages/dashboard/src/types/shot-grid.ts"
      provides: "SequenceStats interface + ShotGridRow.is_stale + ShotGridResponse.stats"
      contains: "export interface SequenceStats"
    - path: "packages/dashboard/src/styles/theme.css"
      provides: "--color-shot-stale theme token in both light and dark blocks"
      contains: "--color-shot-stale"
    - path: "src/store/shot-status-repo.ts"
      provides: "getSequenceStats() function + getSequenceStatsStaleSqlText() + listShotsForGrid is_stale extension"
      contains: "export function getSequenceStats"
    - path: "src/store/__tests__/shot-status-repo-stats.test.ts"
      provides: "Stats repo behavior + EXPLAIN tests (Wave 0 stub fulfilled)"
      contains: "describe('getSequenceStats"
  key_links:
    - from: "src/store/shot-status-repo.ts:getSequenceStats"
      to: "shots table via idx_shots_status (sequence_id, status) covering index"
      via: "Drizzle sql`SELECT status, COUNT(*) FROM shots WHERE sequence_id = ? GROUP BY status`"
      pattern: "SELECT status.*COUNT\\(\\*\\).*FROM shots.*GROUP BY status"
    - from: "src/store/shot-status-repo.ts:getSequenceStats stale_count Q2"
      to: "versions table via shot_id autoindex + completed_at"
      via: "EXISTS (SELECT 1 FROM versions v WHERE v.shot_id = shots.id AND v.status = 'completed' AND v.completed_at < ?)"
      pattern: "EXISTS \\(SELECT 1 FROM versions"
    - from: "src/store/shot-status-repo.ts:listShotsForGrid CTE outer SELECT"
      to: "is_stale CASE expression inline"
      via: "CASE WHEN s.status IN ('wip','pending-review') AND r.completed_at IS NOT NULL AND r.completed_at < ? THEN 1 ELSE 0 END AS is_stale"
      pattern: "CASE\\s+WHEN s\\.status IN \\('wip','pending-review'\\)"
---

<objective>
Phase 23 Wave 1 — foundations. This plan establishes the read-only data path for whole-sequence production stats: server-side repo functions (the GROUP BY counts query, the EXISTS-clause stale_count query, the per-row is_stale CASE extension to the existing listShotsForGrid CTE), the dashboard wire-shape types (`SequenceStats` interface + `is_stale` on `ShotGridRow` + `stats` on `ShotGridResponse`), and the two-theme `--color-shot-stale` token. No engine facade work, no UI work — those land in Wave 2/3 against the contracts published here.

Purpose: Lock the wire contract + SQL query plan + theme palette so every downstream consumer (engine.listShotGrid composition in Plan 02; `<ProgressBar/>` + `<SequenceHeader/>` subrow + `<ShotGridCard/>` border in Plan 03) builds against deterministic types and a contrast-verified color value. The EXPLAIN QUERY PLAN tests (per OVR-01 "single GROUP BY query — no N+1") run BEFORE any engine composition lands so a regression is caught at the repo layer.

Output: 4 modified files + 1 new test file. Repo function publicly exported. Theme tokens in both theme blocks. SequenceStats type available for cross-tree import. EXPLAIN test green and asserts no per-row subquery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-production-stats/23-CONTEXT.md
@.planning/phases/23-production-stats/23-RESEARCH.md
@.planning/phases/23-production-stats/23-PATTERNS.md
@.planning/phases/23-production-stats/23-UI-SPEC.md

<!-- Existing files this plan modifies / reads patterns from -->
@src/store/shot-status-repo.ts
@src/store/__tests__/shot-status-repo-grid.test.ts
@packages/dashboard/src/types/shot-grid.ts
@packages/dashboard/src/styles/theme.css

<interfaces>
<!-- Existing exports the executor must extend (not duplicate). All verified in 23-PATTERNS.md and 23-RESEARCH.md. -->

From src/store/shot-status-repo.ts (Phase 20+21 surface — line refs from 2026-05-15 inspection):
  export const STALE_SHOT_DAYS = 14;                                      // line 54 — single source of truth
  export interface ShotGridQueryRow { id; name; status; version_count; lcv_id; lcv_completed_at; }  // line 186 — extend with is_stale: number
  export interface ShotGridQueryResult { items: ShotGridQueryRow[]; ... }  // line 196
  export function listShotsForGrid(db, sequenceId, opts): ShotGridQueryResult;  // line 228 — extend CTE outer SELECT with is_stale CASE
  export function listShotsForGridSqlText(): string;                       // line 301 — Phase 21 EXPLAIN harness precedent

From packages/dashboard/src/types/shot-grid.ts (Phase 21 wire surface):
  export interface ShotGridSequenceMeta { id: string; name: string; }     // line 30
  export interface ShotGridRow { id; name; status: ShotStatus; version_count: number; latest_completed_version: {id; thumbnail_url; completed_at;} | null; }  // line 42 — gain is_stale: boolean
  export interface ShotGridResponse { sequence; shots; next_cursor; total_count; }  // line 62 — gain stats: SequenceStats

From packages/dashboard/src/styles/theme.css (Phase 21 5-token block precedent):
  @theme { ... --color-shot-status-pending-review: #fbbf24; ... }          // dark block (around line 54-58)
  [data-theme="light"] { ... --color-shot-status-pending-review: #d97706; ... }  // light block (around line 114-119)

From src/store/__tests__/shot-status-repo-grid.test.ts (Phase 21 EXPLAIN harness — extend in place):
  describe('listShotsForGrid CTE EXPLAIN QUERY PLAN (Phase 21 GRID-04 single-scan lock)') with planRows = testDb.sqlite.prepare('EXPLAIN QUERY PLAN ' + listShotsForGridSqlText()).all(...binds);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-01: Extend dashboard wire types (SequenceStats interface, is_stale, stats envelope field)</name>

  <read_first>
    - packages/dashboard/src/types/shot-grid.ts (the file being modified — read in full; existing lines 30, 42-54, 62-69 are the extension anchors)
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-02 envelope shape is LOCKED; D-03 per-row is_stale is LOCKED)
    - .planning/phases/23-production-stats/23-PATTERNS.md §4 "types/shot-grid.ts" (analog: itself; snake_case envelope convention from existing lines 11-14)
    - .planning/phases/23-production-stats/23-UI-SPEC.md §"Component Inventory > Modified files" row for types/shot-grid.ts
  </read_first>

  <files>packages/dashboard/src/types/shot-grid.ts</files>

  <behavior>
    - Test 1: `SequenceStats` interface exists with fields `total: number`, `approved_pct: number`, `counts: Record<ShotStatus, number>`, `pending_review_backlog: number`, `stale_count: number` (exactly these 5 fields, snake_case where applicable; per D-02 LOCKED shape).
    - Test 2: `ShotGridRow` gains required field `is_stale: boolean`.
    - Test 3: `ShotGridResponse` gains required field `stats: SequenceStats`.
    - Test 4: All other fields (id, name, status, version_count, latest_completed_version, next_cursor, total_count) are preserved verbatim — pre-existing imports of `ShotGridRow` and `ShotGridResponse` elsewhere in the codebase still typecheck under their original shape (only the additive `is_stale` / `stats` fields appear).
  </behavior>

  <behavior>
    Test execution surface: `npx tsc --noEmit -p packages/dashboard` produces zero errors after the change. Since this file is the contract source, the "tests" for this task are (a) the typecheck pass, and (b) the downstream Wave 1/2 test files (shot-status-repo-stats.test.ts in this same plan, dashboard-routes-shot-grid.test.ts in Plan 02) importing `SequenceStats` from this module without error.
  </behavior>

  <action>
    Open packages/dashboard/src/types/shot-grid.ts. Insert (per 23-PATTERNS.md §4 verbatim shape, per D-02 LOCKED envelope shape, per D-03 per-row flag):

    1. NEW export `interface SequenceStats { total: number; approved_pct: number; counts: Record<ShotStatus, number>; pending_review_backlog: number; stale_count: number; }`. Place this interface block above the existing `ShotGridRow` interface (after the `ShotGridSequenceMeta` block at line ~30). JSDoc above the interface must explain (i) D-02 LOCKED shape, (ii) snake_case mirror of existing envelope convention from lines 11-14 (`next_cursor`, `total_count`, `version_count`), (iii) `pending_review_backlog` duplicates `counts['pending-review']` BY DESIGN (D-02) to keep backlog-callout component data-source independent from the mini-pills row.

    2. EXTEND existing `interface ShotGridRow` with `is_stale: boolean;` — required (not optional). JSDoc on the new field must reference D-03 + D-15 (server-computed via inline CASE; zero-version shots NOT marked stale).

    3. EXTEND existing `interface ShotGridResponse` with `stats: SequenceStats;` — required (not optional). JSDoc on the new field must reference D-02 (whole-sequence aggregate, independent of pagination).

    4. DO NOT touch any other field. DO NOT change `ShotStatus` re-export. DO NOT change `ShotGridCursor`. DO NOT add new exports beyond `SequenceStats`.

    The shape mirrors the snake_case discipline already established at types/shot-grid.ts:11-14 (existing `next_cursor`, `total_count`, `version_count` precedent). Use `ShotStatus` type from the existing import at line 4 (or wherever it currently lives in this file) — DO NOT redeclare it.

    Reference: 23-PATTERNS.md §4 code excerpts (lines 230-278) show the exact JSDoc + field arrangement; copy that arrangement, not the verbatim words.
  </action>

  <verify>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx tsc --noEmit -p packages/dashboard 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "^export interface SequenceStats" packages/dashboard/src/types/shot-grid.ts` outputs `1`
    - Source assertion: `grep -c "is_stale: boolean" packages/dashboard/src/types/shot-grid.ts` outputs `1` (single occurrence on the ShotGridRow extension)
    - Source assertion: `grep -c "stats: SequenceStats" packages/dashboard/src/types/shot-grid.ts` outputs `1` (single occurrence on the ShotGridResponse extension)
    - Source assertion: `grep -c "pending_review_backlog: number" packages/dashboard/src/types/shot-grid.ts` outputs `1`
    - Source assertion: `grep -c "stale_count: number" packages/dashboard/src/types/shot-grid.ts` outputs `1`
    - Source assertion: `grep -c "approved_pct: number" packages/dashboard/src/types/shot-grid.ts` outputs `1`
    - Typecheck assertion: `npx tsc --noEmit -p packages/dashboard` exits 0 (the dashboard build is the source-of-truth typecheck — see CLAUDE.md project commands)
    - Diff hygiene: `git diff --stat packages/dashboard/src/types/shot-grid.ts` shows only this file modified; no neighboring files touched.
  </acceptance_criteria>

  <done>
    Dashboard package compiles with the new types. SequenceStats is importable from `packages/dashboard/src/types/shot-grid.js` (note: TypeScript `.ts` source, `.js` extension in import path per ESM convention used in the repo). is_stale and stats fields are required (not optional) so downstream consumers will receive a hard typecheck error if they forget to populate them.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-02: Add --color-shot-stale token to theme.css (both light and dark blocks; LOCKED contrast-verified values)</name>

  <read_first>
    - packages/dashboard/src/styles/theme.css (the file being modified — read in full; existing dark `@theme` block ~lines 29-87, existing light `[data-theme="light"]` block ~lines 101-120)
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-08 token rule; D-16 both-blocks rule)
    - .planning/phases/23-production-stats/23-RESEARCH.md §"Common Pitfalls > Pitfall 6" (theme.css token added to dark-only is the landmine)
    - .planning/phases/23-production-stats/23-UI-SPEC.md §"Color > Token 1: --color-shot-stale (REQUIRED)" — the LOCKED hex values supersede CONTEXT D-08 recommendations per the A5 escalation: dark `#f97316`, light `#b45309` (do NOT use `#fbbf24` / `#f59e0b` — those failed contrast / collapsed visually)
    - .planning/phases/23-production-stats/23-PATTERNS.md §8 "theme.css" (analog: Phase 21 5-token precedent at theme.css:53-58 dark + 114-119 light)
  </read_first>

  <files>packages/dashboard/src/styles/theme.css</files>

  <behavior>
    - Test 1: `grep -c "^\s*--color-shot-stale" packages/dashboard/src/styles/theme.css | awk '{print $1}'` returns `>= 2` (one declaration in each theme block, regardless of leading whitespace).
    - Test 2: `grep "^\s*--color-shot-stale" packages/dashboard/src/styles/theme.css` shows both `#f97316` (dark) and `#b45309` (light) — these exact hex values, not the RESEARCH-suggested `#fbbf24` / `#f59e0b` (UI-SPEC LOCKS the escalated values per A5).
    - Test 3: The dark token line appears AFTER the existing `--color-shot-status-omit` line in the `@theme` block (Phase 21 5-token block at ~lines 54-58). The light token line appears AFTER the existing light `--color-shot-status-omit` override (Phase 21 light block at ~lines 114-119).
    - Test 4: No other tokens added in this task. `--color-stats-backlog-callout` is DEFERRED per UI-SPEC Open Question 1 — the backlog callout reuses `--color-accent`.
  </behavior>

  <action>
    Open packages/dashboard/src/styles/theme.css. Locate the existing `@theme {}` block's Phase 21 status token cluster (5 tokens: `--color-shot-status-wip`, `--color-shot-status-pending-review`, `--color-shot-status-approved`, `--color-shot-status-on-hold`, `--color-shot-status-omit`, around lines 54-58). IMMEDIATELY AFTER the last of those 5 dark tokens, on a new line, add:

    ```
    /* ===== Phase 23 — sequence stats. WCAG 2.1 AA UI-component contrast vs --color-bg ≥ 3:1. ===== */
    --color-shot-stale: #f97316;  /* orange-500 — DISTINCT from pending-review amber per D-08 */
    ```

    Then locate the `[data-theme="light"]` override block (around lines 101-120), specifically the cluster where Phase 21 added 5 light-theme status overrides. IMMEDIATELY AFTER the last of those 5 light tokens, on a new line, add:

    ```
    /* ===== Phase 23 — light-theme stale border (4.62:1 vs #fafafa per UI-SPEC L122). ===== */
    --color-shot-stale: #b45309;  /* amber-700 — passes 4.62:1 vs #fafafa; distinct from pending-review #d97706 */
    ```

    The hex values are LOCKED per UI-SPEC §"Color > Token 1" (a deviation from CONTEXT D-08's recommendations of `#fbbf24` dark / `#f59e0b` light — those would fail WCAG and/or visually collapse with `--color-shot-status-pending-review`). The dark `#f97316` orange-500 + light `#b45309` amber-700 are contrast-verified at 5.41:1 / 4.62:1 against the respective `--color-bg` values.

    DO NOT modify any other token. DO NOT add `--color-stats-backlog-callout` (deferred — backlog callout reuses `--color-accent`). DO NOT change the comment block headers above the status section.

    Pitfall 6 enforcement: BOTH blocks MUST be patched in this single commit; an executor that touches only the dark block silently breaks light-theme contrast.
  </action>

  <verify>
    <automated>grep -c "^\s*--color-shot-stale" /Users/macapple/comfyui-vfx-mcp/packages/dashboard/src/styles/theme.css</automated>
    <automated>grep "#f97316\|#b45309" /Users/macapple/comfyui-vfx-mcp/packages/dashboard/src/styles/theme.css</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "^\s*--color-shot-stale" packages/dashboard/src/styles/theme.css` outputs `2` (one per block; CRITICAL — Pitfall 6 explicitly rejects single-block-only edits)
    - Source assertion: `grep "^\s*--color-shot-stale: #f97316" packages/dashboard/src/styles/theme.css` returns one matching line (dark block)
    - Source assertion: `grep "^\s*--color-shot-stale: #b45309" packages/dashboard/src/styles/theme.css` returns one matching line (light block)
    - Negative assertion: `grep "^\s*--color-shot-stale: #fbbf24" packages/dashboard/src/styles/theme.css` returns ZERO matches (the RESEARCH-suggested dark value was REJECTED at UI-SPEC stage for visual collapse with pending-review pill)
    - Negative assertion: `grep "^\s*--color-shot-stale: #f59e0b" packages/dashboard/src/styles/theme.css` returns ZERO matches (the RESEARCH-suggested light value was REJECTED at UI-SPEC stage for failing 3:1 WCAG against #fafafa)
    - Negative assertion: `grep -c "color-stats-backlog-callout" packages/dashboard/src/styles/theme.css` returns `0` (deferred — see UI-SPEC Open Question 1 resolution)
    - Build assertion: `cd packages/dashboard && npx vite build` exits 0 (CSS still parses; the dashboard build is the project-canonical CSS smoke test)
  </acceptance_criteria>

  <done>
    Both theme blocks reference `--color-shot-stale` with the LOCKED contrast-verified hex values. Light-theme users see `#b45309` (amber-700, 4.62:1 vs #fafafa); dark-theme users see `#f97316` (orange-500, 5.41:1 vs #202020). Pitfall 6 mitigated by patching both blocks in the same commit. Vite production build succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-03: Add getSequenceStats repo function + extend listShotsForGrid with is_stale CASE + write the SQL tests</name>

  <read_first>
    - src/store/shot-status-repo.ts (the file being modified — read in full; existing lines 54 STALE_SHOT_DAYS, 186-201 ShotGridQueryRow + ShotGridQueryResult, 228-285 listShotsForGrid body, 289-323 listShotsForGridSqlText)
    - src/store/__tests__/shot-status-repo-grid.test.ts (the existing Phase 21 EXPLAIN harness file — read in full; lines 37-50 fixture setup, lines 88-109 EXPLAIN test pattern, lines 112-203 behavior tests pattern)
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-14 GROUP BY + TypeScript approved_pct; D-15 EXISTS-clause zero-version grace)
    - .planning/phases/23-production-stats/23-RESEARCH.md §"Pattern 1" + §"Code Example 1" + §"Code Example 2" (verbatim function shape + CASE phrasing) + §"Common Pitfalls > Pitfall 4" (CASE phrasing to avoid CORRELATED SCALAR planner output)
    - .planning/phases/23-production-stats/23-PATTERNS.md §9 "shot-status-repo.ts" (analog: itself; inline CASE pattern + new function shape) + §12 "shot-status-repo-stats.test.ts" (NEW test fixture + EXPLAIN harness pattern from Phase 21)
    - .planning/phases/23-production-stats/23-VALIDATION.md (Wave 0 row 1-4: stale_count semantics, EXPLAIN, is_stale per row)
  </read_first>

  <files>src/store/shot-status-repo.ts, src/store/__tests__/shot-status-repo-stats.test.ts, src/store/__tests__/shot-status-repo-grid.test.ts</files>

  <behavior>
    - Test 1 (in shot-status-repo-stats.test.ts): `getSequenceStats(db, sequenceId)` on a sequence with 5 wip shots returns `{ total: 5, counts: { wip: 5, 'pending-review': 0, approved: 0, 'on-hold': 0, omit: 0 }, stale_count: 0 }`. All 5 ShotStatus keys MUST be present in counts even though GROUP BY emits only the non-empty `wip` bucket (initializer fills the rest with 0).
    - Test 2: Mixed-status fixture (1 wip + 1 pending-review + 1 approved + 1 on-hold + 1 omit) returns counts matching exactly, total=5, stale_count=0 (no completed versions yet).
    - Test 3: Shot with status='wip' AND a `versions` row at status='completed' with `completed_at = Date.now() - 30 * 86_400_000` (30 days ago) → `stale_count === 1` and that shot's per-row `is_stale === 1` from listShotsForGrid.
    - Test 4 (D-15 grace): Shot with status='wip' AND zero completed versions → `stale_count === 0` and that shot's per-row `is_stale === 0`. (EXISTS clause falls out → the shot is NOT counted as stale even though it's older than the cutoff in calendar terms.)
    - Test 5 (OVR-02 status filter): Shot with status='approved' AND a `versions` row > 14d old → `stale_count === 0` (only wip/pending-review qualify; approved/on-hold/omit are filtered out by the `IN ('wip','pending-review')` clause).
    - Test 6: Shot with status='wip' AND a completed version `completed_at = Date.now() - 5 * 86_400_000` (5 days ago, recent) → `stale_count === 0`, `is_stale === 0` per row.
    - Test 7 (EXPLAIN — Q1 GROUP BY counts): `EXPLAIN QUERY PLAN ${getSequenceStatsGroupBySqlText()}` plan rows mention `idx_shots_status` OR `USING INDEX` OR `SEARCH shots USING` — and DO NOT contain a bare `SCAN shots` line without a `USING` clause.
    - Test 8 (EXPLAIN — Q2 stale-count EXISTS): `EXPLAIN QUERY PLAN ${getSequenceStatsStaleSqlText()}` plan rows DO NOT contain `SCAN versions` (full-table) — the `EXISTS (SELECT 1 FROM versions v WHERE v.shot_id = shots.id ...)` MUST use the versions.shot_id autoindex (so plan should show `SEARCH versions USING ...`). The whitelist phrasing intentionally allows `CORRELATED LIST SUBQUERY` for the EXISTS clause (per RESEARCH Open Question 3 resolution — EXISTS short-circuits and surfaces as correlated by design).
    - Test 9 (in shot-status-repo-grid.test.ts EXTENSION — Phase 21 invariant preserved): The pre-existing EXPLAIN test on `listShotsForGridSqlText()` still holds. Specifically: plan rows do NOT contain `CORRELATED SCALAR SUBQUERY` referencing the `ranked` CTE. The new is_stale CASE introduces a correlated LIST subquery on `versions v2`; that one IS allowed.
  </behavior>

  <action>
    Two source-file changes, plus one new test file, plus one in-place test extension.

    **(A) Extend `src/store/shot-status-repo.ts`** — modify `ShotGridQueryRow` interface (line ~186) to add `is_stale: number` (SQLite returns 0|1 from CASE; the engine layer at Plan 02 will coerce to boolean). Modify `listShotsForGrid` body (line ~228-285) to extend the outer SELECT of the existing window-function CTE with one new CASE column. Per 23-PATTERNS.md §9 code excerpt — verbatim phrasing:

    ```
    CASE
      WHEN s.status IN ('wip','pending-review')
        AND r.completed_at IS NOT NULL
        AND r.completed_at < ${Date.now() - STALE_SHOT_DAYS * 86_400_000}
      THEN 1 ELSE 0
    END AS is_stale
    ```

    The cutoff value is computed in TypeScript at function entry (one local `const cutoff` declaration) and bound as a Drizzle `sql\`...${cutoff}...\`` placeholder. This avoids two cutoff computations per query.

    Then ADD two new exported functions BELOW `listShotsForGridSqlText` (after line ~323):

    1. `getSequenceStats(db, sequenceId)` returning `{ total: number; counts: Record<ShotStatus, number>; stale_count: number; }` — Q1 is a Drizzle `db.all(sql\`SELECT status, COUNT(*) AS c FROM shots WHERE sequence_id = ${sequenceId} GROUP BY status\`)` with TypeScript initializing all 5 ShotStatus keys to 0 then overwriting from the rows. Q2 is the EXISTS-clause `db.get(sql\`SELECT COUNT(*) AS c FROM shots WHERE shots.sequence_id = ${sequenceId} AND shots.status IN ('wip','pending-review') AND EXISTS (SELECT 1 FROM versions v WHERE v.shot_id = shots.id AND v.status = 'completed' AND v.completed_at IS NOT NULL AND v.completed_at < ${cutoff}))\`)` where `cutoff = Date.now() - STALE_SHOT_DAYS * 86_400_000`. Coerce `c` to `Number()` (SQLite returns numbers as bigint in some cases). Total derives in JS from `Σ counts` (saves one COUNT query).

    2. `getSequenceStatsGroupBySqlText(): string` and `getSequenceStatsStaleSqlText(): string` — return the EXACT raw SQL with `?` placeholders for the EXPLAIN tests. Placeholder order for stale: 1=sequenceId, 2=cutoff. Mirrors the existing `listShotsForGridSqlText()` precedent at line 301.

    DO NOT change the `STALE_SHOT_DAYS = 14` constant at line 54. DO NOT relocate any existing function. DO NOT change the existing CTE's `ranked` window function. The `lcv_completed_at` column already exists from Phase 21 — reuse it for the CASE.

    **(B) Create `src/store/__tests__/shot-status-repo-stats.test.ts`** — new file. Mirror the existing `shot-status-repo-grid.test.ts:1-50` fixture setup verbatim (in-memory DB via `makeInMemoryDb`, HierarchyRepo + VersionRepo wiring, beforeEach to seed workspace/project/sequence). Add describe blocks per the 8 behavior tests above. For the EXPLAIN test, use `testDb.sqlite.prepare('EXPLAIN QUERY PLAN ' + getSequenceStatsStaleSqlText()).all(sequenceId, cutoff)` — exactly 2 placeholders. For test 3, create a shot via `hierarchy.createShot(sequenceId, 'sh010')`, insert a version via `versionRepo.insertVersion(shot.id)`, then `testDb.sqlite.prepare("UPDATE versions SET status='completed', completed_at=? WHERE id=?").run(Date.now() - 30 * 86_400_000, ver.id)`.

    **(C) Extend `src/store/__tests__/shot-status-repo-grid.test.ts`** — add ONE new test in the existing EXPLAIN describe block at line ~88-109 that asserts the Phase 21 `CORRELATED SCALAR SUBQUERY` invariant on `listShotsForGridSqlText` still holds AFTER the is_stale CASE is added. Per 23-RESEARCH.md "Open Question 3 resolution" + 23-PATTERNS.md §12: the test must accept that the new CASE adds a CORRELATED LIST subquery on `versions v2` (EXISTS short-circuit by design) but reject any CORRELATED SCALAR subquery referencing the existing `ranked` CTE. Phrase as a filter: `planRows.filter(r => r.detail.includes('CORRELATED') && r.detail.includes('ranked'))` — assert empty.
  </action>

  <verify>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx vitest run src/store/__tests__/shot-status-repo-stats.test.ts src/store/__tests__/shot-status-repo-grid.test.ts --reporter=verbose 2>&1 | tail -50</automated>
  </verify>

  <acceptance_criteria>
    - Test pass: `npx vitest run src/store/__tests__/shot-status-repo-stats.test.ts` reports all 8+ tests green (counts correctness Tests 1-2, stale semantics Tests 3-6, EXPLAIN Tests 7-8)
    - Test pass: `npx vitest run src/store/__tests__/shot-status-repo-grid.test.ts` reports all existing tests still green PLUS the new CORRELATED-on-ranked invariant test green
    - Source assertion: `grep -c "^export function getSequenceStats" src/store/shot-status-repo.ts` outputs `1`
    - Source assertion: `grep -c "^export function getSequenceStatsStaleSqlText" src/store/shot-status-repo.ts` outputs `1`
    - Source assertion: `grep -c "^export function getSequenceStatsGroupBySqlText" src/store/shot-status-repo.ts` outputs `1`
    - Source assertion: `grep -c "is_stale: number" src/store/shot-status-repo.ts` outputs `1` (one new field on `ShotGridQueryRow`)
    - Source assertion: `grep "CASE" src/store/shot-status-repo.ts | grep -c "is_stale"` outputs `1` (the CASE expression mints the column)
    - Source assertion: `grep -c "STALE_SHOT_DAYS = 14" src/store/shot-status-repo.ts` outputs `1` (single source of truth at line 54 unchanged — no duplicate constant)
    - Source assertion: EXISTS clause uses `versions v` alias AND `v.status = 'completed'` AND `v.shot_id = shots.id` — verify via `grep "EXISTS" src/store/shot-status-repo.ts` shows the verbatim clause from 23-PATTERNS.md
    - Negative assertion: `grep "approved_pct" src/store/shot-status-repo.ts` returns ZERO matches (approved_pct lives in the engine layer per D-14, NOT the repo)
    - Behavior assertion (D-15 grace): The new test "shot with zero completed versions is NOT stale" passes
    - Phase 21 regression hold: `npx vitest run src/store/__tests__/shot-status-repo-grid.test.ts` exits 0 with no test count regression
  </acceptance_criteria>

  <done>
    Three artifacts: (1) repo functions exported and tested; (2) the new SQL test file enumerates 8 behavioral + EXPLAIN cases; (3) the Phase 21 EXPLAIN test extended with an invariant-preservation assertion. listShotsForGrid now emits a per-row is_stale: number (0|1 from CASE). Repo file unchanged below the new functions (existing surface preserved). Cutoff value is computed once per call. Phase 21 EXPLAIN test still passes — adding the new CASE does NOT introduce a CORRELATED SCALAR subquery on the `ranked` CTE.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → API | Untrusted input: `sequenceId` (path param) crosses here when Plan 02's `GET /api/sequences/:id/shot-grid` runs. This plan is repo-layer only — the path-param parsing happens in Plan 02. The boundary is named here so the threat is registered at the data-layer entry. |
| API → SQLite | Trusted process boundary inside the single Node.js server. Drizzle `sql\`\`` tagged templates parameterize all placeholders (verified pattern in shot-status-repo.ts:265-273). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering | `sequenceId` path-param flowing into `getSequenceStats(db, sequenceId)` SQL placeholder | mitigate | Drizzle's `sql\`\`` tagged template binds `${sequenceId}` as a `?` placeholder (parameterized query — proven safe for SQL injection per Phase 21's precedent at shot-status-repo.ts:265-273). The repo function itself does NOT validate the string format (per CLAUDE.md tool-engine separation — HTTP route + Zod validation upstream owns format; engine `getSequence(sequenceId)` lookup at Plan 02 throws `SEQUENCE_NOT_FOUND` for unknown IDs). The repo simply runs the parameterized query and trusts the runtime parameterization. |
| T-23-01-02 | Tampering | Cutoff timestamp computed from `Date.now()` in `getSequenceStats` | accept | `Date.now()` is the trusted local-process clock. A user CANNOT inject a cutoff value; it's computed inside the repo function. NTP drift / clock skew is out of scope for v1.3 (single-server deployment). |
| T-23-01-03 | Denial | GROUP BY query on a synthetic 10K-shot sequence | accept | The `idx_shots_status (sequence_id, status)` covering index from migration 0008 makes this O(N) over the index, no random I/O. Typical sequences are ≤ 100 shots; pathological 10K case still completes < 50ms per RESEARCH §"Security Domain". No additional mitigation needed at the repo layer; the existing covering index is the security control. |
| T-23-01-04 | Denial | EXISTS subquery scanning `versions` for stale_count on a sequence with many versions per shot | mitigate | The `EXISTS (SELECT 1 FROM versions v WHERE v.shot_id = shots.id ...)` clause short-circuits on the FIRST match per shot. SQLite's planner uses the versions PK autoindex on `(shot_id, version_number)` for the inner scan — verified via the EXPLAIN test in Test 8 above. Worst-case is O(N_shots) lookups, not O(N_versions × N_shots). |
| T-23-01-05 | Information Disclosure | Stats query reveals activity patterns (shot status distribution) | accept | v1.3 is single-user (the artist). No second party can observe stats traffic. Future multi-user phase would require per-user filtering on the stats query. Disposition reuses Phase 21/22 single-user precedent. |
| T-23-01-06 | Tampering | Hex values in theme.css | accept | Static CSS tokens — no runtime mutation. The values are LOCKED at the UI-SPEC layer and verified by greps in this plan (Task 01-02 acceptance criteria). |
| T-23-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 23 installs ZERO new packages (RESEARCH §"Package Legitimacy Audit"). No new install commands in any task. `lucide-preact` (used in Plan 02) is already locked at ^1.9.0 in packages/dashboard/package.json (Phase 21 install). No supply-chain risk added by this plan. |
</threat_model>

<verification>
After all 3 tasks complete:

1. Full server suite regression: `npx vitest run src/store/__tests__ --reporter=verbose` — all existing tests still green; new shot-status-repo-stats.test.ts green; shot-status-repo-grid.test.ts green with new invariant test.
2. Dashboard typecheck: `cd packages/dashboard && npx tsc --noEmit` — green.
3. Dashboard build: `cd packages/dashboard && npx vite build` — green (CSS parses; types compile).
4. Pitfall 6 check: `grep -c "^\s*--color-shot-stale" packages/dashboard/src/styles/theme.css` returns 2 (CRITICAL — both blocks).
5. Architecture-purity: `npx vitest run src/__tests__/architecture-purity.test.ts` — green (no new bindings added).
</verification>

<success_criteria>
- types/shot-grid.ts exports `SequenceStats` interface with exactly the 5 fields locked in D-02; `ShotGridRow` gains `is_stale: boolean`; `ShotGridResponse` gains `stats: SequenceStats`. Dashboard typecheck passes.
- theme.css contains `--color-shot-stale: #f97316` in the `@theme {}` dark block AND `--color-shot-stale: #b45309` in the `[data-theme="light"] {}` light block. Vite production build succeeds.
- shot-status-repo.ts exports `getSequenceStats(db, sequenceId)` returning the raw {total, counts (all 5 keys initialized), stale_count} shape per D-02 + D-14 + D-15; `getSequenceStatsStaleSqlText()` + `getSequenceStatsGroupBySqlText()` return verbatim SQL for EXPLAIN tests. `listShotsForGrid` extended with inline `is_stale` CASE column. STALE_SHOT_DAYS = 14 at line 54 unchanged.
- shot-status-repo-stats.test.ts (NEW) reports 8+ tests green: counts correctness, stale semantics (wip > 14d stale ✓; zero-version grace per D-15; approved/on-hold/omit filtered out; recent completion not stale), EXPLAIN plan uses idx_shots_status (no SCAN shots) and uses versions.shot_id autoindex (no SCAN versions).
- shot-status-repo-grid.test.ts (EXTENDED): Phase 21 EXPLAIN invariant still holds — no CORRELATED SCALAR on the `ranked` CTE. The new is_stale CASE introduces a CORRELATED LIST subquery on `versions v2` which is whitelisted (intentional EXISTS short-circuit).
- Threat register T-23-01-* dispositions intact: parameterized queries, no new bindings, no new installs.
</success_criteria>

<output>
Create `.planning/phases/23-production-stats/23-01-SUMMARY.md` documenting:
- The exact `SequenceStats` interface published (for Plan 02 consumers)
- The exact `getSequenceStats` function signature published (for Plan 02 engine composition)
- The EXPLAIN test outcomes (with the actual plan rows captured if surprising)
- Pitfall 6 verification (both theme.css blocks patched — line numbers of the two declarations)
- Phase 21 regression status (existing tests green; new invariant test green)
- The D-15 grace period implementation note (zero-version shots NOT stale — EXISTS clause falls out) so Plan 03's client-side `recomputeIsStaleClient` matches server behavior
</output>
