---
phase: 23-production-stats
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/engine/pipeline.ts
  - src/http/__tests__/dashboard-routes-shot-grid.test.ts
  - packages/dashboard/src/components/ProgressBar.tsx
  - packages/dashboard/src/components/__tests__/ProgressBar.test.tsx
  - packages/dashboard/src/lib/copy.ts
autonomous: true
requirements: [OVR-01, OVR-02]
must_haves:
  truths:
    - "D-01: engine.listShotGrid returns the existing payload PLUS top-level stats: SequenceStats AND per-row is_stale: boolean (coerced from SQLite 0|1 to true|false); single endpoint with both top-level fields (D-13 Option C) — explicit whole-sequence semantics for stats; HTTP route signature unchanged"
    - "D-02: SequenceStats envelope shape returned by engine: { total, approved_pct, counts: Record<ShotStatus, number>, pending_review_backlog, stale_count } — LOCKED 5-field shape; pending_review_backlog emitted alongside counts['pending-review'] for component data-source independence"
    - "D-05: ProgressBar visual treatment — horizontal bar fills left→right to approved_pct with numeric label; backlog renders as distinct callout pill (`! N awaiting review`) in accent color separate from Phase 21 mini-pills; stale count renders inline next to backlog when >0, hidden entirely when zero"
    - "D-06: <ProgressBar/> component renders <div role=\"progressbar\" aria-valuenow={clamped} aria-valuemin=0 aria-valuemax=100 aria-label={ariaLabel}> with a fill child whose width is the clamped integer percentage; bar fill reuses --color-shot-status-approved; track uses --color-border; WCAG 2.1 AA — text label ≥4.5:1, UI components ≥3:1"
    - "D-14: approved_pct is computed in TypeScript (NOT SQL) via Math.round((counts.approved/total)*100), with total === 0 → 0 (no NaN)"
    - "D-18: Tool budget still === 7 (Phase 23 adds zero server.registerTool calls — verified by existing tool-budget.test.ts running green)"
    - "D-20: Backlog callout uses lucide-preact AlertCircle icon; hidden when pending_review_backlog === 0 (same hide-on-zero rule as Phase 21 mini-pills at SequenceHeader.tsx:99)"
    - "D-21: Animation discipline — no mount/unmount animations; progress bar width transition ≤150ms with motion-reduce:transition-none Tailwind variant honoring prefers-reduced-motion"
    - "GET /api/sequences/:id/shot-grid HTTP response envelope test asserts the body.stats top-level field AND every row's is_stale field — verified via the existing FakeEngine harness"
    - "9 Phase 23 copy constants are exported from lib/copy.ts (STATS_PROGRESS_ARIA_PREFIX, STATS_APPROVED_LABEL_SUFFIX, STATS_BACKLOG_CALLOUT_SINGULAR, STATS_BACKLOG_CALLOUT_PLURAL, STATS_BACKLOG_CALLOUT_ARIA_PREFIX, STATS_STALE_INLINE_SINGULAR, STATS_STALE_INLINE_PLURAL, STATS_STALE_INLINE_ARIA_PREFIX, SHOT_CARD_STALE_ARIA_SUFFIX) with verbatim UI-SPEC values"
    - "TypedError('SEQUENCE_NOT_FOUND') still throws for unknown sequenceId (pre-existing Phase 21 behavior preserved through the new compose layer)"
  artifacts:
    - path: "src/engine/pipeline.ts"
      provides: "engine.listShotGrid extended to compose getSequenceStats + listShotsForGrid into a single envelope"
      contains: "getSequenceStats"
    - path: "src/http/__tests__/dashboard-routes-shot-grid.test.ts"
      provides: "Two new tests asserting body.stats envelope field AND body.shots[i].is_stale field"
      contains: "is_stale"
    - path: "packages/dashboard/src/components/ProgressBar.tsx"
      provides: "WCAG 2.1 AA progressbar primitive with required aria-* attributes"
      contains: "role=\"progressbar\""
    - path: "packages/dashboard/src/components/__tests__/ProgressBar.test.tsx"
      provides: "ProgressBar a11y + clamp + motion-reduce tests"
      contains: "aria-valuenow"
    - path: "packages/dashboard/src/lib/copy.ts"
      provides: "9 new Phase 23 copy constants for the stats subrow and stale-aria-suffix"
      contains: "STATS_PROGRESS_ARIA_PREFIX"
  key_links:
    - from: "src/engine/pipeline.ts:listShotGrid"
      to: "src/store/shot-status-repo.ts:getSequenceStats (Plan 23-01)"
      via: "import + call inside the existing facade body"
      pattern: "getSequenceStats\\(this\\.db, sequenceId\\)"
    - from: "src/engine/pipeline.ts:listShotGrid"
      to: "src/store/shot-status-repo.ts:listShotsForGrid (existing Phase 21)"
      via: "items.map row reshape — Boolean(r.is_stale) coercion AND inline approved_pct math AND envelope merge"
      pattern: "Boolean\\(r\\.is_stale\\)"
    - from: "src/http/dashboard-routes.ts (unchanged)"
      to: "engine.listShotGrid response"
      via: "c.json passthrough — Hono auto-serializes the new envelope shape"
      pattern: "c\\.json\\(engine\\.listShotGrid"
    - from: "packages/dashboard/src/components/ProgressBar.tsx"
      to: "theme.css --color-shot-status-approved (Phase 21) + --color-border (existing)"
      via: "CSS variable references in className"
      pattern: "var\\(--color-shot-status-approved\\)"
---

<objective>
Phase 23 Wave 2 — server composition + HTTP envelope test + UI primitives + copy. Three deliverables wire the new repo function into the production HTTP path, prove the wire shape via integration test, and lay the dashboard primitives (`<ProgressBar/>` + 9 copy constants) that Plan 23-03 composes into the `<SequenceHeader/>` stats subrow.

Purpose: Bridge Wave 1's data layer to Wave 3's view layer with the smallest possible coupling — the engine composition (extends an existing facade, no new tools, no new routes), the HTTP envelope assertion (extends an existing test harness in place), the `<ProgressBar/>` primitive (one new file, mirrors `<WarningPill/>` shape), and the copy block (append-only at end of file). All four artifacts are independent enough that the executor can interleave but should land in commit order types→repo→engine→http→ui→copy for clean review.

Output: 2 modified server files (one of them the engine facade), 1 extended HTTP test file, 2 NEW dashboard files (ProgressBar + its test), 1 modified copy file. Zero new MCP tools, zero new HTTP routes, zero new third-party dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-production-stats/23-CONTEXT.md
@.planning/phases/23-production-stats/23-RESEARCH.md
@.planning/phases/23-production-stats/23-PATTERNS.md
@.planning/phases/23-production-stats/23-UI-SPEC.md
@.planning/phases/23-production-stats/23-01-PLAN.md

<!-- Existing files this plan modifies / reads patterns from -->
@src/engine/pipeline.ts
@src/http/__tests__/dashboard-routes-shot-grid.test.ts
@src/http/dashboard-routes.ts
@packages/dashboard/src/components/WarningPill.tsx
@packages/dashboard/src/lib/copy.ts

<interfaces>
<!-- Plan 23-01 publishes these contracts (typecheck this plan against them after 23-01 lands): -->

From src/store/shot-status-repo.ts (Plan 23-01):
  export function getSequenceStats(db: Db, sequenceId: string): { total: number; counts: Record<ShotStatus, number>; stale_count: number; };
  export function getSequenceStatsGroupBySqlText(): string;
  export function getSequenceStatsStaleSqlText(): string;
  ShotGridQueryRow gains `is_stale: number` (0|1 from SQLite CASE).

From packages/dashboard/src/types/shot-grid.ts (Plan 23-01):
  export interface SequenceStats { total: number; approved_pct: number; counts: Record<ShotStatus, number>; pending_review_backlog: number; stale_count: number; }
  ShotGridRow gains `is_stale: boolean`.
  ShotGridResponse gains `stats: SequenceStats`.

From packages/dashboard/src/styles/theme.css (Plan 23-01):
  --color-shot-stale: #f97316 (dark @theme block); --color-shot-stale: #b45309 (light [data-theme="light"] block).
  Plus existing Phase 21 tokens: --color-shot-status-approved (the ProgressBar fill), --color-border (the ProgressBar track), --color-fg-muted (the trailing label text), --color-bg (the inverse text on accent bg).

From src/engine/pipeline.ts (existing Phase 21):
  listShotGrid(sequenceId, opts): { sequence; shots: Array<{ id; name; status; version_count; latest_completed_version: {...} | null; }>; next_cursor; total_count; }  // line 822-874 — extend with stats AND is_stale per row
  Imports `listShotsForGrid` from '../store/shot-status-repo.js' — add `getSequenceStats` import here.

From src/http/__tests__/dashboard-routes-shot-grid.test.ts (existing Phase 21):
  FakeEngine harness with `engine.calls.push` and `engine.listShotGrid = ((seqId, opts) => {...}) as never`. EMPTY_GRID_RESPONSE fixture at lines ~48-63.

From packages/dashboard/src/components/WarningPill.tsx (Phase 18 — visual analog for the new ProgressBar primitive):
  export interface WarningPillProps { ... };
  export function WarningPill({ ... }: WarningPillProps) { return <div class="inline-flex items-center gap-1 ..." role="status">...</div>; }
  (Pattern: small, pure, named-export function, no callbacks, Tailwind utility classes, `class=` Preact convention.)

From packages/dashboard/src/lib/copy.ts (existing — Phase 22 block at lines 314-398):
  Block headers at lines 186 (Phase 21) and 314 (Phase 22) use `// ================================================================` rule lines + `// Phase NN — ...` section comments. Phase 23 appends at end of file (after line ~398).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-01: Compose engine.listShotGrid with stats (D-01 + D-14) + extend HTTP envelope tests</name>

  <read_first>
    - src/engine/pipeline.ts (the file being modified — focus lines 822-874 for listShotGrid; line 27 for TypedError import location; lines around 800-820 for existing import block from '../store/shot-status-repo.js')
    - src/http/__tests__/dashboard-routes-shot-grid.test.ts (the file being extended — read in full; existing FakeEngine override pattern at lines 65-99; EMPTY_GRID_RESPONSE fixture at lines 48-63)
    - src/http/dashboard-routes.ts (line 370-375 — confirm the thin handler `return c.json(engine.listShotGrid(...))` is unchanged after this task)
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-01 single endpoint both top-level; D-13 Option C; D-14 approved_pct in TypeScript)
    - .planning/phases/23-production-stats/23-RESEARCH.md §"Pattern 1" + §"Code Example 3" (verbatim facade extension shape)
    - .planning/phases/23-production-stats/23-PATTERNS.md §10 "pipeline.ts" + §11 "dashboard-routes.ts" + §16 "dashboard-routes-shot-grid.test.ts"
    - .planning/phases/23-production-stats/23-VALIDATION.md (Wave 0 row 5-6: envelope assertions + engine compose)
  </read_first>

  <files>src/engine/pipeline.ts, src/http/__tests__/dashboard-routes-shot-grid.test.ts</files>

  <behavior>
    - Test 1 (engine — via the HTTP test surface): With a stubbed `getSequenceStats` returning `{ total: 10, counts: { wip: 4, 'pending-review': 0, approved: 6, 'on-hold': 0, omit: 0 }, stale_count: 2 }` and a stubbed `listShotsForGrid` returning empty items, `engine.listShotGrid` returns envelope `{ sequence, shots: [], stats: { total: 10, approved_pct: 60, counts: {...}, pending_review_backlog: 0, stale_count: 2 }, next_cursor: null, total_count: 0 }`. Note: `approved_pct === 60` proves the TypeScript arithmetic (`Math.round((6/10)*100)`).
    - Test 2 (engine — divide-by-zero guard): With stubbed `getSequenceStats` returning `{ total: 0, counts: {...all zero}, stale_count: 0 }`, `engine.listShotGrid` returns `stats.approved_pct === 0` (NOT NaN).
    - Test 3 (engine — is_stale boolean coercion): With stubbed `listShotsForGrid` returning `[{ ..., is_stale: 1 }]`, `engine.listShotGrid`'s envelope contains `shots[0].is_stale === true` (the row goes through `Boolean(r.is_stale)` coercion).
    - Test 4 (HTTP — stats envelope field present): `GET /api/sequences/seq_1/shot-grid` returns 200 with `body.stats` populated per the stubbed engine return. JSON serialization roundtrips snake_case fields verbatim (no camelCase conversion).
    - Test 5 (HTTP — is_stale per row): Each row in `body.shots[]` has a `is_stale: boolean` field. Boolean serializes as JSON `true` / `false` (not `1` / `0`).
    - Test 6 (HTTP — SEQUENCE_NOT_FOUND propagation): `engine.listShotGrid` throwing `TypedError('SEQUENCE_NOT_FOUND', ...)` produces the existing 404 response (Phase 21 behavior preserved through the new compose code).
    - Test 7 (HTTP — pre-existing tests unchanged): All Phase 21 + 22 tests in this harness file still pass with the extended FakeEngine fixture (the existing tests' fixtures must add `stats` + `is_stale` to satisfy the new required fields, but their assertions are unchanged otherwise).
  </behavior>

  <action>
    Two surgical edits.

    **(A) Extend `src/engine/pipeline.ts` `listShotGrid` (lines ~822-874)** per 23-RESEARCH.md §"Code Example 3" + 23-PATTERNS.md §10 verbatim shape:

    1. Add `getSequenceStats` to the existing `import { listShotsForGrid, ... } from '../store/shot-status-repo.js'` block (named import — same module as the existing `listShotsForGrid` import).

    2. Inside the `listShotGrid` body, AFTER the existing `const { items, next_cursor, total_count } = listShotsForGrid(this.db, sequenceId, opts)` call: add `const rawStats = getSequenceStats(this.db, sequenceId);`.

    3. Compute `const approved = rawStats.counts.approved` (guaranteed present per Plan 23-01's initializer); compute `const approved_pct = rawStats.total === 0 ? 0 : Math.round((approved / rawStats.total) * 100);`.

    4. Assemble `const stats: SequenceStats = { total: rawStats.total, approved_pct, counts: rawStats.counts, pending_review_backlog: rawStats.counts['pending-review'], stale_count: rawStats.stale_count, };` — note bracket access for the hyphenated key per Pitfall 10.

    5. Inside the existing `items.map((r) => ({ ... }))` row reshape: add `is_stale: Boolean(r.is_stale),` — converts the SQLite 0|1 number to a real boolean.

    6. Update the return statement to include `stats` between `shots` and `next_cursor`: `return { sequence: { id: sequence.id, name: sequence.name }, shots, stats, next_cursor, total_count };`.

    7. Update the TypeScript return type annotation on `listShotGrid(...)`: add `is_stale: boolean;` to the inline shot row shape AND `stats: SequenceStats;` to the envelope shape. Import `SequenceStats` from the dashboard types module — verify the existing cross-tree import precedent in `src/engine/pipeline.ts` before deciding the import path (if the existing precedent allows server→dashboard type imports, use `import type { SequenceStats } from '../../packages/dashboard/src/types/shot-grid.js';`; if architecture-purity forbids cross-tree imports, inline the shape in the return type annotation and document the duplication with a comment). Verify via `grep "packages/dashboard" src/engine/pipeline.ts` BEFORE making this decision; if the grep returns ZERO matches, you MUST inline (cross-tree imports are forbidden by the existing architecture). Document the inlined shape with a JSDoc comment `// Inlined mirror of SequenceStats from packages/dashboard/src/types/shot-grid.ts — keep in sync.`

    DO NOT change the existing `TypedError('SEQUENCE_NOT_FOUND', ...)` throw at line ~843 (must propagate through unchanged). DO NOT change the existing `listShotsForGrid` signature or call shape. DO NOT add new imports beyond `getSequenceStats` + optionally `SequenceStats`.

    **(B) Extend `src/http/__tests__/dashboard-routes-shot-grid.test.ts`** per 23-PATTERNS.md §16 verbatim FakeEngine override + EMPTY_GRID_RESPONSE pattern.

    1. Update the EMPTY_GRID_RESPONSE fixture (around lines 48-63): add `is_stale: false` to the shots array element type AND add a top-level `stats: { total: 0, approved_pct: 0, counts: { wip: 0, 'pending-review': 0, approved: 0, 'on-hold': 0, omit: 0 }, pending_review_backlog: 0, stale_count: 0, }` field. All EXISTING tests in this file remain green because the empty stats fixture is a valid envelope.

    2. Add ONE new `it('response envelope includes top-level stats field (Phase 23 OVR-01)')` block per 23-PATTERNS.md §16: stub `engine.listShotGrid` to return the EMPTY_GRID_RESPONSE plus override `stats: { total: 10, approved_pct: 60, counts: { wip: 4, 'pending-review': 0, approved: 6, 'on-hold': 0, omit: 0 }, pending_review_backlog: 0, stale_count: 2 }`. Build the Hono app, make the request, assert `res.status === 200`, `body.stats === { ...stubbed value }` (use `toEqual` for deep equality).

    3. Add ONE new `it('shot rows include per-row is_stale field (Phase 23 OVR-02)')` block: stub `engine.listShotGrid` to return a shots array with one element `{ id: 'shot_1', name: 'sh010', status: 'wip', version_count: 1, is_stale: true, latest_completed_version: { id: 'ver_old', thumbnail_url: '/api/versions/ver_old/thumbnail', completed_at: Date.now() - 30 * 86_400_000 } }`. Assert `body.shots[0].is_stale === true` (boolean, NOT 1).

    DO NOT change `src/http/dashboard-routes.ts`. The route is `app.get('/api/sequences/:id/shot-grid', (c) => c.json(engine.listShotGrid(sequenceId, { cursor, limit })))` — Hono auto-serializes the wider response shape; no code change to the handler.
  </action>

  <verify>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx vitest run src/http/__tests__/dashboard-routes-shot-grid.test.ts --reporter=verbose 2>&1 | tail -40</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - Test pass: `npx vitest run src/http/__tests__/dashboard-routes-shot-grid.test.ts` reports ALL tests green (existing tests + the 2 new Phase 23 tests; total count = previous count + 2)
    - Source assertion: `grep -c "^import.*getSequenceStats" src/engine/pipeline.ts` outputs `>= 1` (in the existing shot-status-repo.js import block)
    - Source assertion: `grep -c "Boolean(r.is_stale)" src/engine/pipeline.ts` outputs `1` (single occurrence in the items.map row reshape)
    - Source assertion: `grep -c "rawStats.counts\['pending-review'\]" src/engine/pipeline.ts` outputs `1` (Pitfall 10 — bracket access for hyphenated key)
    - Source assertion: `grep -c "Math.round" src/engine/pipeline.ts | xargs -I{} test {} -ge 1` — at least one `Math.round` in the file (the approved_pct computation; existing file may have other Math.rounds — at minimum one new one)
    - Behavior assertion: `grep "rawStats.total === 0 ? 0 :" src/engine/pipeline.ts` matches one line (D-14 divide-by-zero guard)
    - Negative assertion: `grep "approved_pct.*FROM\|CAST.*FLOAT" src/engine/pipeline.ts` returns ZERO matches (the approved_pct logic lives in TypeScript NOT SQL per D-14)
    - Typecheck assertion: `npx tsc --noEmit` exits 0 (server tree typecheck)
    - Route signature unchanged: `git diff src/http/dashboard-routes.ts` shows no changes to the file in this task (the thin handler passes through Hono auto-serialization)
    - Tool budget unchanged: `npx vitest run src/__tests__/tool-budget.test.ts` still green
    - Architecture purity unchanged: `npx vitest run src/__tests__/architecture-purity.test.ts` still green (no new native bindings; no forbidden cross-tree imports)
  </acceptance_criteria>

  <done>
    Engine composes `getSequenceStats` + `listShotsForGrid` into the same envelope. Approved_pct is computed in TypeScript with divide-by-zero guard. Per-row is_stale is coerced from SQLite int to JS boolean. HTTP route is byte-identical (passthrough). HTTP test harness covers the two new envelope fields with deterministic assertions. SEQUENCE_NOT_FOUND propagation unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-02: Build <ProgressBar/> primitive + tests (WCAG 2.1 AA, defensive clamp, motion-reduce)</name>

  <read_first>
    - packages/dashboard/src/components/WarningPill.tsx (the closest analog per 23-PATTERNS.md §1; pure component with rounded outer + saturated inner + Tailwind utility class string; the structural template for the new primitive)
    - packages/dashboard/src/components/__tests__/WarningPill.test.tsx (if exists) OR packages/dashboard/src/__tests__/WarningPill.test.tsx (Phase 18 component test harness using @testing-library/preact)
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-06 a11y attributes; D-21 animation discipline)
    - .planning/phases/23-production-stats/23-RESEARCH.md §"Pattern 3" + §"Don't Hand-Roll" row "Progress bar primitive"
    - .planning/phases/23-production-stats/23-UI-SPEC.md §"Component Inventory > New files" (ProgressBar.tsx full spec) + §"Color > Subrow color element matrix" (track + fill + label colors)
    - .planning/phases/23-production-stats/23-PATTERNS.md §1 "ProgressBar.tsx" (verbatim component shape lines 30-95)
    - .planning/phases/23-production-stats/23-VALIDATION.md (Wave 0 row 7-8: progress bar a11y, reduced motion)
  </read_first>

  <files>packages/dashboard/src/components/ProgressBar.tsx, packages/dashboard/src/components/__tests__/ProgressBar.test.tsx</files>

  <behavior>
    - Test 1: Render `<ProgressBar value={60} ariaLabel="Approval progress for SEQ_010" />`. Query for `[role="progressbar"]`. Assert `aria-valuenow === '60'`, `aria-valuemin === '0'`, `aria-valuemax === '100'`, `aria-label === 'Approval progress for SEQ_010'`.
    - Test 2 (clamp — over 100): `<ProgressBar value={120} ariaLabel="..." />`. Assert `aria-valuenow === '100'` (clamped to max).
    - Test 3 (clamp — under 0): `<ProgressBar value={-5} ariaLabel="..." />`. Assert `aria-valuenow === '0'` (clamped to 0).
    - Test 4 (clamp — float): `<ProgressBar value={42.7} ariaLabel="..." />`. Assert `aria-valuenow === '43'` (rounded).
    - Test 5 (clamp — custom max): `<ProgressBar value={50} max={200} ariaLabel="..." />`. Assert `aria-valuenow === '50'` and `aria-valuemax === '200'`, and the visible fill div's style.width equals `25%` (50/200 → 25%).
    - Test 6 (optional label): `<ProgressBar value={60} label="60% approved" ariaLabel="..." />` renders a `<span>` whose text is `60% approved`. When `label` prop is omitted, no label span is rendered.
    - Test 7 (motion-reduce): Snapshot the className on the fill div. Assert the string contains `motion-reduce:transition-none` (Tailwind v4 variant — D-21 + A6 compliance).
    - Test 8 (theme tokens): The track div's className contains `bg-[var(--color-border)]` and the fill div's className contains `bg-[var(--color-shot-status-approved)]` (verifies D-06 + UI-SPEC §"Subrow color element matrix").
  </behavior>

  <action>
    **(A) Create `packages/dashboard/src/components/ProgressBar.tsx`** per 23-PATTERNS.md §1 verbatim shape + 23-RESEARCH.md §"Pattern 3" + 23-UI-SPEC.md component spec.

    File header JSDoc must mention: (i) WCAG 2.1 AA progressbar primitive, (ii) Phase 23 D-06 a11y attributes, (iii) Pure component (props-in, no callbacks, no state) — mirroring WarningPill.tsx:1-20 style, (iv) SECURITY note: dynamic content (ariaLabel, label) flows as JSX text children → Preact auto-escapes (T-5-06 carry-forward).

    Component interface:
    ```
    export interface ProgressBarProps {
      value: number;              // 0-max range; clamped + integer-rounded at render
      max?: number;                // default 100
      label?: string;              // optional visible label rendered next to the bar
      ariaLabel: string;           // REQUIRED a11y label
    }
    ```

    Component body:
    1. Compute `const clamped = Math.max(0, Math.min(max, Math.round(value)));`.
    2. Compute `const widthPct = max === 0 ? '0%' : \`${(clamped / max) * 100}%\`;`.
    3. Return JSX: an outer `<div class="inline-flex items-center gap-1">` containing (a) an inner `<div role="progressbar" aria-valuenow={clamped} aria-valuemin={0} aria-valuemax={max} aria-label={ariaLabel} class="relative h-2 w-32 overflow-hidden rounded bg-[var(--color-border)]">` with a child `<div class="h-full bg-[var(--color-shot-status-approved)] transition-[width] duration-150 motion-reduce:transition-none" style={{ width: widthPct }} />`, and (b) `{label ? <span class="num text-xs text-[var(--color-fg-muted)]">{label}</span> : null}`.

    Use Preact convention `class=` (NOT `className=`). The default `max = 100` is set in the destructuring. `Math.round` is called BEFORE clamp so floats are integer-coerced first.

    DO NOT add `transition-all` or any animation other than the locked `transition-[width] duration-150 motion-reduce:transition-none`. DO NOT add focus styles (the bar is not focusable per UI-SPEC §"Keyboard accessibility"). DO NOT add `onClick`. DO NOT use `lucide-preact` icons (Plan 03's `<SequenceHeader/>` adds the AlertCircle around the bar in the subrow).

    **(B) Create `packages/dashboard/src/components/__tests__/ProgressBar.test.tsx`** per WarningPill.test.tsx analog (component-render harness using @testing-library/preact).

    Imports: `import { describe, it, expect, afterEach } from 'vitest'; import { render, cleanup } from '@testing-library/preact'; import { ProgressBar } from '../ProgressBar.js';`.

    Wire `afterEach(() => cleanup())`. Add 8 describe-block tests per the behavior list above. Test 7 specifically: `const { container } = render(<ProgressBar value={60} ariaLabel="x" />); const fill = container.querySelector('[role="progressbar"] > div'); expect(fill?.className).toContain('motion-reduce:transition-none');`. Test 5 specifically: `expect(fill?.getAttribute('style')).toContain('width: 25%');` (Preact serializes style obj to string).

    DO NOT add a snapshot test (UI-SPEC §"Animation & Motion" does not mandate snapshots; behavior assertions are sufficient and avoid Tailwind class-order churn).
  </action>

  <verify>
    <automated>cd /Users/macapple/comfyui-vfx-mcp/packages/dashboard && npx vitest run src/components/__tests__/ProgressBar.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx tsc --noEmit -p packages/dashboard 2>&1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - Test pass: `cd packages/dashboard && npx vitest run src/components/__tests__/ProgressBar.test.tsx` reports 8 tests green
    - Source assertion: `grep -c "role=\"progressbar\"" packages/dashboard/src/components/ProgressBar.tsx` outputs `1`
    - Source assertion: `grep -c "aria-valuenow={clamped}" packages/dashboard/src/components/ProgressBar.tsx` outputs `1`
    - Source assertion: `grep -c "aria-valuemin={0}" packages/dashboard/src/components/ProgressBar.tsx` outputs `1`
    - Source assertion: `grep -c "aria-valuemax={max}" packages/dashboard/src/components/ProgressBar.tsx` outputs `1`
    - Source assertion: `grep -c "Math.max(0, Math.min(max, Math.round(value)))" packages/dashboard/src/components/ProgressBar.tsx` outputs `1` (verbatim defensive-clamp expression)
    - Source assertion: `grep -c "motion-reduce:transition-none" packages/dashboard/src/components/ProgressBar.tsx` outputs `1` (D-21 + A6 — prefers-reduced-motion compliance)
    - Source assertion: `grep -c "bg-\[var(--color-shot-status-approved)\]" packages/dashboard/src/components/ProgressBar.tsx` outputs `1` (D-06 fill color)
    - Source assertion: `grep -c "bg-\[var(--color-border)\]" packages/dashboard/src/components/ProgressBar.tsx` outputs `1` (track color)
    - Negative assertion: `grep "onClick\|onFocus\|tabIndex" packages/dashboard/src/components/ProgressBar.tsx` returns ZERO matches (bar is not interactive)
    - Negative assertion: `grep "className=" packages/dashboard/src/components/ProgressBar.tsx` returns ZERO matches (Preact convention is `class=`)
    - Typecheck assertion: `cd packages/dashboard && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    `<ProgressBar/>` is a WCAG 2.1 AA-compliant primitive with the full aria-* attribute set, defensive clamp + integer coerce, two-element layout (bar + optional label), CSS-only width transition that honors `prefers-reduced-motion`. Tests cover clamp boundaries (over, under, float, custom max), label conditional render, motion-reduce variant, and theme-token references. Zero new third-party deps.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-03: Append Phase 23 copy block (9 constants per UI-SPEC verbatim values)</name>

  <read_first>
    - packages/dashboard/src/lib/copy.ts (the file being modified — read in full; locate the Phase 22 block at lines 314-398 ending with `RESTORE_NOTE_SYSTEM_TEXT` at line 398; new Phase 23 block appends after this)
    - .planning/phases/23-production-stats/23-UI-SPEC.md §"Copywriting Contract" (full verbatim values for all 9 constants)
    - .planning/phases/23-production-stats/23-PATTERNS.md §7 "copy.ts" (block-append pattern with section comment header; singular/plural pair pattern from Phase 21 SHOT_CARD_VERSION_COUNT_SINGULAR/PLURAL_SUFFIX)
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-20 backlog hide-on-zero — drives the singular/plural existence but values are identical)
  </read_first>

  <files>packages/dashboard/src/lib/copy.ts</files>

  <behavior>
    - Test 1: `import * as copy from 'packages/dashboard/src/lib/copy.js'` — the 9 named exports below are present:
      `STATS_PROGRESS_ARIA_PREFIX`, `STATS_APPROVED_LABEL_SUFFIX`, `STATS_BACKLOG_CALLOUT_SINGULAR`, `STATS_BACKLOG_CALLOUT_PLURAL`, `STATS_BACKLOG_CALLOUT_ARIA_PREFIX`, `STATS_STALE_INLINE_SINGULAR`, `STATS_STALE_INLINE_PLURAL`, `STATS_STALE_INLINE_ARIA_PREFIX`, `SHOT_CARD_STALE_ARIA_SUFFIX`.
    - Test 2: Each constant has the VERBATIM value from UI-SPEC §"Copywriting Contract":
      - `STATS_PROGRESS_ARIA_PREFIX === 'Approval progress for '`
      - `STATS_APPROVED_LABEL_SUFFIX === '% approved'`
      - `STATS_BACKLOG_CALLOUT_SINGULAR === 'awaiting review'`
      - `STATS_BACKLOG_CALLOUT_PLURAL === 'awaiting review'`
      - `STATS_BACKLOG_CALLOUT_ARIA_PREFIX === 'Pending review backlog: '`
      - `STATS_STALE_INLINE_SINGULAR === 'stale'`
      - `STATS_STALE_INLINE_PLURAL === 'stale'`
      - `STATS_STALE_INLINE_ARIA_PREFIX === 'Stale shots: '`
      - `SHOT_CARD_STALE_ARIA_SUFFIX === ' — stale'` (leading space + em-dash + space + "stale")
    - Test 3: Total exported constant count in copy.ts grows by exactly 9 (or 8 if Phase 22 already shipped `SHOT_CARD_STALE_ARIA_SUFFIX` — verify the Phase 22 block does NOT contain a duplicate first).
    - Test 4: No existing Phase 1-22 exports are mutated, removed, or renamed (additive-only).
  </behavior>

  <action>
    Append a Phase 23 block at the end of `packages/dashboard/src/lib/copy.ts` (after the existing `RESTORE_NOTE_SYSTEM_TEXT` at line ~398) per 23-PATTERNS.md §7 block-append pattern.

    Block header (mirror Phase 22 block at line 314 verbatim):

    ```
    // ================================================================
    // Phase 23 — production stats copy
    // (UI-SPEC §"Copywriting Contract" — verbatim named-constant exports)
    //
    // All Phase 23 surfaces — <ProgressBar/>, the new stats subrow inside
    // <SequenceHeader/>, and the new stale aria-suffix on <ShotGridCard/> —
    // import from this block. Zero inline string literals in component files
    // (architectural rule).
    // ================================================================
    ```

    Then 4 sub-section dividers `// ---------- ProgressBar (% approved) ----------`, `// ---------- Backlog callout (pending-review) ----------`, `// ---------- Inline stale count ----------`, `// ---------- Per-shot stale indicator ARIA suffix ----------`.

    Per UI-SPEC §"Copywriting Contract" exact values:

    ```
    export const STATS_PROGRESS_ARIA_PREFIX = 'Approval progress for ';
    export const STATS_APPROVED_LABEL_SUFFIX = '% approved';

    export const STATS_BACKLOG_CALLOUT_SINGULAR = 'awaiting review';
    export const STATS_BACKLOG_CALLOUT_PLURAL = 'awaiting review';
    export const STATS_BACKLOG_CALLOUT_ARIA_PREFIX = 'Pending review backlog: ';

    export const STATS_STALE_INLINE_SINGULAR = 'stale';
    export const STATS_STALE_INLINE_PLURAL = 'stale';
    export const STATS_STALE_INLINE_ARIA_PREFIX = 'Stale shots: ';

    export const SHOT_CARD_STALE_ARIA_SUFFIX = ' — stale';
    ```

    Note: `SHOT_CARD_STALE_ARIA_SUFFIX` uses an em-dash (U+2014) with a leading space + trailing word — UI-SPEC line 230 spells this exactly. The value is `' — stale'` (3 spaces: one before em-dash, one after, then "stale"). Verify the em-dash codepoint by `python3 -c "import sys; print([hex(ord(c)) for c in ' — stale'])"` if uncertain — should show `0x20, 0x2014, 0x20, 0x73, 0x74, 0x61, 0x6c, 0x65`.

    Each constant gets a one-line JSDoc above it per UI-SPEC §"Copywriting Contract" usage notes (e.g., `/** Caller concatenates: ${PREFIX}${sequenceName} for aria-label */`). The 4 prefix/suffix-style constants explicitly note "Caller concatenates: ..." per 23-PATTERNS.md §7 lines 471-496.

    DO NOT modify any existing constant. DO NOT add `--color-stats-backlog-callout` (DEFERRED). DO NOT abbreviate the section comments. The singular/plural pairs MAY have identical values (English: "1 awaiting" = "3 awaiting" verb-noun phrasing; kept as 2 constants for future i18n and to mirror Phase 21 `SHOT_CARD_VERSION_COUNT_SINGULAR/PLURAL_SUFFIX` precedent).
  </action>

  <verify>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && grep -c "^export const STATS_\|^export const SHOT_CARD_STALE_ARIA_SUFFIX" packages/dashboard/src/lib/copy.ts</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx tsc --noEmit -p packages/dashboard 2>&1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "^export const STATS_\|^export const SHOT_CARD_STALE_ARIA_SUFFIX" packages/dashboard/src/lib/copy.ts` outputs `9` (exactly the 9 new constants)
    - Source assertion (verbatim values): `grep "^export const STATS_PROGRESS_ARIA_PREFIX" packages/dashboard/src/lib/copy.ts` matches `= 'Approval progress for ';`
    - Source assertion: `grep "^export const STATS_APPROVED_LABEL_SUFFIX" packages/dashboard/src/lib/copy.ts` matches `= '% approved';`
    - Source assertion: `grep "^export const SHOT_CARD_STALE_ARIA_SUFFIX" packages/dashboard/src/lib/copy.ts` matches `= ' — stale';` (with em-dash codepoint U+2014, leading space, trailing word)
    - Source assertion: `grep "^export const STATS_BACKLOG_CALLOUT_SINGULAR" packages/dashboard/src/lib/copy.ts` matches `= 'awaiting review';`
    - Source assertion: `grep "^export const STATS_BACKLOG_CALLOUT_ARIA_PREFIX" packages/dashboard/src/lib/copy.ts` matches `= 'Pending review backlog: ';`
    - Source assertion: `grep "^export const STATS_STALE_INLINE_ARIA_PREFIX" packages/dashboard/src/lib/copy.ts` matches `= 'Stale shots: ';`
    - Block-header presence: `grep -c "Phase 23 — production stats copy" packages/dashboard/src/lib/copy.ts` outputs `1`
    - Section-divider presence: `grep -c "// ---------- ProgressBar\|// ---------- Backlog callout\|// ---------- Inline stale count\|// ---------- Per-shot stale" packages/dashboard/src/lib/copy.ts` outputs `4` (4 sub-section dividers)
    - Typecheck assertion: `cd packages/dashboard && npx tsc --noEmit` exits 0
    - Non-regression: All existing Phase 1-22 exports still present — `grep -c "^export const " packages/dashboard/src/lib/copy.ts` should be `previous_count + 9`
  </acceptance_criteria>

  <done>
    Nine new copy constants exported from `packages/dashboard/src/lib/copy.js`. Verbatim UI-SPEC values. Block header + 4 sub-section dividers added. Em-dash codepoint correct in SHOT_CARD_STALE_ARIA_SUFFIX. All existing exports preserved. Plan 23-03's `<SequenceHeader/>` subrow + `<ShotGridCard/>` border can consume these constants without any inline string literals (architectural rule preserved).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → API | Untrusted input: `sequenceId` path param crosses here at `GET /api/sequences/:id/shot-grid`. The route already validated this in Phase 21 — Phase 23 widens the response only. |
| Browser DOM → ProgressBar render | Untrusted input: `value`, `ariaLabel`, `label` props flow into JSX. Preact auto-escapes text children. |
| copy.ts → component imports | Trusted process boundary inside the bundled dashboard. Copy values are static constants, not user-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering | `sequenceId` path param flowing into `engine.listShotGrid(sequenceId, ...)` | mitigate | Phase 21's existing route validation handles this (Zod via the engine's `getSequence()` lookup that throws `SEQUENCE_NOT_FOUND`). Plan 23-02 does NOT add new validation surface — the new code is purely additive composition. The Plan 23-01 repo function parameterizes the SQL placeholder. |
| T-23-02-02 | Information Disclosure | Stats values (per-status counts, stale_count) returned in HTTP body | accept | v1.3 single-user model: no second party can observe the response. Stats reveal activity patterns but no PII, no auth tokens, no secrets. |
| T-23-02-03 | Cross-Site Scripting | `ariaLabel` / `label` props in `<ProgressBar/>` | mitigate | Preact auto-escapes JSX text children. The `aria-label` attribute is also auto-escaped by Preact's prop handling. Verified pattern at Phase 21 SequenceHeader.tsx aria-label composition. The component does NOT use `dangerouslySetInnerHTML`. |
| T-23-02-04 | Tampering | Hardcoded hex/text values in copy.ts (e.g., the em-dash in `SHOT_CARD_STALE_ARIA_SUFFIX`) | accept | Static module-scope constants. No runtime mutation. Built into the JS bundle. |
| T-23-02-05 | Denial | ProgressBar render with float values triggering React/Preact re-render loops | mitigate | Defensive clamp `Math.max(0, Math.min(max, Math.round(value)))` runs once per render. No useEffect, no useState, no signal subscriptions inside the component — it's a pure function of props. |
| T-23-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 23 Plan 02 installs ZERO new packages. ProgressBar uses ZERO third-party imports (only preact JSX from the existing pipeline). The 9 copy constants use no imports. `lucide-preact` is referenced in the architectural design (used by Plan 03's `<SequenceHeader/>`) but Plan 02 does not import it. No supply-chain risk added by this plan. |
</threat_model>

<verification>
After all 3 tasks complete:

1. Server suite regression: `npx vitest run` — all existing tests still green; new dashboard-routes-shot-grid.test.ts assertions green.
2. Dashboard suite regression: `cd packages/dashboard && npx vitest run` — all existing tests green; new ProgressBar.test.tsx green.
3. Cross-tree typecheck: `npx tsc --noEmit` (server) AND `cd packages/dashboard && npx tsc --noEmit` (dashboard) — both exit 0.
4. Vite production build: `cd packages/dashboard && npx vite build` — succeeds.
5. Tool budget invariant: `npx vitest run src/__tests__/tool-budget.test.ts` — still `=== 7` (Phase 23 adds zero MCP tools).
6. Architecture purity: `npx vitest run src/__tests__/architecture-purity.test.ts` — green (no new native bindings, no new allowed-set entries needed).
7. Route signature unchanged: `git diff src/http/dashboard-routes.ts` returns no diff (the thin Hono handler passes the wider response through unchanged).
</verification>

<success_criteria>
- `engine.listShotGrid(sequenceId, opts)` returns `{ sequence, shots: [{ ..., is_stale: boolean }], stats: SequenceStats, next_cursor, total_count }` envelope. The shots' is_stale field is a real boolean (coerced from SQLite 0|1). The stats.approved_pct is `Math.round((counts.approved / total) * 100)` with `total === 0 ? 0` divide-by-zero guard. The stats.pending_review_backlog mirrors `counts['pending-review']` (bracket access — Pitfall 10).
- HTTP test harness covers the two new envelope fields (top-level `stats` and per-row `is_stale`) with deterministic FakeEngine stubs. The route itself (`src/http/dashboard-routes.ts`) is byte-identical (passthrough).
- `<ProgressBar/>` renders the full WCAG 2.1 AA `role="progressbar"` + `aria-value{now,min,max}` + `aria-label` set. Defensive clamp handles over/under/float values. Track is `--color-border`, fill is `--color-shot-status-approved`. `motion-reduce:transition-none` honors `prefers-reduced-motion`. No focus, no callbacks, no state.
- 9 Phase 23 copy constants exported from `packages/dashboard/src/lib/copy.js` with verbatim UI-SPEC values. Block header + 4 sub-section dividers added. SHOT_CARD_STALE_ARIA_SUFFIX uses em-dash U+2014.
- All existing tests pass. Tool budget === 7. Architecture purity holds. Dashboard build succeeds.
</success_criteria>

<output>
Create `.planning/phases/23-production-stats/23-02-SUMMARY.md` documenting:
- The exact `engine.listShotGrid` extended return shape (for Plan 03's view/state consumers)
- Confirmation that `src/http/dashboard-routes.ts` is byte-identical (passthrough)
- The cross-tree import decision for `SequenceStats` (imported from dashboard types — or inlined with sync comment, depending on what the architecture-purity allowed-set permitted)
- Pitfall 10 evidence (bracket access for `counts['pending-review']`)
- The 9 copy constants' final values (verbatim list — for QA + Plan 03 reference)
- ProgressBar.tsx published API (props interface — for Plan 03's SequenceHeader integration)
- Tool budget invariant proof (line of tool-budget.test.ts output: `expect(...).toBe(7)` passing)
</output>
