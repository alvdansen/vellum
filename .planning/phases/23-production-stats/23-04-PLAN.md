---
phase: 23-production-stats
plan: 04
type: execute
wave: 4
depends_on: ["23-01", "23-02", "23-03"]
files_modified:
  - .planning/phases/23-production-stats/23-VALIDATION.md
autonomous: false
requirements: [OVR-01, OVR-02, OVR-03]
must_haves:
  truths:
    - "Full server suite (npx vitest run at repo root) is green or matches the pre-Phase-23 pre-existing-failure baseline exactly (21 failures from STATE.md line 35 are pre-existing and not introduced by Phase 23)"
    - "Full dashboard suite (cd packages/dashboard && npx vitest run) is 100% green — previous baseline 361/361 (STATE.md line 35) + Phase 23 added 28 new tests = expected 389/389 (or higher if additional regression tests were added)"
    - "architecture-purity.test.ts is 54/54 green (Phase 23 introduces no new native bindings; allowed-set unchanged)"
    - "tool-budget.test.ts asserts === 7 green (Phase 23 adds zero server.registerTool() calls)"
    - "Theme.css token check: grep -c '^\\s*--color-shot-stale' packages/dashboard/src/styles/theme.css === 2 (Pitfall 6 — both light AND dark blocks patched)"
    - "Append-only invariant grep: grep -r 'UPDATE shot_status_events' src/ returns zero matches (carry-forward from Phase 20)"
    - "Phase 21 EXPLAIN test invariant: shot-status-repo-grid.test.ts CORRELATED-on-ranked-CTE assertion still green (Phase 23 is_stale CASE introduces a CORRELATED LIST subquery on versions v2 — intentional and whitelisted; CORRELATED SCALAR on ranked CTE is still rejected)"
    - "Vite production build succeeds (cd packages/dashboard && npx vite build exits 0); resulting bundle smaller than the Phase 22 hard ceiling per STATE.md line 41 (127KB JS / 29KB CSS baseline)"
    - "User performs WCAG visual smoke test: stale border distinguishable from pending-review pill on both themes; backlog callout legible against accent bg; progress bar fill animation honors prefers-reduced-motion"
    - "User performs SSE-driven counter visual smoke test: change a shot's status in one tab (or via MCP set_status), watch the sequence header stats update without a full page reload"
    - "VALIDATION.md gets nyquist_compliant: true flip in the frontmatter after the visual smoke completes (this is the gate signal for Phase 23 ready-to-ship)"
  artifacts:
    - path: ".planning/phases/23-production-stats/23-VALIDATION.md"
      provides: "frontmatter nyquist_compliant: true + per-task verification map updated with actual task IDs from Plans 23-01..23-03 (replace TBD)"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "full-suite regression gate"
      to: "STATE.md baseline counts (1853 server / 361 dashboard / 21 pre-existing failures)"
      via: "explicit diff against pre-Phase-23 baseline"
      pattern: "1853|361|=== 7"
    - from: "Visual smoke gate (human-verify)"
      to: "UI-SPEC §Accessibility Checklist + §Color contrast verification + §Layout Reference Diagram"
      via: "12-step manual checklist in this plan task description"
      pattern: "stale border|backlog callout|progress bar"
---

<objective>
Phase 23 Wave 4 — phase gate. Two tasks lock the regression baseline and flip the VALIDATION.md nyquist_compliant bit to `true`. Task 04-01 runs the full automated check suite and asserts every invariant from the cross-cutting constraints in REQUIREMENTS.md. Task 04-02 is a human-verify checkpoint covering the visual surfaces that automation cannot reliably check (WCAG contrast perception, animation perception, SSE-driven counter delta perception).

Purpose: Make Phase 23 ready-to-ship. After this plan completes, Phase 23 has shipped its OVR-01..03 surfaces with: a single GROUP BY whole-sequence stats query (no N+1, EXPLAIN-verified), a 14-day stale detection at grid query time with D-15 pragmatic interpretation (zero-version shots NOT stale), SSE-driven counter deltas using a Preact computed signal — all without changing the existing endpoint signature, without adding any MCP tools, without adding any native bindings.

Output: VALIDATION.md frontmatter updated; per-task verification map filled with real task IDs (replacing TBDs); checkpoint sign-off recorded.

This plan has ONE checkpoint and therefore is NOT autonomous (`autonomous: false`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-production-stats/23-CONTEXT.md
@.planning/phases/23-production-stats/23-RESEARCH.md
@.planning/phases/23-production-stats/23-VALIDATION.md
@.planning/phases/23-production-stats/23-UI-SPEC.md
@.planning/phases/23-production-stats/23-01-PLAN.md
@.planning/phases/23-production-stats/23-02-PLAN.md
@.planning/phases/23-production-stats/23-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 04-01: Full-suite automated regression gate + invariant greps + VALIDATION.md update</name>

  <read_first>
    - .planning/STATE.md (baseline test counts at line 35 — 1853 server / 361 dashboard / 21 pre-existing-failures; Vite build at line 41 — 127KB JS / 29KB CSS)
    - .planning/phases/23-production-stats/23-VALIDATION.md (the file being modified — frontmatter `nyquist_compliant: false` + `wave_0_complete: false` need to flip to true; per-task verification map has TBD rows that need to be filled with actual task IDs from Plans 23-01..23-03)
    - .planning/REQUIREMENTS.md §"Cross-Cutting Constraints" (the invariant list to verify — tool cap, append-only, transaction discipline, null-coalesce, architecture-purity, WCAG, signal-driven routing, WAL+busy_timeout, migration count holds)
    - .planning/phases/23-production-stats/23-01-PLAN.md / 23-02-PLAN.md / 23-03-PLAN.md (collect actual task IDs T01, T02, T03 of each plan for the verification map)
  </read_first>

  <files>.planning/phases/23-production-stats/23-VALIDATION.md</files>

  <action>
    Execute the full automated check suite, capture results, and update VALIDATION.md. Do NOT proceed if any check fails — escalate the failure and stop.

    **(A) Run the full server suite from repo root:**

    Command: `npx vitest run --reporter=verbose 2>&1 | tail -50`

    Expected: green or matching the pre-Phase-23 baseline of 21 pre-existing failures (STATE.md line 35 enumerates them: validation-flags, generation-tool, Phase 18 SORT-03 attribution). Any NEW failure introduced by Phase 23 = blocker. If all 21 pre-existing fails are present and exactly those 21, the suite is "green relative to baseline" and acceptable.

    **(B) Run the full dashboard suite:**

    Command: `cd packages/dashboard && npx vitest run --reporter=verbose 2>&1 | tail -50`

    Expected: 361 (baseline) + 28 (Phase 23 new) = 389 tests, all passing. Or higher if any test author added incidental coverage during execution.

    **(C) Run targeted invariant tests:**

    Commands: `npx vitest run src/__tests__/architecture-purity.test.ts --reporter=verbose 2>&1 | tail -5` AND `npx vitest run src/__tests__/tool-budget.test.ts --reporter=verbose 2>&1 | tail -5`

    Expected: 54/54 architecture-purity green; tool-budget assertion `expect(...).toBe(7)` green.

    **(D) Verify invariant greps:**

    1. `grep -c "^\s*--color-shot-stale" packages/dashboard/src/styles/theme.css` → expected `2` (Pitfall 6 — both blocks).
    2. `grep -r "UPDATE shot_status_events" src/ 2>/dev/null` → expected zero matches (Phase 20 append-only invariant carry-forward).
    3. `grep -c "^export const STATS_\|^export const SHOT_CARD_STALE_ARIA_SUFFIX" packages/dashboard/src/lib/copy.ts` → expected `9` (Phase 23 copy constants from Plan 02 Task 03).
    4. `grep -c "is_stale" packages/dashboard/src/types/shot-grid.ts` → expected at least `1` (Phase 23 type extension).

    **(E) Vite production build:**

    Command: `cd packages/dashboard && npx vite build 2>&1 | tail -10`

    Expected: build succeeds. Record the new bundle sizes; expect roughly +1-2KB JS for the new ProgressBar component + helpers, +0KB CSS (single new theme token is negligible). STATE.md baseline: 127KB JS / 29KB CSS.

    **(F) Update VALIDATION.md frontmatter:**

    Open `.planning/phases/23-production-stats/23-VALIDATION.md`. Set the frontmatter `nyquist_compliant` field to the value `false` initially — the FINAL flip to `true` happens in Task 04-02 after the human-verify smoke completes. Set `wave_0_complete: true` (the new test files are landed). Update `status` from `draft` to `pending-uat` (the human gate is in Task 04-02).

    **(G) Fill the per-task verification map in VALIDATION.md:**

    The existing table at §"Per-Task Verification Map" has 16 rows with `Task ID: TBD` and `Plan: TBD`. Replace TBD with the actual Plan + Task IDs from this phase:

    | Plan | Wave | Task IDs to assign |
    |------|------|--------------------|
    | 23-01 | 1 | 23-01-01 (types), 23-01-02 (theme), 23-01-03 (repo+EXPLAIN+tests) |
    | 23-02 | 2 | 23-02-01 (engine+HTTP), 23-02-02 (ProgressBar), 23-02-03 (copy) |
    | 23-03 | 3 | 23-03-01 (state), 23-03-02 (header+card), 23-03-03 (view wire) |
    | 23-04 | 4 | 23-04-01 (this task), 23-04-02 (human-verify) |

    For each VALIDATION.md row, assign the most-relevant Plan+Task pair AND update the `Status` column from `⬜ pending` to `✅ green` (after running the per-row automated command and confirming it passes).

    **(H) Do NOT commit yet.** The commit happens in Task 04-02 after the human-verify gate signs off. This task ONLY runs checks + updates the VALIDATION.md draft.

    **(I) If ANY check fails:** STOP. Report the failure to the orchestrator with file path + line number + failure detail. Do NOT modify VALIDATION.md if the suite is not green. The phase gate cannot pass with a broken regression.
  </action>

  <verify>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Suites|Failed" | head -5</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp/packages/dashboard && npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files|Failed" | head -5</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx vitest run src/__tests__/architecture-purity.test.ts src/__tests__/tool-budget.test.ts --reporter=verbose 2>&1 | tail -10</automated>
    <automated>grep -c "^\s*--color-shot-stale" /Users/macapple/comfyui-vfx-mcp/packages/dashboard/src/styles/theme.css</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp/packages/dashboard && npx vite build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Server suite: `npx vitest run` reports pre-existing baseline (21 known-failures from STATE.md line 35) OR fully green; NO new failures introduced by Phase 23. Capture the exact "Failed: N" line and confirm N <= 21 AND all failure file paths match the pre-existing baseline (validation-flags, generation-tool, Phase 18 SORT-03 attribution).
    - Dashboard suite: `cd packages/dashboard && npx vitest run` reports >= 389 tests passing (361 baseline + 28 new Phase 23) with ZERO failures. If a test author added incidental coverage during Plan 23-01..03 execution, the count may be higher.
    - architecture-purity.test.ts: 54/54 green; no new native binding additions (Phase 23 adds zero new bindings — RESEARCH §"Standard Stack" confirms).
    - tool-budget.test.ts: `expect(...).toBe(7)` green (Phase 23 adds zero MCP tools — RESEARCH D-18 confirms).
    - Pitfall 6: `grep -c "^\s*--color-shot-stale" packages/dashboard/src/styles/theme.css` returns `2` (CRITICAL — both blocks).
    - Append-only invariant (Phase 20 carry-forward): `grep -r "UPDATE shot_status_events" src/` returns ZERO matches.
    - Phase 23 copy floor: `grep -c "^export const STATS_\|^export const SHOT_CARD_STALE_ARIA_SUFFIX" packages/dashboard/src/lib/copy.ts` returns `9`.
    - Phase 23 type extension: `grep -c "is_stale" packages/dashboard/src/types/shot-grid.ts` returns at least `1`.
    - Vite production build: exits 0; bundle sizes recorded (expected near baseline +1-2KB JS).
    - VALIDATION.md frontmatter: `wave_0_complete: true`, `status: pending-uat`. The `nyquist_compliant` field remains `false` until Task 04-02 flips it.
    - VALIDATION.md per-task table: all TBD rows replaced with `23-01-NN`, `23-02-NN`, `23-03-NN`, `23-04-NN` task IDs; Status column updated to ✅ green for every automated row.
    - No git commit yet (commit deferred to Task 04-02 after human sign-off).
  </acceptance_criteria>

  <done>
    All automated checks pass. VALIDATION.md draft updated with real task IDs. Phase 23 is regression-clean and ready for the visual smoke gate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-02: Human-verify visual + interaction smoke + flip nyquist_compliant true</name>

  <read_first>
    - .planning/phases/23-production-stats/23-VALIDATION.md §"Manual-Only Verifications" (4 manual verification items)
    - .planning/phases/23-production-stats/23-UI-SPEC.md §"Accessibility Checklist" + §"Color > WCAG 2.1 AA contrast verification" + §"Layout Reference Diagram"
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-08 light-theme contrast risk note; D-19 best-effort caveat acceptance)
  </read_first>

  <files>.planning/phases/23-production-stats/23-VALIDATION.md (frontmatter flip + sign-off entry on user approval)</files>

  <what-built>
    Plans 23-01..23-03 shipped:
    - Server: getSequenceStats repo function + listShotsForGrid is_stale CASE + engine.listShotGrid composition (route signature unchanged).
    - Dashboard types: SequenceStats interface + is_stale per row + stats envelope field.
    - Dashboard UI: new ProgressBar primitive; SequenceHeader gains a NEW stats subrow between chevron+name row and existing mini-pills row; ShotGridCard gains conditional amber stale border + aria-label suffix.
    - Dashboard state: sequenceStats signal + extended onShotStatusChanged with delta arithmetic + applyStatsDelta + recomputeIsStaleClient helpers (all Pitfalls covered by automated tests).
    - copy.ts: 9 new constants exported with verbatim UI-SPEC values.
    - theme.css: --color-shot-stale token added to both light (#b45309) and dark (#f97316) blocks.

    Task 04-01 ran the full regression suite and updated VALIDATION.md with task IDs. Now the visual + interaction surfaces need a human eyeball check on real browser pixels.
  </what-built>

  <action>
    The executor sets up the dev environment, the user walks the 12-step smoke checklist below, and the executor records the result. Steps to execute:

    **(A) Setup — start two terminals:**

    Terminal A — server in HTTP mode: `npx tsx src/server.ts --http`
    Terminal B — dashboard dev server: `cd packages/dashboard && npx vite`

    Open the URL Vite prints (typically http://localhost:5173) in a modern browser.

    **(B) Walk the 12-step smoke checklist (see how-to-verify below for the full enumeration).**

    **(C) On user approval ('approved'):**

    1. Edit `.planning/phases/23-production-stats/23-VALIDATION.md` frontmatter: set `nyquist_compliant: true`, set `status: signed-off`.
    2. Append a "Validation Sign-Off" entry at the bottom of VALIDATION.md with timestamp + approver + the 12 steps' pass status.
    3. Commit all Phase 23 changes via the project's `gsd-sdk query commit` helper with the message `feat(23): production stats — sequence-level stats widget + 14-day stale detection + SSE-driven counter deltas` and the list of all Phase 23 modified/created files.

    **(D) On block ('block: $reason'):**

    Document the regression in this task's done block, do NOT flip nyquist_compliant, do NOT commit. Escalate to the orchestrator with a precise failure description.
  </action>

  <how-to-verify>
    **Setup (one terminal each):**

    Terminal A — server in HTTP mode: `npx tsx src/server.ts --http`
    Terminal B — dashboard dev server: `cd packages/dashboard && npx vite`

    Open the URL Vite prints (typically `http://localhost:5173` or similar) in a modern browser.

    **12-step smoke checklist:**

    1. **Sequence with stats (basic visibility):** Navigate via tree sidebar to a sequence that has at least 5 shots in mixed statuses (use tree → workspace → project → sequence → grid icon to switch to shot-grid view). Confirm the SequenceHeader shows THREE stacked rows: (a) chevron + name, (b) NEW Phase 23 stats subrow with a horizontal progress bar + "% approved" label, (c) the existing Phase 21 mini-pills row. If your sequence has zero shots, create some via MCP tools or pick a different sequence.

    2. **Progress bar fill correctness:** Note the displayed approved_pct (e.g., "60% approved"). Confirm the progress bar fill width visually matches that percentage (60% fill should look 60% of the way across the 128px track, not 100% or 30%).

    3. **Chevron collapse hides all three rows:** Click the chevron next to the sequence name. Confirm rows (a) only remains; (b) and (c) disappear. Click again to expand — all three rows return.

    4. **Backlog callout (pending-review):** If your sequence has any shot in pending-review status (use MCP `shot.set_status` or the review panel from Phase 22 to set one), confirm a callout pill appears in the stats subrow with text like "3 awaiting review" (uppercase, accent purple bg, with AlertCircle icon).

    5. **Backlog callout hides on zero:** If no shots are in pending-review status, confirm NO callout pill renders (no "0 awaiting" visible).

    6. **Stale shot detection — visual:** Find or create a shot with: status=wip OR pending-review, AND a completed version older than 14 days. If your fixtures don't have an old version, you can either (a) MCP-create a version then run a sqlite3 cli UPDATE setting completed_at to Date.now() minus 30 days in ms, OR (b) skip this step and confirm visually with synthetic data once you can produce a real stale candidate. Confirm: (i) the card has a 2px amber border around the entire outer wrapper, (ii) the stats subrow shows "N stale" inline text in the amber stale color, (iii) the border color is DISTINGUISHABLE from the pending-review pill's amber (per A5 escalation — the colors should not visually collapse).

    7. **Stale + omit logical exclusion:** Set a stale shot to omit status. Confirm the card now has the opacity-40 dimming wrapper AND the stale border is no longer visible (server should compute is_stale=false because status is no longer wip/pending-review — per OVR-02 filter).

    8. **SSE-driven counter update:** Open a second terminal (Terminal C). In Terminal C, use MCP to change a shot's status: `shot.set_status` with shotId from the grid and a different toStatus. Watch the dashboard browser window: within ~200ms (per UI-SPEC §"Manual-Only Verifications" row 3), the stats counters should update WITHOUT a page reload. Confirm: (i) the counts pill in the appropriate position updates, (ii) approved_pct progress bar fills correctly if the transition affected approved count, (iii) the affected shot's status pill in the grid updates in place.

    9. **WCAG light-theme contrast check (A5 risk):** Open browser dev tools color picker. Sample a stale border pixel against the page background. Confirm: contrast ratio ≥ 3:1 (UI-component threshold). The locked light hex is #b45309; UI-SPEC §"Color" claims 4.62:1 against #fafafa page bg — confirm by measurement.

    10. **WCAG dark-theme contrast check:** Toggle to dark theme (if your dashboard has a theme toggle). Confirm the stale border at #f97316 is visible against #202020 bg (calculated 5.41:1 per UI-SPEC).

    11. **Reduced-motion respect:** In OS settings, toggle on "Reduce motion" (macOS: System Settings → Accessibility → Display → Reduce motion). Refresh the dashboard. Trigger a status change that flips the approved_pct (via the review panel or MCP). Confirm the progress bar fill JUMPS to the new width without any animation. Toggle reduce-motion OFF; refresh; trigger another change; confirm the bar now ANIMATES over ~150ms.

    12. **Keyboard accessibility (regression check):** Tab through the dashboard with the keyboard. Confirm: (i) the existing Phase 21 + Phase 22 tab order is preserved, (ii) the new stats subrow elements (ProgressBar, backlog callout, stale-count text) are NOT focusable (no tabIndex per UI-SPEC §"Keyboard accessibility"), (iii) the existing focus rings on inner thumbnail/pill/quick-approve buttons render correctly INSIDE the stale border (no z-fight per Pitfall 5).

    **If ANY step fails:**
    Describe the failure precisely (which step, what you observed vs. expected, browser screenshot if possible). Resume signal: type 'block: $reason' with details. Phase 23 is NOT shippable until all 12 pass.

    **If all 12 pass:**
    Resume signal: type 'approved'.
  </how-to-verify>

  <verify>
    <human-check>User completes the 12-step browser smoke checklist; user reports 'approved' or 'block: $reason'</human-check>
  </verify>

  <resume-signal>Type "approved" if all 12 steps pass. Type "block: $reason" with a precise description (which step failed, observed vs. expected) if any step fails. After approval the executor flips VALIDATION.md nyquist_compliant: true and creates the phase-completion git commit.</resume-signal>

  <acceptance_criteria>
    - User reports 'approved' for all 12 steps OR documents specific failure(s) for follow-up.
    - On approval: VALIDATION.md frontmatter shows `nyquist_compliant: true` AND `status: signed-off`.
    - On approval: VALIDATION.md has a new "Validation Sign-Off" section at the bottom with timestamp + 12-step pass record.
    - On approval: git log shows a new commit with subject `feat(23): production stats — sequence-level stats widget + 14-day stale detection + SSE-driven counter deltas` AND the commit includes all Phase 23 Plan 23-01..23-04 modified files.
    - On block: VALIDATION.md remains at `nyquist_compliant: false`; the block description is captured in the orchestrator's escalation log.
  </acceptance_criteria>

  <done>
    User has approved the 12-step visual smoke. VALIDATION.md frontmatter is `nyquist_compliant: true` + `status: signed-off`. The Phase 23 commit has landed. Phase 23 is ready-to-ship.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test results → release decision | Trusted process boundary inside the executor's controlled environment |
| Human visual perception → contrast/animation assertion | Trusted human-in-the-loop; the executor cannot reliably measure these from automation alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Information Disclosure | Test failure reveals false positives (the suite is green but a hidden regression exists) | accept | The Phase 21/22 tests cover the surrounding surfaces; the new Phase 23 tests cover every Pitfall. The combination is robust. Edge cases not covered by automation are caught by the visual smoke. |
| T-23-04-02 | Repudiation | Human sign-off cannot be reproduced | mitigate | The "approved" sign-off + VALIDATION.md Validation Sign-Off entry timestamp creates a permanent record. Git commit message includes the phase + plan reference. |
| T-23-04-03 | Tampering | Visual smoke fails to detect a subtle issue (e.g., stale border off by 1px) | accept | The 12-step checklist focuses on observable behavior, not pixel-perfect measurements. If the user reports a follow-up visual concern, Phase 24+ candidate. |
| T-23-04-04 | Denial | Server suite fails in CI but passes locally (or vice versa) | mitigate | Task 04-01 acceptance criteria require the suite to match the STATE.md pre-existing baseline EXACTLY — any drift triggers a block. Re-run if flaky; investigate root cause if persistent. |
| T-23-04-SC | Tampering | npm/pip/cargo installs | accept | Phase 23 Plan 04 installs ZERO new packages. The full-suite regression + Vite build use existing dev dependencies installed in Phase 21-22. |
</threat_model>

<verification>
After both tasks complete:

1. Server suite green relative to STATE.md baseline (21 pre-existing failures unchanged).
2. Dashboard suite green at 389+ tests (361 baseline + 28 new).
3. architecture-purity 54/54 + tool-budget === 7.
4. Vite build succeeds; bundle near baseline +1-2KB JS.
5. Pitfall 6 grep returns 2.
6. Append-only invariant grep returns 0.
7. 12-step visual smoke approved by user.
8. VALIDATION.md frontmatter shows `nyquist_compliant: true` + `status: signed-off`.
9. Git commit landed referencing Phase 23 completion.
</verification>

<success_criteria>
- All automated regression checks pass (full server suite, full dashboard suite, architecture-purity, tool-budget, Pitfall 6 grep, append-only grep, Vite production build).
- 12-step manual visual smoke approved by user.
- VALIDATION.md updated: nyquist_compliant flipped from false to true; status flipped from draft to signed-off; per-task verification map has all 16 TBD rows replaced with real Plan+Task IDs; Validation Sign-Off entry recorded with timestamp.
- Final git commit lands with conventional message `feat(23): production stats — sequence-level stats widget + 14-day stale detection + SSE-driven counter deltas` including all Phase 23 modified/created files.
- Phase 23 ROADMAP entry updates to [x] complete with the actual completion date.
- Tool budget invariant === 7. Architecture-purity unchanged. Append-only carry-forward intact. Migration count holds at 0008.
</success_criteria>

<output>
Create `.planning/phases/23-production-stats/23-04-SUMMARY.md` documenting:
- Final test counts: server suite (matches STATE.md baseline 1853 + new tests added by Phase 23 — Plan 23-01's shot-status-repo-stats.test.ts at ~8 tests + extension test in shot-status-repo-grid.test.ts at ~1 test + HTTP envelope tests in dashboard-routes-shot-grid.test.ts at +2 tests); dashboard suite (361 baseline + 28 new Phase 23 = 389+; capture actual count).
- Final Vite bundle sizes (JS + CSS) compared to STATE.md baseline (127KB / 29KB).
- 12-step visual smoke status: which steps passed, which (if any) required iteration, final approval timestamp.
- VALIDATION.md frontmatter delta: before `nyquist_compliant: false` / `status: draft`; after `nyquist_compliant: true` / `status: signed-off`.
- Phase 23 D-01..D-21 coverage matrix: confirm every decision is implemented in the shipped artifacts.
- Roadmap update note: Phase 23 line in ROADMAP.md flipped from [ ] to [x] with completion date.
- "Next: Phase 24 — UX Polish Bundle (adversarial review mandatory at plan stage)" — one-line pointer to the next phase.
</output>
