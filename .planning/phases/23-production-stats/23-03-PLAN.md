---
phase: 23-production-stats
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - packages/dashboard/src/state/shot-grid.ts
  - packages/dashboard/src/state/__tests__/shot-grid-stats-delta.test.ts
  - packages/dashboard/src/components/SequenceHeader.tsx
  - packages/dashboard/src/components/__tests__/SequenceHeader.stats.test.tsx
  - packages/dashboard/src/components/ShotGridCard.tsx
  - packages/dashboard/src/components/__tests__/ShotGridCard.stale.test.tsx
  - packages/dashboard/src/views/ShotGridView.tsx
autonomous: true
requirements: [OVR-01, OVR-02, OVR-03]
must_haves:
  truths:
    - "sequenceStats: Signal<SequenceStats | null> is exported from state/shot-grid.ts; seeded by ShotGridView fetchShotGrid resolution; cleared to null on sequence change"
    - "onShotStatusChanged is EXTENDED (not relocated) at state/shot-grid.ts:160 — preserves reference equality for App.tsx onSseEvent registration. Body now applies stats deltas via applyStatsDelta helper per D-11."
    - "applyStatsDelta decrements counts[fromStatus] clamped at 0 (Pitfall 1), increments counts[toStatus], recomputes pending_review_backlog from counts['pending-review'] (bracket access — Pitfall 10), recomputes approved_pct from counts.approved/total (divide-by-zero guard), applies stale_count delta only when shotInBuffer is not null (D-12 + D-19 best-effort caveat)."
    - "null fromStatus is coalesced to 'wip' (Pitfall 2 — default starting state per Phase 20 schema). NaN never appears in counts."
    - "SequenceHeader grows a NEW stats subrow INSIDE the existing flex-col container, BETWEEN the chevron+name row AND the existing Phase 21 D-14 mini-pills row. The subrow renders ONLY when stats is not null and stats.total > 0 (Open Question 2 + symmetric with mini-pills hide-on-zero)."
    - "ProgressBar + conditional backlog callout (hide when pending_review_backlog === 0 per D-20) + conditional inline stale count (hide when stale_count === 0). Chevron collapse hides ALL three rows (the subrow is a sibling of the mini-pills row inside the existing collapsible parent)."
    - "ShotGridCard outer `<div class='group relative'>` gains conditional `border-2 border-[var(--color-shot-stale)]` class when shot.is_stale === true. Thumbnail button aria-label appends SHOT_CARD_STALE_ARIA_SUFFIX when is_stale. No other DOM change."
    - "ShotGridView passes stats={sequenceStats.value} to SequenceHeader alongside the existing counts={aggregateCounts.value} prop. fetchShotGrid resolution seeds sequenceStats.value = res.stats; fetch effect cleanup sets sequenceStats.value = null on sequence switch."
  artifacts:
    - path: "packages/dashboard/src/state/shot-grid.ts"
      provides: "sequenceStats signal + extended onShotStatusChanged + applyStatsDelta helper + recomputeIsStaleClient helper + STALE_SHOT_DAYS_MS constant"
      contains: "export const sequenceStats"
    - path: "packages/dashboard/src/state/__tests__/shot-grid-stats-delta.test.ts"
      provides: "Stats delta arithmetic + Pitfall guards (cross-sequence, fromStatus=null, out-of-buffer, pre-fetch null, clamp-at-zero)"
      contains: "applyStatsDelta"
    - path: "packages/dashboard/src/components/SequenceHeader.tsx"
      provides: "Stats subrow inside existing component (D-04 augment-don't-replace)"
      contains: "stats: SequenceStats | null"
    - path: "packages/dashboard/src/components/__tests__/SequenceHeader.stats.test.tsx"
      provides: "Stats subrow a11y + hide-on-zero + hide-on-null + chevron interaction"
      contains: "role=\"progressbar\""
    - path: "packages/dashboard/src/components/ShotGridCard.tsx"
      provides: "Conditional amber stale border + aria-label suffix"
      contains: "shot.is_stale"
    - path: "packages/dashboard/src/components/__tests__/ShotGridCard.stale.test.tsx"
      provides: "Stale border conditional + aria-label append + omit+stale coexistence-safety"
      contains: "border-2"
    - path: "packages/dashboard/src/views/ShotGridView.tsx"
      provides: "sequenceStats seeding from fetchShotGrid + stats prop forwarded to SequenceHeader"
      contains: "sequenceStats.value"
  key_links:
    - from: "packages/dashboard/src/views/ShotGridView.tsx fetchShotGrid.then resolved branch"
      to: "packages/dashboard/src/state/shot-grid.ts:sequenceStats signal"
      via: "sequenceStats.value = res.stats"
      pattern: "sequenceStats\\.value = res\\.stats"
    - from: "packages/dashboard/src/state/shot-grid.ts:onShotStatusChanged"
      to: "packages/dashboard/src/state/shot-grid.ts:applyStatsDelta helper"
      via: "sequenceStats.value = applyStatsDelta(prev, effectiveFromStatus, payload.toStatus, shotInBuffer)"
      pattern: "applyStatsDelta\\("
    - from: "packages/dashboard/src/components/SequenceHeader.tsx props"
      to: "packages/dashboard/src/types/shot-grid.ts:SequenceStats (Plan 23-01)"
      via: "import type + props extension"
      pattern: "stats: SequenceStats \\| null"
    - from: "packages/dashboard/src/components/ShotGridCard.tsx outer div className"
      to: "theme.css --color-shot-stale token (Plan 23-01)"
      via: "Tailwind arbitrary-value class string template"
      pattern: "border-\\[var\\(--color-shot-stale\\)\\]"
    - from: "packages/dashboard/src/components/ShotGridCard.tsx thumbnail button aria-label"
      to: "copy.ts SHOT_CARD_STALE_ARIA_SUFFIX (Plan 23-02)"
      via: "conditional concatenation when shot.is_stale === true"
      pattern: "SHOT_CARD_STALE_ARIA_SUFFIX"
---

<objective>
Phase 23 Wave 3 — state extension + view layer. This plan wires the data published by Waves 1+2 into the dashboard's reactive layer: the new `sequenceStats` signal, the extended SSE handler with delta arithmetic, the `<SequenceHeader/>` stats subrow (D-04 augment-don't-replace), the `<ShotGridCard/>` amber stale border (D-07), and the `<ShotGridView/>` seed wiring. Three test files lock the behavioral contracts.

Purpose: Complete OVR-01..03 user-visible surfaces by reusing the existing signal lifecycle (Phase 21 D-22 onSseEvent registration in App.tsx:88 is unchanged), the existing chevron collapse mechanism, and the existing ShotGridCard structure. Every change is additive — no relocation, no reference-equality break, no new mount points, no router. The hardest pitfalls (cross-sequence guard, fromStatus=null coalesce, out-of-buffer stale-count caveat, pre-fetch null guard, hyphenated-key bracket access, clamp-at-zero) are explicit in the test suite.

Output: 3 modified source files + 3 NEW test files + 1 view edit. Zero new components beyond those landed in Plan 23-02. Zero new mount points. Zero new HTTP routes. Zero new MCP tools. Tool budget still === 7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-production-stats/23-CONTEXT.md
@.planning/phases/23-production-stats/23-RESEARCH.md
@.planning/phases/23-production-stats/23-PATTERNS.md
@.planning/phases/23-production-stats/23-UI-SPEC.md
@.planning/phases/23-production-stats/23-01-PLAN.md
@.planning/phases/23-production-stats/23-02-PLAN.md

@packages/dashboard/src/state/shot-grid.ts
@packages/dashboard/src/state/__tests__/shot-grid.test.ts
@packages/dashboard/src/components/SequenceHeader.tsx
@packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx
@packages/dashboard/src/components/ShotGridCard.tsx
@packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx
@packages/dashboard/src/views/ShotGridView.tsx
@packages/dashboard/src/types/events.ts

<interfaces>
Plan 23-01 published:
  packages/dashboard/src/types/shot-grid.ts exports SequenceStats interface and extends ShotGridRow with is_stale: boolean and ShotGridResponse with stats: SequenceStats.

Plan 23-02 published:
  packages/dashboard/src/components/ProgressBar.tsx exports ProgressBarProps {value, max?, label?, ariaLabel} and ProgressBar function.
  packages/dashboard/src/lib/copy.ts adds 9 new constants:
    STATS_PROGRESS_ARIA_PREFIX = "Approval progress for "
    STATS_APPROVED_LABEL_SUFFIX = "% approved"
    STATS_BACKLOG_CALLOUT_SINGULAR = "awaiting review"
    STATS_BACKLOG_CALLOUT_PLURAL = "awaiting review"
    STATS_BACKLOG_CALLOUT_ARIA_PREFIX = "Pending review backlog: "
    STATS_STALE_INLINE_SINGULAR = "stale"
    STATS_STALE_INLINE_PLURAL = "stale"
    STATS_STALE_INLINE_ARIA_PREFIX = "Stale shots: "
    SHOT_CARD_STALE_ARIA_SUFFIX = " — stale"  (em-dash U+2014)

Existing (Phase 20-22) — extend in place:
  state/shot-grid.ts exports: activeView, headerExpanded, selectedSequenceForGrid, shotGrid, statusFilter, showOmitted, gridIsFetching, gridLoadMoreError, aggregateCounts (computed line 123), onShotStatusChanged (function line 160).
  types/events.ts ShotStatusChangedPayload has fromStatus: ShotStatus | null (null on first-ever transition per Phase 20 line 73).
  components/SequenceHeader.tsx outer structure: header.flex-col gap-2 containing (1) chevron+name row lines 67-88, (2) mini-pills row lines 89-111. Phase 23 inserts subrow BETWEEN these as a sibling.
  components/ShotGridCard.tsx outer div line 75 is `<div class="group relative w-full overflow-hidden rounded">`. Thumbnail button aria-label at line 84. Omit wrapper at line 154 wraps in `<div class="opacity-40 transition-opacity">`.
  views/ShotGridView.tsx fetchShotGrid.then resolved branch at lines 132-145 sets shotGrid.value = res. SequenceHeader render call at lines 290-299 already passes counts={aggregateCounts.value}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-01: Extend state/shot-grid.ts (sequenceStats signal + applyStatsDelta + recomputeIsStaleClient + extend onShotStatusChanged in place)</name>

  <read_first>
    - packages/dashboard/src/state/shot-grid.ts (the file being modified — read in full; lines 1-77 existing signal definitions, lines 108-110 inline constant precedent, lines 123-136 aggregateCounts computed, lines 160-171 onShotStatusChanged body)
    - packages/dashboard/src/state/__tests__/shot-grid.test.ts (existing test file — read in full; lines 35-67 module-singleton reset pattern in beforeEach, lines 73-138 existing onShotStatusChanged test block)
    - packages/dashboard/src/types/events.ts (lines 68-79 — ShotStatusChangedPayload shape; fromStatus is union plus null)
    - packages/dashboard/src/types/shot-grid.ts (verify SequenceStats + is_stale fields are present per Plan 23-01)
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-10 signal placement; D-11 delta algorithm; D-12 stale-count delta; D-17 SSE payload unchanged; D-19 best-effort caveat documentation)
    - .planning/phases/23-production-stats/23-RESEARCH.md §"Pattern 2" + §"Code Example 4" + §"Common Pitfalls > Pitfalls 1, 2, 3, 8, 9, 10"
    - .planning/phases/23-production-stats/23-PATTERNS.md §5 "state/shot-grid.ts" (verbatim handler extension + helper shapes) + §15 "shot-grid-stats-delta.test.ts" (test fixture + Pitfall coverage)
    - .planning/phases/23-production-stats/23-UI-SPEC.md §"Interaction Contracts > SSE-driven counter updates" (pseudocode lock)
    - .planning/phases/23-production-stats/23-VALIDATION.md (Wave 0 rows for OVR-03 SSE deltas)
  </read_first>

  <files>packages/dashboard/src/state/shot-grid.ts, packages/dashboard/src/state/__tests__/shot-grid-stats-delta.test.ts</files>

  <behavior>
    - Test 1: sequenceStats signal exists; initial value is null on module import.
    - Test 2 (delta — wip to approved): Seed shotGrid with 2 wip shots; seed sequenceStats with counts {wip: 5, pending-review: 0, approved: 0, on-hold: 0, omit: 0}, total: 5. Fire onShotStatusChanged with shotId: shot_1, sequenceId: seq_1, fromStatus: wip, toStatus: approved. Assert counts.wip === 4, counts.approved === 1, approved_pct === 20, pending_review_backlog === 0.
    - Test 3 (cross-sequence guard — Pitfall 1): Same seed. Fire event with sequenceId: seq_DIFFERENT. Assert sequenceStats.value is unchanged (reference equality — same object).
    - Test 4 (fromStatus null coalesce — Pitfall 2): Same seed. Fire event with fromStatus: null, toStatus: approved. Assert counts.wip === 4 (decrement applied — null coalesced to wip) AND counts.approved === 1.
    - Test 5 (out-of-buffer stale_count — Pitfall 3 + D-19): Seed stale_count: 3. Fire event with shotId: shot_NOT_IN_BUFFER. Assert counts mutate (still decrement+increment) BUT stale_count === 3 unchanged.
    - Test 6 (pre-fetch null guard — Pitfall 9): sequenceStats.value = null. Fire event. Assert NO throw AND sequenceStats.value === null still (no spurious seed).
    - Test 7 (counts decrement clamp at 0 — Pitfall 1): Seed counts.wip = 0. Fire fromStatus: wip, toStatus: approved. Assert counts.wip === 0 (NOT -1; defensive Math.max(0, ...)).
    - Test 8 (stale_count delta — in-buffer, became stale): Seed a shot with latest_completed_version.completed_at = Date.now() - 30 * 86_400_000 (30 days ago), is_stale: false (status was approved). Fire event flipping to toStatus: wip. After delta: stale_count === prev + 1 (it is now stale per recomputeIsStaleClient).
    - Test 9 (stale_count delta — in-buffer, no longer stale): Seed a shot with is_stale: true, latest_completed_version.completed_at = Date.now() - 30 * 86_400_000. Fire event flipping to toStatus: approved. After delta: stale_count === prev - 1 AND clamped at 0 if prev === 0.
    - Test 10 (D-15 mirror — zero-version shots NEVER stale): Seed a shot with latest_completed_version === null. Fire event flipping to toStatus: wip. After delta: stale_count unchanged (recomputeIsStaleClient returns false for null version regardless of new status).
    - Test 11 (approved_pct on divide-by-zero): total === 0. After delta: approved_pct === 0 (NOT NaN).
    - Test 12 (pending_review_backlog tracks counts['pending-review']): Fire fromStatus: wip, toStatus: pending-review. Assert pending_review_backlog === counts['pending-review'] (mirrors via bracket access — Pitfall 10).
    - Test 13 (immutable replace — new object reference): Capture before = sequenceStats.value; fire event; assert sequenceStats.value !== before (signal MUST emit a fresh object so Preact re-renders).
  </behavior>

  <action>
    Two file edits: extend state/shot-grid.ts in place + create the new test file.

    **(A) Extend packages/dashboard/src/state/shot-grid.ts** per 23-PATTERNS.md §5 verbatim + 23-RESEARCH.md §"Pattern 2" + §"Code Example 4".

    1. Locate the existing shotGrid signal declaration at line 76. INSERT immediately below it (before the statusFilter signal at line 84):
       - Export the sequenceStats signal: `export const sequenceStats = signal<SequenceStats | null>(null);` with JSDoc covering (a) D-10 placement next to existing signals, (b) Plan 23-01 type origin, (c) D-19 best-effort caveat: stale_count delta correct only for shots within shotGrid.value.shots[]; out-of-buffer status changes leave stale_count at server-snapshot value; supervisor refreshes the sequence for a fresh snapshot.
       - Import SequenceStats from `../types/shot-grid.js` at the existing import block (additive — ShotGridRow is already imported; widen the named-imports list).

    2. Add a NEW module-scope private constant near the new signal:
       - `const STALE_SHOT_DAYS_MS = 14 * 86_400_000;` with JSDoc per 23-PATTERNS.md §5 lines 304-313: "Dashboard-side mirror of STALE_SHOT_DAYS = 14 at src/store/shot-status-repo.ts:54. MUST stay in sync — if the server threshold changes, this constant must be updated in lockstep. Phase 24+ candidate: hoist to a shared types module when the threshold becomes configurable." NOT exported (private to this module).

    3. Locate the existing onShotStatusChanged function at line 160. REPLACE the function BODY (NOT the function itself — preserve the name, signature, and export shape; line 160's `export function onShotStatusChanged(payload: ShotStatusChangedPayload): void {` stays verbatim). Per 23-PATTERNS.md §5 lines 316-353 + 23-UI-SPEC.md §"Interaction Contracts > SSE-driven counter updates" pseudocode:

       The new body MUST:
       - Keep `const current = shotGrid.value; if (current === null) return;` (line 162 unchanged).
       - Keep `if (current.sequence.id !== payload.sequenceId) return;` (line 163 unchanged — Pitfall 1 cross-sequence guard).
       - Find the matching shot in current.shots[] BEFORE mutating: `const idx = current.shots.findIndex((s) => s.id === payload.shotId);` and `const shotInBuffer = idx >= 0 ? current.shots[idx] : null;`. This capture is REQUIRED before the .map below because the new is_stale recomputation needs the OLD latest_completed_version.completed_at AND the OLD is_stale flag.
       - Compute the post-transition is_stale value: `const isStaleNow = shotInBuffer !== null ? recomputeIsStaleClient({ ...shotInBuffer, status: payload.toStatus }) : false;` BEFORE the .map (used in both the .map row update and applyStatsDelta).
       - Run the existing immutable .map to update shotGrid.value, BUT now the row gets both `status: payload.toStatus` AND `is_stale: isStaleNow` (replaces the existing single-field update at lines 167-169).
       - AFTER the shotGrid mutation: `const prevStats = sequenceStats.value; if (prevStats === null) return;` (Pitfall 9 — pre-fetch null guard; the existing per-row map still ran above, so the row update is preserved even without stats).
       - Compute `const effectiveFromStatus = payload.fromStatus ?? 'wip';` (Pitfall 2 — null fromStatus defaults to 'wip', the materialized starting status per Phase 20 schema default).
       - Call `sequenceStats.value = applyStatsDelta(prevStats, effectiveFromStatus, payload.toStatus, shotInBuffer);` — passes the PRE-mutation shotInBuffer reference because applyStatsDelta needs the OLD is_stale (the wasStale value).

    4. ADD two new private (module-scope, not exported) helpers BELOW onShotStatusChanged:

       `function applyStatsDelta(prev: SequenceStats, fromStatus: ShotStatus, toStatus: ShotStatus, shotInBuffer: ShotGridRow | null): SequenceStats` — per 23-PATTERNS.md §5 lines 355-390 verbatim:
       - `const counts = { ...prev.counts };` (spread for immutability).
       - `counts[fromStatus] = Math.max(0, counts[fromStatus] - 1);` (Pitfall 1 clamp).
       - `counts[toStatus] = counts[toStatus] + 1;` (no clamp; growth is always valid).
       - `const newBacklog = counts['pending-review'];` (Pitfall 10 — bracket access for hyphenated key).
       - `const newApprovedPct = prev.total === 0 ? 0 : Math.round((counts.approved / prev.total) * 100);` (divide-by-zero guard).
       - `let newStaleCount = prev.stale_count;` then conditional: if shotInBuffer is not null, compute wasStale = shotInBuffer.is_stale; isStaleNow = recomputeIsStaleClient({ ...shotInBuffer, status: toStatus }); if wasStale && !isStaleNow, newStaleCount = Math.max(0, newStaleCount - 1); if !wasStale && isStaleNow, newStaleCount = newStaleCount + 1. (No mutation outside this guard — Pitfall 3 + D-19 best-effort caveat.)
       - Return `{ total: prev.total, approved_pct: newApprovedPct, counts, pending_review_backlog: newBacklog, stale_count: newStaleCount }`. NOTE: total is never mutated by status transitions (a shot exists or it doesn't; status changes don't add/remove rows).

       `function recomputeIsStaleClient(row: ShotGridRow): boolean` — per 23-PATTERNS.md §5 lines 392-397 verbatim AND 23-UI-SPEC.md §"Interaction Contracts > recomputeIsStaleClient" pseudocode:
       - `if (row.latest_completed_version === null) return false;` (D-15 grace — zero-version shots NEVER stale, mirrors server behavior).
       - `if (row.status !== 'wip' && row.status !== 'pending-review') return false;` (OVR-02 status filter).
       - `return Date.now() - row.latest_completed_version.completed_at > STALE_SHOT_DAYS_MS;`

    DO NOT modify aggregateCounts computed (line 123) — it stays in place for the Phase 21 D-14 mini-pills row. DO NOT modify any existing signal. DO NOT relocate onShotStatusChanged (App.tsx:88 captures it by reference — relocation breaks the offSseEvent lifecycle). DO NOT change the existing imports beyond adding SequenceStats to the types/shot-grid.js named imports.

    **(B) Create packages/dashboard/src/state/__tests__/shot-grid-stats-delta.test.ts** per 23-PATTERNS.md §15 verbatim fixture pattern. Module-singleton reset in beforeEach MUST include `sequenceStats.value = null` (Phase 21 PATTERNS §14 landmine — state leaks across tests at module scope).

    Implement the 13 behavior tests above. Use seedStats() helper per 23-PATTERNS.md §15 lines 1106-1115 (default 5-wip baseline; override individual fields per test). Use seedShotGrid() helper per 23-PATTERNS.md §15 lines 1117-1142 (2 shots, both wip, no completed versions). For tests requiring stale shots, override latest_completed_version: { id: 'ver_old', thumbnail_url: '/api/versions/ver_old/thumbnail', completed_at: Date.now() - 30 * 86_400_000 } in the seedShotGrid call.

    Import surface: sequenceStats, shotGrid, selectedSequenceForGrid, statusFilter, showOmitted, activeView, onShotStatusChanged from `../shot-grid.js`. Import types from `../../types/shot-grid.js`.

    Test 13 (immutable replace) is critical for Preact rerender — assert with `expect(sequenceStats.value).not.toBe(before);` (reference inequality after delta).
  </action>

  <verify>
    <automated>cd /Users/macapple/comfyui-vfx-mcp/packages/dashboard && npx vitest run src/state/__tests__/shot-grid-stats-delta.test.ts src/state/__tests__/shot-grid.test.ts --reporter=verbose 2>&1 | tail -50</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx tsc --noEmit -p packages/dashboard 2>&1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - Test pass: `npx vitest run src/state/__tests__/shot-grid-stats-delta.test.ts` reports 13 tests green
    - Test pass: `npx vitest run src/state/__tests__/shot-grid.test.ts` (existing Phase 21 file) reports all tests STILL green — extending onShotStatusChanged body does NOT break the existing cross-sequence guard test or per-row map test
    - Source assertion: `grep -c "^export const sequenceStats" packages/dashboard/src/state/shot-grid.ts` outputs `1`
    - Source assertion: `grep -c "applyStatsDelta" packages/dashboard/src/state/shot-grid.ts` outputs a value greater than or equal to 2 (1 call site in onShotStatusChanged + 1 function declaration)
    - Source assertion: `grep -c "recomputeIsStaleClient" packages/dashboard/src/state/shot-grid.ts` outputs a value greater than or equal to 2 (1 or more call sites + 1 function declaration)
    - Source assertion: `grep -c "STALE_SHOT_DAYS_MS" packages/dashboard/src/state/shot-grid.ts` outputs a value greater than or equal to 2 (constant declaration + 1 or more use in recomputeIsStaleClient)
    - Source assertion: `grep -c "counts\['pending-review'\]" packages/dashboard/src/state/shot-grid.ts` outputs `1` (Pitfall 10 — bracket access for hyphenated key in applyStatsDelta)
    - Source assertion: `grep -c "Math.max(0," packages/dashboard/src/state/shot-grid.ts` outputs a value greater than or equal to 2 (counts[fromStatus] clamp + stale_count clamp)
    - Source assertion: `grep -c "payload.fromStatus ?? 'wip'" packages/dashboard/src/state/shot-grid.ts` outputs `1` (Pitfall 2 null-coalesce)
    - Source assertion: `grep -c "if (prevStats === null) return" packages/dashboard/src/state/shot-grid.ts` outputs `1` (Pitfall 9 pre-fetch guard — the second null check after the first shotGrid null check)
    - Pre-existing invariant: `grep -c "if (current.sequence.id !== payload.sequenceId) return" packages/dashboard/src/state/shot-grid.ts` outputs `1` (Pitfall 1 cross-sequence guard — Phase 21 D-22 line 163 preserved)
    - Pre-existing invariant: `grep -c "export const aggregateCounts" packages/dashboard/src/state/shot-grid.ts` outputs `1` (Phase 21 D-14 mini-pills source — Phase 23 does NOT replace)
    - Typecheck assertion: `cd packages/dashboard && npx tsc --noEmit` exits 0
    - JSDoc check: `grep -c "best-effort" packages/dashboard/src/state/shot-grid.ts` outputs a value greater than or equal to 1 (D-19 caveat documented per RESEARCH Pitfall 3 recommendation)
  </acceptance_criteria>

  <done>
    sequenceStats signal exported with null initial. onShotStatusChanged body extended in place (function identity preserved for App.tsx:88 onSseEvent registration). applyStatsDelta + recomputeIsStaleClient private helpers added. STALE_SHOT_DAYS_MS = 14 * 86_400_000 mirror documented as "MUST stay in sync with server". 13 tests cover every Pitfall (cross-sequence, fromStatus=null, out-of-buffer, pre-fetch null, clamp-at-zero, hyphenated-key bracket access, immutable replace, divide-by-zero, D-15 grace, in-buffer became/stopped-being-stale). Phase 21 tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02: Extend SequenceHeader with stats subrow (D-04 augment-don't-replace) + ShotGridCard with conditional stale border + tests</name>

  <read_first>
    - packages/dashboard/src/components/SequenceHeader.tsx (the file being modified — read in full; lines 1-30 imports, lines 30-35 SequenceHeaderProps, line 66 outer flex-col container, lines 67-88 existing chevron+name row, lines 89-111 existing mini-pills row)
    - packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx (existing Phase 21 test — read in full; lines 1-48 fixture pattern, lines 50-119 existing render-assertion patterns)
    - packages/dashboard/src/components/ShotGridCard.tsx (the file being modified — read in full; line 75 outer div, line 84 thumbnail button aria-label, lines 153-156 omit opacity-40 wrapper)
    - packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx (existing Phase 21 test — read in full; lines 1-48 fixture + buildShot helper at lines 26-39, lines 78-90 existing wrapper class test)
    - packages/dashboard/src/components/ProgressBar.tsx (Plan 23-02 — the primitive being imported)
    - packages/dashboard/src/lib/copy.ts (Plan 23-02 — the 9 new constants being consumed)
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-04 + D-05 stats subrow composition; D-07 stale border treatment; D-09 coexistence; D-20 backlog callout; D-21 no animations)
    - .planning/phases/23-production-stats/23-RESEARCH.md §"Code Example 6" + §"Pattern 4" verbatim component shapes
    - .planning/phases/23-production-stats/23-PATTERNS.md §2 "SequenceHeader.tsx" + §3 "ShotGridCard.tsx" + §13 "SequenceHeader.stats.test.tsx" + §14 "ShotGridCard.stale.test.tsx"
    - .planning/phases/23-production-stats/23-UI-SPEC.md §"Layout Reference Diagram" (visual structure lock) + §"Color > Subrow color element matrix" (token-per-element table) + §"Copywriting Contract" (verbatim aria-label compositions) + §"Accessibility Checklist" (every aria attribute required)
    - .planning/phases/23-production-stats/23-VALIDATION.md (Wave 0 component test rows)
  </read_first>

  <files>packages/dashboard/src/components/SequenceHeader.tsx, packages/dashboard/src/components/__tests__/SequenceHeader.stats.test.tsx, packages/dashboard/src/components/ShotGridCard.tsx, packages/dashboard/src/components/__tests__/ShotGridCard.stale.test.tsx</files>

  <behavior>
    SequenceHeader.stats.test.tsx (NEW file):
    - Test 1: Render with stats = buildStats({ approved_pct: 60 }). Query [role="progressbar"]. Assert aria-valuenow === '60', aria-valuemin === '0', aria-valuemax === '100', aria-label === 'Approval progress for SEQ_010'.
    - Test 2: Render with stats = buildStats({ pending_review_backlog: 3 }). Query [role="status"] (the backlog callout pill). Assert it exists, aria-label === 'Pending review backlog: 3 awaiting review', textContent contains '3' AND 'awaiting review' (case-insensitive).
    - Test 3 (hide-on-zero — backlog): Render with stats = buildStats({ pending_review_backlog: 0 }). Query [role="status"]. Assert it does NOT exist.
    - Test 4 (hide-on-zero — stale): Render with stats = buildStats({ stale_count: 0 }). Assert no element with text containing "stale" (case-insensitive).
    - Test 5 (hide-on-zero — stale present): Render with stats = buildStats({ stale_count: 2 }). Assert an element with aria-label === 'Stale shots: 2' exists AND textContent includes "2 stale" (lowercase per UI-SPEC).
    - Test 6 (subrow hidden — null stats): Render with stats = null. Query [role="progressbar"]. Assert it does NOT exist.
    - Test 7 (subrow hidden — total === 0): Render with stats = buildStats({ total: 0 }). Query [role="progressbar"]. Assert it does NOT exist (Open Question 2 resolution; symmetric with mini-pills hide-on-zero).
    - Test 8 (mini-pills row preserved — Phase 21 D-14 invariant): Render with both counts AND stats. Assert ALL Phase 21 mini-pill assertions still pass (existing test file has these; the new file extends, not replaces).
    - Test 9 (chevron collapse hides subrow): Render with expanded = false. Assert the entire stats subrow does NOT render (mini-pills + subrow both hidden — both are siblings inside the collapsible parent). Note: actual chevron collapse logic depends on whether SequenceHeader renders rows conditionally on expanded — if it does, just assert hide; if the parent owns the conditional, this test asserts the props pass-through.

    ShotGridCard.stale.test.tsx (NEW file):
    - Test 1: Render with shot = buildShot({ is_stale: true }). Query for the outer .group.relative div. Assert className contains 'border-2' AND 'border-[var(--color-shot-stale)]'.
    - Test 2: Render with shot = buildShot({ is_stale: false }). Query outer .group.relative. Assert className does NOT contain 'border-2' OR 'border-[var(--color-shot-stale)]'.
    - Test 3 (aria-label append): Render with shot = buildShot({ is_stale: true, name: 'SH_020' }). Query the thumbnail button (first button element). Assert aria-label === 'Open version drawer for SH_020 — stale' (or whatever the existing Phase 21 SHOT_CARD_OPEN_ARIA_PREFIX produces concatenated with SHOT_CARD_STALE_ARIA_SUFFIX).
    - Test 4 (aria-label NOT appended when not stale): Render with shot = buildShot({ is_stale: false, name: 'SH_020' }). Assert thumbnail button aria-label does NOT contain ' — stale'.
    - Test 5 (omit+stale logical exclusion — defensive coexistence): Render with shot = buildShot({ status: 'omit', is_stale: true }) (intentionally inconsistent — server should never emit this but defensive). Assert: the outer omit wrapper exists (.opacity-40 query truthy) AND inside it the card body wrapper has the amber border class. The two wrappers coexist without crashing (D-09 coexistence-safety).
    - Test 6 (existing Phase 22 D-13 structure unchanged): Render with shot = buildShot({ is_stale: false }). Assert the existing thumbnail button + status pill + quick-approve hover icon + version-count badge ALL still render as before (no regressions to Phase 22 functionality).
  </behavior>

  <action>
    Four file edits: extend SequenceHeader.tsx + create SequenceHeader.stats.test.tsx + extend ShotGridCard.tsx + create ShotGridCard.stale.test.tsx.

    **(A) Extend packages/dashboard/src/components/SequenceHeader.tsx** per 23-PATTERNS.md §2 + 23-UI-SPEC.md §"Layout Reference Diagram".

    1. Extend the imports block per 23-PATTERNS.md §2 lines 104-122. Add `AlertCircle` to the existing lucide-preact import line; add `SequenceStats` to the existing types import; add `ProgressBar` from `./ProgressBar.js`; add the 8 new copy constants STATS_PROGRESS_ARIA_PREFIX, STATS_APPROVED_LABEL_SUFFIX, STATS_BACKLOG_CALLOUT_SINGULAR, STATS_BACKLOG_CALLOUT_PLURAL, STATS_BACKLOG_CALLOUT_ARIA_PREFIX, STATS_STALE_INLINE_SINGULAR, STATS_STALE_INLINE_PLURAL, STATS_STALE_INLINE_ARIA_PREFIX from `../lib/copy.js`.

    2. Extend SequenceHeaderProps interface: add `stats: SequenceStats | null;` — REQUIRED field (NOT optional `?`), null-allowed. JSDoc per 23-PATTERNS.md §2 lines 127-134.

    3. Extend the function signature destructuring to accept stats.

    4. Insert a NEW subrow as a sibling between the existing chevron+name div at lines 67-88 and the existing mini-pills div at lines 89-111. The subrow MUST be a direct child of the outer header.flex-col-gap-2 parent — this ensures the chevron's collapse mechanism affects it automatically. Per 23-PATTERNS.md §2 lines 139-181 verbatim shape + 23-UI-SPEC.md §"Edge Case 8" recommendation, use the defensive `stats != null && stats.total > 0` conditional (double-equals to match both null and undefined).

       The subrow JSX is a single outer div with `class="flex items-center gap-3"` containing three children in order:
       1. ProgressBar primitive with props value={stats.approved_pct}, max={100}, label set to the template `${stats.approved_pct}${STATS_APPROVED_LABEL_SUFFIX}` and ariaLabel set to `${STATS_PROGRESS_ARIA_PREFIX}${sequenceName}`.
       2. CONDITIONAL `stats.pending_review_backlog > 0 && (...)` — span with role="status", aria-label set to a template concatenating STATS_BACKLOG_CALLOUT_ARIA_PREFIX + the backlog count + space + the singular-or-plural noun, class="inline-flex items-center gap-1 rounded-full bg-[var(--color-accent)] px-2 py-0.5 text-xs uppercase tracking-widest text-[var(--color-bg)]". Inside: AlertCircle size={12} aria-hidden="true"; span.num with the count number; span with the singular-or-plural noun (singular when backlog === 1, plural otherwise — both happen to be 'awaiting review' but kept as 2 constants per UI-SPEC).
       3. CONDITIONAL `stats.stale_count > 0 && (...)` — span with aria-label set to STATS_STALE_INLINE_ARIA_PREFIX + the stale count, class="text-xs text-[var(--color-shot-stale)]". Inside: span.num with the count number; space; the singular-or-plural noun (both 'stale').

       Source for verbatim JSX skeleton: 23-PATTERNS.md §2 lines 142-181 (entire subrow rendered inline). Use the exact class strings from that excerpt.

       Subrow lives BETWEEN the existing chevron row and the existing mini-pills row. DO NOT replace the mini-pills row. DO NOT remove or modify any existing element. The gap-3 on the subrow matches UI-SPEC §"Spacing > Subrow horizontal gap between stat elements".

    5. DO NOT change the chevron toggle button, the sequence name h2, or any pre-existing class strings. DO NOT animate the subrow on mount (D-21 — instant render).

    **(B) Create packages/dashboard/src/components/__tests__/SequenceHeader.stats.test.tsx** per 23-PATTERNS.md §13 verbatim fixture + render-assertion pattern. Use buildStats({ ... }) factory helper (default produces full-non-zero baseline: total=20, approved_pct=60, counts mixed, backlog=3, stale_count=1). Use renderHeader({ stats: ... }) helper that wraps the component render with sensible defaults.

    Implement the 9 tests above. Test 9 (chevron collapse): if SequenceHeader renders rows unconditionally on expanded, the test asserts via the prop pass-through only; if the component conditionally renders on expanded, test queries the subrow's [role="progressbar"] and asserts absent when collapsed.

    **(C) Extend packages/dashboard/src/components/ShotGridCard.tsx** per 23-PATTERNS.md §3 + 23-UI-SPEC.md §"Layout Reference Diagram" + 23-RESEARCH.md §"Pattern 4".

    1. Add SHOT_CARD_STALE_ARIA_SUFFIX to the existing `../lib/copy.js` import block.

    2. Update the outer div at line ~75. Replace the static class string with a template-literal class string per 23-PATTERNS.md §3 lines 192-204. The new class string is the existing `group relative w-full overflow-hidden rounded` concatenated with a conditional that, when shot.is_stale === true, appends ' border-2 border-[var(--color-shot-stale)]' (note the leading space). Tailwind v4 arbitrary-value syntax `border-[var(--color-shot-stale)]` resolves the new theme token from Plan 23-01.

       Source for verbatim shape: 23-PATTERNS.md §3 lines 194-204.

    3. Update the thumbnail button's aria-label at line ~84 per 23-PATTERNS.md §3 lines 209-216. The existing line composes SHOT_CARD_OPEN_ARIA_PREFIX with shot.name. Append the new suffix conditionally: when shot.is_stale === true, append SHOT_CARD_STALE_ARIA_SUFFIX. The full template is the existing prefix + shot.name + the conditional suffix (empty string when not stale).

    4. DO NOT change ANY other element. DO NOT change the omit wrapper at line ~154. DO NOT change the existing QuickApproveButton, ShotStatusPill, WarningPill, or focus-ring placement. Per D-09 coexistence: omit shots are by-def NOT stale (mutual exclusion via OVR-02 status filter); but the defensive border + opacity-40 coexistence MUST work without DOM crash even if the server emits an invalid combination.

    **(D) Create packages/dashboard/src/components/__tests__/ShotGridCard.stale.test.tsx** per 23-PATTERNS.md §14 verbatim fixture + buildShot factory (extend with is_stale: false default; tests opt-in via overrides). Import the existing test patterns from ShotGridCard.test.tsx — render via render(ShotGridCard with shot prop and onSelect prop set to vi.fn()).

    Implement the 6 tests above. Test 5 (omit+stale defensive): the test seeds an inconsistent state to verify DOM doesn't crash; the assertion is that the omit wrapper exists AND the inner card body has the border (per 23-PATTERNS.md §14 lines 1077-1081 querying `.opacity-40 > .group.relative`).
  </action>

  <verify>
    <automated>cd /Users/macapple/comfyui-vfx-mcp/packages/dashboard && npx vitest run src/components/__tests__/SequenceHeader.stats.test.tsx src/components/__tests__/ShotGridCard.stale.test.tsx src/components/__tests__/SequenceHeader.test.tsx src/components/__tests__/ShotGridCard.test.tsx --reporter=verbose 2>&1 | tail -60</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx tsc --noEmit -p packages/dashboard 2>&1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - Test pass: `npx vitest run src/components/__tests__/SequenceHeader.stats.test.tsx` reports 9 tests green
    - Test pass: `npx vitest run src/components/__tests__/ShotGridCard.stale.test.tsx` reports 6 tests green
    - Test pass: `npx vitest run src/components/__tests__/SequenceHeader.test.tsx` (existing Phase 21 file) reports all tests STILL green — adding the subrow MUST NOT break any existing assertion
    - Test pass: `npx vitest run src/components/__tests__/ShotGridCard.test.tsx` (existing Phase 21+22 file) reports all tests STILL green — adding the conditional border class and aria-suffix MUST NOT break any existing assertion
    - Source assertion (SequenceHeader): `grep -c "stats: SequenceStats \\| null" packages/dashboard/src/components/SequenceHeader.tsx` outputs `1` (props extension)
    - Source assertion (SequenceHeader): `grep -c "role=\"progressbar\"" packages/dashboard/src/components/SequenceHeader.tsx` outputs `0` — the role is on the ProgressBar primitive itself, NOT duplicated in SequenceHeader (the only [role="progressbar"] in the DOM comes from the ProgressBar component)
    - Source assertion (SequenceHeader): `grep -c "role=\"status\"" packages/dashboard/src/components/SequenceHeader.tsx` outputs `1` (the backlog callout pill — Phase 23 NEW; no other role="status" in the file)
    - Source assertion (SequenceHeader): `grep -c "AlertCircle size={12}" packages/dashboard/src/components/SequenceHeader.tsx` outputs `1` (Phase 23 backlog callout icon)
    - Source assertion (SequenceHeader): `grep -c "STATS_PROGRESS_ARIA_PREFIX\|STATS_APPROVED_LABEL_SUFFIX\|STATS_BACKLOG_CALLOUT" packages/dashboard/src/components/SequenceHeader.tsx` outputs a value greater than or equal to 4 (Phase 23 copy constants referenced — verifies copy imports landed)
    - Source assertion (SequenceHeader): `grep -c "stats != null && stats.total > 0" packages/dashboard/src/components/SequenceHeader.tsx` outputs `1` (defensive subrow visibility guard per UI-SPEC Edge Case 8)
    - Source assertion (ShotGridCard): `grep -c "shot.is_stale" packages/dashboard/src/components/ShotGridCard.tsx` outputs a value greater than or equal to 2 (1 in className conditional + 1 in aria-label conditional)
    - Source assertion (ShotGridCard): `grep -c "border-\\[var(--color-shot-stale)\\]" packages/dashboard/src/components/ShotGridCard.tsx` outputs `1` (theme token referenced)
    - Source assertion (ShotGridCard): `grep -c "SHOT_CARD_STALE_ARIA_SUFFIX" packages/dashboard/src/components/ShotGridCard.tsx` outputs `1` (aria suffix imported and applied)
    - Pre-existing invariant (ShotGridCard): `grep -c "opacity-40 transition-opacity" packages/dashboard/src/components/ShotGridCard.tsx` outputs `1` (Phase 22 D-13 omit wrapper preserved)
    - Pre-existing invariant (SequenceHeader): existing chevron toggle aria-label + existing mini-pills with hide-on-zero — `grep -c "AGGREGATE_COUNTS_REGION_LABEL_PREFIX" packages/dashboard/src/components/SequenceHeader.tsx` outputs `1` (mini-pills row preserved per D-04)
    - Typecheck assertion: `cd packages/dashboard && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    SequenceHeader has 3 stacked rows: existing chevron+name row, NEW Phase 23 stats subrow with ProgressBar + backlog callout + stale-count text, existing Phase 21 D-14 mini-pills row. Chevron collapses all 3. Hide-on-zero rules: subrow hidden when null OR total === 0; backlog callout hidden when backlog === 0; stale-count text hidden when stale_count === 0. ShotGridCard outer div gets conditional amber border when is_stale; thumbnail button aria-label gets ' — stale' suffix when is_stale. Existing Phase 22 structure preserved verbatim. Two new test files (15 tests total) cover every visibility rule + aria-label composition + coexistence-safety case. Existing test files still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03: Wire ShotGridView to seed sequenceStats and pass stats to SequenceHeader</name>

  <read_first>
    - packages/dashboard/src/views/ShotGridView.tsx (the file being modified — read in full; lines 47-63 imports, lines 90-150 fetch effect + state seed, lines 290-299 SequenceHeader render call)
    - packages/dashboard/src/state/shot-grid.ts (Plan 23-01 + 23-03 Task 01 — verify sequenceStats signal is exported)
    - packages/dashboard/src/components/SequenceHeader.tsx (Plan 23-03 Task 02 — verify stats prop is required)
    - .planning/phases/23-production-stats/23-CONTEXT.md (D-10 sequenceStats co-location)
    - .planning/phases/23-production-stats/23-RESEARCH.md §"Code Example 5" verbatim seed + prop-pass pattern
    - .planning/phases/23-production-stats/23-PATTERNS.md §6 "ShotGridView.tsx" (verbatim three-line edits)
    - .planning/phases/23-production-stats/23-UI-SPEC.md §"Component Inventory" row for ShotGridView
  </read_first>

  <files>packages/dashboard/src/views/ShotGridView.tsx</files>

  <behavior>
    The component-level test for this wiring lives in the existing ShotGridView test (if present) OR is implicitly covered by Plan 23-03 Task 01's shot-grid-stats-delta.test.ts (which verifies sequenceStats is mutated by SSE events). The wire-up itself has two observable behaviors:
    - On fetchShotGrid resolution, sequenceStats.value is set to res.stats (per Plan 23-02's wider envelope shape).
    - On sequence change (selectedSequenceForGrid.value mutation), sequenceStats.value is cleared to null BEFORE fetch fires (no stale snapshot from prior sequence).
    - SequenceHeader render call now passes stats={sequenceStats.value} alongside the existing counts={aggregateCounts.value} prop. Without this prop, the dashboard typecheck fails (Plan 23-03 Task 02 declared stats as REQUIRED on the props interface).

    Test approach: typecheck pass (the props are required, so omitting them fails compilation), plus the integrated Vite production build asserting the whole dashboard compiles.

    Optional follow-up test (if a ShotGridView.test.tsx exists): assert that after a mocked fetchShotGrid resolution returning res = { stats: someValue, ... }, the SequenceHeader renders with the stats subrow visible. This is covered indirectly by the integration tests at Plan 04.
  </behavior>

  <action>
    Three surgical line additions to packages/dashboard/src/views/ShotGridView.tsx per 23-PATTERNS.md §6 + 23-RESEARCH.md §"Code Example 5":

    1. **Import sequenceStats** at the existing `from '../state/shot-grid.js'` import block (around lines 53-63). Add `sequenceStats` to the named-imports list (additive — the existing `aggregateCounts` and other signals stay in the same block).

    2. **Seed on fetch resolution** at the existing fetchShotGrid.then resolved branch (around line 132-145). After the existing `shotGrid.value = res;` line, ADD ONE NEW LINE: `sequenceStats.value = res.stats;`. This is a single-line addition; the .catch branch does NOT clear sequenceStats (same pattern as the existing shotGrid handling — stays at prior snapshot on error).

    3. **Clear on sequence change** at the existing useEffect prologue. The existing effect already sets `shotGrid.value = null` BEFORE firing fetch (line ~124 area; if absent in the current code, the effect at lines 122-150 must clear stale state first). After the existing `shotGrid.value = null;` clear line, ADD ONE NEW LINE: `sequenceStats.value = null;`. This ensures switching from sequence A to sequence B does not briefly show A's stats while B's fetch is in flight (Pitfall 9 — pre-fetch null guard works because sequenceStats is already null when stale SSE events arrive).

    4. **Pass stats prop** at the existing SequenceHeader render call (around lines 290-299). Inside the JSX block, ADD ONE NEW LINE: `stats={sequenceStats.value}` — placed between the existing `counts={aggregateCounts.value}` line and the closing `/>`. The signal `.value` access is required so Preact tracks the dependency.

    5. **Do not change anything else in this file.** Do NOT change the loadMore effect at lines ~172-214 (per 23-PATTERNS.md §6 lines 423-424 — "Load-more does NOT re-seed stats: stats are sequence-wide and pagination-independent; the server response on subsequent pages still includes stats but ignore it"). Do NOT change the fetch URL or query params. Do NOT change the existing error handling.

    Reference: 23-PATTERNS.md §6 lines 408-440 contain the exact three-edit pattern.
  </action>

  <verify>
    <automated>cd /Users/macapple/comfyui-vfx-mcp && npx tsc --noEmit -p packages/dashboard 2>&1 | head -20</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp/packages/dashboard && npx vite build 2>&1 | tail -10</automated>
    <automated>cd /Users/macapple/comfyui-vfx-mcp/packages/dashboard && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - Typecheck pass: `cd packages/dashboard && npx tsc --noEmit` exits 0 (the new required stats prop on SequenceHeader is satisfied)
    - Build pass: `cd packages/dashboard && npx vite build` exits 0 (production bundle compiles)
    - Source assertion: `grep -c "sequenceStats" packages/dashboard/src/views/ShotGridView.tsx` outputs a value greater than or equal to 3 (1 import + 1 seed on fetch resolution + 1 clear on sequence change + 1 prop pass to SequenceHeader = 4; allowing some leniency for the grep boundary — minimum 3)
    - Source assertion: `grep -c "sequenceStats.value = res.stats" packages/dashboard/src/views/ShotGridView.tsx` outputs `1` (the fetch.then seed line)
    - Source assertion: `grep -c "sequenceStats.value = null" packages/dashboard/src/views/ShotGridView.tsx` outputs `1` (the sequence-change clear line)
    - Source assertion: `grep -c "stats={sequenceStats.value}" packages/dashboard/src/views/ShotGridView.tsx` outputs `1` (the SequenceHeader prop pass)
    - Pre-existing invariant: `grep -c "counts={aggregateCounts.value}" packages/dashboard/src/views/ShotGridView.tsx` outputs `1` (Phase 21 prop preserved; not replaced)
    - Pre-existing invariant: `grep -c "shotGrid.value = res" packages/dashboard/src/views/ShotGridView.tsx` outputs `1` (Phase 21 seed preserved)
    - Pre-existing invariant: `grep -c "fetchShotGrid" packages/dashboard/src/views/ShotGridView.tsx` outputs a value greater than or equal to 2 (initial fetch + loadMore — both preserved)
    - Dashboard full suite: `cd packages/dashboard && npx vitest run` exits 0 (existing tests + new tests from Tasks 03-01 and 03-02 all green)
  </acceptance_criteria>

  <done>
    ShotGridView seeds sequenceStats from fetchShotGrid response and clears on sequence change. SequenceHeader receives stats prop wired to the new signal. No other view-layer change. Dashboard typecheck + build + test suite all green. loadMore effect untouched (stats are sequence-wide, not pagination-bound).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE wire → state/shot-grid handler | Untrusted-ish payload: `shotStatusChangedPayload` arrives via SSE; though server-emitted, the client must validate cross-sequence + null-fromStatus guards |
| Browser DOM → component render | Untrusted input: `sequenceName`, count numbers, stale_count flow into JSX text. Preact auto-escapes. |
| Dashboard fetch → SequenceHeader render | The fetched envelope (stats + per-row is_stale) crosses to the component tree. The wire shape is locked by Plan 23-01 types + Plan 23-02 HTTP test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Tampering | SSE payload `payload.fromStatus` could be malformed (null when shot has no history; an unexpected value if Phase 20 schema drifts) | mitigate | Pitfall 2 mitigation: `payload.fromStatus ?? 'wip'` coalesces null to a valid ShotStatus. TypeScript enum narrows toStatus to the 5-value union (no null on toStatus per events.ts:76). Unexpected status values would surface as TypeScript compile errors at the SSE typing layer — out of scope for runtime mitigation. |
| T-23-03-02 | Tampering | Cross-sequence SSE event manipulating stats for the wrong sequence | mitigate | Pitfall 1 mitigation: existing cross-sequence guard at state/shot-grid.ts:163 (`if (current.sequence.id !== payload.sequenceId) return;`) — preserved in the extended onShotStatusChanged body. The new applyStatsDelta call is AFTER both guards. Verified by Test 3 in the new test file. |
| T-23-03-03 | Denial | Rapid successive SSE events for the same shot trigger excess signal recomputation | accept | applyStatsDelta is O(1) per event; no batching needed (verified Edge Case 6 in UI-SPEC). Each event re-emits a fresh sequenceStats.value object; Preact re-render is dedupable via signal reference equality. |
| T-23-03-04 | Cross-Site Scripting | Stats count numbers + sequenceName + aria-label compositions flow into JSX | mitigate | All dynamic content rendered as JSX text children (Preact auto-escapes). Numeric values (counts, percentages, stale_count) cannot contain markup. The aria-label compositions use template literals → string concatenation, no innerHTML, no dangerouslySetInnerHTML. |
| T-23-03-05 | Information Disclosure | sequenceStats reveals activity patterns visible in the DOM | accept | v1.3 single-user scope. No third party can observe the dashboard DOM. The stats are computed from data the user already has access to (shots in their own project). |
| T-23-03-06 | Tampering | Stale-count drift outside paginated buffer (D-19 best-effort caveat) | accept | Documented in state/shot-grid.ts JSDoc as a known limitation. Supervisor refreshes the sequence for a fresh snapshot. Drift cannot exceed the count of shots outside the buffer (bounded). |
| T-23-03-07 | Repudiation | A status transition emits SSE → onShotStatusChanged applies delta → server snapshot mismatch | accept | Acceptable for v1.3. The next fetchShotGrid call re-seeds sequenceStats from server truth, overwriting any client-side drift. Phase 24+ candidate: periodic background poll if drift becomes a user complaint. |
| T-23-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 23 Plan 03 installs ZERO new packages. All imports are first-party or already in Phase 21-22 lock files (preact, @preact/signals, lucide-preact AlertCircle — already in node_modules at ^1.9.0 per Phase 21 install). No supply-chain risk added by this plan. |
</threat_model>

<verification>
After all 3 tasks complete:

1. Dashboard typecheck: `cd packages/dashboard && npx tsc --noEmit` — green.
2. Dashboard production build: `cd packages/dashboard && npx vite build` — green.
3. Dashboard full suite: `cd packages/dashboard && npx vitest run` — green; new tests reported: shot-grid-stats-delta (13), SequenceHeader.stats (9), ShotGridCard.stale (6) = 28 new tests. Existing test count must not regress.
4. Server full suite: `npx vitest run` — green (Phase 23 makes no server-side changes in Plan 03; this is a safety regression check).
5. Architecture purity: `npx vitest run src/__tests__/architecture-purity.test.ts` — still green (no new bindings, no forbidden cross-tree imports — dashboard does not reach into src/).
6. Tool budget: `npx vitest run src/__tests__/tool-budget.test.ts` — still === 7.
7. Pitfall 6 (theme.css both blocks): `grep -c "^\s*--color-shot-stale" packages/dashboard/src/styles/theme.css` returns 2 (carried over from Plan 23-01; verify nothing regressed).
8. Pre-existing component test files (SequenceHeader.test.tsx + ShotGridCard.test.tsx + shot-grid.test.ts) STILL pass with the new code (extension MUST NOT break Phase 21 / Phase 22 / Phase 20 assertions).
</verification>

<success_criteria>
- state/shot-grid.ts: sequenceStats signal exported (initial null), onShotStatusChanged body extended in place with applyStatsDelta call, two private helpers added, STALE_SHOT_DAYS_MS constant documented as "MUST stay in sync with server". 13 stats-delta tests green covering every Pitfall (cross-sequence, fromStatus=null, out-of-buffer, pre-fetch null, clamp-at-zero, hyphenated-key bracket access, immutable replace, divide-by-zero, D-15 grace, in-buffer became/stopped-being-stale).
- SequenceHeader.tsx: stats: SequenceStats | null prop added (REQUIRED). New stats subrow inserted between chevron+name row and mini-pills row. Subrow contains ProgressBar (always when visible), conditional backlog callout (hide-on-zero), conditional inline stale count (hide-on-zero). Subrow hidden when stats null OR total === 0. 9 component tests cover every visibility rule.
- ShotGridCard.tsx: outer div className gets conditional border-2 border-[var(--color-shot-stale)] when shot.is_stale === true. Thumbnail button aria-label appends SHOT_CARD_STALE_ARIA_SUFFIX when is_stale. 6 component tests cover both states + omit-stale coexistence + Phase 22 D-13 invariants.
- ShotGridView.tsx: sequenceStats imported, seeded on fetchShotGrid resolution, cleared on sequence change, passed as stats prop to SequenceHeader. Dashboard typecheck + build + full suite green.
- All Phase 21 + Phase 22 existing tests still pass. Tool budget === 7. Architecture purity holds.
</success_criteria>

<output>
Create `.planning/phases/23-production-stats/23-03-SUMMARY.md` documenting:
- The 4 modified source files + 3 new test files + their test count totals
- Reference equality preservation proof: `git diff packages/dashboard/src/state/shot-grid.ts | grep "^-export function onShotStatusChanged"` MUST be empty (no removal — only body change inside the existing function); link to App.tsx:88 confirming the onSseEvent registration still works against the same function reference
- 13 + 9 + 6 = 28 new tests landed; existing test count + 28 = post-Phase-23-Wave-3 total
- The 9 copy constants verified consumed in components (grep -c results from acceptance criteria)
- Phase 21 + Phase 22 regression status (all existing tests green; no assertions broken by additive changes)
- Pitfall coverage matrix: which test in which file covers which pitfall (12-row table)
- One-line preview of Plan 23-04 scope (Phase gate — full-suite regression + WCAG visual smoke + VALIDATION.md nyquist flip)
</output>
