---
plan_id: 21-01
phase: 21
phase_name: shot-grid-view
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/types/events.ts
  - packages/dashboard/src/types/shot-grid.ts
  - packages/dashboard/src/styles/theme.css
  - packages/dashboard/src/lib/copy.ts
  - packages/dashboard/src/lib/time.ts
  - packages/dashboard/src/lib/__tests__/time.test.ts
  - src/store/shot-status-repo.ts
  - src/store/__tests__/shot-status-repo-grid.test.ts
autonomous: true
requirements: [GRID-02, GRID-04]

must_haves:
  truths:
    - "Dashboard EngineEventMap includes 'shot.status_changed': ShotStatusChangedPayload (camelCase wire-shape)"
    - "theme.css declares 5 --color-shot-status-* tokens in both @theme and [data-theme=\"light\"] blocks"
    - "lib/copy.ts exports the full Phase 21 copy block (≥ 27 new constants)"
    - "lib/time.ts exports formatRelativeTime(epochMs, nowMs?) producing 'just now' | 'Nm ago' | 'Nh ago' | 'Nd ago' | 'Nw ago' | 'Nmo ago' buckets"
    - "types/shot-grid.ts exports ShotGridResponse, ShotGridRow, ShotGridSequenceMeta, ShotStatus"
    - "shot-status-repo.ts exports listShotsForGrid via window-function CTE; EXPLAIN QUERY PLAN test confirms no CORRELATED SCALAR SUBQUERY referencing 'ranked'"
    - "shot-status-repo.ts exports encodeShotGridCursor / decodeShotGridCursor / listShotsForGridSqlText"
  artifacts:
    - path: "packages/dashboard/src/types/events.ts"
      provides: "ShotStatusChangedPayload interface and EngineEventMap['shot.status_changed'] entry"
      contains: "ShotStatusChangedPayload"
    - path: "packages/dashboard/src/types/shot-grid.ts"
      provides: "ShotGridResponse, ShotGridRow, ShotGridSequenceMeta, ShotStatus types"
      exports: ["ShotGridResponse", "ShotGridRow", "ShotGridSequenceMeta", "ShotStatus"]
    - path: "packages/dashboard/src/styles/theme.css"
      provides: "5 --color-shot-status-* tokens × 2 themes (10 declarations)"
      contains: "--color-shot-status-wip"
    - path: "packages/dashboard/src/lib/copy.ts"
      provides: "27 Phase 21 copy constants"
      contains: "HEADER_HOME_ARIA_LABEL"
    - path: "packages/dashboard/src/lib/time.ts"
      provides: "formatRelativeTime pure function"
      exports: ["formatRelativeTime"]
    - path: "packages/dashboard/src/lib/__tests__/time.test.ts"
      provides: "formatRelativeTime unit tests covering all 6 buckets and edges"
    - path: "src/store/shot-status-repo.ts"
      provides: "listShotsForGrid + ShotGridCursor + encode/decode + listShotsForGridSqlText"
      exports: ["listShotsForGrid", "encodeShotGridCursor", "decodeShotGridCursor", "listShotsForGridSqlText"]
    - path: "src/store/__tests__/shot-status-repo-grid.test.ts"
      provides: "EXPLAIN QUERY PLAN no-correlated-ranked assertion + cursor walk + null-coalesce tests"
  key_links:
    - from: "packages/dashboard/src/types/shot-grid.ts"
      to: "packages/dashboard/src/types/events.ts"
      via: "type alias ShotStatus = ShotStatusChangedPayload['toStatus']"
      pattern: "ShotStatusChangedPayload"
    - from: "src/store/shot-status-repo.ts"
      to: "src/store/schema.ts"
      via: "import { shots, shotStatusEvents }"
      pattern: "import.*shots.*from.*schema"
    - from: "src/store/__tests__/shot-status-repo-grid.test.ts"
      to: "src/store/shot-status-repo.ts"
      via: "imports listShotsForGrid, decodeShotGridCursor, listShotsForGridSqlText"
      pattern: "from.*shot-status-repo"
---

<objective>
Close the load-bearing Phase-20-to-21 wire-shape gap and lay all Wave 1 foundations: the dashboard-local `ShotStatusChangedPayload` type mirror, 5 new theme color tokens, the Phase 21 copy block, the `formatRelativeTime` helper, the shared `types/shot-grid.ts` types, AND the SQL-side `listShotsForGrid` window-function CTE with its EXPLAIN QUERY PLAN no-N+1 assertion. All tasks are independent leaves with no cross-file dependencies; together they unblock Waves 2-5.

Purpose: Without `ShotStatusChangedPayload` in `EngineEventMap`, every SSE-handling task downstream fails to type-check (RESEARCH Pitfall 1). Without the 5 theme tokens, `<ShotStatusPill/>` (Wave 2) has nothing to render. Without `listShotsForGrid`, the engine facade and HTTP route (Wave 2) have nothing to delegate to. Without copy constants, components inline-string-litter against UI-SPEC. These foundations are what the rest of Phase 21 assembles.

Output: 8 file artifacts (5 NEW + 3 MODIFIED) verified by 2 new test files asserting EXPLAIN QUERY PLAN structure, cursor walk uniqueness, null-coalesce semantics, and time bucket boundaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-shot-grid-view/21-CONTEXT.md
@.planning/phases/21-shot-grid-view/21-UI-SPEC.md
@.planning/phases/21-shot-grid-view/21-RESEARCH.md
@.planning/phases/21-shot-grid-view/21-PATTERNS.md
@.planning/phases/21-shot-grid-view/21-VALIDATION.md
@.planning/phases/20-shot-status-engine/20-04-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase reads. -->
<!-- Executor must use these directly — no codebase exploration needed beyond <read_first> files. -->

From packages/dashboard/src/types/events.ts (current state, lines 17-67):
```typescript
export interface VersionStatusChangedPayload {
  versionId: string;
  status: 'queued' | 'running' | 'complete' | 'failed';
  jobId?: string;
}
// ... (4 other interfaces follow same shape)
export type EngineEventMap = {
  'version.status_changed': VersionStatusChangedPayload;
  'version.created': VersionCreatedPayload;
  'tag.changed': TagChangedPayload;
  'metadata.changed': MetadataChangedPayload;
  'hierarchy.created': HierarchyCreatedPayload;
  // MISSING — must add: 'shot.status_changed': ShotStatusChangedPayload;
};
```

From src/store/shot-status-repo.ts (existing shape, lines 1-9):
```typescript
import { desc, eq } from 'drizzle-orm';
import type { BetterSQLite3Database } from 'drizzle-orm/better-sqlite3';
import * as schema from './schema.js';
import { shots, shotStatusEvents } from './schema.js';
import type { ShotStatus } from '../types/hierarchy.js';
import { newId } from '../utils/id.js';
type Db = BetterSQLite3Database<typeof schema>;
```

Server-side SSE wire-shape (camelCase, src/http/sse.ts:135-148 toDashboardPayload):
The dashboard receives `{ shotId, sequenceId, fromStatus, toStatus, changedBy, note? }` — note is undefined when absent (server coerces null→undefined at the adapter).

ShotStatus union (the 5 states): `'wip' | 'pending-review' | 'approved' | 'on-hold' | 'omit'`

ShotGridCursor shape (Phase 21 new):
```typescript
interface ShotGridCursor { n: string; sid: string; }
```
Encoded as base64url(JSON). `n` = last shot.name on page; `sid` = last shot.id (tiebreaker).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 21-01-T01: Close the ShotStatusChangedPayload type-mirror gap in dashboard events.ts (BLOCKING)</name>
  <files>packages/dashboard/src/types/events.ts</files>
  <read_first>
    - packages/dashboard/src/types/events.ts (current state of the file — MUST see existing 5 interfaces and EngineEventMap)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §1 lines 67-117 (full guidance: shape, landmine "DO NOT import from src/types/hierarchy.js", camelCase rationale)
    - .planning/phases/21-shot-grid-view/21-RESEARCH.md lines 641-666 (the exact interface body to add, including the JSDoc preamble)
    - .planning/phases/21-shot-grid-view/21-CONTEXT.md D-22 (the SSE handler depends on this type)
    - .planning/phases/20-shot-status-engine/20-04-SUMMARY.md (Phase 20 wire-shape source of truth)
  </read_first>
  <behavior>
    - Test 1: `EngineEventMap['shot.status_changed']` is assignable to `ShotStatusChangedPayload`
    - Test 2: `ShotStatusChangedPayload` exposes `shotId: string`, `sequenceId: string`, `toStatus` is the 5-value union, `fromStatus` is the union ∪ null, `changedBy: string`, `note?: string`
    - Test 3: Importing `ShotStatusChangedPayload` from this file compiles without errors
  </behavior>
  <action>
    Modify `packages/dashboard/src/types/events.ts`:

    1. After the existing `HierarchyCreatedPayload` interface (currently the last interface, around line 50), append a new interface `ShotStatusChangedPayload` with these EXACT fields (camelCase, mirroring src/http/sse.ts:135-148 toDashboardPayload wire shape):
       - `shotId: string`
       - `sequenceId: string`
       - `fromStatus: 'wip' | 'pending-review' | 'approved' | 'on-hold' | 'omit' | null` (the 5-value union ∪ null; null occurs when the shot transitions from no prior history)
       - `toStatus: 'wip' | 'pending-review' | 'approved' | 'on-hold' | 'omit'` (5-value union, never null)
       - `changedBy: string`
       - `note?: string` (optional; the server adapter coerces null → undefined)

       JSDoc the interface verbatim per PATTERNS.md §1 (3-line block referencing Phase 20 STAT-04 + src/http/sse.ts toDashboardPayload).

    2. Add `'shot.status_changed': ShotStatusChangedPayload;` to `EngineEventMap` (currently lines 61-67) as the LAST entry, after `'hierarchy.created': HierarchyCreatedPayload;`. The append preserves all 5 existing entries.

    3. DO NOT import the union from `src/types/hierarchy.ts` — D-WEBUI-31 architecture-purity forbids cross-tree imports. Inline-duplicate the 5-string literal union.

    4. Per Pattern F (Phase 21 append section comment), prefix the new interface with a banner comment `// ===== Phase 21 — shot grid SSE wire shape =====`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p packages/dashboard/tsconfig.json 2>&amp;1 | grep -i "shot.status_changed\|ShotStatusChangedPayload"; test $? -ne 0 &amp;&amp; echo "OK no type errors"</automated>
    <automated>grep -c "ShotStatusChangedPayload" packages/dashboard/src/types/events.ts | awk '$1 &gt;= 2 {print "OK"} $1 &lt; 2 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "'shot.status_changed':" packages/dashboard/src/types/events.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface ShotStatusChangedPayload" packages/dashboard/src/types/events.ts` returns ≥ 1
    - `grep -c "'shot.status_changed': ShotStatusChangedPayload" packages/dashboard/src/types/events.ts` returns 1
    - `grep -c "from '../../../src/" packages/dashboard/src/types/events.ts` returns 0 (no server-tree imports)
    - `npx tsc --noEmit -p packages/dashboard/tsconfig.json` reports no new errors mentioning ShotStatusChangedPayload or EngineEventMap
    - The 5 pre-existing interfaces (VersionStatusChangedPayload, VersionCreatedPayload, TagChangedPayload, MetadataChangedPayload, HierarchyCreatedPayload) are NOT modified
  </acceptance_criteria>
  <done>The dashboard-local mirror of the Phase 20 SSE event type is present and downstream tasks (state/shot-grid.ts, App.tsx) can subscribe with full type safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-01-T02: Add 5 shot-status color tokens to theme.css (both themes)</name>
  <files>packages/dashboard/src/styles/theme.css</files>
  <read_first>
    - packages/dashboard/src/styles/theme.css (current state — MUST see existing @theme block lines 29-71 and [data-theme="light"] block ~lines 85-97)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §3 lines 173-208 (exact hex values, insertion points, landmine list)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Color" lines 113-152 (WCAG AA proof tables — hex values MUST match)
    - .planning/phases/21-shot-grid-view/21-CONTEXT.md D-17 (token naming pattern mirrors --color-status-running precedent)
    - .planning/phases/21-shot-grid-view/21-RESEARCH.md lines 752-806 (Tailwind v4 CSS-native @theme rationale; landmine: NO auto-generated utility classes)
  </read_first>
  <action>
    Modify `packages/dashboard/src/styles/theme.css`:

    1. In the existing `@theme` block (currently lines 29-71), AFTER the line `--color-status-failed: #ff4444;` and BEFORE the comment `/* Layout fixed widths */` (or whatever line follows --color-status-failed), insert a banner comment + 5 lines:
       - banner: `/* ===== Phase 21 — shot production-state pill colors (5 statuses). WCAG 2.1 AA against --color-bg. ===== */`
       - `--color-shot-status-wip:             #94a3b8;  /* slate-400 — in progress */`
       - `--color-shot-status-pending-review:  #fbbf24;  /* amber-400 — pending action */`
       - `--color-shot-status-approved:        #4ade80;  /* green-400 — terminal success */`
       - `--color-shot-status-on-hold:         #60a5fa;  /* blue-400 — paused/blocked */`
       - `--color-shot-status-omit:            #64748b;  /* slate-500 — dimmed/excluded */`

    2. In the existing `[data-theme="light"]` block (currently ~lines 85-97), AFTER `--color-destructive: #d73535;` and BEFORE the closing `}` brace, insert a banner comment + 5 lines:
       - banner: `/* ===== Phase 21 — light-theme shot status overrides ===== */`
       - `--color-shot-status-wip:             #64748b;`
       - `--color-shot-status-pending-review:  #d97706;`
       - `--color-shot-status-approved:        #16a34a;`
       - `--color-shot-status-on-hold:         #2563eb;`
       - `--color-shot-status-omit:            #94a3b8;`

    3. Hex values are LOCKED per UI-SPEC §"Color" — DO NOT round or substitute. WCAG AA proof was computed against these exact values.

    4. DO NOT add Tailwind utility class declarations for these tokens. Consumers use the `bg-[var(--color-shot-status-*)]` arbitrary-value syntax (Tailwind v4 pattern).

    5. DO NOT modify the 60/30/10 split tokens, font tokens, spacing tokens, or destructive token. Append-only modification.
  </action>
  <verify>
    <automated>grep -v '^\s*\*\|^\s*/' packages/dashboard/src/styles/theme.css | grep -c -- '--color-shot-status-' | awk '$1 &gt;= 10 {print "OK"} $1 &lt; 10 {print "FAIL got " $1; exit 1}'</automated>
    <automated>grep -c '#94a3b8' packages/dashboard/src/styles/theme.css | awk '$1 &gt;= 2 {print "OK"} $1 &lt; 2 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c '#4ade80' packages/dashboard/src/styles/theme.css | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>cd packages/dashboard &amp;&amp; npx vite build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--color-shot-status-wip' packages/dashboard/src/styles/theme.css` returns ≥ 2 (one per theme block)
    - `grep -c -- '--color-shot-status-pending-review' packages/dashboard/src/styles/theme.css` returns ≥ 2
    - `grep -c -- '--color-shot-status-approved' packages/dashboard/src/styles/theme.css` returns ≥ 2
    - `grep -c -- '--color-shot-status-on-hold' packages/dashboard/src/styles/theme.css` returns ≥ 2
    - `grep -c -- '--color-shot-status-omit' packages/dashboard/src/styles/theme.css` returns ≥ 2
    - Pre-existing `--color-status-running`, `--color-status-completed`, `--color-status-failed` tokens remain present (grep returns same counts as before)
    - `cd packages/dashboard && npx vite build` succeeds with no CSS errors
  </acceptance_criteria>
  <done>5 shot-status color tokens are declared in both themes; consumers can reference them via `bg-[var(--color-shot-status-wip)]` etc. WCAG AA contrast proof from UI-SPEC §"Color" tables remains valid because hex values match exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-01-T03: Append Phase 21 copy block (27 constants) to lib/copy.ts</name>
  <files>packages/dashboard/src/lib/copy.ts</files>
  <read_first>
    - packages/dashboard/src/lib/copy.ts (current state, 184 lines, 19 existing exports — MUST see prior phase banner-comment + constant style)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §4 lines 211-291 (verbatim 27-constant enumeration; landmine: U+2026 ellipsis; FILTER_PILL_* lowercase; SHOT_GRID_EMPTY_FILTER_PREFIX ends with single quote)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Copywriting Contract" lines 170-247 (source of truth — every string verbatim)
  </read_first>
  <action>
    Append to the END of `packages/dashboard/src/lib/copy.ts` (after the existing Phase 19 block):

    1. Banner: `// ===== Phase 21 — shot grid copy =====` followed by a JSDoc block referencing UI-SPEC §"Copywriting Contract".

    2. Filter bar constants (9):
       - `FILTER_BAR_STATUS_LABEL = 'Status'`
       - `FILTER_PILL_ALL = 'All'`
       - `FILTER_PILL_WIP = 'wip'` (lowercase verbatim; CSS text-transform uppercases at render)
       - `FILTER_PILL_PENDING_REVIEW = 'pending-review'`
       - `FILTER_PILL_APPROVED = 'approved'`
       - `FILTER_PILL_ON_HOLD = 'on-hold'`
       - `FILTER_PILL_OMIT = 'omit'`
       - `SHOW_OMITTED_TOGGLE_LABEL = 'Show omitted'`
       - `SHOW_OMITTED_TOGGLE_ARIA = 'Toggle omitted shots'`

    3. Sequence header constants (3):
       - `SEQUENCE_HEADER_TOGGLE_ARIA_PREFIX_OPEN = 'Collapse '` (trailing space)
       - `SEQUENCE_HEADER_TOGGLE_ARIA_PREFIX_CLOSED = 'Expand '` (trailing space)
       - `AGGREGATE_COUNTS_REGION_LABEL_PREFIX = 'Status counts for '` (trailing space)

    4. Shot card constants (5):
       - `SHOT_CARD_OPEN_ARIA_PREFIX = 'Open version drawer for '` (trailing space)
       - `SHOT_CARD_VERSION_COUNT_SINGULAR = '1 version'`
       - `SHOT_CARD_VERSION_COUNT_PLURAL_SUFFIX = ' versions'` (leading space)
       - `SHOT_CARD_NO_VERSIONS = 'No versions yet'`
       - `SHOT_CARD_LAST_UPDATED_PREFIX = 'Updated '` (trailing space)

    5. Time helper suffixes (6):
       - `TIME_JUST_NOW = 'just now'`
       - `TIME_MINUTES_SUFFIX = 'm ago'`
       - `TIME_HOURS_SUFFIX = 'h ago'`
       - `TIME_DAYS_SUFFIX = 'd ago'`
       - `TIME_WEEKS_SUFFIX = 'w ago'`
       - `TIME_MONTHS_SUFFIX = 'mo ago'`

    6. Empty / loading / error constants (6):
       - `SHOT_GRID_EMPTY_NO_SHOTS = 'No shots in this sequence yet. Shots are created via the MCP agent.'`
       - `SHOT_GRID_EMPTY_FILTER_PREFIX = "No shots with status '"` (ends with single quote)
       - `SHOT_GRID_EMPTY_FILTER_ALL_NO_OMITTED_PREFIX = 'No active shots in '` (trailing space)
       - `SHOT_GRID_EMPTY_FILTER_OMIT_HIDDEN = 'Hidden. Toggle "Show omitted" to view.'`
       - `SHOT_GRID_LOADING_LABEL = 'Loading shots…'` (use literal U+2026 ellipsis character, NOT three dots)
       - `SHOT_GRID_FETCH_ERROR_PREFIX = 'Failed to load shots'`

    7. TreeSidebar grid-icon constants (2):
       - `TREE_GRID_ICON_ARIA_PREFIX = 'Open shot grid for '` (trailing space)
       - `TREE_GRID_ICON_ACTIVE_ARIA_SUFFIX = ' (current)'` (leading space)

    8. Header home button (1):
       - `HEADER_HOME_ARIA_LABEL = 'Back to home view'`

    Total: 9 + 3 + 5 + 6 + 6 + 2 + 1 = 32 (counted as ≥ 27 per UI-SPEC; the time suffixes + 5 status pill literals overlap with what UI-SPEC counted as "27 named constants"; the exact count of new exports is ≥ 27).

    DO NOT modify any existing constants. DO NOT add inline string literals in components — they MUST import from copy.ts (architectural rule).
  </action>
  <verify>
    <automated>grep -c '^export const ' packages/dashboard/src/lib/copy.ts | awk '$1 &gt;= 46 {print "OK got " $1} $1 &lt; 46 {print "FAIL got " $1; exit 1}'</automated>
    <automated>grep -c "HEADER_HOME_ARIA_LABEL" packages/dashboard/src/lib/copy.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "SHOW_OMITTED_TOGGLE_LABEL" packages/dashboard/src/lib/copy.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "TIME_JUST_NOW = 'just now'" packages/dashboard/src/lib/copy.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "Loading shots…" packages/dashboard/src/lib/copy.ts | awk '$1 == 1 {print "OK ellipsis"} $1 != 1 {print "FAIL ellipsis"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export const ' packages/dashboard/src/lib/copy.ts` returns ≥ 46 (19 pre-existing + ≥ 27 new)
    - `grep -c 'HEADER_HOME_ARIA_LABEL' packages/dashboard/src/lib/copy.ts` returns 1
    - `grep -c "TIME_JUST_NOW = 'just now'" packages/dashboard/src/lib/copy.ts` returns 1
    - `grep -c "TIME_MONTHS_SUFFIX = 'mo ago'" packages/dashboard/src/lib/copy.ts` returns 1
    - `grep -c 'Loading shots…' packages/dashboard/src/lib/copy.ts` returns 1 (literal U+2026 ellipsis, NOT `...`)
    - `grep -c "SHOT_GRID_EMPTY_FILTER_PREFIX" packages/dashboard/src/lib/copy.ts` returns 1
    - All 19 pre-existing constants remain present and unmodified
  </acceptance_criteria>
  <done>Phase 21's full 27+ copy constant block lives in `lib/copy.ts`. Component tasks in Waves 2-4 import from here; zero inline string literals in component files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-01-T04: Create lib/time.ts with formatRelativeTime + unit tests</name>
  <files>packages/dashboard/src/lib/time.ts, packages/dashboard/src/lib/__tests__/time.test.ts</files>
  <read_first>
    - packages/dashboard/src/lib/copy.ts (after Task T03 — to confirm TIME_* constants exist)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §5 lines 293-348 (full implementation; bucket boundaries; landmine list)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §6 lines 352-403 (test scaffold; REF_NOW pattern; bucket-edge assertions)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md lines 208-216 (TIME_* constants spec)
    - .planning/phases/21-shot-grid-view/21-VALIDATION.md line 53 (test command: `npx vitest run packages/dashboard/src/lib/__tests__/time.test.ts`)
  </read_first>
  <behavior>
    - Test 1: `formatRelativeTime(REF_NOW - 30_000, REF_NOW)` returns `'just now'` (< 60s)
    - Test 2: `formatRelativeTime(REF_NOW, REF_NOW)` returns `'just now'` (zero delta)
    - Test 3: `formatRelativeTime(REF_NOW - 5*60_000, REF_NOW)` returns `'5m ago'`
    - Test 4: `formatRelativeTime(REF_NOW - 59*60_000, REF_NOW)` returns `'59m ago'`
    - Test 5: `formatRelativeTime(REF_NOW - 60*60_000, REF_NOW)` returns `'1h ago'` (boundary)
    - Test 6: `formatRelativeTime(REF_NOW - 23*60*60_000, REF_NOW)` returns `'23h ago'`
    - Test 7: `formatRelativeTime(REF_NOW - 24*60*60_000, REF_NOW)` returns `'1d ago'` (boundary)
    - Test 8: `formatRelativeTime(REF_NOW - 6*24*60*60_000, REF_NOW)` returns `'6d ago'`
    - Test 9: `formatRelativeTime(REF_NOW - 7*24*60*60_000, REF_NOW)` returns `'1w ago'` (boundary)
    - Test 10: `formatRelativeTime(REF_NOW - 3*7*24*60*60_000, REF_NOW)` returns `'3w ago'`
    - Test 11: `formatRelativeTime(REF_NOW - 30*24*60*60_000, REF_NOW)` returns `'1mo ago'` (month bucket; approx 30 days)
    - Test 12: Future timestamp `formatRelativeTime(REF_NOW + 60_000, REF_NOW)` clamps to `'just now'`
  </behavior>
  <action>
    1. Create `packages/dashboard/src/lib/time.ts` (NEW, ~30 LOC):

       - Import the 6 TIME_* constants from `./copy.js`: `TIME_JUST_NOW`, `TIME_MINUTES_SUFFIX`, `TIME_HOURS_SUFFIX`, `TIME_DAYS_SUFFIX`, `TIME_WEEKS_SUFFIX`, `TIME_MONTHS_SUFFIX`.
       - Export `function formatRelativeTime(epochMs: number, nowMs: number = Date.now()): string` with signature exactly as specified.
       - JSDoc the bucket ladder verbatim per PATTERNS.md §5 lines 300-313.
       - Implementation: compute `deltaSec = Math.max(0, Math.floor((nowMs - epochMs) / 1000))` (the `Math.max(0, ...)` clamps future timestamps to "just now"); ladder through 60s → 60m → 24h → 7d → 4w → else (months). For each bucket, return template literal like `` `${n}${SUFFIX}` ``. The `just now` bucket is the only one returning a bare constant.
       - DO NOT import `date-fns`, `dayjs`, or any other time library. Pure helper, no new dependencies.
       - DO NOT import from `../../src/` (architecture-purity). Only `./copy.js`.

    2. Create `packages/dashboard/src/lib/__tests__/time.test.ts` (NEW directory + NEW file, ~50 LOC):

       - Import `{ describe, it, expect } from 'vitest'`.
       - Import `{ formatRelativeTime } from '../time.js'`.
       - Declare `const REF_NOW = 1_700_000_000_000;` at module top (deterministic epoch — NOT Date.now()).
       - Write 12 test cases covering all bucket boundaries described in <behavior>. Use `expect(formatRelativeTime(REF_NOW - delta, REF_NOW)).toBe(expected)`.
       - Use `describe('formatRelativeTime (Phase 21)', () =&gt; { ... })` outer wrapper.
       - DO NOT use `vi.setSystemTime` — the helper accepts `nowMs` explicitly; pass it directly for determinism.
  </action>
  <verify>
    <automated>npx vitest run packages/dashboard/src/lib/__tests__/time.test.ts</automated>
    <automated>grep -c "export function formatRelativeTime" packages/dashboard/src/lib/time.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "from '../../../src/" packages/dashboard/src/lib/time.ts | awk '$1 == 0 {print "OK no server imports"} $1 != 0 {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/lib/time.ts` exists with `export function formatRelativeTime(epochMs: number, nowMs?: number): string` signature
    - `packages/dashboard/src/lib/__tests__/time.test.ts` exists with ≥ 12 `it()` cases
    - `npx vitest run packages/dashboard/src/lib/__tests__/time.test.ts` exits 0 (all tests pass)
    - `grep -c "import.*date-fns\|import.*dayjs" packages/dashboard/src/lib/time.ts` returns 0
    - `grep -c "from '../../../src/" packages/dashboard/src/lib/time.ts` returns 0 (no server-tree imports)
    - `grep -c "from './copy" packages/dashboard/src/lib/time.ts` returns 1 (imports TIME_* constants)
  </acceptance_criteria>
  <done>`formatRelativeTime` is available for ShotGridCard (Wave 3) and any future relative-time surface. Tested against fixed reference time for reproducibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-01-T05: Create types/shot-grid.ts (ShotGridResponse, ShotGridRow, ShotGridSequenceMeta, ShotStatus)</name>
  <files>packages/dashboard/src/types/shot-grid.ts</files>
  <read_first>
    - packages/dashboard/src/types/events.ts (after Task T01 — confirms ShotStatusChangedPayload exists)
    - packages/dashboard/src/lib/api.ts lines 209-215 (PaginatedVersionsResponse — the analog envelope shape)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §2 lines 120-170 (exact type body to write; landmine: snake_case field names)
    - .planning/phases/21-shot-grid-view/21-CONTEXT.md D-13 (LOCKED endpoint payload shape)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Component Inventory" line 281 (file location: packages/dashboard/src/types/shot-grid.ts)
  </read_first>
  <action>
    Create `packages/dashboard/src/types/shot-grid.ts` (NEW, ~30 LOC):

    1. Import `import type { ShotStatusChangedPayload } from './events.js';` at top.

    2. Export `ShotStatus` as a type alias derived from the event payload:
       ```
       export type ShotStatus = ShotStatusChangedPayload['toStatus'];
       ```
       This re-derives the 5-value union from the canonical wire-shape definition; if Phase 22+ adds a status, the change propagates here automatically.

    3. Export `interface ShotGridSequenceMeta`:
       - `id: string`
       - `name: string`

    4. Export `interface ShotGridRow` per D-13:
       - `id: string`
       - `name: string`
       - `status: ShotStatus`
       - `version_count: number` (snake_case per server convention)
       - `latest_completed_version: { id: string; thumbnail_url: string; completed_at: number } | null` (the nested object snake_case for fields)

    5. Export `interface ShotGridResponse`:
       - `sequence: ShotGridSequenceMeta`
       - `shots: ShotGridRow[]`
       - `next_cursor: string | null` (snake_case, mirrors PaginatedVersionsResponse)
       - `total_count: number` (snake_case)

    6. JSDoc each interface; reference D-13 (CONTEXT.md) as the source of truth for the payload shape.

    7. DO NOT import from `../../../src/` (architecture-purity). Only `./events.js`.
    8. DO NOT inline-duplicate the 5-value status union (use the alias instead).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p packages/dashboard/tsconfig.json 2>&amp;1 | grep -i "shot-grid.ts" | grep -v "^$"; test $? -ne 0 &amp;&amp; echo "OK no tsc errors"</automated>
    <automated>grep -c "export type ShotStatus" packages/dashboard/src/types/shot-grid.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "export interface ShotGridRow" packages/dashboard/src/types/shot-grid.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "export interface ShotGridResponse" packages/dashboard/src/types/shot-grid.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "export interface ShotGridSequenceMeta" packages/dashboard/src/types/shot-grid.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "from '../../../src/" packages/dashboard/src/types/shot-grid.ts | awk '$1 == 0 {print "OK no server imports"} $1 != 0 {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/types/shot-grid.ts` exists and exports: `ShotStatus`, `ShotGridSequenceMeta`, `ShotGridRow`, `ShotGridResponse`
    - `ShotStatus` is derived as `ShotStatusChangedPayload['toStatus']` (not inlined union)
    - `ShotGridRow.latest_completed_version` is `{ id, thumbnail_url, completed_at } | null`
    - `ShotGridResponse` field names use snake_case (`next_cursor`, `total_count`) per server convention
    - `grep -c "from '../../../src/" packages/dashboard/src/types/shot-grid.ts` returns 0
    - `npx tsc --noEmit -p packages/dashboard/tsconfig.json` reports no errors specific to shot-grid.ts
  </acceptance_criteria>
  <done>Wire-shape types for the Phase 21 GET endpoint are defined and shared with the dashboard fetch helper (Wave 2) and component layer (Wave 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-01-T06: Extend shot-status-repo.ts with listShotsForGrid + cursor encode/decode + listShotsForGridSqlText helper</name>
  <files>src/store/shot-status-repo.ts</files>
  <read_first>
    - src/store/shot-status-repo.ts (current state, ~134 lines — MUST see existing imports, Db type alias, getCurrentStatus null-coalesce)
    - src/store/sort.ts lines 60-196 (encode/decodeVersionCursor — the verbatim analog pattern to mirror)
    - src/store/version-repo.ts lines 227-269 (listByShot — limit+1 has_more pattern with Drizzle sql template)
    - src/store/schema.ts lines 60-71 (shots table — confirms NOT NULL constraints on shots.name + shots.sequence_id)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §8 lines 463-664 (exact body of listShotsForGrid + landmines)
    - .planning/phases/21-shot-grid-view/21-RESEARCH.md §"Pattern 1" lines 228-398 (SQL CTE rationale; window-function vs correlated subquery)
    - .planning/phases/21-shot-grid-view/21-RESEARCH.md §"Pattern 2" lines 400-498 (cursor shape rationale)
    - .planning/phases/21-shot-grid-view/21-CONTEXT.md D-13 (payload shape lock); D-04 (the cursor is opaque)
  </read_first>
  <behavior>
    - Test 1: A fresh repo with 5 shots in a sequence returns `items.length === 5` with default `status: 'wip'`
    - Test 2: When a shot has 2 completed versions, `lcv_id` matches the version with the highest `completed_at` (and `id` as tiebreaker when `completed_at` ties)
    - Test 3: When a shot has zero completed versions, `lcv_id` and `lcv_completed_at` are `null`
    - Test 4: `version_count` counts ALL versions (including non-completed) per shot
    - Test 5: Cursor walk with `limit=2` over 5 shots: 3 pages, every shot visited exactly once, no duplicates
    - Test 6: `total_count` is cursor-independent (same value page 1 and page 2)
    - Test 7: `decodeShotGridCursor('not-base64')` returns `null` (NEVER throws)
    - Test 8: `encodeShotGridCursor` then `decodeShotGridCursor` round-trip preserves `{ n, sid }`
  </behavior>
  <action>
    Extend `src/store/shot-status-repo.ts`:

    1. Add `sql` to the existing drizzle-orm import line: `import { desc, eq, sql } from 'drizzle-orm';`

    2. After the existing exports, append a banner: `// ===== Phase 21 — listShotsForGrid (GRID-04 single-query, window-function CTE) =====`

    3. Export `interface ShotGridCursor`:
       - `n: string` (the sort key — last shot.name on the page; JSDoc: "Last shot name on the current page; shots.name is NOT NULL per schema.ts:60-66")
       - `sid: string` (the tiebreaker — shot.id is UNIQUE PRIMARY KEY)

    4. Export `function encodeShotGridCursor(c: ShotGridCursor): string` — `Buffer.from(JSON.stringify(c), 'utf8').toString('base64url')`. Mirror sort.ts:169-173 verbatim.

    5. Export `function decodeShotGridCursor(s: string): ShotGridCursor | null` — try/catch wrapping `JSON.parse(Buffer.from(s, 'base64url').toString('utf8'))`. Structural validation: `obj.n` is `string`; `obj.sid` is non-empty `string`. Returns `null` on any failure path (NEVER throws). Mirror sort.ts:175-196 defensive shape verbatim.

    6. Export `interface ShotGridQueryRow`:
       - `id: string`
       - `name: string`
       - `status: ShotStatus`
       - `version_count: number`
       - `lcv_id: string | null` (snake_case, mirrors version-repo)
       - `lcv_completed_at: number | null`

    7. Export `interface ShotGridQueryResult`:
       - `items: ShotGridQueryRow[]`
       - `next_cursor: string | null`
       - `total_count: number`

    8. Export `function listShotsForGrid(db: Db, sequenceId: string, opts: { cursor: ShotGridCursor | null; limit: number }): ShotGridQueryResult`:
       - Inside: compute `total_count` via separate `db.select({ c: sql<number>\`COUNT(*)\` }).from(shots).where(eq(shots.sequence_id, sequenceId)).get()` (one-row scalar query — see PATTERNS.md §8 lines 577-582).
       - Compute `cursorName = cursor?.n ?? null` and `cursorSid = cursor?.sid ?? null`.
       - Run the window-function CTE query via `db.all(sql\`...\`)` using the EXACT SQL text from PATTERNS.md §8 lines 588-615:
         - `WITH ranked AS (SELECT v.id, v.shot_id, v.completed_at, ROW_NUMBER() OVER (PARTITION BY v.shot_id ORDER BY v.completed_at DESC, v.id ASC) AS rn FROM versions v WHERE v.status = 'completed' AND v.completed_at IS NOT NULL)`
         - Outer SELECT joins `shots s` to `ranked r` on `r.shot_id = s.id AND r.rn = 1` (LEFT JOIN so versionless shots return lcv_id=NULL).
         - WHERE clause: `s.sequence_id = ${sequenceId} AND (${cursorName} IS NULL OR s.name > ${cursorName} OR (s.name = ${cursorName} AND s.id > ${cursorSid}))`.
         - ORDER BY `s.name ASC, s.id ASC`.
         - `LIMIT ${limit + 1}` for has_more probe.
         - The `version_count` is an uncorrelated scalar subquery `(SELECT COUNT(*) FROM versions vc WHERE vc.shot_id = s.id)` — this is benign per RESEARCH A5 and is INTENTIONALLY uncorrelated (the EXPLAIN test only fences the `ranked` join).
       - After fetching: `hasMore = rows.length > limit`; slice items; if `hasMore && items.length > 0`, set `next_cursor = encodeShotGridCursor({ n: last.name, sid: last.id })`.
       - Return `{ items, next_cursor, total_count: Number(totalRow?.c ?? 0) }` (Number() coerces better-sqlite3 bigint defensively per PATTERNS.md landmine).

    9. Export `function listShotsForGridSqlText(): string` that returns the EXACT SAME SQL as the query in step 8 (with `?` placeholders for the 5 bind params: sequenceId, cursorName, cursorName, cursorName, cursorSid, and `LIMIT ?` for the limit+1). PATTERNS.md §8 lines 631-653 has the verbatim string. The EXPLAIN test (next task) introspects via `testDb.sqlite.prepare('EXPLAIN QUERY PLAN ' + listShotsForGridSqlText())`.

    10. DO NOT inline raw string concatenation of `sequenceId` (SQL injection guard — Drizzle parameterises via the `sql\`${var}\`` template syntax).
    11. DO NOT remove or reformat any existing exports in the file (`getStatusHistory`, `getCurrentStatus`, `insertStatusEvent`, `STALE_SHOT_DAYS`).
  </action>
  <verify>
    <automated>grep -c "export function listShotsForGrid" src/store/shot-status-repo.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "export function encodeShotGridCursor\|export function decodeShotGridCursor\|export function listShotsForGridSqlText" src/store/shot-status-repo.ts | awk '$1 == 3 {print "OK"} $1 != 3 {print "FAIL got " $1; exit 1}'</automated>
    <automated>grep -c "ROW_NUMBER() OVER" src/store/shot-status-repo.ts | awk '$1 &gt;= 2 {print "OK"} $1 &lt; 2 {print "FAIL got " $1; exit 1}'</automated>
    <automated>grep -c "interface ShotGridCursor" src/store/shot-status-repo.ts | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -i "shot-status-repo.ts"; test $? -ne 0 &amp;&amp; echo "OK no tsc errors in this file"</automated>
  </verify>
  <acceptance_criteria>
    - `src/store/shot-status-repo.ts` exports: `listShotsForGrid`, `encodeShotGridCursor`, `decodeShotGridCursor`, `listShotsForGridSqlText`, `ShotGridCursor`, `ShotGridQueryRow`, `ShotGridQueryResult`
    - The SQL inside `listShotsForGrid` uses `WITH ranked AS (... ROW_NUMBER() OVER (PARTITION BY v.shot_id ...) AS rn ...)` window-function CTE
    - `listShotsForGridSqlText()` returns a SQL string with exactly 5 `?` placeholders in the WHERE clause (sequenceId, cursorName, cursorName, cursorName, cursorSid) plus 1 in LIMIT — total 6 bind params
    - `decodeShotGridCursor` is wrapped in try/catch and returns `null` on failure (NEVER throws)
    - Pre-existing functions (`getStatusHistory`, `getCurrentStatus`, `insertStatusEvent`) and `STALE_SHOT_DAYS` constant remain present and unmodified
    - `grep -c "import.*from.*MCP\|@modelcontextprotocol" src/store/shot-status-repo.ts` returns 0 (architecture-purity: no MCP in store/)
  </acceptance_criteria>
  <done>The repo layer exposes the GRID-04 single-query function; the engine facade (Wave 2) and HTTP route (Wave 2) will delegate to it. The SQL text helper enables the EXPLAIN QUERY PLAN test (next task) to introspect without duplicating SQL strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-01-T07: Create shot-status-repo-grid.test.ts with EXPLAIN QUERY PLAN no-N+1 + cursor walk + null-coalesce tests</name>
  <files>src/store/__tests__/shot-status-repo-grid.test.ts</files>
  <read_first>
    - src/store/shot-status-repo.ts (after Task T06 — confirms listShotsForGrid + helpers exist)
    - src/store/__tests__/shot-status-repo.test.ts lines 29-90 (fixture pattern — makeInMemoryDb + HierarchyRepo seeding)
    - src/store/__tests__/version-repo-cursor.test.ts lines 1-100 (walkAllPages pattern + total_count parity test shape at lines 326-348)
    - src/test-utils/fixtures.ts (makeInMemoryDb shape — exposes `testDb.sqlite` for raw better-sqlite3 access)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §9 lines 668-805 (full test scaffold with EXPLAIN pattern)
    - .planning/phases/21-shot-grid-view/21-VALIDATION.md lines 42-52, 95, 112-119 (full per-task verification map + EXPLAIN test pattern)
    - .planning/phases/21-shot-grid-view/21-RESEARCH.md §"Validation Architecture" lines 1230-1365 (full EXPLAIN pattern reference)
  </read_first>
  <behavior>
    - Test 1: EXPLAIN QUERY PLAN over `listShotsForGridSqlText()` returns rows where NONE have `detail` containing both `'CORRELATED'` AND `'ranked'`
    - Test 2: EXPLAIN QUERY PLAN rows include at least one row whose `detail` contains `'CO-ROUTINE'` OR `'MATERIALIZE'` OR `'ranked'` (CTE materialization defence-in-depth)
    - Test 3: `listShotsForGrid` over 5 fresh shots returns `items.length === 5` and every item has `status === 'wip'`
    - Test 4: A shot with 2 completed versions populates `lcv_id` (latest completed_at first) and `lcv_completed_at`; both non-null
    - Test 5: A shot with 0 completed versions has `lcv_id === null` and `lcv_completed_at === null`
    - Test 6: `version_count` counts ALL versions (e.g., 2 completed + 1 not-completed → `version_count === 3`)
    - Test 7: Cursor walk: 5 shots, pageSize=2 → 3 pages, every shot.id collected exactly once
    - Test 8: `total_count === 5` on every page of the walk
    - Test 9: `decodeShotGridCursor('garbage')` returns `null`; `decodeShotGridCursor('')` returns `null`
    - Test 10: `encodeShotGridCursor({ n: 'sh010', sid: 'shot_x' })` round-trips through `decodeShotGridCursor` and returns the same object
  </behavior>
  <action>
    Create `src/store/__tests__/shot-status-repo-grid.test.ts` (NEW, ~200 LOC):

    1. Imports (mirror PATTERNS.md §9 lines 736-746 scaffold):
       - `import { describe, test, expect, beforeEach } from 'vitest';`
       - `import '../../test-utils/matchers.js';`
       - `import { makeInMemoryDb, type TestDb } from '../../test-utils/fixtures.js';`
       - `import { HierarchyRepo } from '../hierarchy-repo.js';`
       - `import { VersionRepo } from '../version-repo.js';`
       - `import { listShotsForGrid, listShotsForGridSqlText, encodeShotGridCursor, decodeShotGridCursor, type ShotGridCursor } from '../shot-status-repo.js';`

    2. Module-level `let testDb: TestDb`, `let hierarchy: HierarchyRepo`, `let versionRepo: VersionRepo`, `let sequenceId: string`.

    3. `beforeEach` sets up: makeInMemoryDb, HierarchyRepo, VersionRepo. Seed workspace → project → sequence. Capture `sequenceId = seq.id`. DO NOT seed shots in beforeEach — leave that per-describe to allow targeted fixtures.

    4. Define a helper `walkAllShotsForGrid(pageSize: number)` mirroring `version-repo-cursor.test.ts` walkAllPages: iterate pages with cursor decode, collect `items`, `totals`, return `{ allItems, pageCount, totals }`. Safety cap at 100 iterations.

    5. `describe('listShotsForGrid — EXPLAIN QUERY PLAN (GRID-04 N+1 lock)', () => { ... })`:
       - Seed 5 shots before each test in this block (e.g., names `sh010`, `sh020`, ..., `sh050`).
       - **Test:** "plan rows do NOT contain CORRELATED SCALAR SUBQUERY referencing ranked"
         - Execute `testDb.sqlite.prepare('EXPLAIN QUERY PLAN ' + listShotsForGridSqlText()).all(sequenceId, null, null, null, null, 21) as Array<{ detail: string }>`.
         - Assert `planRows.filter(r => r.detail.includes('CORRELATED') && r.detail.includes('ranked')).length === 0`.
       - **Test:** "plan rows reference CTE materialization (CO-ROUTINE / MATERIALIZE / ranked)"
         - Same prepare/all, assert at least one row's `detail` includes any of `'CO-ROUTINE'`, `'MATERIALIZE'`, `'ranked'`.

    6. `describe('listShotsForGrid — status null-coalesce', () => { ... })`:
       - Seed 5 fresh shots (no status events). Call `listShotsForGrid(testDb.db, sequenceId, { cursor: null, limit: 20 })`.
       - Assert all items have `status === 'wip'`.

    7. `describe('listShotsForGrid — latest_completed_version', () => { ... })`:
       - Seed 1 shot. Insert 2 versions via `versionRepo.insertVersion(shot.id)`; mark both completed via `versionRepo.markCompleted(versionId, '[]')`. The second version is the "latest" — its id MUST appear as `lcv_id`.
       - Assert `result.items[0].lcv_id === secondVersion.id`.
       - Separately seed 1 shot with ONE submitted-but-NOT-completed version (call only `versionRepo.insertVersion`, skip markCompleted).
       - Assert `lcv_id === null` and `lcv_completed_at === null`.

    8. `describe('listShotsForGrid — version_count counts ALL versions', () => { ... })`:
       - Seed 1 shot. Insert 3 versions; markCompleted on 2 of them (one stays submitted).
       - Assert `result.items[0].version_count === 3` (not 2).

    9. `describe('listShotsForGrid — cursor walk', () => { ... })`:
       - Seed 5 shots. Run `walkAllShotsForGrid(2)`.
       - Assert `allItems.length === 5`, every id is unique (use Set check), `pageCount === 3`.

    10. `describe('listShotsForGrid — total_count parity', () => { ... })`:
        - Seed 5 shots. Run `walkAllShotsForGrid(2)`.
        - Assert `totals.every(t => t === 5)` (every page reports the same total).

    11. `describe('decodeShotGridCursor defensive', () => { ... })`:
        - `expect(decodeShotGridCursor('not-base64')).toBe(null);`
        - `expect(decodeShotGridCursor('')).toBe(null);`
        - Round-trip: `const c: ShotGridCursor = { n: 'sh010', sid: 'shot_abc' }; expect(decodeShotGridCursor(encodeShotGridCursor(c))).toEqual(c);`

    12. Landmines (per PATTERNS.md §9 lines 800-805):
        - `hierarchy.createShot(seqId, name)` requires names matching `/^sh\d{3,}$/`. Use `sh010, sh020, sh030, sh040, sh050`.
        - DO NOT seed via raw INSERT. Always use `HierarchyRepo.createShot` so the shots.status default fires.
        - Bind params for EXPLAIN: ALL 6 placeholders required even when null — `(sequenceId, null, null, null, null, 21)`.
  </action>
  <verify>
    <automated>npx vitest run src/store/__tests__/shot-status-repo-grid.test.ts</automated>
    <automated>grep -c "EXPLAIN QUERY PLAN" src/store/__tests__/shot-status-repo-grid.test.ts | awk '$1 &gt;= 1 {print "OK"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "walkAllShotsForGrid\|walkAllPages" src/store/__tests__/shot-status-repo-grid.test.ts | awk '$1 &gt;= 1 {print "OK"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `src/store/__tests__/shot-status-repo-grid.test.ts` exists with ≥ 10 test cases covering all 10 behaviors above
    - `npx vitest run src/store/__tests__/shot-status-repo-grid.test.ts` exits 0
    - The EXPLAIN QUERY PLAN test ACTUALLY runs (not skipped/xit) — its assertion is the GRID-04 N+1 lock
    - Cursor walk test asserts uniqueness via Set (`expect(new Set(ids).size).toBe(allItems.length)`)
    - `total_count` parity test confirms all pages return the same total
    - Defensive decode test asserts decoder returns `null` (NEVER throws) for `'not-base64'` and empty string
  </acceptance_criteria>
  <done>The GRID-04 single-pass SQL lock is automatically enforced. Any future regression that re-introduces a correlated subquery in the latest-version join will fail the EXPLAIN test. Cursor walk and null-coalesce semantics are also covered.</done>
</task>

</tasks>

<verification>
After all 7 tasks complete:

```bash
# Type-check across both packages
npx tsc --noEmit
npx tsc --noEmit -p packages/dashboard/tsconfig.json

# Per-task targeted tests
npx vitest run packages/dashboard/src/lib/__tests__/time.test.ts
npx vitest run src/store/__tests__/shot-status-repo-grid.test.ts

# Plan-scope sanity (Phase 21 store layer + dashboard lib)
npx vitest run src/store/__tests__/shot-status-repo-grid.test.ts packages/dashboard/src/lib/__tests__/time.test.ts

# Architecture-purity (must remain unchanged — no new native bindings)
npx vitest run src/__tests__/architecture-purity.test.ts

# Tool-budget invariant (must remain unchanged at 7)
npx vitest run src/__tests__/tool-budget.test.ts

# Theme.css token count
grep -c -- '--color-shot-status-' packages/dashboard/src/styles/theme.css   # → ≥ 10

# Copy.ts new export count
grep -c '^export const ' packages/dashboard/src/lib/copy.ts   # → ≥ 46 (19 pre-existing + ≥ 27 new)

# Event mirror gap-closure check
grep -c "'shot.status_changed': ShotStatusChangedPayload" packages/dashboard/src/types/events.ts   # → 1
```
</verification>

<success_criteria>
- `ShotStatusChangedPayload` interface and `EngineEventMap['shot.status_changed']` entry present in `packages/dashboard/src/types/events.ts`
- 5 new shot-status color tokens declared in both `@theme` and `[data-theme="light"]` blocks of `theme.css` (10 declarations total)
- `lib/copy.ts` total exports ≥ 46 (19 pre-existing + 27 new)
- `lib/time.ts` exists; `formatRelativeTime` passes all 12 bucket-edge unit tests
- `types/shot-grid.ts` exports `ShotGridResponse`, `ShotGridRow`, `ShotGridSequenceMeta`, `ShotStatus` (the last derived from `ShotStatusChangedPayload['toStatus']`)
- `shot-status-repo.ts` exports `listShotsForGrid`, `encodeShotGridCursor`, `decodeShotGridCursor`, `listShotsForGridSqlText`
- `shot-status-repo-grid.test.ts` exists with ≥ 10 test cases; EXPLAIN QUERY PLAN test confirms no CORRELATED-ranked plan rows
- `architecture-purity.test.ts` and `tool-budget.test.ts` remain green with zero edits
- All tasks committed atomically (one commit per task) per CLAUDE.md "Atomic commits: one logical change per commit"
</success_criteria>

<output>
After completion, create `.planning/phases/21-shot-grid-view/21-01-SUMMARY.md` documenting:
- The exact files created/modified
- Whether the EXPLAIN QUERY PLAN test confirmed single-pass (cite the plan-rows count and the boolean assertion)
- Any deviations from the planned implementation (with rationale)
- Confirmation that `ShotStatusChangedPayload` is now in `EngineEventMap` and downstream tasks can subscribe
</output>
