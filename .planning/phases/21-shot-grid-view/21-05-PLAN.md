---
plan_id: 21-05
phase: 21
phase_name: shot-grid-view
plan: 5
type: execute
wave: 5
depends_on: [21-01, 21-02, 21-03, 21-04]
files_modified: []
autonomous: false
requirements: [GRID-01, GRID-02, GRID-03, GRID-04, GRID-05]

must_haves:
  truths:
    - "Full vitest suite is green (server + dashboard)"
    - "architecture-purity.test.ts is green unchanged (no new native bindings, no new sole-importers)"
    - "tool-budget.test.ts asserts === 7 unchanged (Phase 21 adds zero new MCP tools)"
    - "Grep test: no 'shot.status_changed' SSE subscription leak outside App.tsx / state/shot-grid.ts (D-22 lock)"
    - "Grep test: no selectedShotId mutation in ShotGridView.tsx or ShotGridCard.tsx (D-04 lock)"
    - "Grep test: no history.pushState in state/shot-grid.ts (D-09 lock)"
    - "Manual WCAG smoke: 5 shot-status-* tokens render with correct hex values; pill contrast against --color-bg is visually legible in both themes"
    - "Manual end-to-end smoke: TreeSidebar grid icon → ShotGridView mounts → cards render → status change reflects in-place via SSE → click card opens VersionDrawer → home button returns to HomeView with state preserved"
  artifacts:
    - path: ".planning/phases/21-shot-grid-view/21-05-SUMMARY.md"
      provides: "Phase 21 verification record + green-suite proof + manual smoke checklist"
  key_links:
    - from: ".planning/phases/21-shot-grid-view/21-05-SUMMARY.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 21 plan checkboxes update + traceability table → 'Complete'"
      pattern: "Phase 21"
---

<objective>
Phase-gate verification. Run the full vitest suite, confirm the architecture-purity and tool-budget invariants remain unchanged, run grep-based architectural rule checks, perform manual WCAG color verification against UI-SPEC's pre-computed contrast tables, and execute an end-to-end manual smoke test of the shot grid view in the running dashboard. After this plan: Phase 21 is complete; v1.3 progress moves from 1/5 phases to 2/5; Phase 22 (Review and Approval) and Phase 23 (Production Stats) are unblocked.

Purpose: GSD verification pattern — every plan's tests prove its own work, but the phase gate proves nothing regressed in adjacent code. The grep-based architectural rule checks (D-04, D-09, D-22 locks) are cheap defensive scans. Manual WCAG + visual smoke verifies the things automated tests cannot reach (color contrast in real browser, lazy thumbnail loading, in-place SSE updates).

Output: ROADMAP.md updated (Phase 21 ticked complete); STATE.md updated (current position advances to Phase 22); 21-05-SUMMARY.md records green-suite + manual checklist results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-shot-grid-view/21-CONTEXT.md
@.planning/phases/21-shot-grid-view/21-UI-SPEC.md
@.planning/phases/21-shot-grid-view/21-VALIDATION.md
@.planning/phases/21-shot-grid-view/21-01-SUMMARY.md
@.planning/phases/21-shot-grid-view/21-02-SUMMARY.md
@.planning/phases/21-shot-grid-view/21-03-SUMMARY.md
@.planning/phases/21-shot-grid-view/21-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 21-05-T01: Run full vitest suite + architecture-purity + tool-budget invariants + grep-based rule checks</name>
  <files></files>
  <read_first>
    - .planning/phases/21-shot-grid-view/21-VALIDATION.md (sampling rate spec + manual-only verifications)
    - .planning/phases/21-shot-grid-view/21-CONTEXT.md D-04, D-09, D-22 (the three architectural locks to grep-verify)
    - src/__tests__/architecture-purity.test.ts (current state — must run green unchanged)
    - src/__tests__/tool-budget.test.ts (current state — must assert === 7 unchanged)
  </read_first>
  <action>
    Execute the following commands in order. Capture stdout + exit code per command. Any non-zero exit = STOP and remediate before continuing.

    1. **Full vitest suite (server + dashboard):**
       ```
       npx vitest run
       ```
       Expected: exit 0; all suites green. Note total test count (should reflect Phase 21 additions — at minimum the new test files: shot-status-repo-grid, dashboard-routes-shot-grid, ShotStatusPill, ShotGridCard, ShotGridFilterBar, SequenceHeader, ShotGridView, App, shot-grid state, time helper, and the extended TreeSidebar + api.test additions).

    2. **Architecture-purity invariant:**
       ```
       npx vitest run src/__tests__/architecture-purity.test.ts
       ```
       Expected: exit 0; test count unchanged from Phase 20 baseline. Phase 21 introduces NO new native bindings and NO new sole-importer entries (it's all assembly on existing primitives).

    3. **Tool-budget invariant:**
       ```
       npx vitest run src/__tests__/tool-budget.test.ts
       ```
       Expected: exit 0; assertion `expect(toolCount).toBe(7)` still passes. Phase 21 added an HTTP route (dashboard-only), NOT an MCP tool.

    4. **Grep check D-22: no `shot.status_changed` SSE subscription outside App.tsx + state/shot-grid.ts:**
       ```
       grep -rn "onSseEvent('shot.status_changed'\|onSseEvent(\"shot.status_changed\"" packages/dashboard/src/ --include='*.ts' --include='*.tsx' | grep -v 'App.tsx\|state/shot-grid.ts\|__tests__' | wc -l
       ```
       Expected: 0. If non-zero, locate the offender and refactor.

    5. **Grep check D-04: no `selectedShotId` mutation in ShotGridView or ShotGridCard:**
       ```
       grep -E "selectedShotId\.value\s*=" packages/dashboard/src/views/ShotGridView.tsx packages/dashboard/src/components/ShotGridCard.tsx 2>/dev/null | wc -l
       ```
       Expected: 0. Phase 21 card clicks mutate `selectedVersionId`, NEVER `selectedShotId`.

    6. **Grep check D-09: no `history.pushState` in Phase 21 state:**
       ```
       grep -c "history\.pushState" packages/dashboard/src/state/shot-grid.ts
       ```
       Expected: 0. Phase 21 uses `replaceState` ONLY (D-09 lock).

    7. **Grep check (REQ-03 client-side filter): no server `statusFilter` or `showOmitted` query param:**
       ```
       grep -E "'\?statusFilter='|'\?showOmitted='|statusFilter=.*c\.req\.query|showOmitted=.*c\.req\.query" src/http/dashboard-routes.ts | wc -l
       ```
       Expected: 0. The /shot-grid route accepts only `?cursor=` and `?limit=`.

    8. **Grep check (Phase 21 cap): no new `server.registerTool(` calls:**
       ```
       git diff --name-only HEAD~15 HEAD -- 'src/**/*.ts' | xargs grep -l 'server.registerTool\|server\.tool(' 2>/dev/null | wc -l
       ```
       Expected: 0 NEW occurrences (the existing 7 from Phase 20 don't count — the diff scope is Phase 21 commits only). If non-zero, investigate.

    9. **Theme token sanity:**
       ```
       grep -v '^\s*\*\|^\s*/' packages/dashboard/src/styles/theme.css | grep -c -- '--color-shot-status-'
       ```
       Expected: ≥ 10 (5 tokens × 2 themes).

    10. **Copy.ts new exports count:**
        ```
        grep -c '^export const ' packages/dashboard/src/lib/copy.ts
        ```
        Expected: ≥ 46 (19 pre-existing + ≥ 27 new).

    11. **Dashboard build (Vite production build):**
        ```
        cd packages/dashboard && npx vite build
        ```
        Expected: exit 0; no CSS errors, no JS errors. The build's output bundle should be present.

    Record EACH command's exit code and any non-empty stdout in the 21-05-SUMMARY.md draft. If any check fails, STOP and create a remediation task before proceeding to T02.
  </action>
  <verify>
    <automated>npx vitest run</automated>
    <automated>npx vitest run src/__tests__/architecture-purity.test.ts</automated>
    <automated>npx vitest run src/__tests__/tool-budget.test.ts</automated>
    <automated>grep -rn "onSseEvent('shot.status_changed'" packages/dashboard/src/ --include='*.ts' --include='*.tsx' | grep -v 'App.tsx\|state/shot-grid.ts\|__tests__' | wc -l | awk '$1 == 0 {print "OK D-22"} $1 != 0 {print "FAIL D-22"; exit 1}'</automated>
    <automated>grep -E "selectedShotId\.value\s*=" packages/dashboard/src/views/ShotGridView.tsx packages/dashboard/src/components/ShotGridCard.tsx 2>/dev/null | wc -l | awk '$1 == 0 {print "OK D-04"} $1 != 0 {print "FAIL D-04"; exit 1}'</automated>
    <automated>grep -c "history\.pushState" packages/dashboard/src/state/shot-grid.ts | awk '$1 == 0 {print "OK D-09"} $1 != 0 {print "FAIL D-09"; exit 1}'</automated>
    <automated>grep -v '^\s*\*\|^\s*/' packages/dashboard/src/styles/theme.css | grep -c -- '--color-shot-status-' | awk '$1 &gt;= 10 {print "OK theme tokens"} $1 &lt; 10 {print "FAIL got " $1; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full suite green)
    - `npx vitest run src/__tests__/architecture-purity.test.ts` exits 0 with unchanged test count
    - `npx vitest run src/__tests__/tool-budget.test.ts` exits 0; assertion still `=== 7`
    - All 8 grep-based rule checks return the expected counts (0 violations)
    - `cd packages/dashboard && npx vite build` exits 0
    - All command outputs + exit codes are recorded for the 21-05-SUMMARY.md
  </acceptance_criteria>
  <done>Automated phase gate is green; no architectural lock has been violated; full suite passes; dashboard builds cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 21-05-T02: Manual WCAG color smoke + end-to-end visual smoke (HUMAN GATE)</name>
  <files></files>
  <read_first>
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Color" lines 113-152 (the WCAG AA contrast proof tables — source of truth for hex values + contrast ratios)
    - .planning/phases/21-shot-grid-view/21-VALIDATION.md §"Manual-Only Verifications" lines 123-128 (the manual checklist driving this task)
    - packages/dashboard/src/styles/theme.css (current state after Wave 1 — confirm the 5 shot-status tokens match UI-SPEC values exactly)
  </read_first>
  <what-built>
    The Phase 21 shot grid view: TreeSidebar grid-icon affordance, signal-driven view routing, ShotGridFilterBar + SequenceHeader + ShotGridCard composition, SSE-reactive aggregate counts, in-place status badge updates, client-side status filter, "Show omitted" toggle, URL state mirror.

    Five new shot-status color tokens were added with mathematical WCAG AA contrast proof in UI-SPEC. Manual verification confirms the runtime rendering matches the proof.
  </what-built>
  <how-to-verify>
    **Part A — WCAG color hex audit (≤ 5 min):**

    1. Open `packages/dashboard/src/styles/theme.css` in your editor.
    2. In the `@theme` block (dark theme), confirm these EXACT hex values appear:
       - `--color-shot-status-wip: #94a3b8;` (slate-400)
       - `--color-shot-status-pending-review: #fbbf24;` (amber-400)
       - `--color-shot-status-approved: #4ade80;` (green-400)
       - `--color-shot-status-on-hold: #60a5fa;` (blue-400)
       - `--color-shot-status-omit: #64748b;` (slate-500)
    3. In the `[data-theme="light"]` block, confirm:
       - `--color-shot-status-wip: #64748b;`
       - `--color-shot-status-pending-review: #d97706;`
       - `--color-shot-status-approved: #16a34a;`
       - `--color-shot-status-on-hold: #2563eb;`
       - `--color-shot-status-omit: #94a3b8;`
    4. Open WebAIM Contrast Checker (https://webaim.org/resources/contrastchecker/) in a browser.
    5. For each of the 10 hex values above, pair against the matching theme's `--color-bg`:
       - Dark theme bg: `#202020`
       - Light theme bg: `#fafafa`
    6. Confirm each contrast ratio passes WCAG 2.1 AA threshold for text (4.5:1):
       - All 5 dark-theme pairs should meet 4.5:1 (UI-SPEC table proves up to 11.1:1)
       - All 5 light-theme pairs should meet 4.5:1 (UI-SPEC table proves down to 4.5:1 exact threshold for omit)
    7. If ANY ratio fails: NOTE the failing pair and STOP. Do not approve.

    **Part B — End-to-end visual smoke (≤ 15 min):**

    1. In one terminal: `npx tsx src/server.ts --http` (server on :3000).
    2. In a second terminal: `cd packages/dashboard && npx vite` (dashboard dev server, typically on :5173).
    3. In a browser, navigate to the dashboard URL.
    4. Confirm initial state: TreeSidebar visible on left, HomeView on right, "VFX Familiar" brand text + Home button (accent-colored) in the header.

    5. **TreeSidebar grid-icon affordance check (D-01 / D-02 / D-05):**
       - Expand a project to reveal sequences.
       - Confirm each SEQUENCE row has a LayoutGrid icon at its right edge.
       - Confirm workspace and project rows do NOT have grid icons (D-01 scoping).
       - Hover over a sequence's grid icon — confirm hover color change (muted → fg per UI-SPEC §"Animation & Motion").
       - Click the grid icon.
       - Confirm: `activeView` switches to shot-grid; the ShotGridView mounts on the right pane; the clicked sequence's grid icon becomes accent-filled (aria-current="page" — open DevTools, inspect the active icon button, confirm the `aria-current` attribute equals `"page"`); the URL updates to include `?view=shot-grid&seq=<seqId>` (use DevTools Network or Address Bar to verify).
       - Confirm the home button in the header transitions from accent to muted (since activeView is no longer 'home').

    6. **ShotGridView rendering smoke (GRID-01 / GRID-02):**
       - SequenceHeader at the top: sequence name in larger Inter Tight font (verify via DevTools computed style or by visual comparison to v1.0 dashboard's headings).
       - Aggregate count mini-pills under the sequence name: each non-zero status appears as a small color-coded pill (e.g., `[wip 5] [approved 12]`); zero counts are hidden.
       - Sticky filter bar below the header with: Status label + 5 pills (All, wip, pending-review, approved, on-hold) + right-aligned Show omitted toggle.
       - Below the filter bar: CSS Grid of ShotGridCards. Each card is ~220px wide, has a 16:9 thumbnail at top, then a row with ShotStatusPill + version count + shot name, with the Updated relative timestamp below.
       - Scroll the grid: confirm the filter bar sticks to the top (sticky positioning).
       - Resize the browser window narrower/wider: confirm the grid wraps via `minmax(220px, 1fr)` — fewer columns on narrow viewports, more on wide.

    7. **Lazy thumbnail check (REQ-02 / GRID-02 CLS=0):**
       - Open DevTools → Network tab → filter by "img" or "thumbnail".
       - Hard refresh the shot grid view.
       - Scroll DOWN the grid. Confirm thumbnails BELOW the fold are NOT loaded until they enter the viewport (lazy-load via `loading="lazy"`).
       - Confirm thumbnails ABOVE the fold load immediately with no Cumulative Layout Shift (the placeholder dimensions equal the loaded image dimensions per Phase 17 `explicit width/height`).

    8. **Filter + Show omitted smoke (GRID-03 / GRID-05):**
       - Click the "approved" pill: only approved-status cards visible; URL updates with `&statusFilter=approved`.
       - Click "All": all non-omit cards re-appear.
       - Toggle Show omitted ON: omit cards appear with `opacity-40` dimming (visually washed out compared to active cards). URL updates with `&showOmitted=1`.
       - Toggle Show omitted OFF: omit cards disappear; URL updates.
       - With Show omitted ON and `statusFilter='omit'`, toggle Show omitted OFF: confirm `statusFilter` auto-resets to `'all'` (the All pill becomes active).
       - Reload the page with `?statusFilter=approved&showOmitted=1` in the URL: confirm the filter state hydrates correctly (URL > signal on first mount per D-09).
       - Test malformed URL: navigate to `?statusFilter=DROP_TABLE`. Confirm: filter falls back to "All" + a `console.warn` appears in DevTools Console (graceful fallback, NEVER throws).

    9. **SSE in-place update smoke (REQ-04 / D-22):**
       - With the shot grid open on a specific sequence, switch to a separate terminal.
       - Trigger a shot status change via an MCP client OR directly via the engine (e.g., `npx tsx -e "const { Engine } = require('./src/engine/pipeline'); const e = new Engine(...); e.setShotStatus('shot_xyz', 'approved', 'system_test');"` — exact invocation depends on how you have the project wired).
       - Return to the browser. Confirm:
         - The affected card's ShotStatusPill updates IN PLACE (no full grid re-fetch, no scroll jump).
         - The aggregate counts mini-pills in the SequenceHeader re-derive automatically.
         - No flash, no thumbnail re-decode, no scroll position change.
         - DevTools Network tab confirms NO `GET /api/sequences/.../shot-grid` request was made during the SSE-driven update.

    10. **Card click + VersionDrawer smoke (D-19):**
        - Click a card that has a `latest_completed_version` (visible thumbnail).
        - Confirm: VersionDrawer overlay opens for the latest completed version. ShotGridView is preserved behind the overlay.
        - Close the drawer (escape or close button per Phase 5).
        - Confirm focus returns to the card you clicked (Phase 5 drawer-close precedent).
        - Click a card WITHOUT a completed version (SkeletonThumbnail visible).
        - Confirm: nothing happens; the button is keyboard-inactive too (Tab to it, Enter — no effect).

    11. **Home button return smoke (D-04 / D-06):**
        - From ShotGridView, click the Home button in the header.
        - Confirm: activeView returns to 'home'; HomeView mounts; the home button becomes accent-colored again.
        - In HomeView, confirm any previously-selected shot (selectedShotId) is still selected — D-04 LOCKED: HomeView state is preserved across view switches.
        - Click another sequence's grid icon. Confirm: ShotGridView mounts on the new sequence; previous sequence's filter/showOmitted state was carried over (signal persistence) but the data is for the new sequence.

    12. **Architecture-purity sanity (read-only):**
        - Confirm no new browser console errors during the smoke flow.
        - Confirm no new dependencies appeared in `package.json` (Phase 21 adds zero — `lucide-preact`, `@preact/signals`, `zod` were already installed).

    If ALL parts pass: Type `approved` to advance to T03 (ROADMAP + STATE update).

    If ANY part fails: describe the failure precisely (which step, what was expected vs observed). The plan is BLOCKED on remediation.
  </how-to-verify>
  <resume-signal>Type "approved" if all parts pass. Otherwise describe what failed and which step.</resume-signal>
</task>

<task type="auto">
  <name>Task 21-05-T03: Update ROADMAP.md + STATE.md to mark Phase 21 complete + write 21-05-SUMMARY.md</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/21-shot-grid-view/21-05-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — find Phase 21 section, plans list, progress table)
    - .planning/STATE.md (current state — find Current Position section + Pending Todos)
    - .planning/phases/21-shot-grid-view/21-01-SUMMARY.md through 21-04-SUMMARY.md (the four plan summaries — extract artifact counts + key wins + any deviations)
  </read_first>
  <action>
    1. **Update `.planning/ROADMAP.md`:**

       - Locate the `### v1.3 Production Shot Grid (Phases 20-24)` section (around line 148).
       - In the bulleted list of phases, change:
         - `- [ ] **Phase 21: Shot Grid View** — ...` to `- [x] **Phase 21: Shot Grid View** — ... (completed YYYY-MM-DD)` — substitute today's actual date.
       - Locate `### Phase 21: Shot Grid View` (around line 183).
       - Change `**Plans:** TBD (estimated 4-5 plans)` to `**Plans:** 5/5 plans complete`.
       - Update the `Plans:` block to mark each of 21-01 through 21-05 as `[x]` (completed):
         - `- [x] 21-01-PLAN.md — Wave 1 foundations (gap closure, theme tokens, repo SQL, copy, time, types)`
         - `- [x] 21-02-PLAN.md — Wave 2 backend + state plumbing (engine facade, HTTP route, ShotStatusPill, state/shot-grid, fetchShotGrid)`
         - `- [x] 21-03-PLAN.md — Wave 3 component layer (ShotGridCard, ShotGridFilterBar, SequenceHeader, TreeSidebar grid-icon)`
         - `- [x] 21-04-PLAN.md — Wave 4 view + root integration (ShotGridView, App.tsx)`
         - `- [x] 21-05-PLAN.md — Wave 5 phase gate (full-suite + WCAG smoke + visual smoke)`
       - In the bottom Progress table, change the Phase 21 row from `0/TBD | Not started | —` to `5/5 | Complete | YYYY-MM-DD`.
       - In the traceability table (REQUIREMENTS.md if it's mirrored in ROADMAP, otherwise skip — the canonical traceability lives in REQUIREMENTS.md and is read-only from ROADMAP's perspective), update GRID-01..05 rows to `Complete`.

    2. **Update `.planning/STATE.md`:**

       - `current_position`: update phase to `22` (or whichever is the next planned phase per ROADMAP — should be `22`, the Review and Approval phase).
       - `last_updated`: today's ISO timestamp.
       - `last_activity`: today's date.
       - `stopped_at`: `Phase 21 complete`.
       - Add to `Pending Todos` (replacing the Phase 20 ones already completed): something like `- [ ] Plan Phase 22 — run /gsd-plan-phase 22 (Review and Approval; depends on Phase 20 ✓)`. Remove any Phase 20 or Phase 21 todos that are now done.
       - Update the `Progress` block: `completed_phases: 2` (Phase 20 + Phase 21); `completed_plans: 4 + 5 = 9`; percent: `(2/5) * 100 = 40%`.
       - Append to the `Decisions` list under "Accumulated Context":
         - `- [Phase 21]: Signal-driven view routing established (activeView 'home' | 'shot-grid'); URL state mirror via history.replaceState; client-side filter; single-query window-function CTE for GRID-04 (EXPLAIN QUERY PLAN test enforces no-N+1)`
       - Update the "Performance Metrics" → "By Phase" table to add a row for Phase 21 (5 plans).

    3. **Write `.planning/phases/21-shot-grid-view/21-05-SUMMARY.md`:**

       Frontmatter:
       ```yaml
       ---
       plan_id: 21-05
       phase: 21
       phase_name: shot-grid-view
       completed: <YYYY-MM-DD>
       wave: 5
       result: green
       ---
       ```

       Body sections:
       - **Phase 21 — Plan 21-05 — Phase Gate (SUMMARY)**: top-level header
       - **Result: ALL GREEN** (or describe failures + remediation if any task required iteration)
       - **Test suite metrics**: total test count, dashboard test count, server test count, runtime. Note any new files contributed by Phase 21 (the 12 Wave-0 test files from VALIDATION.md).
       - **Architectural invariants confirmed**:
         - architecture-purity: green unchanged
         - tool-budget: still 7 of 12
         - D-22 grep check: 0 violations
         - D-04 grep check: 0 violations
         - D-09 grep check: 0 violations
         - REQ-03 server-filter grep check: 0 violations
         - Phase 21 new MCP tools: 0
       - **Theme token sanity**: count of --color-shot-status-* tokens (≥ 10).
       - **Copy.ts additions**: total export count (≥ 46), new constants count (≥ 27).
       - **Manual WCAG + visual smoke (T02 record)**:
         - WCAG color hex audit: PASS / FAIL (link to UI-SPEC tables)
         - TreeSidebar grid-icon affordance: PASS / FAIL
         - ShotGridView CSS Grid + sticky filter bar: PASS / FAIL
         - Lazy thumbnail loading + CLS=0: PASS / FAIL
         - Filter + Show omitted toggle: PASS / FAIL
         - URL hydration on first mount: PASS / FAIL
         - Malformed URL graceful fallback: PASS / FAIL
         - SSE in-place card update: PASS / FAIL
         - Aggregate counts re-derivation on SSE: PASS / FAIL
         - Card click → VersionDrawer: PASS / FAIL
         - Home button return preserves HomeView state (D-04): PASS / FAIL
       - **Files delivered by Phase 21** (full list across all 5 plans):
         - 9 NEW dashboard files (ShotGridCard, ShotGridFilterBar, SequenceHeader, ShotStatusPill, ShotGridView, state/shot-grid.ts, lib/time.ts, types/shot-grid.ts, plus the 9 new test files)
         - 1 EXTENDED server file (shot-status-repo.ts +listShotsForGrid)
         - 2 MODIFIED server files (pipeline.ts +listShotGrid facade, dashboard-routes.ts +GET /shot-grid)
         - 1 NEW server test file (shot-status-repo-grid.test.ts)
         - 1 NEW server integration test file (dashboard-routes-shot-grid.test.ts)
         - 4 MODIFIED dashboard files (types/events.ts +ShotStatusChangedPayload, styles/theme.css +5 tokens × 2 themes, lib/copy.ts +27 constants, lib/api.ts +fetchShotGrid, TreeSidebar.tsx +grid icon, App.tsx +activeView routing, HomeView.tsx +TreeSidebar wiring)
       - **Requirements coverage**: GRID-01 / GRID-02 / GRID-03 / GRID-04 / GRID-05 all marked complete with the plan that delivered each.
       - **Tool count**: 7 of 12 unchanged.
       - **Next phase unblocked**: Phase 22 (Review and Approval). Phase 23 (Production Stats) also unblocked (depends on Phase 20 + Phase 21).
       - **Deviations from plans**: List anything that landed differently from what 21-01..21-04 plans specified (with rationale). If none: "No deviations."
       - **Velocity**: total commits (sum of atomic commits across all 5 plans); total time elapsed; notes on cost patterns.

       Save the file. This is the canonical phase summary.
  </action>
  <verify>
    <automated>grep -c "Phase 21.*Complete\|Phase 21.*completed" .planning/ROADMAP.md | awk '$1 &gt;= 1 {print "OK roadmap"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "5/5" .planning/ROADMAP.md | awk '$1 &gt;= 1 {print "OK plans complete"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "Phase: 22\|current_position\|^Phase: 22" .planning/STATE.md | awk '$1 &gt;= 1 {print "OK state advanced"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
    <automated>test -f .planning/phases/21-shot-grid-view/21-05-SUMMARY.md &amp;&amp; echo "OK summary exists" || (echo "FAIL"; exit 1)</automated>
    <automated>grep -c "result: green\|ALL GREEN" .planning/phases/21-shot-grid-view/21-05-SUMMARY.md | awk '$1 &gt;= 1 {print "OK green summary"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` shows Phase 21 as complete with `[x]` markers on all 5 plans and `5/5` in the progress table
    - `.planning/STATE.md` `current_position` advanced to Phase 22 (or next planned); `Pending Todos` updated; `completed_phases` and `completed_plans` counts incremented
    - `.planning/phases/21-shot-grid-view/21-05-SUMMARY.md` exists with frontmatter (`result: green`) and full body sections including test metrics, architectural invariants, manual smoke checklist, files delivered, requirements coverage, and deviations
    - All three files are committed atomically (one logical change: "phase 21 complete: ROADMAP + STATE + summary")
  </acceptance_criteria>
  <done>Phase 21 is officially complete; STATE.md points to Phase 22 as the next plannable phase; the v1.3 Production Shot Grid milestone is 40% complete (2/5 phases shipped).</done>
</task>

</tasks>

<verification>
Final phase-gate verification:

```bash
# Full suite
npx vitest run

# Architectural invariants
npx vitest run src/__tests__/architecture-purity.test.ts src/__tests__/tool-budget.test.ts

# Phase 21 scope (full)
npx vitest run \
  src/store/__tests__/shot-status-repo-grid.test.ts \
  src/http/__tests__/dashboard-routes-shot-grid.test.ts \
  packages/dashboard/src/lib/__tests__/time.test.ts \
  packages/dashboard/src/state/__tests__/shot-grid.test.ts \
  packages/dashboard/src/components/__tests__/ShotStatusPill.test.tsx \
  packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx \
  packages/dashboard/src/components/__tests__/ShotGridFilterBar.test.tsx \
  packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx \
  packages/dashboard/src/views/__tests__/ShotGridView.test.tsx \
  packages/dashboard/src/__tests__/App.test.tsx \
  packages/dashboard/src/__tests__/TreeSidebar.test.tsx

# Dashboard build (production)
cd packages/dashboard && npx vite build

# Architectural lock greps (all should return 0)
grep -rn "onSseEvent('shot.status_changed'" packages/dashboard/src/ --include='*.ts' --include='*.tsx' | grep -v 'App.tsx\|state/shot-grid.ts\|__tests__' | wc -l
grep -E "selectedShotId\.value\s*=" packages/dashboard/src/views/ShotGridView.tsx packages/dashboard/src/components/ShotGridCard.tsx 2>/dev/null | wc -l
grep -c "history\.pushState" packages/dashboard/src/state/shot-grid.ts

# ROADMAP + STATE sanity
grep -c "5/5" .planning/ROADMAP.md
grep -c "Phase: 22\|current_position" .planning/STATE.md
test -f .planning/phases/21-shot-grid-view/21-05-SUMMARY.md
```
</verification>

<success_criteria>
- Full vitest suite green (all server tests + all dashboard tests + Phase 21 scope tests)
- architecture-purity and tool-budget invariants remain at green with zero edits (tool count still 7 of 12)
- 8 grep-based architectural rule checks (D-04, D-09, D-22, REQ-03, etc.) all return 0 violations
- Manual WCAG color hex audit passes — all 10 hex values match UI-SPEC §"Color" tables
- Manual end-to-end visual smoke passes — TreeSidebar → ShotGridView → SSE update → card click → home button return all work
- ROADMAP.md updated: Phase 21 marked `[x]` complete; plans list shows `5/5`; progress table row updated
- STATE.md updated: current position advances to Phase 22; pending todos refreshed; decisions log appended
- `.planning/phases/21-shot-grid-view/21-05-SUMMARY.md` written with full verification record
- Phase 22 (Review and Approval) is unblocked
- All tasks committed atomically
</success_criteria>

<output>
After completion, `.planning/phases/21-shot-grid-view/21-05-SUMMARY.md` IS the output (T03 writes it). It documents the entire Phase 21 verification record. No further summary file needs to be created — 21-05-SUMMARY is the canonical phase close-out.
</output>
