---
plan_id: 21-06
phase: 21
phase_name: shot-grid-view
plan: 6
type: execute
wave: 6
gap_closure: true
depends_on: [21-01, 21-02, 21-03, 21-04]
files_modified:
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/views/HomeView.tsx
  - packages/dashboard/src/views/ShotGridView.tsx
  - packages/dashboard/src/views/VersionDrawerHost.tsx  # NEW
  - packages/dashboard/src/views/__tests__/VersionDrawerHost.test.tsx  # NEW
  - packages/dashboard/src/state/shot-grid.ts
  - packages/dashboard/src/__tests__/App.test.tsx
  - packages/dashboard/src/views/__tests__/ShotGridView.test.tsx
autonomous: true
requirements: [GRID-01, GRID-02, GRID-03, GRID-04, GRID-05]
source: 21-AUDIT.md (4-channel audit found 7 bugs)

must_haves:
  truths:
    - "hydrateShotGridUrlState() AND hydrateSortState() are called in App.tsx boot useEffect BEFORE startSse() (Bug 1 fix; closes D-09 boot-scope chicken-and-egg)"
    - "<VersionDrawerHost/> is rendered at App.tsx level outside the {isHome ? <HomeView/> : <ShotGridView/>} switch, reading selectedVersionId from the global signal (Bug 2 fix; D-19 contract honored across both views)"
    - "VersionDrawerHost resolves the version object via versions.value FIRST (fast path) then falls back to fetchVersion(id) when the id is not in the local cache (Bug 5 fix; works for any version id from any view)"
    - "ShotGridView Load More handler captures the dispatch-time seqId and rejects late responses whose seqId no longer matches selectedSequenceForGrid.value (Bug 3 BLOCKING race fix; uses both `alive` latch AND seqId guard)"
    - "ShotGridView's initial fetch effect resets shotGrid.value=null at the start of every sequence change BEFORE awaiting the fetch (Bug 4 fix; no stale data bleeding between sequences)"
    - "ShotGridView's initial fetch .catch() sets shotGrid.value=[] AND gridLoadMoreError.value to a copy constant, and the render switch checks gridLoadMoreError BEFORE the empty-state branch (Bug 6 fix)"
    - "HomeView passes currentGridSequenceId={activeView.value === 'shot-grid' ? selectedSequenceForGrid.value : undefined} (Bug 7 fix; aria-current='page' clears on return-to-home)"
    - "App.test.tsx contains a URL-deep-link integration test (seeds window.location.search BEFORE render, asserts ShotGridView surface mounts, clicks card, asserts <VersionDrawer/> present) — single test catches Bugs 1+2+5"
    - "ShotGridView.test.tsx contains a sequence-switch test (load seq A, switch to seq B with A's fetch still pending, assert no shot-A cards leak into seq-B grid) — catches Bugs 3+4"
    - "ShotGridView.test.tsx contains an initial-fetch-rejection test (mock fetchShotGrid to reject, render, assert error state surface) — catches Bug 6"

  artifacts:
    - path: "packages/dashboard/src/App.tsx"
      provides: "Boot useEffect (hydrate + sse), VersionDrawerHost at root, view-toggle slot only renders body view"
    - path: "packages/dashboard/src/views/VersionDrawerHost.tsx"
      provides: "Self-resolving overlay: reads selectedVersionId, resolves Version via versions.value cache then fetchVersion fallback, renders <VersionDrawer/> when resolved"
      exports: ["VersionDrawerHost"]
    - path: "packages/dashboard/src/views/HomeView.tsx"
      provides: "Pure view content (tree + version list); NO VersionDrawer render, NO hydrate calls, NO selected-version resolver"
    - path: "packages/dashboard/src/views/ShotGridView.tsx"
      provides: "Pure view content with race-free Load More, seq-clear-on-switch, error-state render, headerExpanded signal"
    - path: "packages/dashboard/src/state/shot-grid.ts"
      provides: "Adds headerExpanded signal (persists across view remounts; D-15 session-only)"

  key_links:
    - from: "packages/dashboard/src/App.tsx"
      to: "packages/dashboard/src/state/shot-grid.ts"
      via: "useEffect calls hydrateShotGridUrlState() before startSse()"
      pattern: "hydrateShotGridUrlState"
    - from: "packages/dashboard/src/views/VersionDrawerHost.tsx"
      to: "packages/dashboard/src/lib/api.ts"
      via: "fetchVersion(id) fallback when versions.value cache miss"
      pattern: "fetchVersion"
---

<objective>
Resolve the 7 bugs documented in 21-AUDIT.md by refactoring Phase 21's composition pattern. All bugs share one root cause: globally-keyed signals were authored as leaves of view components, then severed by the view-toggle mount switch at App.tsx:105.

After this plan: the boot useEffect at App.tsx becomes the canonical place for all mount-time hydration; VersionDrawer renders unconditionally as a self-resolving overlay; ShotGridView's async lifecycle is race-free and renders failure states. Integration tests at the App-level catch any future cross-view-seam regression.

Purpose: prevent the same pattern from recurring in Phases 22-24, which both add new views and new global overlays.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-shot-grid-view/21-CONTEXT.md
@.planning/phases/21-shot-grid-view/21-AUDIT.md
@.planning/phases/21-shot-grid-view/21-01-SUMMARY.md
@.planning/phases/21-shot-grid-view/21-02-SUMMARY.md
@.planning/phases/21-shot-grid-view/21-03-SUMMARY.md
@.planning/phases/21-shot-grid-view/21-04-SUMMARY.md

<interfaces>
<!-- Already-shipped contracts the executor must preserve verbatim -->

From packages/dashboard/src/lib/api.ts (Phase 04+):
```typescript
export function fetchVersion(id: string): Promise<Version>;
// GET /api/versions/:id — returns full Version object including version_number
// for prior-version lookups inside VersionDrawer's View Diff feature.
```

From packages/dashboard/src/state/versions.ts:
```typescript
export const selectedVersionId = signal<string | null>(null);
export const versions = signal<Version[]>([]);
// versions.value holds whichever shot is currently selected in HomeView.
// For grid-card clicks, the clicked version is unlikely to be in this cache.
```

From packages/dashboard/src/state/shot-grid.ts:
```typescript
export const activeView = signal<'home' | 'shot-grid'>('home');
export const selectedSequenceForGrid = signal<string | null>(null);
export const shotGrid = signal<ShotGridResponse | null>(null);
export const gridIsFetching = signal<boolean>(false);
export const gridLoadMoreError = signal<string | null>(null);
export function hydrateShotGridUrlState(): void;
export function persistShotGridUrlState(): void;
```

From packages/dashboard/src/views/VersionDrawer.tsx:
```typescript
export interface VersionDrawerProps {
  version: Version;
  priorVersion: Version | null;
  onClose: () => void;
}
// MUST NOT change this interface — too many consumers
// (DiffDrawer, SummarySection, C2paShield) depend on the Version object shape.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 21-06-T01: Add headerExpanded signal + hoist hydrate to App.tsx boot scope (Bug 1)</name>
  <files>
    - packages/dashboard/src/state/shot-grid.ts
    - packages/dashboard/src/App.tsx
    - packages/dashboard/src/views/ShotGridView.tsx
    - packages/dashboard/src/views/HomeView.tsx
  </files>
  <read_first>
    - packages/dashboard/src/state/shot-grid.ts (add headerExpanded signal alongside activeView)
    - packages/dashboard/src/App.tsx (current useEffect at lines 54-69)
    - packages/dashboard/src/views/ShotGridView.tsx lines 90, 95-97 (headerExpanded local state; hydrate call to remove)
    - packages/dashboard/src/views/HomeView.tsx lines 180-184 (hydrateSortState call to remove)
  </read_first>
  <action>
    1. **In state/shot-grid.ts:** Add a new `export const headerExpanded = signal<boolean>(true)` near `activeView` (D-15: open by default, session-only — natural fit for signal scope). Add a brief JSDoc explaining the move from useState.
    2. **In App.tsx:** Import `hydrateShotGridUrlState` from `./state/shot-grid.js` and `hydrateSortState` from wherever it lives in HomeView (likely `./state/versions.js` or similar — grep first). Update the useEffect to call BOTH hydrate functions BEFORE the `onSseEvent`/`startSse` calls. Preserve the cleanup function.
    3. **In ShotGridView.tsx:** Remove the `useState`-based `headerExpanded` at line 90. Replace `headerExpanded` references with `headerExpanded.value` from the signal. Remove the entire useEffect at lines 92-97 that called `hydrateShotGridUrlState()`. Update imports accordingly.
    4. **In HomeView.tsx:** Remove the useEffect at lines 180-184 that called `hydrateSortState()`. Remove the import. Add a JSDoc comment noting "Phase 21-06: hydrate moved to App.tsx boot scope".
  </action>
  <verify>
    <automated>grep -n "hydrateShotGridUrlState\|hydrateSortState" packages/dashboard/src/App.tsx | wc -l | awk '$1 >= 2 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "hydrateShotGridUrlState" packages/dashboard/src/views/ShotGridView.tsx | awk '$1 == 0 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "hydrateSortState" packages/dashboard/src/views/HomeView.tsx | awk '$1 == 0 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "headerExpanded = signal" packages/dashboard/src/state/shot-grid.ts | awk '$1 == 1 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>npx tsc --noEmit -p packages/dashboard/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx boot useEffect calls both hydrate functions before SSE setup
    - ShotGridView has no hydrate call and no useState for headerExpanded
    - HomeView has no hydrate call
    - state/shot-grid.ts exports a new headerExpanded signal
    - Dashboard typecheck clean
  </acceptance_criteria>
  <done>Bug 1 (chicken-and-egg) is dismantled. headerExpanded survives view remounts.</done>
</task>

<task type="auto">
  <name>Task 21-06-T02: Create VersionDrawerHost overlay, hoist to App.tsx, remove from HomeView (Bugs 2 + 5)</name>
  <files>
    - packages/dashboard/src/views/VersionDrawerHost.tsx (NEW)
    - packages/dashboard/src/App.tsx
    - packages/dashboard/src/views/HomeView.tsx
  </files>
  <read_first>
    - packages/dashboard/src/views/VersionDrawer.tsx (preserve interface — accepts version + priorVersion + onClose)
    - packages/dashboard/src/views/HomeView.tsx lines 448-466 (selected-version + priorVersion resolver — move to VersionDrawerHost)
    - packages/dashboard/src/views/HomeView.tsx lines 561-569 (VersionDrawer render block — delete)
    - packages/dashboard/src/lib/api.ts line 253 (fetchVersion helper)
    - packages/dashboard/src/state/versions.ts (selectedVersionId + versions signals)
  </read_first>
  <action>
    1. **Create packages/dashboard/src/views/VersionDrawerHost.tsx** — a new component that reads `selectedVersionId.value` from the signal and:
       - Returns null when selectedVersionId is null
       - First checks `versions.value` for a hit by id (fast path, no fetch — preserves home flow's existing UX)
       - On cache miss, calls `fetchVersion(id)` and stores the result in local component state with an `alive` latch
       - Also resolves `priorVersion`: looks in versions.value first; if not present, fetchVersion(priorVersionId) when the prior id is derivable; else null
       - Once `version` is resolved, renders `<VersionDrawer version={...} priorVersion={...} onClose={() => selectedVersionId.value = null} />`
       - Renders a SkeletonDrawer (or just a transparent placeholder) during the fetch
       - Logs `console.warn` on fetch failure and clears selectedVersionId (graceful degradation — same defensive pattern as hydrate)
    2. **In App.tsx:** Import VersionDrawerHost. Render it at the same level as `<ActiveGenerationsPanel/>` so it's always mounted regardless of activeView.
    3. **In HomeView.tsx:** Delete the VersionDrawer render block (lines 561-569). Delete the selectedVersion + priorVersion derivation (lines 448-466). Remove the VersionDrawer import. Leave selectedVersionId mutation alone (onSelect callback at line 491 — that signal is now read by VersionDrawerHost).
  </action>
  <verify>
    <automated>test -f packages/dashboard/src/views/VersionDrawerHost.tsx && echo "OK" || echo "FAIL"</automated>
    <automated>grep -c "VersionDrawerHost" packages/dashboard/src/App.tsx | awk '$1 >= 1 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "VersionDrawer " packages/dashboard/src/views/HomeView.tsx | awk '$1 == 0 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "fetchVersion" packages/dashboard/src/views/VersionDrawerHost.tsx | awk '$1 >= 1 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>npx tsc --noEmit -p packages/dashboard/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - VersionDrawerHost exists, self-resolves from versionId, renders VersionDrawer when ready
    - App.tsx mounts VersionDrawerHost unconditionally
    - HomeView no longer renders VersionDrawer or owns the resolver
    - Dashboard typecheck clean
  </acceptance_criteria>
  <done>Bugs 2 and 5 are fixed. Drawer opens from any view; fetch-by-id fallback handles versions not in local cache.</done>
</task>

<task type="auto">
  <name>Task 21-06-T03: Add seqId guard to ShotGridView Load More handler (Bug 3 BLOCKING)</name>
  <files>
    - packages/dashboard/src/views/ShotGridView.tsx
  </files>
  <read_first>
    - packages/dashboard/src/views/ShotGridView.tsx lines 136-155 (the Load More click handler)
    - packages/dashboard/src/views/ShotGridView.tsx lines 100-130 (the initial fetch effect with `alive` latch — replicate the pattern)
  </read_first>
  <action>
    Locate the Load More click handler (lines 136-155, the `onClick` callback that calls `fetchShotGrid(seqId, { cursor: cursor.value, limit: 20 })` and pushes results into `shotGrid.value`).

    Refactor to:
    1. Capture `const requestSeqId = selectedSequenceForGrid.value;` at handler entry
    2. After the await, check `if (selectedSequenceForGrid.value !== requestSeqId) return;` BEFORE mutating shotGrid.value
    3. Add the existing `alive` latch pattern if not present (a local mutable `aborted = false` flag isn't quite right for an event handler — use the seqId guard as the primary defense)
    4. Add a JSDoc note explaining the race-condition prevention (cite Bug 3 in 21-AUDIT.md)
  </action>
  <verify>
    <automated>grep -n "requestSeqId\|capturedSeqId\|dispatchSeqId" packages/dashboard/src/views/ShotGridView.tsx | wc -l | awk '$1 >= 2 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "selectedSequenceForGrid.value !==" packages/dashboard/src/views/ShotGridView.tsx | awk '$1 >= 1 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Load More handler captures dispatch-time seqId and rejects late responses for a different sequence
    - JSDoc comment cites Bug 3 and explains the guard
  </acceptance_criteria>
  <done>Bug 3 (BLOCKING) is fixed. Stale Load More responses cannot leak into a different sequence's grid.</done>
</task>

<task type="auto">
  <name>Task 21-06-T04: Clear shotGrid.value on sequence change + render error state (Bugs 4 + 6 + 7)</name>
  <files>
    - packages/dashboard/src/views/ShotGridView.tsx
    - packages/dashboard/src/views/HomeView.tsx
  </files>
  <read_first>
    - packages/dashboard/src/views/ShotGridView.tsx lines 100-130 (initial fetch effect)
    - packages/dashboard/src/views/ShotGridView.tsx lines 230-260 (render branches — empty state, loading, etc)
    - packages/dashboard/src/views/HomeView.tsx line 506 (currentGridSequenceId pass-through)
    - packages/dashboard/src/lib/copy.ts (find or add a SHOT_GRID_FETCH_ERROR copy constant)
  </read_first>
  <action>
    1. **In ShotGridView.tsx initial fetch effect:** Add `shotGrid.value = null;` AND `gridLoadMoreError.value = null;` at the top of the effect body, BEFORE the `await fetchShotGrid(...)` call. This makes the next sequence's load show a clean loading state.
    2. **In the .catch() block:** Set `gridLoadMoreError.value = SHOT_GRID_FETCH_ERROR;` (add this copy constant to lib/copy.ts if absent: `"Couldn't load shots. Try refreshing the page."` or similar).
    3. **In the render switch:** Add a new branch BEFORE the empty-state check: `if (gridLoadMoreError.value && !shotGrid.value) { return <EmptyState ... /> with the error copy + a Retry button that re-triggers the fetch }`. Use existing EmptyState component if it accepts an actionable button; otherwise a plain div with role="alert".
    4. **In HomeView.tsx line 506:** Change `currentGridSequenceId={selectedSequenceForGrid.value ?? undefined}` to `currentGridSequenceId={activeView.value === 'shot-grid' ? (selectedSequenceForGrid.value ?? undefined) : undefined}`. Add a brief comment citing Bug 7 in 21-AUDIT.md.
  </action>
  <verify>
    <automated>grep -c "shotGrid.value = null" packages/dashboard/src/views/ShotGridView.tsx | awk '$1 >= 1 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "SHOT_GRID_FETCH_ERROR" packages/dashboard/src/views/ShotGridView.tsx packages/dashboard/src/lib/copy.ts | awk '$1 >= 2 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "activeView.value === 'shot-grid'" packages/dashboard/src/views/HomeView.tsx | awk '$1 >= 1 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>npx tsc --noEmit -p packages/dashboard/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Sequence change clears stale shotGrid before fetching
    - Initial fetch failure renders a visible error state (not a blank pane)
    - aria-current="page" only applies when activeView==='shot-grid'
  </acceptance_criteria>
  <done>Bugs 4, 6, 7 fixed.</done>
</task>

<task type="auto">
  <name>Task 21-06-T05: Add VersionDrawerHost unit tests + integration tests catching cross-seam bugs</name>
  <files>
    - packages/dashboard/src/views/__tests__/VersionDrawerHost.test.tsx (NEW)
    - packages/dashboard/src/__tests__/App.test.tsx
    - packages/dashboard/src/views/__tests__/ShotGridView.test.tsx
  </files>
  <read_first>
    - packages/dashboard/src/views/__tests__/ShotGridView.test.tsx (existing pattern — beforeEach setup, renderAndWait helper)
    - packages/dashboard/src/__tests__/App.test.tsx (existing pattern + history.replaceState reset at lines 121-128)
  </read_first>
  <action>
    1. **Create VersionDrawerHost.test.tsx** with 5 cases:
       - "renders null when selectedVersionId is null"
       - "renders VersionDrawer with cached version (versions.value hit, no fetch)"
       - "fetches via fetchVersion on cache miss, renders drawer with result"
       - "logs console.warn and clears selectedVersionId on fetch failure"
       - "passes priorVersion=null when not derivable (grid-card click scenario)"
    2. **Add to App.test.tsx** — single integration test catching Bugs 1+2+5:
       ```
       it('URL deep-link → shot-grid → card click → drawer renders', async () => {
         window.history.replaceState(null, '', '?view=shot-grid&seq=seq_1');
         vi.mocked(fetchShotGrid).mockResolvedValue({...one shot with version_id ver_a});
         vi.mocked(fetchVersion).mockResolvedValue({...full Version for ver_a});
         render(<App />);
         await waitFor(() => expect(screen.getByText(FILTER_BAR_STATUS_LABEL)).toBeInTheDocument());
         await userEvent.click(screen.getByRole('button', { name: /Open version drawer/ }));
         await waitFor(() => expect(screen.getByRole('dialog')).toBeInTheDocument());
       });
       ```
       Mock `fetchVersion` in lib/api.ts (add to the existing vi.mock setup).
       Remove or guard the `history.replaceState(null, '', window.location.pathname)` in beforeEach for this specific test — it deliberately tests the URL-driven path.
    3. **Add to ShotGridView.test.tsx** — two new cases:
       - "sequence switch clears stale shotGrid before new fetch resolves" (mock A's fetch as never-resolving, switch to seq B with B's fetch resolving immediately, assert no A-shots visible during transition)
       - "initial fetch rejection renders error state, not blank pane" (mock fetchShotGrid to reject, assert error copy is in the document)
    4. **Pre-existing test cleanup:** ShotGridView.test.tsx may need adjustments since headerExpanded moved from useState to signal. Update any tests that asserted on initial-render-header-state via re-render to use the signal value directly.
  </action>
  <verify>
    <automated>test -f packages/dashboard/src/views/__tests__/VersionDrawerHost.test.tsx && echo "OK" || echo "FAIL"</automated>
    <automated>grep -c "URL deep-link\|deep-link\|seq_1.*view=shot-grid\|view=shot-grid.*seq_1" packages/dashboard/src/__tests__/App.test.tsx | awk '$1 >= 1 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "sequence switch\|rejection renders error\|stale" packages/dashboard/src/views/__tests__/ShotGridView.test.tsx | awk '$1 >= 2 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
    <automated>cd packages/dashboard && npx vitest run --reporter=default 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - VersionDrawerHost test file exists with 5+ cases
    - App.test.tsx has a URL-deep-link integration test
    - ShotGridView.test.tsx has sequence-switch + rejection test cases
    - All dashboard tests pass (361 + ~10 new = ~371 total expected)
  </acceptance_criteria>
  <done>Test coverage now catches the cross-view-seam composition bugs that the original suite was blind to.</done>
</task>

<task type="auto">
  <name>Task 21-06-T06: Full dashboard test sweep + verify against 21-AUDIT bug list</name>
  <files></files>
  <read_first>
    - .planning/phases/21-shot-grid-view/21-AUDIT.md §1 (the 7-bug inventory)
  </read_first>
  <action>
    Run the full dashboard test suite plus targeted greps that verify each fix from 21-AUDIT.md:
    1. `cd packages/dashboard && npx vitest run`  → expect 371+ tests passing (361 prior + ~10 new)
    2. `npx tsc --noEmit -p packages/dashboard/tsconfig.json` → clean
    3. `npx tsc --noEmit` (root) → clean
    4. **Bug 1 grep:** `grep "hydrateShotGridUrlState" packages/dashboard/src/App.tsx` → ≥1
    5. **Bug 2 grep:** `grep -c "VersionDrawer" packages/dashboard/src/App.tsx` → ≥1 (via VersionDrawerHost import)
    6. **Bug 3 grep:** seq-id guard in ShotGridView Load More handler → present
    7. **Bug 4 grep:** `shotGrid.value = null` in ShotGridView init effect → present
    8. **Bug 5 grep:** `fetchVersion` in VersionDrawerHost → present
    9. **Bug 6 grep:** `SHOT_GRID_FETCH_ERROR` in copy + ShotGridView → present
    10. **Bug 7 grep:** `activeView.value === 'shot-grid'` near currentGridSequenceId in HomeView → present
    11. `cd packages/dashboard && npx vite build` → clean (no CSS/JS errors)
  </action>
  <verify>
    <automated>cd packages/dashboard && npx vitest run 2>&1 | grep -E "Test Files|Tests" | tail -3</automated>
    <automated>cd packages/dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | head -5</automated>
    <automated>cd packages/dashboard && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All dashboard tests pass
    - Both tsconfigs clean
    - Vite build clean
    - All 7 audit-bug greps return expected counts
  </acceptance_criteria>
  <done>Phase 21 architectural refactor complete; ready to re-run T02 manual smoke.</done>
</task>

</tasks>

<acceptance_criteria>
**Plan-level (all 6 tasks combined):**
- All 7 bugs from 21-AUDIT.md §1 fixed (Bug 1 hydrate; Bug 2 VersionDrawer scope; Bug 3 BLOCKING race; Bug 4 stale grid on switch; Bug 5 drawer data model; Bug 6 fetch failure blank pane; Bug 7 aria-current persistence)
- New VersionDrawerHost component provides clean self-resolving overlay pattern (template for future Phase 22-24 overlays)
- App.tsx boot useEffect becomes the canonical hydrate location (no future chicken-and-egg)
- Integration test pattern catches cross-view-seam composition bugs going forward
- 371+ dashboard tests passing (361 prior + ~10 new)
- Architecture-purity invariant unchanged
- Tool-budget invariant unchanged (no MCP tool changes in this plan)
</acceptance_criteria>
