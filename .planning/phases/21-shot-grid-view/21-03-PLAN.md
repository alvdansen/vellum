---
plan_id: 21-03
phase: 21
phase_name: shot-grid-view
plan: 3
type: execute
wave: 3
depends_on: [21-01, 21-02]
files_modified:
  - packages/dashboard/src/components/ShotGridCard.tsx
  - packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx
  - packages/dashboard/src/components/ShotGridFilterBar.tsx
  - packages/dashboard/src/components/__tests__/ShotGridFilterBar.test.tsx
  - packages/dashboard/src/components/SequenceHeader.tsx
  - packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx
  - packages/dashboard/src/components/TreeSidebar.tsx
  - packages/dashboard/src/__tests__/TreeSidebar.test.tsx
autonomous: true
requirements: [GRID-01, GRID-02, GRID-03, GRID-05]

must_haves:
  truths:
    - "<ShotGridCard/> renders Thumbnail for completed-version shots, SkeletonThumbnail for shots without"
    - "<ShotGridCard/> click invokes onSelect(latest_completed_version.id) ONLY when latest_completed_version !== null"
    - "<ShotGridCard/> is aria-disabled when no completed version; onClick suppressed"
    - "<ShotGridCard/> wraps in opacity-40 div when status === 'omit'"
    - "<ShotGridFilterBar/> renders 5 status pills (All|wip|pending-review|approved|on-hold) plus omit pill when showOmitted=true"
    - "<ShotGridFilterBar/> pill click invokes onChangeStatusFilter; Show omitted button invokes onToggleShowOmitted; sticky top-0 z-10"
    - "<SequenceHeader/> renders sequence name in font-display + collapsible chevron + aggregate count mini-pills (zero counts hidden); aria-expanded reflects state"
    - "TreeSidebar SequenceNode renders LayoutGrid icon button at right edge; clicks invoke onOpenGrid(seqId) with e.stopPropagation; aria-current='page' when currentGridSequenceId === sequence.id"
    - "TreeSidebar grid icon only appears on sequence rows (NOT workspace/project/shot rows per D-01)"
  artifacts:
    - path: "packages/dashboard/src/components/ShotGridCard.tsx"
      provides: "Single-button shot card with thumbnail/skeleton + pill + version count + relative timestamp + omit dimming"
      exports: ["ShotGridCard"]
    - path: "packages/dashboard/src/components/ShotGridFilterBar.tsx"
      provides: "Sticky filter bar with status pills + Show omitted toggle"
      exports: ["ShotGridFilterBar"]
    - path: "packages/dashboard/src/components/SequenceHeader.tsx"
      provides: "Collapsible header with sequence name + chevron + aggregate count mini-pills"
      exports: ["SequenceHeader"]
    - path: "packages/dashboard/src/components/TreeSidebar.tsx"
      provides: "Existing tree + new grid-icon affordance on sequence rows"
  key_links:
    - from: "packages/dashboard/src/components/ShotGridCard.tsx"
      to: "packages/dashboard/src/components/ShotStatusPill.tsx"
      via: "renders <ShotStatusPill status={shot.status} />"
      pattern: "ShotStatusPill"
    - from: "packages/dashboard/src/components/ShotGridCard.tsx"
      to: "packages/dashboard/src/lib/time.ts"
      via: "formatRelativeTime(latest_completed_version.completed_at)"
      pattern: "formatRelativeTime"
    - from: "packages/dashboard/src/components/ShotGridFilterBar.tsx"
      to: "packages/dashboard/src/lib/copy.ts"
      via: "imports FILTER_PILL_* + SHOW_OMITTED_* constants"
      pattern: "FILTER_PILL_"
    - from: "packages/dashboard/src/components/TreeSidebar.tsx"
      to: "packages/dashboard/src/lib/copy.ts"
      via: "imports TREE_GRID_ICON_ARIA_PREFIX + TREE_GRID_ICON_ACTIVE_ARIA_SUFFIX"
      pattern: "TREE_GRID_ICON"
---

<objective>
Build the four composite components that the Phase 21 view composes: `<ShotGridCard/>`, `<ShotGridFilterBar/>`, `<SequenceHeader/>`, and the `<TreeSidebar/>` grid-icon affordance. Each is pure (props in, callbacks out) — no signal reads, no fetches, no state mutations. After this plan: every visual primitive for the shot grid view exists and is component-tested in isolation; Wave 4 wires them together inside `<ShotGridView/>`.

Purpose: Keeping components pure lets `<ShotGridView/>` (Wave 4) own all the state plumbing in one place — easier to test, easier to revise. The TreeSidebar grid-icon affordance is the entry-point UX for the entire phase (D-01 / D-02 — VFX artists discover the feature by clicking the grid icon on a sequence row).

Output: 4 new component files + 4 new test files + 1 modified file (TreeSidebar.tsx). Plan-scope tests all pass; TreeSidebar's existing tests (`TreeSidebar.test.tsx` already in `__tests__/`) remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-shot-grid-view/21-CONTEXT.md
@.planning/phases/21-shot-grid-view/21-UI-SPEC.md
@.planning/phases/21-shot-grid-view/21-RESEARCH.md
@.planning/phases/21-shot-grid-view/21-PATTERNS.md
@.planning/phases/21-shot-grid-view/21-VALIDATION.md
@.planning/phases/21-shot-grid-view/21-01-SUMMARY.md
@.planning/phases/21-shot-grid-view/21-02-SUMMARY.md

<interfaces>
<!-- Contracts from Waves 1 + 2, in-tree after those plans complete -->

From packages/dashboard/src/types/shot-grid.ts:
```typescript
export interface ShotGridRow {
  id: string; name: string; status: ShotStatus;
  version_count: number;
  latest_completed_version: { id: string; thumbnail_url: string; completed_at: number } | null;
}
```

From packages/dashboard/src/components/ShotStatusPill.tsx (Wave 2):
```typescript
export interface ShotStatusPillProps { status: ShotStatus }
export function ShotStatusPill({ status }: ShotStatusPillProps);
```

From packages/dashboard/src/components/Thumbnail.tsx (Phase 17 existing):
```typescript
// Accepts version: { id, label, status: 'queued' | 'running' | 'complete' | 'failed' }, size: 'card' | etc.
export function Thumbnail({ version, size, c2paStatus? }: ThumbnailProps): VNode;
```

From packages/dashboard/src/components/SkeletonThumbnail.tsx (Phase 17 existing):
```typescript
// Renders a placeholder with explicit width/height for CLS=0
export function SkeletonThumbnail({ width, height }: SkeletonThumbnailProps): VNode;
```

From packages/dashboard/src/lib/copy.ts (Wave 1):
- `SHOT_CARD_OPEN_ARIA_PREFIX = 'Open version drawer for '`
- `SHOT_CARD_VERSION_COUNT_SINGULAR = '1 version'`
- `SHOT_CARD_VERSION_COUNT_PLURAL_SUFFIX = ' versions'`
- `SHOT_CARD_NO_VERSIONS = 'No versions yet'`
- `SHOT_CARD_LAST_UPDATED_PREFIX = 'Updated '`
- `FILTER_BAR_STATUS_LABEL = 'Status'`
- `FILTER_PILL_ALL`, `FILTER_PILL_WIP`, `FILTER_PILL_PENDING_REVIEW`, `FILTER_PILL_APPROVED`, `FILTER_PILL_ON_HOLD`, `FILTER_PILL_OMIT`
- `SHOW_OMITTED_TOGGLE_LABEL = 'Show omitted'`
- `SHOW_OMITTED_TOGGLE_ARIA = 'Toggle omitted shots'`
- `SEQUENCE_HEADER_TOGGLE_ARIA_PREFIX_OPEN = 'Collapse '`
- `SEQUENCE_HEADER_TOGGLE_ARIA_PREFIX_CLOSED = 'Expand '`
- `AGGREGATE_COUNTS_REGION_LABEL_PREFIX = 'Status counts for '`
- `TREE_GRID_ICON_ARIA_PREFIX = 'Open shot grid for '`
- `TREE_GRID_ICON_ACTIVE_ARIA_SUFFIX = ' (current)'`

From packages/dashboard/src/lib/time.ts (Wave 1):
```typescript
export function formatRelativeTime(epochMs: number, nowMs?: number): string;
```

From packages/dashboard/src/components/TreeSidebar.tsx (current, lines 204-258 SequenceNode + 280-330 TreeRow):
- `TreeRow` accepts `label`, `depth`, `expanded`, `hasChildren`, `isSelected`, `onClick`, `onToggle`, `thumbnail?`, `Icon?`
- `SequenceNode` props: `sequence, selectedShotId, onSelectShot, expandedIds, onToggleExpand`
- TreeSidebar exports a single component; props flow down via prop-drilling

Lucide icons available in `lucide-preact@1.9.0`:
- `Home`, `LayoutGrid` (Phase 21 new uses)
- `ChevronDown`, `ChevronRight` (Phase 21 SequenceHeader uses)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 21-03-T01: Create <ShotGridCard/> component + tests</name>
  <files>packages/dashboard/src/components/ShotGridCard.tsx, packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx</files>
  <read_first>
    - packages/dashboard/src/components/VersionCard.tsx (exact analog — single-button card pattern with Thumbnail + StatusPill)
    - packages/dashboard/src/components/Thumbnail.tsx (Phase 17 — size="card" variant, alt text from version.label)
    - packages/dashboard/src/components/SkeletonThumbnail.tsx (Phase 17 — width/height props for CLS=0)
    - packages/dashboard/src/components/ShotStatusPill.tsx (after Wave 2)
    - packages/dashboard/src/lib/time.ts (after Wave 1)
    - packages/dashboard/src/lib/copy.ts (after Wave 1 — confirms SHOT_CARD_* constants)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §17 lines 1475-1640 (full body of ShotGridCard; landmines on disabled/aria-disabled; key handling)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §18 lines 1643-1711 (test scaffold)
    - .planning/phases/21-shot-grid-view/21-CONTEXT.md D-16, D-17, D-19 (LOCKED design)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Spacing Scale" lines 50-52 (220×124 thumbnail dimensions for CLS=0)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Typography" lines 73-80 (text-xs/text-sm sizes, `num` class on numeric surfaces)
  </read_first>
  <behavior>
    - Test 1: `<ShotGridCard shot={withCompletedVersion} onSelect={fn} />` renders an `<img>` (from Thumbnail)
    - Test 2: `<ShotGridCard shot={noCompletedVersion} onSelect={fn} />` renders NO `<img>` (SkeletonThumbnail instead); button has `aria-disabled="true"`; click does NOT invoke onSelect
    - Test 3: Click on enabled card invokes `onSelect(latest_completed_version.id)`
    - Test 4: When `shot.status === 'omit'`, the wrapper has class `opacity-40`; the inner pill does NOT have opacity-40
    - Test 5: When `shot.version_count === 1`, text contains `'1 version'`; when `shot.version_count === 3`, text contains `'3 versions'`; when `shot.version_count === 0`, text contains `'No versions yet'`
    - Test 6: aria-label is `'Open version drawer for {shotName}'` (exact concatenation)
    - Test 7: When card has a completed version, text contains `'Updated '` followed by a relative timestamp (e.g., `'1m ago'` for a 60s-old completed_at against system time)
  </behavior>
  <action>
    1. Create `packages/dashboard/src/components/ShotGridCard.tsx` (NEW, ~80 LOC):

       Imports:
       - `import type { ShotGridRow } from '../types/shot-grid.js';`
       - `import { ShotStatusPill } from './ShotStatusPill.js';`
       - `import { Thumbnail } from './Thumbnail.js';`
       - `import { SkeletonThumbnail } from './SkeletonThumbnail.js';`
       - `import { formatRelativeTime } from '../lib/time.js';`
       - `import { SHOT_CARD_OPEN_ARIA_PREFIX, SHOT_CARD_VERSION_COUNT_SINGULAR, SHOT_CARD_VERSION_COUNT_PLURAL_SUFFIX, SHOT_CARD_NO_VERSIONS, SHOT_CARD_LAST_UPDATED_PREFIX } from '../lib/copy.js';`

       Export `interface ShotGridCardProps`:
       - `shot: ShotGridRow`
       - `onSelect: (versionId: string) => void`

       Private helper `function formatVersionCount(n: number): string`:
       - `if (n === 0) return SHOT_CARD_NO_VERSIONS;`
       - `if (n === 1) return SHOT_CARD_VERSION_COUNT_SINGULAR;`
       - `return \`${n}${SHOT_CARD_VERSION_COUNT_PLURAL_SUFFIX}\`;`

       Export `function ShotGridCard({ shot, onSelect }: ShotGridCardProps)`:
       - Compute `hasVersion = shot.latest_completed_version !== null`.
       - Compute `disabled = !hasVersion`.
       - Compute `isOmit = shot.status === 'omit'`.
       - Compute `handleClick = hasVersion ? () => onSelect(shot.latest_completed_version!.id) : undefined`.
       - Build the inner `<button>`:
         - `type="button"`
         - `onClick={handleClick}`
         - `aria-label={\`${SHOT_CARD_OPEN_ARIA_PREFIX}${shot.name}\`}`
         - `aria-disabled={disabled || undefined}` (string 'true' is what assistive tech expects; using `|| undefined` removes the attribute when not disabled)
         - `disabled={disabled}` (HTML attribute — handles keyboard + click natively)
         - `class={\`w-full overflow-hidden rounded text-left transition-shadow focus-visible:ring-2 focus-visible:ring-[var(--color-accent)] ${disabled ? 'cursor-default' : 'hover:shadow-[0_0_0_1px_var(--color-border)]'}\`}`
       - Inside the button: conditionally render `<Thumbnail version={{ id: shot.latest_completed_version!.id, label: shot.name, status: 'complete' }} size="card" />` when hasVersion, else `<SkeletonThumbnail width={220} height={124} />`.
       - Below the thumbnail: a `<div class="flex flex-col gap-1 p-2 text-[var(--color-fg)]">` containing:
         - A row `<div class="flex items-center justify-between gap-2">` with `<ShotStatusPill status={shot.status} />` and `<span class="num text-xs text-[var(--color-fg-muted)]">{formatVersionCount(shot.version_count)}</span>`
         - `<span class="truncate text-sm font-normal">{shot.name}</span>`
         - When hasVersion: `<span class="num text-xs text-[var(--color-fg-muted)]">{SHOT_CARD_LAST_UPDATED_PREFIX}{formatRelativeTime(shot.latest_completed_version!.completed_at)}</span>`
       - When `isOmit`: wrap the entire `<button>` in `<div class="opacity-40 transition-opacity">{button}</div>` and return the wrapper. Otherwise return the button directly. (D-17: pill remains 100% opacity even on dimmed card.)

       JSDoc: reference D-16, D-17, D-19; mention that key handling is the PARENT's responsibility (`.map((s) => <ShotGridCard key={s.id} ... />)`).

       Landmines (per PATTERNS.md §17 lines 1633-1640):
       - DO NOT invoke `onSelect` from the disabled branch.
       - Use BOTH `disabled` (HTML) AND `aria-disabled` (a11y).
       - `key={shot.id}` is set by the parent — DO NOT set inside the component.
       - Thumbnail accepts `status: 'complete'` hard-coded — that's the contract (shot grid only renders cards for shots whose latest version IS complete, when hasVersion is true).

    2. Create `packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx` (NEW, ~120 LOC):

       Imports: vitest describe/it/expect/vi/afterEach; render/fireEvent/cleanup from `@testing-library/preact`; ShotGridCard from `'../ShotGridCard.js'`; `type ShotGridRow` from `'../../types/shot-grid.js'`.

       `afterEach(() => cleanup());`

       Build a module-level `function buildShot(overrides: Partial<ShotGridRow>): ShotGridRow` helper returning a sane default shot with `id: 'shot_1', name: 'sh010', status: 'approved', version_count: 3, latest_completed_version: { id: 'ver_abc', thumbnail_url: '/api/versions/ver_abc/thumbnail', completed_at: Date.now() - 60_000 }`, overridable via `...overrides`.

       Tests per <behavior>:
       - "renders Thumbnail when latest_completed_version !== null": render with default shot; `expect(container.querySelector('img')).toBeTruthy()`.
       - "renders SkeletonThumbnail + aria-disabled + no onClick when no completed version": render with `buildShot({ latest_completed_version: null })`; assert no `<img>` in DOM, `button.getAttribute('aria-disabled') === 'true'`, fireEvent.click does NOT invoke `onSelect`.
       - "click invokes onSelect with latest_completed_version.id": render with default; fireEvent.click on button; `expect(onSelect).toHaveBeenCalledWith('ver_abc')`.
       - "omit status wraps in opacity-40": render with `buildShot({ status: 'omit' })`; assert `container.querySelector('.opacity-40')` is truthy.
       - "version_count text variants":
         - `count=0` → expect text to contain `'No versions yet'`
         - `count=1` → expect text to contain `'1 version'` (singular, no plural 's')
         - `count=3` → expect text to contain `'3 versions'`
       - "aria-label includes shot name with prefix": render with `buildShot({ name: 'sh042' })`; assert button.getAttribute('aria-label') === 'Open version drawer for sh042'.
       - "renders Updated prefix + relative timestamp when hasVersion": render with completed_at = Date.now() - 60_000 ago; assert text includes 'Updated ' followed by either '1m ago' or 'just now' (depending on the exact second of Date.now()).

       Landmines (per PATTERNS.md §18 lines 1707-1711):
       - Use `fireEvent.click` (synchronous synthetic event); `vi.fn()` records the call.
       - The "stable DOM across status mutation" check is naturally a parent-level test (ShotGridView in Wave 4).
  </action>
  <verify>
    <automated>npx vitest run packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx</automated>
    <automated>grep -c "export function ShotGridCard\|export interface ShotGridCardProps" packages/dashboard/src/components/ShotGridCard.tsx | awk '$1 == 2 {print "OK"} $1 != 2 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "aria-disabled" packages/dashboard/src/components/ShotGridCard.tsx | awk '$1 &gt;= 1 {print "OK"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "opacity-40" packages/dashboard/src/components/ShotGridCard.tsx | awk '$1 &gt;= 1 {print "OK"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/components/ShotGridCard.tsx` exists with `ShotGridCardProps` interface and `ShotGridCard` component
    - Component wraps in `opacity-40` div ONLY when `shot.status === 'omit'`
    - Component sets `aria-disabled="true"` ONLY when `shot.latest_completed_version === null`
    - Component invokes `onSelect(latest_completed_version.id)` ONLY when hasVersion (when disabled, onClick is undefined)
    - Test file has ≥ 7 test cases; `npx vitest run packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx` exits 0
    - The version_count text variants (0/1/3) match the copy constants exactly
    - aria-label format: `'Open version drawer for {name}'`
  </acceptance_criteria>
  <done>ShotGridCard is built and tested. Wave 4 ShotGridView .map()s over `filteredShots` rendering `<ShotGridCard key={shot.id} ... />`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-03-T02: Create <ShotGridFilterBar/> component + tests</name>
  <files>packages/dashboard/src/components/ShotGridFilterBar.tsx, packages/dashboard/src/components/__tests__/ShotGridFilterBar.test.tsx</files>
  <read_first>
    - packages/dashboard/src/components/StatusPill.tsx (design vocabulary for pill styling)
    - packages/dashboard/src/components/LoadMoreButton.tsx lines 103-133 (button + focus-visible pattern)
    - packages/dashboard/src/lib/copy.ts (after Wave 1 — confirms FILTER_PILL_* + SHOW_OMITTED_* constants)
    - packages/dashboard/src/types/shot-grid.ts (ShotStatus type)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §19 lines 1715-1844 (full body; landmines on omit pill visibility being parent-controlled)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §20 lines 1847-1905 (test scaffold)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Color" lines 154-165 (active vs inactive pill styling)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Responsive Behavior" lines 412-426 (sticky filter bar CSS)
    - .planning/phases/21-shot-grid-view/21-CONTEXT.md D-07, D-08, D-10, D-11 (LOCKED — filter bar shape, pill order, active styling)
  </read_first>
  <behavior>
    - Test 1: Render with `statusFilter='all', showOmitted=false` → 5 pills rendered (All, wip, pending-review, approved, on-hold) in order; NO omit pill
    - Test 2: Render with `showOmitted=true` → 6 pills (the 5 above + omit at the end)
    - Test 3: Click on 'approved' pill invokes `onChangeStatusFilter('approved')`
    - Test 4: Click on 'All' pill invokes `onChangeStatusFilter('all')`
    - Test 5: Click on Show omitted toggle invokes `onToggleShowOmitted()` (no args)
    - Test 6: Active pill (matching `statusFilter`) has `aria-pressed="true"` AND `bg-[var(--color-accent)]` class; inactive pills have `aria-pressed="false"` AND `border` class
    - Test 7: Show omitted toggle is a `<button role="switch">` with `aria-checked` reflecting `showOmitted` prop
    - Test 8: Container has class `sticky top-0 z-10`
  </behavior>
  <action>
    1. Create `packages/dashboard/src/components/ShotGridFilterBar.tsx` (NEW, ~90 LOC):

       Imports:
       - `import type { ShotStatus } from '../types/shot-grid.js';`
       - `import { FILTER_BAR_STATUS_LABEL, FILTER_PILL_ALL, FILTER_PILL_WIP, FILTER_PILL_PENDING_REVIEW, FILTER_PILL_APPROVED, FILTER_PILL_ON_HOLD, FILTER_PILL_OMIT, SHOW_OMITTED_TOGGLE_LABEL, SHOW_OMITTED_TOGGLE_ARIA } from '../lib/copy.js';`

       Type alias `type FilterValue = 'all' | ShotStatus;`

       Export `interface ShotGridFilterBarProps`:
       - `statusFilter: FilterValue`
       - `showOmitted: boolean`
       - `onChangeStatusFilter: (next: FilterValue) => void`
       - `onToggleShowOmitted: () => void`

       Private `interface PillSpec { value: FilterValue; label: string; }`

       Export `function ShotGridFilterBar({ statusFilter, showOmitted, onChangeStatusFilter, onToggleShowOmitted }: ShotGridFilterBarProps)`:
       - Build the pill array dynamically:
         ```
         const pills: PillSpec[] = [
           { value: 'all', label: FILTER_PILL_ALL },
           { value: 'wip', label: FILTER_PILL_WIP },
           { value: 'pending-review', label: FILTER_PILL_PENDING_REVIEW },
           { value: 'approved', label: FILTER_PILL_APPROVED },
           { value: 'on-hold', label: FILTER_PILL_ON_HOLD },
         ];
         if (showOmitted) { pills.push({ value: 'omit', label: FILTER_PILL_OMIT }); }
         ```
       - Return the sticky bar:
         - Wrapper `<div class="sticky top-0 z-10 flex items-center gap-2 border-b border-[var(--color-border)] bg-[var(--color-bg)] px-4 py-3" aria-label="Shot status filters">`
         - Inside: a `<span class="label-uppercase text-[var(--color-fg-muted)]">{FILTER_BAR_STATUS_LABEL}</span>`
         - `pills.map((p) => { const active = statusFilter === p.value; return <button key={p.value} type="button" onClick={() => onChangeStatusFilter(p.value)} aria-pressed={active} class={active ? 'inline-flex items-center rounded-full px-3 py-1 text-xs uppercase tracking-widest transition-colors focus-visible:ring-2 focus-visible:ring-[var(--color-accent)] bg-[var(--color-accent)] text-[var(--color-bg)]' : 'inline-flex items-center rounded-full px-3 py-1 text-xs uppercase tracking-widest transition-colors focus-visible:ring-2 focus-visible:ring-[var(--color-accent)] border border-[var(--color-border)] text-[var(--color-fg-muted)] hover:bg-[var(--color-surface-alt)] hover:text-[var(--color-fg)]'}>{p.label}</button>; })`
         - Right-aligned toggle wrapped in `<div class="ml-auto">`:
           ```
           <button
             type="button"
             role="switch"
             aria-checked={showOmitted}
             aria-label={SHOW_OMITTED_TOGGLE_ARIA}
             onClick={onToggleShowOmitted}
             class={`inline-flex items-center gap-2 rounded px-3 py-1 text-sm transition-colors focus-visible:ring-2 focus-visible:ring-[var(--color-accent)] ${showOmitted ? 'text-[var(--color-fg)]' : 'text-[var(--color-fg-muted)]'}`}
           >
             <span class={`inline-block h-3 w-6 rounded-full transition-colors ${showOmitted ? 'bg-[var(--color-accent)]' : 'bg-[var(--color-border)]'}`} aria-hidden="true" />
             {SHOW_OMITTED_TOGGLE_LABEL}
           </button>
           ```

       JSDoc: reference D-07/D-08/D-10/D-11; explicitly note that this is a pure component (callbacks-out); the parent handles the "reset to 'all' when toggling off omit while filter=='omit'" logic.

       Landmines (per PATTERNS.md §19 lines 1837-1844):
       - Use `key={p.value}` on mapped pills.
       - The omit pill's visibility is controlled SOLELY by the `showOmitted` prop — it's not toggleable by clicking the pill itself.
       - The toggle is `<button role="switch">` (NOT `<input type="checkbox">`).
       - Use the `label-uppercase` utility class from theme.css for the "Status" label.

    2. Create `packages/dashboard/src/components/__tests__/ShotGridFilterBar.test.tsx` (NEW, ~80 LOC):

       Imports: vitest + render/fireEvent/cleanup + ShotGridFilterBar.

       `afterEach(() => cleanup());`

       Build `function renderBar(overrides: Partial<{ statusFilter: ..., showOmitted: boolean, onChangeStatusFilter: ..., onToggleShowOmitted: ... }>)` helper returning the render result.

       Tests per <behavior>:
       - "renders 5 pills when showOmitted=false; omit pill absent": render with `showOmitted=false`; `expect(Array.from(container.querySelectorAll('button[aria-pressed]')).length).toBe(5)`; assert no button has textContent 'omit'.
       - "renders 6 pills when showOmitted=true; omit pill present": render with `showOmitted=true`; assert pill count is 6; assert one button has textContent 'omit'.
       - "click on pill invokes onChangeStatusFilter": render; find approved pill (`Array.from(container.querySelectorAll('button[aria-pressed]')).find(b => b.textContent === 'approved')`); fireEvent.click; assert `onChangeStatusFilter` called with `'approved'`.
       - "click on All pill invokes onChangeStatusFilter('all')": similar, finding the All pill.
       - "click on Show omitted toggle invokes onToggleShowOmitted": find via `container.querySelector('[role="switch"]')`; fireEvent.click; assert `onToggleShowOmitted` called.
       - "active pill has aria-pressed='true'": render with `statusFilter='approved'`; find the approved pill; assert `getAttribute('aria-pressed') === 'true'`. Find another pill (e.g., 'wip'); assert `aria-pressed === 'false'`.
       - "Show omitted toggle has role=switch and aria-checked reflects prop": render with `showOmitted=true`; find `[role="switch"]`; assert `getAttribute('aria-checked') === 'true'`. Render fresh with `showOmitted=false`; assert `aria-checked === 'false'`.
       - "Container has sticky positioning classes": assert root div has `sticky` and `top-0` in className.

       Landmines (per PATTERNS.md §20 lines 1903-1905):
       - Use `.find((b) => b.textContent === 'omit')` not `getByText('omit')` to avoid theme/icon ambiguities.
       - The Show omitted toggle is `role="switch"` not a checkbox.
  </action>
  <verify>
    <automated>npx vitest run packages/dashboard/src/components/__tests__/ShotGridFilterBar.test.tsx</automated>
    <automated>grep -c "export function ShotGridFilterBar\|export interface ShotGridFilterBarProps" packages/dashboard/src/components/ShotGridFilterBar.tsx | awk '$1 == 2 {print "OK"} $1 != 2 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c 'role="switch"' packages/dashboard/src/components/ShotGridFilterBar.tsx | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "sticky top-0" packages/dashboard/src/components/ShotGridFilterBar.tsx | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "aria-pressed" packages/dashboard/src/components/ShotGridFilterBar.tsx | awk '$1 &gt;= 1 {print "OK"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/components/ShotGridFilterBar.tsx` exists with `ShotGridFilterBarProps` interface and `ShotGridFilterBar` component
    - Component renders 5 pills (All, wip, pending-review, approved, on-hold) when `showOmitted=false`; 6 (+ omit) when `showOmitted=true`
    - Pills are `<button aria-pressed={active}>` (toggle-button semantics, NOT listbox)
    - Show omitted is a `<button role="switch" aria-checked={showOmitted}>` (NOT a checkbox)
    - Container has `sticky top-0 z-10` classes
    - Active pill has `bg-[var(--color-accent)]` AND `text-[var(--color-bg)]`; inactive has `border border-[var(--color-border)]` AND `text-[var(--color-fg-muted)]`
    - Test file has ≥ 8 test cases; `npx vitest run packages/dashboard/src/components/__tests__/ShotGridFilterBar.test.tsx` exits 0
    - All copy constants imported from `lib/copy.ts` (no inline string literals)
  </acceptance_criteria>
  <done>Filter bar is built and tested. Wave 4 ShotGridView renders it sticky-top above the grid and wires the two callbacks to statusFilter + showOmitted signal mutations + persistShotGridUrlState.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-03-T03: Create <SequenceHeader/> component + tests</name>
  <files>packages/dashboard/src/components/SequenceHeader.tsx, packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx</files>
  <read_first>
    - packages/dashboard/src/components/TreeSidebar.tsx lines 280-329 (TreeRow chevron toggle precedent)
    - packages/dashboard/src/components/ShotStatusPill.tsx (after Wave 2 — color token names for mini-pills)
    - packages/dashboard/src/lib/copy.ts (after Wave 1 — confirms SEQUENCE_HEADER_* + AGGREGATE_COUNTS_* constants)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §21 lines 1910-2045 (full body of SequenceHeader)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §22 lines 2048-2063 (test scaffold)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Typography" line 76 (sequence header is 20px/600 Inter Tight)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Spacing Scale" line 56 (chevron size=18)
    - .planning/phases/21-shot-grid-view/21-CONTEXT.md D-14, D-15 (LOCKED — aggregate mini-pills, collapsible chevron)
  </read_first>
  <behavior>
    - Test 1: Renders `<h2>` with sequence name + `style.fontFamily` containing `var(--font-display)`
    - Test 2: Renders chevron button with `aria-expanded={expanded}` prop value
    - Test 3: `expanded=true` → ChevronDown icon rendered; `expanded=false` → ChevronRight icon rendered (assert via class or icon SVG identification)
    - Test 4: Click on chevron button invokes `onToggleExpanded`
    - Test 5: aria-label format: `'Collapse {sequenceName}'` when expanded, `'Expand {sequenceName}'` when not
    - Test 6: Renders mini-pills for non-zero counts in the order wip → pending-review → approved → on-hold → omit
    - Test 7: A status with count=0 has NO mini-pill rendered (zero counts hidden)
    - Test 8: Aggregate counts container has `role="group"` and `aria-label` starting with `'Status counts for '`
  </behavior>
  <action>
    1. Create `packages/dashboard/src/components/SequenceHeader.tsx` (NEW, ~80 LOC):

       Imports:
       - `import { ChevronDown, ChevronRight } from 'lucide-preact';`
       - `import type { ShotStatus } from '../types/shot-grid.js';`
       - `import { SEQUENCE_HEADER_TOGGLE_ARIA_PREFIX_OPEN, SEQUENCE_HEADER_TOGGLE_ARIA_PREFIX_CLOSED, AGGREGATE_COUNTS_REGION_LABEL_PREFIX } from '../lib/copy.js';`

       Export `interface SequenceHeaderProps`:
       - `sequenceName: string`
       - `expanded: boolean`
       - `onToggleExpanded: () => void`
       - `counts: Record<ShotStatus, number>`

       Private constants:
       - `const STATUS_BG: Record<ShotStatus, string> = { 'wip': 'bg-[var(--color-shot-status-wip)] text-[var(--color-bg)]', 'pending-review': 'bg-[var(--color-shot-status-pending-review)] text-[var(--color-bg)]', 'approved': 'bg-[var(--color-shot-status-approved)] text-[var(--color-bg)]', 'on-hold': 'bg-[var(--color-shot-status-on-hold)] text-[var(--color-bg)]', 'omit': 'bg-[var(--color-shot-status-omit)] text-[var(--color-bg)]' };`
       - `const ORDER: ShotStatus[] = ['wip', 'pending-review', 'approved', 'on-hold', 'omit'];`

       Export `function SequenceHeader({ sequenceName, expanded, onToggleExpanded, counts }: SequenceHeaderProps)`:
       - `const Icon = expanded ? ChevronDown : ChevronRight;`
       - `const ariaPrefix = expanded ? SEQUENCE_HEADER_TOGGLE_ARIA_PREFIX_OPEN : SEQUENCE_HEADER_TOGGLE_ARIA_PREFIX_CLOSED;`
       - Return:
         ```
         <header class="flex flex-col gap-2 px-4 py-6">
           <div class="flex items-center gap-2">
             <button
               type="button"
               onClick={onToggleExpanded}
               aria-expanded={expanded}
               aria-label={`${ariaPrefix}${sequenceName}`}
               class="flex items-center justify-center rounded p-1 text-[var(--color-fg-muted)] hover:text-[var(--color-fg)] focus-visible:ring-2 focus-visible:ring-[var(--color-accent)]"
             >
               <Icon size={18} />
             </button>
             <h2
               class="text-xl font-semibold text-[var(--color-fg)]"
               style={{ fontFamily: 'var(--font-display)', lineHeight: 1.2 }}
             >
               {sequenceName}
             </h2>
           </div>
           <div
             role="group"
             aria-label={`${AGGREGATE_COUNTS_REGION_LABEL_PREFIX}${sequenceName}`}
             class="flex items-center gap-2"
           >
             {ORDER.map((status) => {
               const n = counts[status];
               if (n === 0) return null;
               return (
                 <span
                   key={status}
                   class={`inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-xs uppercase tracking-widest ${STATUS_BG[status]}`}
                   data-status={status}
                 >
                   <span class="num">{n}</span>
                   <span>{status}</span>
                 </span>
               );
             })}
           </div>
         </header>
         ```

       JSDoc: reference D-14 + D-15; pure component (no signal reads).

       Landmines (per PATTERNS.md §21 lines 2039-2045):
       - DO NOT subscribe to `aggregateCounts` here — parent reads it and passes the resolved value.
       - DO NOT auto-collapse when counts are empty (D-15: open by default, manual toggle only).
       - Chevron size 18 (one step up from TreeSidebar's 14).
       - `aria-expanded` is the WCAG-blessed attribute (NOT `aria-collapsed`).

    2. Create `packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx` (NEW, ~80 LOC):

       Imports: vitest + render/fireEvent/cleanup + SequenceHeader; `type ShotStatus` from `'../../types/shot-grid.js'`.

       Build `function renderHeader(overrides)` helper with sane defaults: `sequenceName: 'SEQ_010', expanded: true, onToggleExpanded: vi.fn(), counts: { 'wip': 5, 'pending-review': 3, 'approved': 12, 'on-hold': 1, 'omit': 2 }`.

       Tests per <behavior>:
       - "renders sequence name in <h2> with font-display": render; `const h2 = container.querySelector('h2')!;` `expect(h2.textContent).toBe('SEQ_010');` `expect(h2.getAttribute('style') ?? '').toContain('font-display');` (or check `'var(--font-display)'`).
       - "chevron button has aria-expanded reflecting prop": render with `expanded=true`; assert `container.querySelector('button')?.getAttribute('aria-expanded') === 'true'`. Render fresh with `expanded=false`; assert `'false'`.
       - "click on chevron invokes onToggleExpanded": render; fireEvent.click on the chevron button; assert `onToggleExpanded` called.
       - "aria-label reflects expanded state": render with `expanded=true`; assert button.getAttribute('aria-label') === 'Collapse SEQ_010'. Render with `expanded=false`; assert `'Expand SEQ_010'`.
       - "renders mini-pills only for non-zero counts": render with `counts: { 'wip': 0, 'pending-review': 3, 'approved': 0, 'on-hold': 0, 'omit': 0 }`; assert `container.querySelectorAll('[data-status]').length === 1` and the one rendered has `data-status === 'pending-review'`.
       - "renders mini-pills in ORDER": render with all 5 counts non-zero; collect `Array.from(container.querySelectorAll('[data-status]')).map(e => e.getAttribute('data-status'))`; assert order is `['wip', 'pending-review', 'approved', 'on-hold', 'omit']`.
       - "aggregate counts container has role=group with prefix label": render; assert `container.querySelector('[role="group"]')?.getAttribute('aria-label') === 'Status counts for SEQ_010'`.
  </action>
  <verify>
    <automated>npx vitest run packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx</automated>
    <automated>grep -c "export function SequenceHeader\|export interface SequenceHeaderProps" packages/dashboard/src/components/SequenceHeader.tsx | awk '$1 == 2 {print "OK"} $1 != 2 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "ChevronDown\|ChevronRight" packages/dashboard/src/components/SequenceHeader.tsx | awk '$1 &gt;= 2 {print "OK"} $1 &lt; 2 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c 'role="group"' packages/dashboard/src/components/SequenceHeader.tsx | awk '$1 == 1 {print "OK"} $1 != 1 {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/components/SequenceHeader.tsx` exists with `SequenceHeaderProps` and `SequenceHeader` exports
    - Component renders sequence name in `<h2>` with `font-display` inline style
    - Chevron button uses Lucide's `ChevronDown` (expanded) or `ChevronRight` (collapsed) at `size={18}`
    - Aggregate counts container has `role="group"` and `aria-label` derived from `AGGREGATE_COUNTS_REGION_LABEL_PREFIX + sequenceName`
    - Mini-pills are rendered ONLY for non-zero counts, in fixed ORDER (wip → pending-review → approved → on-hold → omit)
    - Test file has ≥ 7 test cases; `npx vitest run packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx` exits 0
    - All copy constants imported from `lib/copy.ts`
  </acceptance_criteria>
  <done>SequenceHeader is built and tested. Wave 4 ShotGridView mounts it ABOVE the filter bar; aggregate counts re-derive on SSE-driven status updates via the parent's computed signal read.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-03-T04: Add grid-icon affordance to TreeSidebar SequenceNode + tests</name>
  <files>packages/dashboard/src/components/TreeSidebar.tsx, packages/dashboard/src/__tests__/TreeSidebar.test.tsx</files>
  <read_first>
    - packages/dashboard/src/components/TreeSidebar.tsx (entire file — current state, ~330 LOC)
    - packages/dashboard/src/__tests__/TreeSidebar.test.tsx (existing test file — confirm tests don't break)
    - packages/dashboard/src/lib/copy.ts (after Wave 1 — TREE_GRID_ICON_ARIA_PREFIX, TREE_GRID_ICON_ACTIVE_ARIA_SUFFIX)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §23 lines 2066-2184 (full modification body; landmines)
    - .planning/phases/21-shot-grid-view/21-PATTERNS.md §24 lines 2187-2197 (test additions)
    - .planning/phases/21-shot-grid-view/21-CONTEXT.md D-01, D-02, D-05 (LOCKED — sequence rows only, click flips activeView, active state via aria-current + accent fill)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Spacing Scale" line 53 (icon button is 24×24 → `h-6 w-6`)
    - .planning/phases/21-shot-grid-view/21-UI-SPEC.md §"Spacing Scale" line 55 (Lucide icon size=16)
  </read_first>
  <behavior>
    - Test 1: SequenceNode with `onOpenGrid` prop renders a `<button>` with `aria-label` starting with `'Open shot grid for '` at the right edge of the sequence row
    - Test 2: Click on the grid icon invokes `onOpenGrid(sequence.id)`
    - Test 3: Click on the grid icon does NOT invoke `onToggleExpand` (stopPropagation works)
    - Test 4: When `currentGridSequenceId === sequence.id`, the icon button has `aria-current="page"` AND class containing `text-[var(--color-accent)]`
    - Test 5: When `currentGridSequenceId !== sequence.id`, the icon button has NO `aria-current` AND class contains `text-[var(--color-fg-muted)]`
    - Test 6: Grid icon button is NOT rendered when `onOpenGrid` is undefined (graceful absence)
    - Test 7: Grid icon button is NOT rendered on workspace/project/shot rows (depth != 2)
  </behavior>
  <action>
    Modify `packages/dashboard/src/components/TreeSidebar.tsx`:

    1. Add `LayoutGrid` to the existing `lucide-preact` import line (which already imports `ChevronDown` / `ChevronRight`). If the import is `import { ChevronDown, ChevronRight } from 'lucide-preact';`, change to `import { ChevronDown, ChevronRight, LayoutGrid } from 'lucide-preact';`.

    2. Add `import { TREE_GRID_ICON_ARIA_PREFIX, TREE_GRID_ICON_ACTIVE_ARIA_SUFFIX } from '../lib/copy.js';`.

    3. Extend the top-level `TreeSidebarProps` interface (or whatever exposes the component's public surface):
       - Add `onOpenGrid?: (sequenceId: string) => void`
       - Add `currentGridSequenceId?: string`
       JSDoc each new prop; reference D-01 (per-sequence grid icon) + D-05 (active state via aria-current).

    4. Thread the two new props from `<TreeSidebar>` → `<WorkspaceNode>` → `<ProjectNode>` → `<SequenceNode>` (in the current prop-drilling chain).

    5. Modify `TreeRow` to accept an optional `trailing?: VNode` slot (mirrors the existing `thumbnail?: VNode` slot pattern at the relevant line). The `trailing` slot renders inside the row's flex container with `class="ml-auto flex-shrink-0"`.

    6. In `SequenceNode`, compute `const isCurrentGrid = currentGridSequenceId === sequence.id;` and pass a `trailing` prop to TreeRow:
       ```
       trailing={
         onOpenGrid ? (
           <button
             type="button"
             onClick={(e) => {
               e.stopPropagation();
               onOpenGrid(sequence.id);
             }}
             aria-label={
               `${TREE_GRID_ICON_ARIA_PREFIX}${sequence.name}` +
               (isCurrentGrid ? TREE_GRID_ICON_ACTIVE_ARIA_SUFFIX : '')
             }
             aria-current={isCurrentGrid ? 'page' : undefined}
             class={`flex h-6 w-6 items-center justify-center rounded transition-colors focus-visible:ring-2 focus-visible:ring-[var(--color-accent)] ${
               isCurrentGrid
                 ? 'text-[var(--color-accent)]'
                 : 'text-[var(--color-fg-muted)] hover:text-[var(--color-fg)]'
             }`}
           >
             <LayoutGrid size={16} />
           </button>
         ) : undefined
       }
       ```

    7. DO NOT add the trailing slot to workspace, project, or shot rows. Only the SequenceNode passes a non-empty trailing (per D-01).

    8. Banner comment above the grid-icon block: `// Phase 21 — D-01: grid-icon affordance (sequence rows only)`.

    9. Landmines (per PATTERNS.md §23 lines 2176-2183):
       - `e.stopPropagation()` is REQUIRED — without it, the row click would also fire (toggling expand).
       - `aria-current="page"` for the WCAG-blessed "you are here" semantic (NOT `aria-selected`).
       - Lucide icon size 16 (one step up from sidebar chevrons' 14).
       - `h-6 w-6` (24×24) for the button hit-target.

    Modify `packages/dashboard/src/__tests__/TreeSidebar.test.tsx`:

    1. Add a new `describe('TreeSidebar — Phase 21 grid-icon affordance (D-01/D-02/D-05)', () => { ... })` block at the end of the existing file (DO NOT modify existing tests).

    2. Build helper fixtures (workspaces with one project with two sequences, each with shots) — match the existing test file's fixture shape.

    3. Tests per <behavior>:
       - "renders grid icon on sequence rows when onOpenGrid provided": render TreeSidebar with `onOpenGrid={vi.fn()}`; find buttons by aria-label pattern (`getAllByLabelText(/^Open shot grid for /)`); assert length matches sequence count.
       - "click on grid icon invokes onOpenGrid(sequence.id), NOT onToggleExpand": render with both callbacks mocked; fireEvent.click on the first grid icon button; assert `onOpenGrid` called with the correct seqId; assert `onToggleExpand` NOT called (or called the SAME number of times as before the click — depending on whether the row click handler was also bound).
       - "active state: aria-current='page' + accent fill when currentGridSequenceId matches": render with `currentGridSequenceId={'<the first seq id>'}`; find the grid icon button for that sequence; assert `getAttribute('aria-current') === 'page'` and `className.includes('text-[var(--color-accent)]')`.
       - "inactive state: no aria-current + muted fill": find a grid icon button for a DIFFERENT sequence; assert `getAttribute('aria-current') === null` and `className.includes('text-[var(--color-fg-muted)]')`.
       - "no grid icon when onOpenGrid is undefined": render without `onOpenGrid` prop; assert `queryAllByLabelText(/^Open shot grid for /).length === 0`.
       - "no grid icon on workspace/project/shot rows": after rendering, count `[aria-label^="Open shot grid for "]` elements; assert count equals sequence count (NOT depth count).

    4. The existing tests in `TreeSidebar.test.tsx` MUST remain green. Run the full file's tests to confirm.
  </action>
  <verify>
    <automated>grep -c "LayoutGrid" packages/dashboard/src/components/TreeSidebar.tsx | awk '$1 &gt;= 2 {print "OK"} $1 &lt; 2 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c "onOpenGrid\|currentGridSequenceId" packages/dashboard/src/components/TreeSidebar.tsx | awk '$1 &gt;= 4 {print "OK"} $1 &lt; 4 {print "FAIL got " $1; exit 1}'</automated>
    <automated>grep -c "stopPropagation" packages/dashboard/src/components/TreeSidebar.tsx | awk '$1 &gt;= 1 {print "OK"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
    <automated>grep -c 'aria-current' packages/dashboard/src/components/TreeSidebar.tsx | awk '$1 &gt;= 1 {print "OK"} $1 &lt; 1 {print "FAIL"; exit 1}'</automated>
    <automated>npx vitest run packages/dashboard/src/__tests__/TreeSidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/components/TreeSidebar.tsx` extends `TreeSidebarProps` with `onOpenGrid?` and `currentGridSequenceId?`
    - `LayoutGrid` icon imported from lucide-preact and rendered inside a `<button>` at the right edge of SequenceNode rows ONLY
    - Click handler uses `e.stopPropagation()` to prevent row-expand from firing
    - Active state (`isCurrentGrid === true`) sets `aria-current="page"` and accent-fill class
    - Pre-existing test cases in `packages/dashboard/src/__tests__/TreeSidebar.test.tsx` remain green
    - New describe block has ≥ 6 test cases covering D-01/D-02/D-05
    - `npx vitest run packages/dashboard/src/__tests__/TreeSidebar.test.tsx` exits 0
    - Workspace/project/shot rows have NO grid icon
  </acceptance_criteria>
  <done>The TreeSidebar grid-icon entry point is wired. Wave 4 App.tsx passes `onOpenGrid={(seqId) => { activeView.value = 'shot-grid'; selectedSequenceForGrid.value = seqId; persistShotGridUrlState(); }}` and `currentGridSequenceId={selectedSequenceForGrid.value ?? undefined}` to TreeSidebar.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete:

```bash
# Per-task targeted tests
npx vitest run packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx
npx vitest run packages/dashboard/src/components/__tests__/ShotGridFilterBar.test.tsx
npx vitest run packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx
npx vitest run packages/dashboard/src/__tests__/TreeSidebar.test.tsx

# Wave 3 scope
npx vitest run packages/dashboard/src/components/__tests__/ShotGridCard.test.tsx packages/dashboard/src/components/__tests__/ShotGridFilterBar.test.tsx packages/dashboard/src/components/__tests__/SequenceHeader.test.tsx packages/dashboard/src/__tests__/TreeSidebar.test.tsx

# Type-check
npx tsc --noEmit -p packages/dashboard/tsconfig.json

# Architecture-purity (must remain unchanged)
npx vitest run src/__tests__/architecture-purity.test.ts

# Tool-budget invariant (must remain at 7)
npx vitest run src/__tests__/tool-budget.test.ts

# Pre-existing TreeSidebar tests must still pass
npx vitest run packages/dashboard/src/__tests__/TreeSidebar.test.tsx
```
</verification>

<success_criteria>
- 4 new component files: `ShotGridCard.tsx`, `ShotGridFilterBar.tsx`, `SequenceHeader.tsx`, plus the TreeSidebar modification
- 4 new/extended test files; ALL plan-scope tests pass
- Component layer is fully decoupled from state (pure components, callbacks-out)
- D-01/D-02/D-05 LOCKED in TreeSidebar: grid icon only on sequence rows; stopPropagation; aria-current="page" on active
- Architecture-purity + tool-budget tests remain green
- Pre-existing TreeSidebar tests + StatusPill tests still pass (no regressions)
- All tasks committed atomically (one commit per task)
</success_criteria>

<output>
After completion, create `.planning/phases/21-shot-grid-view/21-03-SUMMARY.md` documenting:
- Component prop shapes (final API surface for each of the 4 components)
- How `<ShotGridCard/>` handles the 3 states (with-version / without-version / omit dimming)
- The TreeSidebar grid-icon affordance behavior (stopPropagation, active-state visualization)
- Confirmation that pre-existing TreeSidebar tests still pass
- Any deviations from the planned implementation (with rationale)
</output>
