---
phase: 17-visual-thumbnails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/engine/errors.ts
  - src/engine/thumbnails/index.ts
  - src/engine/thumbnails/format-router.ts
  - src/engine/thumbnails/cache.ts
  - src/engine/thumbnails/image-thumbnail.ts
  - src/engine/thumbnails/__tests__/format-router.test.ts
  - src/engine/thumbnails/__tests__/cache.test.ts
  - src/engine/thumbnails/__tests__/image-thumbnail.test.ts
  - src/__tests__/architecture-purity.test.ts
  - src/__tests__/c2pa-key-leak-negative.test.ts
autonomous: true
requirements: [VIS-01, VIS-02]
must_haves:
  truths:
    - "sharp@^0.34.5 is installed and pinned at the root package.json"
    - "src/engine/thumbnails/format-router.ts exports a pure routeFormat() returning a discriminated union {mode:'image',mimeType} | {mode:'video',mimeType} | {mode:'unsupported',reason}"
    - "src/engine/thumbnails/image-thumbnail.ts is the SOLE importer of the sharp package across src/ (D-23)"
    - "image thumbnail derivation produces a 640px-max-edge WebP at quality 80 from PNG/JPEG/WebP/TIFF source, encoded at SOURCE aspect (D-04 — fit:'inside', no padding)"
    - "image thumbnail write is atomic via nanoid(8).partial temp + rename (D-22); no .partial files survive a successful write"
    - "src/engine/thumbnails/cache.ts exports cachePathFor / sentinelPathFor / partialPathFor / writeAtomic / computeETag / isCacheFresh / writeFailedSentinel / invalidateCache pure helpers"
    - "computeETag prefers sha256 strong validator from outputs_json[0].sha256 when present, falls back to mtime: short-hash (D-06)"
    - ".thumb.failed sentinel suppresses retries until source mtime advances (D-07)"
    - "sharp.concurrency(2) and sharp.cache(false) are set ONCE on first successful sharp module load (PITFALL B + F)"
    - "sharp load failure is monotonic: cached failure short-circuits subsequent calls returning null without re-attempting import (D-26)"
    - "TypedError code 'THUMBNAIL_FAILED' exists in src/engine/errors.ts ErrorCode union"
    - "architecture-purity.test.ts asserts sharp imports allowed-set = ['src/engine/thumbnails/image-thumbnail.ts'] via sorted-array deepEqual subset+SET-equality (D-25)"
    - "architecture-purity.test.ts asserts src/engine/thumbnails/ has zero imports from @modelcontextprotocol/sdk, better-sqlite3, drizzle-orm, hono, @hono/node-server"
    - "c2pa-key-leak-negative.test.ts (or equivalent multi-encoding leak scan) extends to scan <outputsDir>/<versionId>/<filename>.thumb.webp + .thumb.failed paths in UTF-8 + UTF-16LE + UTF-16BE + base64 (PITFALL G)"
  artifacts:
    - path: "package.json"
      provides: "dependencies.sharp pinned at ^0.34.5"
      contains: '"sharp"'
    - path: "src/engine/errors.ts"
      provides: "THUMBNAIL_FAILED added to ErrorCode union"
      contains: "'THUMBNAIL_FAILED'"
    - path: "src/engine/thumbnails/format-router.ts"
      provides: "pure routeFormat()"
      exports: ["routeFormat", "FormatRoute"]
      min_lines: 30
    - path: "src/engine/thumbnails/cache.ts"
      provides: "FS path helpers + atomic write + ETag + sentinel + invalidate"
      exports: ["cachePathFor", "sentinelPathFor", "writeAtomic", "computeETag", "isCacheFresh", "writeFailedSentinel", "invalidateCache"]
      min_lines: 80
    - path: "src/engine/thumbnails/image-thumbnail.ts"
      provides: "sole sharp importer; generateImageThumbnail + getImageBrightness"
      exports: ["generateImageThumbnail", "getImageBrightness", "getSharpForVideoReencode", "__resetSharpStateForTests"]
      min_lines: 100
    - path: "src/engine/thumbnails/index.ts"
      provides: "barrel export"
      exports: ["routeFormat", "generateImageThumbnail", "cachePathFor", "computeETag"]
    - path: "src/engine/thumbnails/__tests__/format-router.test.ts"
      provides: "pure router unit coverage"
      min_lines: 40
    - path: "src/engine/thumbnails/__tests__/cache.test.ts"
      provides: "atomic write + ETag + sentinel + invalidate coverage"
      min_lines: 80
    - path: "src/engine/thumbnails/__tests__/image-thumbnail.test.ts"
      provides: "sharp-binding coverage + concurrency cap + monotonic fail"
      min_lines: 80
  key_links:
    - from: "src/engine/thumbnails/image-thumbnail.ts"
      to: "node_modules/sharp"
      via: "lazy await import('sharp') inside getSharp()"
      pattern: "import\\([\\\"']sharp[\\\"']\\)"
    - from: "src/engine/thumbnails/image-thumbnail.ts"
      to: "src/engine/thumbnails/cache.ts"
      via: "writeAtomic + partialPathFor"
      pattern: "from '\\./cache"
    - from: "src/__tests__/architecture-purity.test.ts"
      to: "src/engine/thumbnails/image-thumbnail.ts"
      via: "sorted-array deepEqual on sharp grep result"
      pattern: "allowedSharpImporters"
    - from: "src/__tests__/c2pa-key-leak-negative.test.ts"
      to: "<outputsDir>/<versionId>/<filename>.thumb.webp"
      via: "scanned via assertNotInBuffer in 4 encodings"
      pattern: "\\.thumb\\.webp"
---

<objective>
Establish the engine-side thumbnail module foundation: pure format router, sole-importer sharp wrapper for image derivation, FS cache helpers (path derivation, atomic write, ETag, sentinel, invalidate), and the architecture-purity allowed-set extension that locks sharp into a single importer in the SAME plan that introduces the import. Extend the existing multi-encoding leak-scan to cover the new `.thumb.webp` and `.thumb.failed` cache paths.

Purpose: every later plan in this phase reads from this module surface. The architecture-purity test extension MUST land here (D-25 — same plan that introduces the import; no orphaned imports). The leak-scan extension MUST land here so the cache surface is covered the moment it can be written. The format-router is pure and shared by both image and video paths (Plan 02 imports it).

Output: 9 new files (4 production + 3 unit tests + sharp dependency in package.json + thumbnails barrel + arch-purity additions + leak-scan extension). New ErrorCode 'THUMBNAIL_FAILED' added to src/engine/errors.ts. The Engine facade integration (generateThumbnail / invalidateThumbnail / thumbnailMutex) is deliberately deferred to Plan 03 — this plan ships the building blocks plus the architectural-invariant tests that pin them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-visual-thumbnails/17-CONTEXT.md
@.planning/phases/17-visual-thumbnails/17-RESEARCH.md
@.planning/phases/17-visual-thumbnails/17-PATTERNS.md
@.planning/phases/17-visual-thumbnails/17-VALIDATION.md

# Codebase analogs MUST be read once before code is written:
@src/engine/c2pa/index.ts
@src/engine/c2pa/format-router.ts
@src/engine/c2pa/signer.ts
@src/engine/output-downloader.ts
@src/engine/errors.ts
@src/__tests__/architecture-purity.test.ts
@src/__tests__/c2pa-key-leak-negative.test.ts
@src/engine/c2pa/__tests__/format-router.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/engine/c2pa/format-router.ts (analog shape — Phase 17 mirrors):
```typescript
// Discriminated-union return + case-insensitive ext lookup + const TABLE
export type FormatRoute =
  | { mode: 'image'; mimeType: string }
  | { mode: 'video'; mimeType: string }
  | { mode: 'unsupported'; reason: 'unknown-extension' | 'native-handler-missing'; mimeType?: string };
export function routeFormat(filename: string): FormatRoute;
```

From src/engine/c2pa/signer.ts:39-71 (analog — lazy native-binding pattern):
```typescript
type C2paNodeModule = typeof import('c2pa-node');
let c2paNodeModule: C2paNodeModule | null = null;
let c2paNodeLoadError: Error | null = null;
async function ensureC2paNode(): Promise<C2paNodeModule> { /* lazy import, monotonic fail throws TypedError */ }
export function __resetC2paNodeStateForTests(): void { c2paNodeModule = null; c2paNodeLoadError = null; }
```

From src/engine/errors.ts:
```typescript
export type ErrorCode = 'WORKSPACE_NOT_FOUND' | ... | 'INTERNAL_ERROR';
export class TypedError extends Error {
  constructor(public code: ErrorCode, message: string, public hint?: string) { super(message); this.name = 'TypedError'; }
}
```

From src/engine/output-downloader.ts:184-198 (analog — atomic temp+rename via nanoid(8)):
```typescript
const partialPath = `${destPath}.c2pa-signed.${nanoid(8)}.partial`;
await writeFile(partialPath, result.signed, { mode: 0o644 });
await renameWithFallback(partialPath, destPath);
```

From src/__tests__/architecture-purity.test.ts:166-231 (allowed-set assertion shape):
```typescript
const allowedC2paNodeImporters = new Set<string>([ /* file paths */ ]);
const out = execFileSync('grep', ['-rlE',
  "from[[:space:]]*['\\\"]c2pa-node|import[[:space:]]*\\\\([[:space:]]*['\\\"]c2pa-node",
  'src/'], { encoding: 'utf8' });
const nonTestFiles = files.filter(f => !f.includes('__tests__/'));
const violations = nonTestFiles.filter(f => !allowedC2paNodeImporters.has(f));
expect(violations).toEqual([]);
expect([...nonTestFiles].sort()).toEqual(expectedActualImporters);
```

From src/__tests__/c2pa-key-leak-negative.test.ts:91-107 (multi-encoding scan helper):
```typescript
function assertNotInBuffer(buf: Buffer, secret: string, label: string): void {
  const fragments = [
    secret,
    Buffer.from(secret, 'utf16le').toString('binary'),
    Buffer.from(secret, 'utf16le').reverse().toString('binary'),
    Buffer.from(secret).toString('base64'),
  ];
  const haystack = buf.toString('binary');
  for (const frag of fragments) { expect(haystack.includes(frag)).toBe(false); }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sharp dep, THUMBNAIL_FAILED ErrorCode, format-router + cache modules + tests</name>
  <files>package.json, src/engine/errors.ts, src/engine/thumbnails/format-router.ts, src/engine/thumbnails/cache.ts, src/engine/thumbnails/index.ts, src/engine/thumbnails/__tests__/format-router.test.ts, src/engine/thumbnails/__tests__/cache.test.ts</files>
  <read_first>
    - .planning/phases/17-visual-thumbnails/17-PATTERNS.md §"src/engine/thumbnails/format-router.ts" + §"src/engine/thumbnails/cache.ts"
    - .planning/phases/17-visual-thumbnails/17-RESEARCH.md §"Pattern 3: Atomic write" + §"Pattern 4: ETag" + §"Code Examples — Format router (pure)"
    - src/engine/c2pa/format-router.ts (full file — 100% the analog shape)
    - src/engine/c2pa/__tests__/format-router.test.ts (full file — test shape mirror)
    - src/engine/c2pa/index.ts (barrel shape)
    - src/engine/output-downloader.ts:184-230 (atomic write + EXDEV note — confirm thumbnails do NOT need EXDEV fallback because partial+final are co-located under outputsDir/<versionId>/)
    - src/engine/errors.ts (full file — confirm ErrorCode union shape; the 'INTERNAL_ERROR' line is the fallback that the new 'THUMBNAIL_FAILED' lands AFTER)
    - src/engine/c2pa/redaction.ts:740-749 (atomic rename block — same pattern reused for cache.writeAtomic)
  </read_first>
  <behavior>
    - Test (format-router unit): routeFormat('output.png') === {mode:'image', mimeType:'image/png'}
    - Test (format-router unit): routeFormat('OUTPUT.PNG') === {mode:'image', mimeType:'image/png'} (case-insensitive)
    - Test (format-router unit): routeFormat('render.mp4') === {mode:'video', mimeType:'video/mp4'}
    - Test (format-router unit): routeFormat('file.exr') === {mode:'unsupported', reason:'unknown-extension'}
    - Test (format-router unit): routeFormat('noextension') === {mode:'unsupported', reason:'unknown-extension'}
    - Test (format-router unit): routeFormat covers .png .jpg .jpeg .webp .tif .tiff (image) and .mp4 (video) at minimum
    - Test (cache unit): cachePathFor(root, vid, 'a.png') ends with '/<vid>/a.png.thumb.webp'
    - Test (cache unit): sentinelPathFor(root, vid, 'a.png') ends with '/<vid>/a.png.thumb.failed'
    - Test (cache unit): partialPathFor(root, vid, 'a.png') matches /\.thumb\.webp\.[a-zA-Z0-9_-]{8}\.partial$/ and produces a different path on each call (nanoid(8))
    - Test (cache unit): writeAtomic(finalPath, bytes) leaves no .partial file behind on success and final file content === bytes
    - Test (cache unit): writeAtomic cleans up partial on toFile/throw (test by passing a directory path that errors on rename)
    - Test (cache unit): computeETag(srcPath, sha256) → '"sha256:<sha256>"' (quoted, prefix preserved) when sha256 provided
    - Test (cache unit): computeETag(srcPath) → '"mtime:<16-hex>"' when no sha256; same call across two reads with unchanged mtime returns same etag; ETag changes after touching srcPath with utimes() to advance mtime by ≥1 second
    - Test (cache unit): isCacheFresh returns false when source mtime > cache mtime (D-07 retry-on-source-change)
    - Test (cache unit): isCacheFresh returns true when .thumb.failed mtime ≥ source mtime (don't retry until source advances)
    - Test (cache unit): writeFailedSentinel writes a zero-byte file at sentinelPath (mtime semantics carry the "when failed" marker)
    - Test (cache unit): invalidateCache(root, vid, filename) unlinks both .thumb.webp AND .thumb.failed; idempotent (second call after first does NOT throw — ENOENT swallowed)
  </behavior>
  <action>
**Step 1 — package.json:** Add `"sharp": "^0.34.5"` to the root package.json's `"dependencies"` (alphabetical position; preserve existing field formatting). Run `npm install sharp@^0.34.5` to populate node_modules and update package-lock.json. Verify the install lands the expected platform binary via `ls node_modules/@img/ | head` (should show sharp-darwin-arm64 or platform analogue).

**Step 2 — src/engine/errors.ts:** Add `'THUMBNAIL_FAILED'` to the ErrorCode union literal. Land it AFTER the Phase 16 redaction codes block (alongside REDACT_*) and BEFORE `'INTERNAL_ERROR'` so the union list reads chronologically by phase. Add an inline `// Phase 17 — visual thumbnails (VIS-01..06)` section header comment for parity with existing Phase 14/16 markers.

**Step 3 — src/engine/thumbnails/format-router.ts:** Create as a pure function module mirroring src/engine/c2pa/format-router.ts shape. Header comment declares architecture-purity invariants explicitly: "zero MCP / DB / ORM / HTTP / sharp / ffmpeg imports — pure-function module." Export type `FormatRoute = {mode:'image';mimeType:string} | {mode:'video';mimeType:string} | {mode:'unsupported';reason:'unknown-extension'|'native-handler-missing';mimeType?:string}`. Export `routeFormat(filename: string): FormatRoute`. Use `lastIndexOf('.')` + `.toLowerCase()` for case-insensitive lookup. Two const TABLE objects: IMAGE_TABLE = {.png→'image/png', .jpg→'image/jpeg', .jpeg→'image/jpeg', .webp→'image/webp', .tif→'image/tiff', .tiff→'image/tiff'}; VIDEO_TABLE = {.mp4→'video/mp4'}. Return `{mode:'unsupported', reason:'unknown-extension'}` for unrouted extensions. (No `.exr/.psd/.heic` handler-missing branch in v1.2 — they reduce to unknown-extension uniformly per the v1.1 c2pa format-router pattern; PITFALL D mitigation lives in the engine facade in Plan 03 via the same `unsupported` path.)

**Step 4 — src/engine/thumbnails/cache.ts:** Create pure-ish FS-helper module. Static imports only (node:fs/promises, node:crypto, nanoid — no native bindings). Header comment declares architecture-purity. Exports:
- `cachePathFor(outputRoot: string, versionId: string, filename: string): string` → `path.join(outputRoot, versionId, `${filename}.thumb.webp`)`
- `sentinelPathFor(outputRoot, versionId, filename): string` → `path.join(outputRoot, versionId, `${filename}.thumb.failed`)`
- `partialPathFor(outputRoot, versionId, filename): string` → `${cachePathFor(...)}.${nanoid(8)}.partial`
- `async writeAtomic(finalPath: string, write: (tempPath: string) => Promise<void>): Promise<void>` — generates a partial path via `${finalPath}.${nanoid(8)}.partial`, invokes the writer callback against the temp path, then `rename(temp, final)`; on any error in writer or rename, `unlink(temp).catch(() => {})` and rethrow. (Callback shape supports both sharp's `.toFile(tempPath)` and Node's `writeFile(tempPath, bytes)`.)
- `async computeETag(sourcePath: string, sha256?: string | null): Promise<string>` — when sha256 provided, return `"sha256:${sha256}"` quoted. Else `stat(sourcePath)` and return `"mtime:${createHash('sha256').update(String(st.mtimeMs)).digest('hex').slice(0,16)}"` quoted.
- `async isCacheFresh(cachePath: string, sentinelPath: string, sourcePath: string): Promise<{fresh: boolean; via: 'cache'|'sentinel'|'miss'}>` — stat source mtime, then stat cache (if exists, fresh when cache.mtimeMs ≥ source.mtimeMs → 'cache'), then stat sentinel (if exists, fresh when sentinel.mtimeMs ≥ source.mtimeMs → 'sentinel'). On both ENOENT for cache+sentinel → fresh:false, via:'miss'.
- `async writeFailedSentinel(sentinelPath: string): Promise<void>` — `writeFile(sentinelPath, '')` (zero-byte). Comment-pin: NEVER write the source path or filename or any other identifier into the sentinel content (Pitfall G hygiene).
- `async invalidateCache(outputRoot: string, versionId: string, filename: string): Promise<void>` — `unlink` both cachePathFor and sentinelPathFor, each wrapped in `.catch(() => {})` (idempotent on ENOENT).

EXDEV note in the file header comment: "Cache writes live under the same outputRoot/versionId/ as the source. Partial and final paths are co-located → EXDEV is structurally impossible. The output-downloader.ts renameWithFallback exists to bridge outputsDir/.tmp-c2pa/ → outputsDir/<vid>/; thumbnails never cross that boundary, so plain rename() is correct here. Do NOT add a copyFile fallback — it would be dead code (RESEARCH.md Pattern 3 EXDEV note)."

**Step 5 — src/engine/thumbnails/index.ts:** Barrel re-exports only — no logic, no native imports. Re-export `routeFormat` + `FormatRoute` from `./format-router.js`; re-export `cachePathFor`, `sentinelPathFor`, `partialPathFor`, `writeAtomic`, `computeETag`, `isCacheFresh`, `writeFailedSentinel`, `invalidateCache` from `./cache.js`. Header comment mirrors src/engine/c2pa/index.ts: "Architecture-purity: zero non-thumbnails imports. The sharp + @ffmpeg-installer/ffmpeg imports are confined to ./image-thumbnail.ts and ./video-thumbnail.ts; everything in this barrel is re-export only."

**Step 6 — src/engine/thumbnails/__tests__/format-router.test.ts:** vitest describe blocks mirroring src/engine/c2pa/__tests__/format-router.test.ts. Numbered test names ("Test 1: ..." style). Cover all behavior cases listed above. One `expect().toEqual()` per test against the full discriminated-union object.

**Step 7 — src/engine/thumbnails/__tests__/cache.test.ts:** vitest. Use `mkdtemp(join(tmpdir(), 'phase17-cache-'))` per test in `beforeEach`, cleanup via `rm(root, {recursive:true,force:true})` in `afterEach`. Cover all behavior cases listed above. For mtime-advance tests, use `import { utimes } from 'node:fs/promises'` to set source mtime to `Date.now()/1000 + 5` (5 seconds later) deterministically rather than sleeping.

Use TypeScript ESM .js extensions in import specifiers (matches every existing project file). All new code is zero-MCP / zero-SQLite-driver / zero-ORM (the directory-level guards in Task 3 will lock this).
  </action>
  <verify>
    <automated>npx vitest run --reporter=basic --no-coverage src/engine/thumbnails/__tests__/format-router.test.ts src/engine/thumbnails/__tests__/cache.test.ts && npx tsc --noEmit && grep -E "from[[:space:]]*['\"](sharp|@ffmpeg-installer|@modelcontextprotocol|better-sqlite3|drizzle-orm|hono)" src/engine/thumbnails/format-router.ts src/engine/thumbnails/cache.ts src/engine/thumbnails/index.ts && exit 1 || exit 0</automated>
  </verify>
  <done>format-router.ts + cache.ts + index.ts compile clean, all unit tests pass, format-router and cache modules contain ZERO sharp/ffmpeg/MCP/SQLite/ORM/Hono imports (pure modules), package.json lists sharp ^0.34.5, src/engine/errors.ts ErrorCode union includes 'THUMBNAIL_FAILED'.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/engine/thumbnails/image-thumbnail.ts (sole sharp importer)</name>
  <files>src/engine/thumbnails/image-thumbnail.ts, src/engine/thumbnails/__tests__/image-thumbnail.test.ts, src/engine/thumbnails/index.ts</files>
  <read_first>
    - .planning/phases/17-visual-thumbnails/17-PATTERNS.md §"src/engine/thumbnails/image-thumbnail.ts (sole sharp importer)"
    - .planning/phases/17-visual-thumbnails/17-RESEARCH.md §"Pattern 1: Lazy native-binding import" + §"Code Examples — Image thumbnail derivation (sharp)" + §"Pitfall F: Sharp instance memory pressure"
    - src/engine/c2pa/signer.ts:39-100 (lazy-binding load + monotonic fail + __resetC2paNodeStateForTests test hook)
    - src/engine/c2pa/__tests__/signer.test.ts (full file — concrete vitest shape with lazy-binding mock)
    - src/engine/output-downloader.ts:184-198 (atomic temp+rename via nanoid(8))
  </read_first>
  <behavior>
    - Test 1 (image, IF native binding present): generateImageThumbnail(srcPng, destPath) writes a .thumb.webp; the file is non-empty; sharp.metadata(destPath) reports format='webp', width≤640, height≤360 (source aspect preserved → fit:'inside' may produce 640×N or M×360). Use a tiny (e.g., 4×4 or via in-memory generated) PNG buffer.
    - Test 2 (atomic-write invariant): readdir on the parent directory after a successful generateImageThumbnail returns NO entries matching /\.partial$/. (No .partial file leaks on success.)
    - Test 3 (atomic-write cleanup on failure): mock sharp's .toFile() to reject; catch the rejection in the test; readdir matches NO .partial entries (cleanup ran in catch).
    - Test 4 (sharp tuning is set ONCE on first load): spy on the sharp module; first call to getSharp triggers .concurrency(2) + .cache(false); second call to getSharp does NOT call them again (cached instance returned).
    - Test 5 (monotonic fail): __resetSharpStateForTests + force the dynamic-import to throw (vi.mock 'sharp' to reject); first call returns null; second call ALSO returns null and does NOT re-attempt the import (spy on import asserts call count = 1).
    - Test 6 (brightness): getImageBrightness(srcPng) for a known-bright PNG > 16 (the BT.601 luma threshold from D-29); for a known-black PNG < 16. Use sharp to generate fixtures in-test.
    - Test 7 (getSharpForVideoReencode): exports a function that returns the same cached sharp instance (or null on monotonic fail) — used by Plan 02 video-thumbnail to avoid duplicating the import.
    - Test 8 (D-23 architecture-purity): grep -E "from[[:space:]]*['\"]sharp" src/engine/thumbnails/image-thumbnail.ts → exactly ONE match (the lazy import line itself); grep on src/engine/thumbnails/cache.ts + format-router.ts + index.ts → zero matches.
  </behavior>
  <action>
**Step 1 — Write src/engine/thumbnails/__tests__/image-thumbnail.test.ts FIRST (RED):** vitest describe('Plan 17-01 Task 2 — image thumbnail generation'). beforeEach: mkdtemp + __resetSharpStateForTests; afterEach: rm tempdir + __resetSharpStateForTests. Use `await sharp(...).toBuffer()` inside the test (test code MAY import sharp directly — only src/ production code is allowed-set-restricted) to generate fixtures: a 32×32 RGB PNG of grey (luma~128), a 32×32 black PNG (luma~0), a 32×32 white PNG (luma~255), a 800×800 square PNG (so .resize(640,360,fit:'inside') produces 360×360, NOT padded). Mock the sharp dynamic import via vi.doMock for the monotonic-fail test (Test 5): `vi.doMock('sharp', () => { throw new Error('synthetic platform mismatch'); })` then call __resetSharpStateForTests + assert generateImageThumbnail returns null (or throws sharp_load_failed per implementation). Assert atomic-write invariants via `readdir(parentOf(destPath))` → no entries matching `*.partial`. Run: tests fail (no implementation yet).

**Step 2 — Write src/engine/thumbnails/image-thumbnail.ts (GREEN):** Sole importer of `sharp` across src/. Header comment declares D-23 architecture-purity invariant verbatim: "Sole importer of sharp across src/. Other modules MUST call into this file's exports rather than import sharp directly. Allowed-set assertion at src/__tests__/architecture-purity.test.ts." Module-scoped state:
```typescript
let cachedSharp: typeof import('sharp').default | null = null;
let cachedSharpFailed: { reason: string; loggedOnce: boolean } | null = null;
```
Lazy-load helper `async function getSharp(): Promise<typeof import('sharp').default | null>`:
- if cachedSharp → return it
- if cachedSharpFailed → return null (monotonic fail; do NOT retry)
- try `const mod = await import('sharp')` → cachedSharp = mod.default
- on first successful load ONLY: `cachedSharp.concurrency(2)` (PITFALL B) + `cachedSharp.cache(false)` (PITFALL F)
- catch err → cachedSharpFailed = {reason: String(err), loggedOnce: false}; if (!loggedOnce) console.warn(`vfx-familiar: sharp load failed — thumbnails disabled. ${reason}`); loggedOnce = true; return null

Public exports:
- `export async function generateImageThumbnail(sourcePath: string, destPath: string): Promise<void>` — getSharp() (throw TypedError 'THUMBNAIL_FAILED' with reason='native_binding_unavailable' on null, hint mentioning sharp prebuilds), then `await mkdir(dirname(destPath), {recursive:true})`. Use cache.writeAtomic(destPath, async (tempPath) => { await sharp(sourcePath).resize(640, 360, {fit:'inside', withoutEnlargement:true}).webp({quality:80}).toFile(tempPath); }). Catch sharp errors and rethrow as TypedError 'THUMBNAIL_FAILED' with reason='sharp_failed'.
- `export async function getImageBrightness(imagePath: string): Promise<number>` — getSharp + .stats(); compute BT.601 luma = 0.299*r.mean + 0.587*g.mean + 0.114*b.mean. For grayscale (channels.length<3), return channels[0].mean ?? 0. Throw TypedError 'THUMBNAIL_FAILED' reason='sharp_failed' on stats failure.
- `export async function getSharpForVideoReencode(): Promise<typeof import('sharp').default | null>` — pure delegate to getSharp(); used by Plan 02 video-thumbnail.ts to re-encode the ffmpeg-extracted PNG to WebP without duplicating the lazy import (preserves D-23 sole-importer invariant — video-thumbnail.ts calls into this helper, NEVER imports sharp directly).
- `export function __resetSharpStateForTests(): void` — sets cachedSharp = null and cachedSharpFailed = null (mirrors __resetC2paNodeStateForTests at signer.ts:94-97). NEVER called from production.

Run tests: GREEN.

**Step 3 — REFACTOR (if needed):** Extract any duplicated atomic-write code to call cache.writeAtomic from Task 1. Confirm tests still GREEN.

**Step 4 — Update src/engine/thumbnails/index.ts:** Re-export `generateImageThumbnail`, `getImageBrightness`, `getSharpForVideoReencode`, `__resetSharpStateForTests` from `./image-thumbnail.js`. Add header comment line: "image-thumbnail.ts is the sole sharp importer per D-23."

**Step 5 — Verify D-23:** Run `grep -rlE "from[[:space:]]*['\"]sharp|import[[:space:]]*\\([[:space:]]*['\"]sharp" src/ | grep -v __tests__` → MUST return exactly `src/engine/thumbnails/image-thumbnail.ts` (one line). Any other line is a violation; rewrite to delegate through getSharpForVideoReencode.
  </action>
  <verify>
    <automated>npx vitest run --reporter=basic --no-coverage src/engine/thumbnails/__tests__/image-thumbnail.test.ts && npx tsc --noEmit && test "$(grep -rlE \"from[[:space:]]*['\\\"]sharp|import[[:space:]]*\\([[:space:]]*['\\\"]sharp\" src/ | grep -v __tests__ | wc -l | tr -d ' ')" = "1"</automated>
  </verify>
  <done>image-thumbnail.ts is the sole sharp importer in src/ (verified by grep), all tests pass, atomic write leaves no .partial files on success, monotonic fail confirmed by spy. Test 8's grep returns exactly one file.</done>
</task>

<task type="auto">
  <name>Task 3: Architecture-purity allowed-set extension (sharp + thumbnails directory guards) + multi-encoding leak-scan extension</name>
  <files>src/__tests__/architecture-purity.test.ts, src/__tests__/c2pa-key-leak-negative.test.ts</files>
  <read_first>
    - .planning/phases/17-visual-thumbnails/17-PATTERNS.md §"src/__tests__/architecture-purity.test.ts (modify — add sharp + ffmpeg blocks)"
    - .planning/phases/17-visual-thumbnails/17-RESEARCH.md §"Pattern 8: Architecture-purity allowed-set extension" + §"Pitfall G: Multi-encoding leak scan"
    - src/__tests__/architecture-purity.test.ts:160-235 (full c2pa-node block — exact shape to mirror)
    - src/__tests__/architecture-purity.test.ts:130-165 (directory-level guards for src/engine/c2pa/ — analog for src/engine/thumbnails/)
    - src/__tests__/c2pa-key-leak-negative.test.ts:91-107 (assertNotInBuffer helper — multi-encoding scan primitive)
    - src/__tests__/c2pa-key-leak-negative.test.ts (full file — confirm structure for adding new test cases that scan the thumb cache)
  </read_first>
  <behavior>
    - Test (arch-purity, sharp): grep over src/ for sharp imports (regex matches both static `from 'sharp'` AND dynamic `import('sharp')`); exclude __tests__/; sorted-array deepEqual the result against ['src/engine/thumbnails/image-thumbnail.ts'] (subset check + SET-equality, two layers).
    - Test (arch-purity, thumbnails dir): src/engine/thumbnails/ has zero imports from @modelcontextprotocol/sdk, better-sqlite3, drizzle-orm, hono, @hono/node-server (directory-level grep guards mirroring the existing src/engine/c2pa/ block at architecture-purity.test.ts:130-165).
    - Test (leak-scan): when a thumb is generated for a version whose source asset embedded a sentinel string, the cached <filename>.thumb.webp does NOT contain the sentinel in any of {UTF-8, UTF-16LE, UTF-16BE, base64}. (Sharp re-encodes pixels — a literal text watermark may survive; the test fixture uses a non-text sentinel that must NOT survive WebP re-encoding. The test's value is structural: the .thumb.webp byte surface IS scanned by the same multi-encoding helper that scans output bytes, so a future regression that bypasses the scan is caught.)
    - Test (leak-scan): the .thumb.failed sentinel file content is empty (zero bytes), confirming the cache module never writes the source filename or any identifier into the sentinel (Pitfall G hygiene).
  </behavior>
  <action>
**Step 1 — Add sharp allowed-set assertion** in src/__tests__/architecture-purity.test.ts. Insert a new `it('sharp imports are centralized in src/engine/thumbnails/image-thumbnail.ts (D-23)', () => {...})` block near the c2pa-node assertion at line 166-231. Mirror the c2pa-node block VERBATIM in shape (subset check + SET-equality two-layer assertion). The grep regex pattern must cover both static `from 'sharp'` and dynamic `import('sharp')` — copy the c2pa-node regex byte-for-byte and substitute the package name. The allowed-set is a single entry: `['src/engine/thumbnails/image-thumbnail.ts']`. Add a header comment block above the new it() identical in structure to the c2pa-node block's comment (D-23 reference, two-layer assertion rationale).

**Step 2 — Add directory-level guards for src/engine/thumbnails/** following the existing src/engine/c2pa/ block at lines 130-165. Add five new `it('src/engine/thumbnails/ has zero imports from <package>', () => { expect(grepCount('<package>', 'src/engine/thumbnails/')).toBe(0); })` assertions for: `@modelcontextprotocol/sdk`, `better-sqlite3`, `drizzle-orm`, `hono`, `@hono/node-server`. Use the existing `grepCount(pattern, dir)` helper if it's defined in the same file; otherwise mirror the inline `execFileSync('grep', ['-rlE', ...])` shape from the c2pa block.

**Step 3 — Plan 02 placeholder for ffmpeg:** This task does NOT add the @ffmpeg-installer/ffmpeg allowed-set block — Plan 02 introduces video-thumbnail.ts AND the ffmpeg arch-purity assertion in the SAME plan (D-25 SAME-plan rule). Do NOT pre-empt Plan 02. Add an inline `// Phase 17 Plan 02 — @ffmpeg-installer/ffmpeg allowed-set lands when video-thumbnail.ts is introduced (D-25)` comment as a sentinel marker so a code-grep audit confirms the gap is intentional.

**Step 4 — Extend multi-encoding leak-scan to thumb cache** in src/__tests__/c2pa-key-leak-negative.test.ts. Locate the existing test that scans the source output file (the GET /api/versions/:id/output body bytes) for the secret sentinel via assertNotInBuffer. Add a new `it('Test N — Phase 17 PITFALL G: thumb cache + sentinel paths in 4 encodings', async () => { ... })` test at the end of the appropriate describe block. The test:
  (a) sets up a Phase 14-shaped signed version with a known sentinel string (use the same harness as the existing scan tests — REUSE setupKeyLeakCtx or its analog; do NOT duplicate scaffolding)
  (b) calls a stub or in-process call that writes a .thumb.webp at the cache path (use the helpers from Plan 17-01 Task 1: `import { generateImageThumbnail } from 'src/engine/thumbnails/image-thumbnail.js'` + cachePathFor — both available because this test runs after Tasks 1-2)
  (c) reads the .thumb.webp bytes via `await readFile(cachePath)` and runs assertNotInBuffer(buf, sentinel, 'thumb.webp')
  (d) calls Plan 17-01's writeFailedSentinel against a fresh sentinel path and runs assertNotInBuffer(buf, sentinel, 'thumb.failed') AND asserts the sentinel file size is 0 bytes (no identifier hygiene per Pitfall G)
The sentinel string for this test should be a non-textual marker (e.g., a 32-byte random hex token) so WebP re-encoding does NOT preserve any visual rendering of it. The structural value is locking the scan target list to include the new thumb cache surface.

**Step 5 — Verification grep:** After all edits, run `grep -n "thumb.webp\|thumb.failed" src/__tests__/c2pa-key-leak-negative.test.ts` → MUST return ≥ 2 matches confirming the new test references both surfaces.
  </action>
  <verify>
    <automated>npx vitest run --reporter=basic --no-coverage src/__tests__/architecture-purity.test.ts -t "sharp imports are centralized" src/__tests__/architecture-purity.test.ts -t "src/engine/thumbnails/" src/__tests__/c2pa-key-leak-negative.test.ts -t "Phase 17"</automated>
  </verify>
  <done>architecture-purity.test.ts has the sharp allowed-set assertion (sorted-array deepEqual) + 5 directory-level guards for src/engine/thumbnails/, all green. c2pa-key-leak-negative.test.ts has a new Phase 17 test scanning .thumb.webp + .thumb.failed in 4 encodings, green. The grep `grep -rlE "from[[:space:]]*['\"]sharp|import[[:space:]]*\\([[:space:]]*['\"]sharp" src/ | grep -v __tests__` returns exactly src/engine/thumbnails/image-thumbnail.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → server process | sharp reads source bytes from disk; corrupted/oversized inputs could DoS the libvips process |
| Native binding → Node runtime | `await import('sharp')` may fail on platform mismatch; failure must not crash boot |
| Cache writer → cache reader | Concurrent partial-write reads must never observe truncated bytes |
| Source asset → thumb cache | Sensitive content (prompt-rendered text in image) could leak into the thumb byte surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | D (denial of service) | sharp libvips on giant image | mitigate | sharp.concurrency(2) global cap (PITFALL B) + sharp.cache(false) (PITFALL F) — limits per-process resource pressure. Set on first successful module load in image-thumbnail.ts. |
| T-17-02 | T (tampering) | partial cache file | mitigate | Atomic write via `${final}.${nanoid(8)}.partial` + rename; `nanoid(8)` ensures concurrent writers do NOT collide on partial path. Test 2 (Task 2) asserts no .partial leaks on success. |
| T-17-03 | I (information disclosure) | .thumb.failed sentinel content | mitigate | writeFailedSentinel writes ZERO bytes (no path, no filename, no error reason) — Pitfall G hygiene. Comment-pinned in cache.ts. Tested via assertNotInBuffer in Task 3 leak-scan. |
| T-17-04 | I (information disclosure) | .thumb.webp re-encoded pixels | mitigate | Multi-encoding leak scan (UTF-8 + UTF-16LE + UTF-16BE + base64) extended to .thumb.webp byte surface in Task 3 — locks the scan target list so a future regression that bypasses the scan is caught structurally. |
| T-17-05 | E (elevation of privilege via boot crash) | Missing sharp native binding | mitigate | Lazy `await import('sharp')` + monotonic fail returns null (D-26). Server boots; thumbnails degrade to skeleton. Test 5 (Task 2) asserts the import is NOT retried after first failure. |
| T-17-06 | S (spoofing — wrong import path) | Rogue sharp importer outside allowed-set | mitigate | architecture-purity.test.ts subset+SET-equality assertion (D-25) catches any new file that imports sharp. Sorted-array deepEqual produces a clear test failure naming the violating file. |
| T-17-07 | I (information disclosure) | sharp error message leaks fs path | accept | sharp errors may include the source filename in their message. The downstream error-middleware translation maps TypedError('THUMBNAIL_FAILED', ...) to a public-safe envelope — the source path stays in server logs only. Acceptable: low risk (no PII in path; outputsDir is project-local). |
| T-17-08 | T (tampering — corrupted source) | Malformed PNG/JPEG/WebP | mitigate | sharp throws on decode error → caught and surfaced as TypedError('THUMBNAIL_FAILED', reason='sharp_failed') in generateImageThumbnail. Engine writes .thumb.failed sentinel (Plan 03 facade); UI shows skeleton. No crash. |
</threat_model>

<verification>
- All Plan 17-01 tests green: `npx vitest run --reporter=basic --no-coverage src/engine/thumbnails/__tests__/ src/__tests__/architecture-purity.test.ts -t "sharp\\|src/engine/thumbnails/" src/__tests__/c2pa-key-leak-negative.test.ts -t "Phase 17"`
- TypeScript clean: `npx tsc --noEmit`
- Architecture-purity invariant: `grep -rlE "from[[:space:]]*['\"]sharp|import[[:space:]]*\\([[:space:]]*['\"]sharp" src/ | grep -v __tests__` returns EXACTLY `src/engine/thumbnails/image-thumbnail.ts`
- Sharp dep landed: `node -e "console.log(require('./package.json').dependencies.sharp)"` prints `^0.34.5`
- ErrorCode union: `grep -c "'THUMBNAIL_FAILED'" src/engine/errors.ts` returns 1
- Pre-existing tests still green: `npx vitest run --reporter=basic --no-coverage` reports 0 new failures vs the pre-Phase-17 baseline (the 4 v1.1-audit pre-existing failures noted in STATE.md remain unchanged)
</verification>

<success_criteria>
- VIS-01 partial coverage: image thumbnails resize to ≤640×360 WebP at q=80 from PNG/JPEG/WebP/TIFF source via sharp; cache path is `<outputsDir>/<versionId>/<filename>.thumb.webp`; atomic temp+rename invariant proven by tests
- VIS-02 partial coverage: `.thumb.failed` sentinel mechanism exists (cache.writeFailedSentinel + isCacheFresh sentinel-mtime check); UI integration of skeleton-on-sentinel is Plan 04 scope
- D-23 LOCKED: sharp imports allowed-set = {src/engine/thumbnails/image-thumbnail.ts} via sorted-array deepEqual
- D-25 LOCKED: arch-purity assertion landed in the SAME plan that introduces the import (no orphaned sharp imports)
- D-26 LOCKED: lazy `await import('sharp')` with monotonic fail; server boot succeeds without sharp native binding
- D-22 LOCKED: atomic temp+rename via nanoid(8) suffix; no .partial files survive successful writes
- D-06 LOCKED: ETag derivation prefers sha256 strong validator, falls back to mtime: short-hash
- D-07 LOCKED: .thumb.failed sentinel suppresses retry until source mtime advances
- PITFALL B + F LOCKED: sharp.concurrency(2) and sharp.cache(false) set ONCE on first load
- PITFALL G LOCKED: multi-encoding leak scan extends to .thumb.webp + .thumb.failed cache surface
- All Plan 01 tests pass; pre-existing tests remain green; tsc --noEmit clean
</success_criteria>

<output>
After completion, create `.planning/phases/17-visual-thumbnails/17-01-SUMMARY.md` documenting:
- Sharp version installed and platform binary observed
- Files created (paths, line counts)
- Test count delta (root suite before / after)
- Architecture-purity assertions added (sharp allowed-set + 5 directory guards)
- Multi-encoding leak-scan extension shape (which test file, which encoding fragments scanned)
- Any Rule deviations auto-fixed
- Pre-existing failures count vs baseline (must be unchanged)
- Plans this unblocks: 17-02 (depends on getSharpForVideoReencode + format-router barrel + cache helpers + .thumb.webp/.thumb.failed cache layout), 17-03 (depends on engine-side primitives for the facade integration)
</output>