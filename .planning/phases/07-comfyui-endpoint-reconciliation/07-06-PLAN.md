---
phase: 07
plan: 06
type: execute
wave: 4
depends_on: [02, 03, 04, 05]
files_modified: []
autonomous: false
requirements: []
success_criteria:
  - "SC-2: Live-smoke returns a completed job (both tests pass) end-to-end against the locked endpoint"
  - "Regression: stdio-hygiene, tool-budget, architecture-purity, transport-parity all green"
tags:
  - phase-07
  - live-smoke
  - acceptance-gate
  - endpoint-reconciliation
must_haves:
  truths:
    - "`RUN_LIVE_SMOKE=1 npx vitest run src/comfyui/__tests__/live-smoke.test.ts` exits 0 with both tests passing"
    - "First-submit healthcheck fires exactly once in the live run (observable via stderr or implicit via zero DRIFT errors)"
    - "Both live-smoke tests — `submit → poll → download → output` AND `submit → poll → reproduce → poll → byte-identical prompt_json` — pass"
    - "No regressions: stdio-hygiene + tool-budget (count = 7) + architecture-purity + transport-parity all green"
    - "Full suite: passed ≥ 739, skipped = 3 (live-smoke x2 run as passed, so skipped count drops during this run but reverts on next default run)"
    - "Second back-to-back live-smoke run also green (per Pitfall #5 — confirms drift is truly resolved, not transient)"
  artifacts: []
  key_links:
    - from: "RUN_LIVE_SMOKE=1 npx vitest run live-smoke"
      to: "src/comfyui/client.ts ensureEndpointHealthy() + ComfyUI Cloud"
      via: "Engine.submitGeneration → ComfyUIClient.submit → healthcheck (locked base) → real POST /api/prompt"
      pattern: "RUN_LIVE_SMOKE"
---

<objective>
Execute the live-smoke test against the locked ComfyUI Cloud endpoint using the current `.env` key and record the outcome. This is the Phase 7 acceptance gate for SC-2. No code changes — it's a gated execution task. The plan is `autonomous: false` because running live-smoke requires a valid `COMFYUI_API_KEY` which the executor may not have access to without user opt-in.

Purpose: Every prior Wave 2-3 plan (02, 03, 04, 05) has its own unit-test verification. Plan 06 is the integration checkpoint — it proves the entire chain works end-to-end:
- `.env` base (Plan 03) correctly picked up at server boot
- `DEFAULT_COMFYUI_API_BASE` (Plan 02) aligned with `.env`
- First-submit healthcheck (Plan 02) fires against the live endpoint and passes (200)
- `POST /api/prompt` succeeds and returns a `prompt_id`
- Polling transitions the version through `submitted → running → completed`
- Signed-URL download completes; file lands on disk
- (Second test) Reproduce path produces a byte-identical prompt blob

If any step fails, Phase 7 is not complete — Plan 07 (docs) + Plan 08 (memory hygiene) must wait until live-smoke is reliably green twice in a row (Pitfall #5).

Output: two captured console outputs (both live-smoke runs) for inclusion in `07-VERIFICATION.md §1` and the commit message. No files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-RESEARCH.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-VALIDATION.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-02-SUMMARY.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-03-SUMMARY.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-04-SUMMARY.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-05-SUMMARY.md
@src/comfyui/__tests__/live-smoke.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm all Wave 2/3 plans complete and test suite green</name>
  <files></files>

  <read_first>
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-02-SUMMARY.md through 07-05-SUMMARY.md — confirm all Wave 2 + Wave 3 plans landed cleanly.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-RESEARCH.md §"Common Pitfalls > Pitfall 5" (lines 581-586) — explicitly requires TWO back-to-back green runs for Phase 7 acceptance.
  </read_first>

  <action>
Run the pre-flight checks before committing compute to the live Cloud call:

**Step 1: Full suite green, default skipped count = 3.**

```bash
npx vitest run
```

Expected: `Test Files XX passed | X skipped`, `Tests ≥739 passed | 3 skipped`. If the skipped count is not exactly 3, investigate (Plan 05 may have miswired the gate).

**Step 2: TypeScript clean.**

```bash
npx tsc --noEmit
```

Expected: no output, exit 0.

**Step 3: Architecture + regression suite.**

```bash
npx vitest run src/__tests__/stdio-hygiene.test.ts src/__tests__/architecture-purity.test.ts src/__tests__/tool-budget.test.ts src/__tests__/transport-parity.test.ts
```

Expected: all green. Tool count in `tool-budget.test.ts` should still equal 7 (the Phase 6 baseline — Phase 7 does not add or remove tools).

**Step 4: Confirm `.env` has a usable `COMFYUI_API_KEY`.**

```bash
grep -q '^COMFYUI_API_KEY=.\{10,\}' .env && echo "OK: COMFYUI_API_KEY present and appears non-placeholder" || echo "FAIL: COMFYUI_API_KEY missing or empty"
```

Do NOT print the key value. If the key is empty or clearly a placeholder (e.g., `your-key-here`), halt and raise a checkpoint for the user to populate `.env` before the live-smoke task runs.

**Step 5: Confirm `.env` `COMFYUI_API_BASE` matches the probe winner.**

```bash
grep '^COMFYUI_API_BASE=' .env | sed 's/^COMFYUI_API_BASE=//'
```

Output should match the `<WINNING_BASE>` value from `07-01-SUMMARY.md`. Cross-reference before moving on.

If any pre-flight step fails, STOP and report to the orchestrator. Do NOT attempt the live-smoke run until pre-flight is green.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | ( ! grep -q "error" ) &amp;&amp; npx vitest run src/__tests__/stdio-hygiene.test.ts src/__tests__/architecture-purity.test.ts src/__tests__/tool-budget.test.ts src/__tests__/transport-parity.test.ts 2>&amp;1 | grep -qE "Tests\\s+[0-9]+ passed" &amp;&amp; grep -q '^COMFYUI_API_KEY=.\{10,\}' .env</automated>
  </verify>

  <done>
- Full suite baseline confirmed (739+ passed, 3 skipped)
- TSC clean
- Architecture + regression suite green
- `.env` has a non-placeholder `COMFYUI_API_KEY`
- `.env` `COMFYUI_API_BASE` matches the probe winner (cross-checked against `07-01-SUMMARY.md`)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 1: Operator runs live-smoke TWICE and reports outcome</name>
  <what-built>
    Phase 7 Wave 2 + 3 complete: locked base, healthcheck, typed error, unit tests, sentinel test. The one remaining gate is a live Cloud round-trip using the operator's real `COMFYUI_API_KEY`.
  </what-built>
  <how-to-verify>
    Run live-smoke TWICE back-to-back per Pitfall #5 — a single green run could be a transient ComfyUI Cloud state; two consecutive greens prove the drift is truly resolved. Both runs must complete before Plan 07 (docs) proceeds.

    **Run 1** (from repo root):
    ```bash
    RUN_LIVE_SMOKE=1 npx vitest run src/comfyui/__tests__/live-smoke.test.ts
    ```

    Expected outcome:
    - Test 1: `submit → poll → download → output file on disk` — PASSES in ~2-3 minutes (depends on Cloud queue state).
    - Test 2: `submit → poll → reproduce → poll → byte-identical prompt_json` — PASSES in ~5-7 minutes.
    - Stderr shows `[live-smoke] first-poll entity snapshot: {...}` ONCE (confirms D-EP-07 healthcheck fired without complaint; if it had failed with `COMFYUI_ENDPOINT_DRIFT`, the test would have short-circuited before the first poll).
    - Stderr shows `[live-smoke] observed signed-URL host: ...` — capture this host for any future allowlist widening (not an issue for Phase 7 but useful evidence).
    - Final summary: `Tests 2 passed | 0 skipped`, exit code 0.

    **Run 2** (immediately after Run 1):
    ```bash
    RUN_LIVE_SMOKE=1 npx vitest run src/comfyui/__tests__/live-smoke.test.ts
    ```

    Expected same outcome. Capture both timestamps (e.g., `date -u +%FT%TZ` before each run) for the `07-VERIFICATION.md §1` probe-matrix table.

    **If either run fails:**
    - If the failure is `COMFYUI_ENDPOINT_DRIFT` → the probe winner from Plan 01 no longer authenticates. Either the key rotated since Plan 01 (operator must rotate the key in `.env` and re-run Plan 01) OR the endpoint drifted further. Halt and report.
    - If the failure is `COMFYUI_API_ERROR` with an unrelated message → network issue or ComfyUI Cloud outage. Retry once; if still failing, halt and consult ComfyUI Cloud status.
    - If the failure is on the reproduce-test specifically (`prompt_json` mismatch) → unrelated to Phase 7 scope (Phase 3 contract). Record in summary but treat as blocking for Phase 7 acceptance.

    **Capture for `07-VERIFICATION.md §1`:**
    - Run 1 timestamp + exit code + duration
    - Run 2 timestamp + exit code + duration
    - Both `[live-smoke]` stderr log lines per run (paste verbatim; no key redaction needed — those log lines contain only job IDs and signed-URL hosts, no secrets)
    - Final full-suite test counts
  </how-to-verify>
  <resume-signal>
    Report to Claude with the two live-smoke results:

    ```
    LIVE-SMOKE RESULTS:
    Run 1:
      timestamp: <ISO-8601>
      duration: <seconds>
      exit_code: 0
      tests: 2 passed | 0 skipped
      first-poll snapshot: <paste stderr>
      observed signed-URL host: <paste stderr>
    Run 2:
      timestamp: <ISO-8601>
      duration: <seconds>
      exit_code: 0
      tests: 2 passed | 0 skipped
      first-poll snapshot: <paste stderr>
      observed signed-URL host: <paste stderr>
    Phase 7 SC-2 gate: PASSED
    ```

    Or if failed:
    ```
    LIVE-SMOKE FAILED:
    Run: <1|2>
    Error: <COMFYUI_ENDPOINT_DRIFT | COMFYUI_API_ERROR | other>
    Full output (last 40 lines): <paste>
    Next action: <halt for rotation | halt for investigation | retry>
    ```
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Optional sentinel opt-in run (confirms Plan 05 test works end-to-end)</name>
  <files></files>

  <read_first>
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-05-SUMMARY.md — confirms the sentinel file exists.
  </read_first>

  <action>
After live-smoke is green twice, run the Plan 05 sentinel once to confirm it also lands correctly:

```bash
RUN_PROBE=1 npx vitest run src/comfyui/__tests__/endpoint-probe.test.ts
```

Expected: 1 passed, 0 skipped, exit 0. Completes in < 5 seconds (single GET).

This is a smoke of the smoke — if live-smoke passes but the sentinel fails, there's a disconnect between `HEALTHCHECK_PATH` and the runtime healthcheck (unlikely but worth verifying).

If the sentinel fails, investigate — common cause would be Plan 02 exporting the wrong `HEALTHCHECK_PATH` value. Fix before proceeding to Plan 07.
  </action>

  <verify>
    <automated>RUN_PROBE=1 npx vitest run src/comfyui/__tests__/endpoint-probe.test.ts 2>&amp;1 | grep -qE "1 passed"</automated>
  </verify>

  <done>
- Sentinel test passes with opt-in (1 passed)
- Confirms `HEALTHCHECK_PATH` is correctly shared between runtime and sentinel
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-smoke test → ComfyUI Cloud (real) | Real API key + real GPU compute — credit burn per run |
| Checkpoint output → agent response | Stderr log lines may contain job IDs and signed-URL hosts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DRIFT-01 | Information Disclosure | Checkpoint summary paste | mitigate | The `[live-smoke] first-poll entity snapshot` and `[live-smoke] observed signed-URL host` stderr lines are pre-existing — verified they emit only job_id, version_number, and URL hostnames (no key material). The operator can paste them verbatim. No summary should include the raw `COMFYUI_API_KEY` value. |
| T-DRIFT-15 | Denial of Service (credit burn) | live-smoke double-run | accept | Each live-smoke run burns ~1 credit (cheapest minimal workflow per RESEARCH A6). Two back-to-back runs = 2 credits. Acceptable cost for the acceptance gate; Pitfall #5 rationale justifies the second run. |
| T-DRIFT-16 | Repudiation | live-smoke audit trail | mitigate | Task 1 captures ISO-8601 timestamps, exit codes, durations for both runs. These become the canonical evidence in `07-VERIFICATION.md §1` that Phase 7 SC-2 was met. |
</threat_model>

<verification>
- Pre-flight: full suite green, TSC clean, regression suites green, `.env` populated
- Live-smoke run 1: 2 tests passed, 0 skipped, exit 0
- Live-smoke run 2: 2 tests passed, 0 skipped, exit 0
- Sentinel opt-in: 1 test passed
- All four stderr log lines captured for Plan 07 documentation
</verification>

<success_criteria>
- SC-2 fully satisfied: live-smoke green end-to-end, twice in a row
- No regressions in any invariant test (stdio-hygiene, architecture-purity, tool-budget=7, transport-parity)
- Sentinel test confirms HEALTHCHECK_PATH alignment
- Evidence captured for `07-VERIFICATION.md §1` probe + live-smoke matrix
</success_criteria>

<output>
After completion, create `.planning/phases/07-comfyui-endpoint-reconciliation/07-06-SUMMARY.md` capturing:
- Pre-flight outcomes
- Both live-smoke run outcomes with timestamps, durations, exit codes
- Both `[live-smoke] first-poll entity snapshot` log lines (redact anything that looks like a key)
- Both `[live-smoke] observed signed-URL host` log lines
- Sentinel opt-in result
- A 1-line statement: "Phase 7 SC-2 gate: PASSED — live-smoke green twice; drift resolved."
</output>
