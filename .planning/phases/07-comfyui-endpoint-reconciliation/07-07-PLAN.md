---
phase: 07
plan: 07
type: execute
wave: 5
depends_on: [06]
files_modified:
  - .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md
  - .planning/phases/02-comfyui-generation/02-VERIFICATION.md
autonomous: true
requirements: []
success_criteria:
  - "SC-3: Endpoint decision + credential layout + fallback-if-redirected documented in 07-VERIFICATION.md; cross-referenced from 02-VERIFICATION.md"
tags:
  - phase-07
  - documentation
  - verification
must_haves:
  truths:
    - "`07-VERIFICATION.md` exists and contains all 4 required sections per D-EP-12 (probe matrix + chosen base, credential layout, rotation procedure, fallback-if-redirected + memory hygiene)"
    - "`07-VERIFICATION.md` probe matrix is populated with ACTUAL values from the Plan 01 run (not placeholders)"
    - "`07-VERIFICATION.md` rotation procedure is executable end-to-end without consulting other docs"
    - "`02-VERIFICATION.md` appended with one `## Endpoint Reconciliation (Phase 7, 2026-04-XX)` section + link to `07-VERIFICATION.md`"
  artifacts:
    - path: ".planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md"
      provides: "Canonical Phase 7 resolution document with 4 sections per D-EP-12"
      contains: "## 1. Probe Matrix and Chosen Base"
      contains_also: "## 2. Credential Layout"
      contains_also_2: "## 3. Rotation Procedure"
      contains_also_3: "## 4. Fallback-If-Redirected and Memory Hygiene"
    - path: ".planning/phases/02-comfyui-generation/02-VERIFICATION.md"
      provides: "Cross-reference supplement pointing forward readers at Phase 7 resolution"
      contains: "## Endpoint Reconciliation (Phase 7"
  key_links:
    - from: ".planning/phases/02-comfyui-generation/02-VERIFICATION.md"
      to: ".planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md"
      via: "relative markdown link `../07-comfyui-endpoint-reconciliation/07-VERIFICATION.md`"
      pattern: "07-VERIFICATION"
---

<objective>
Write `07-VERIFICATION.md` — the canonical resolution document for Phase 7 — and append a 1-paragraph cross-reference supplement to `02-VERIFICATION.md`. This is the documentation leg of SC-3: the endpoint decision, credential layout, and fallback-if-redirected behavior are captured in a form that survives the next credential rotation or code audit.

Purpose: Phase 7 touched several files and produced a probe-matrix observation that nobody but Claude and the current operator saw. Without this doc, a future agent picking up "live-smoke regressed" in six months has no trail to the 2026-04-24 resolution. With it, the rotation procedure + probe-matrix evidence + memory-hygiene notes compound into a self-contained runbook.

D-EP-12 locks the four required sections; this plan writes each one against actual observations from Plans 01 and 06 (not placeholders). D-EP-11 specifies the `02-VERIFICATION.md` supplement (one paragraph, one link) — minimal and forward-pointing so Phase 2 readers land on the canonical doc.

Output: one new file (`07-VERIFICATION.md`) + one appended section in an existing file (`02-VERIFICATION.md`). No code changes. No memory updates (that's Plan 08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-RESEARCH.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-VALIDATION.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-01-SUMMARY.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-02-SUMMARY.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-03-SUMMARY.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-06-SUMMARY.md
@.planning/phases/02-comfyui-generation/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md with 4 required sections</name>
  <files>.planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02-comfyui-generation/02-VERIFICATION.md (FULL — 156 lines) — analog for frontmatter style + overall doc structure. Mirror frontmatter shape; section headers may differ because Phase 7 is a resolution runbook, not a 5-truths verification.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md §D-EP-11, D-EP-12 — locks the 4 required section contents.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md §".planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md (NEW — canonical resolution doc)" — suggests section headers + table style.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-01-SUMMARY.md — **MUST read**. Extract the probe matrix (all 16 combos) + the winning (base, path) for §1.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-02-SUMMARY.md — Extract the locked `DEFAULT_COMFYUI_API_BASE` + `HEALTHCHECK_PATH` values.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-06-SUMMARY.md — Extract both live-smoke run timestamps + stderr log lines for §1 evidence block.
    - src/comfyui/client.ts §`DEFAULT_ALLOWED_HOST_PATTERNS` (line 113) — referenced in §4 fallback-if-redirected.
  </read_first>

  <action>
Create `07-VERIFICATION.md` with the exact structure below. Fill in bracketed placeholders from the upstream summaries. Use Markdown. Do NOT include any `COMFYUI_API_KEY` value.

**Frontmatter.**

```
---
phase: 07-comfyui-endpoint-reconciliation
verified: <ISO-8601 timestamp of Plan 07 execution>
status: passed
score: 4/4 sections complete
overrides_applied: 0
re_verification: null
---
```

**Title + header block.**

```
# Phase 7: ComfyUI Endpoint Reconciliation — Resolution Report

**Phase Goal:** Reconcile the `COMFYUI_API_BASE` endpoint drift so live-smoke authenticates and returns 200 across stdio + HTTP transports, closing the Phase 2 infrastructure tech debt captured in project memory.

**Verified:** <timestamp>
**Status:** PASSED
**Re-verification:** No — initial resolution

---
```

**§1 Probe Matrix and Chosen Base.**

Paste the actual Plan 01 probe output here. Include the 16-row matrix (4 bases × 4 paths) + any docs-discovered base. Mark the winner with `**WINNER**`. Include both live-smoke run timestamps from Plan 06.

```
## 1. Probe Matrix and Chosen Base

### Probe Run (<timestamp from 07-01-SUMMARY.md>)

Invocation: `npx tsx scripts/probe-comfy-endpoint.mts` from repo root.

| Base | Path | Status | First 200 bytes | Notes |
|------|------|--------|-----------------|-------|
| <base-1> | /api/queue | <status> | <snippet> | <WINNER if applicable> |
| <base-1> | /api/system_stats | <status> | <snippet> | <note> |
| ... (all 16 combos — paste verbatim from 07-01-SUMMARY.md) ... |

### Chosen Base

**`COMFYUI_API_BASE=<WINNING_BASE>`** with **`HEALTHCHECK_PATH=<HEALTHCHECK_PATH>`**.

Rationale: <1-2 sentences on why this combo was chosen — e.g., "Only combo that returned 200 with the current key. `/api/queue` is documented in docs.comfy.org as a read-only endpoint, zero credit burn, trivially cheap for the first-submit healthcheck (D-EP-07)."

### Live-Smoke Evidence

| Run | Timestamp | Duration | Exit | Tests | Notes |
|-----|-----------|----------|------|-------|-------|
| 1 | <ISO-8601> | <seconds> | 0 | 2 passed / 0 skipped | First-poll snapshot: `<paste>` |
| 2 | <ISO-8601> | <seconds> | 0 | 2 passed / 0 skipped | First-poll snapshot: `<paste>` |

Observed signed-URL host: `<paste from 07-06-SUMMARY.md>` — inside `DEFAULT_ALLOWED_HOST_PATTERNS` at `src/comfyui/client.ts:113`, no allowlist widening needed.

---
```

**§2 Credential Layout / Source-of-Truth.**

```
## 2. Credential Layout / Source-of-Truth

### Where the key lives
- File: `.env` at repo root (NOT `.env.example`)
- Permissions: chmod 600 (non-world-readable; enforced by Plan 03 Task 2)
- Gitignore: `.gitignore` line 1 excludes `.env` (never committed)
- Loading: `import 'dotenv/config'` at `src/server.ts:2` (before any relative imports — matches D-GEN-09 pattern)
- Read by: `Engine` wiring in `src/server.ts` via `process.env.COMFYUI_API_KEY` and `process.env.COMFYUI_API_BASE`

### Where the base lives (three sources, single truth — D-EP-06)
1. **`.env`** — operator's live value. Used at server boot.
2. **`.env.example`** — committed template with the locked base + rotation-procedure reference. Updated by Plan 03.
3. **`src/comfyui/client.ts:34`** — `DEFAULT_COMFYUI_API_BASE` constant. Used when `.env` is absent or variable empty. Updated by Plan 02.

All three MUST agree. Plan 03 Task 3 includes a cross-file consistency check that fails loudly on drift.

### How tests gate on credentials
- **Live-smoke** (`src/comfyui/__tests__/live-smoke.test.ts`): `RUN_LIVE_SMOKE=1` + `COMFYUI_API_KEY`. Burns credits per run; 2 tests. Invocation: `RUN_LIVE_SMOKE=1 npx vitest run src/comfyui/__tests__/live-smoke.test.ts`.
- **Sentinel** (`src/comfyui/__tests__/endpoint-probe.test.ts`): `RUN_PROBE=1` + `COMFYUI_API_KEY`. Zero credit burn (read-only GET against `HEALTHCHECK_PATH`). Invocation: `RUN_PROBE=1 npx vitest run src/comfyui/__tests__/endpoint-probe.test.ts`.
- **Unit tests** (`src/comfyui/__tests__/client.test.ts`): use `fetchImpl` injection with fake `KEY = 'sk-test-fake'`. Never hit the network. Always run.

### Key issuance source
ComfyUI Cloud console: `https://platform.comfy.org/login` → **API Keys** section → **"+ New"** button. Key value is shown ONCE on creation; copy to `.env` immediately. Dashboard shows `****<last4>` thereafter for identification.

---
```

**§3 Rotation Procedure.**

```
## 3. Rotation Procedure

Follow these steps in order when the current key needs to be rotated (e.g., suspected exposure, scheduled rotation, failed probe with all-401 exit code).

1. **Issue a new key.**
   - Visit `https://platform.comfy.org/login` and sign in.
   - Navigate to **API Keys** → **"+ New"**.
   - Copy the full key value from the one-time display. Do NOT close the modal before copying.

2. **Replace the value in `.env`.**
   - Open `.env` at repo root (chmod 600; operator's terminal must have read access).
   - Replace the `COMFYUI_API_KEY=...` line with the new key. Keep quoting style if present.
   - Do NOT commit `.env` — it's gitignored; verify with `git status` showing no new changes staged.

3. **Re-run the probe.**
   - From repo root: `npx tsx scripts/probe-comfy-endpoint.mts`
   - Verify the SUMMARY banner says **WINNER: <base><path> → 200** (exit 0).
   - If it says "NO WORKING COMBO" (exit 1), the new key was issued against a different host than the current `COMFYUI_API_BASE`. Check the Cloud console for the endpoint associated with the key and update step 4 accordingly.

4. **Update base + HEALTHCHECK_PATH if they changed.**
   - Compare the probe winner's `<base>` and `<path>` to the current values in:
     - `.env` (`COMFYUI_API_BASE=<old>`)
     - `.env.example` (`COMFYUI_API_BASE=<old>`)
     - `src/comfyui/client.ts:34` (`DEFAULT_COMFYUI_API_BASE`)
     - `src/comfyui/client.ts` `export const HEALTHCHECK_PATH`
   - If the probe winner differs from any site, update the three `COMFYUI_API_BASE` sites to match and update `HEALTHCHECK_PATH` if the path changed.
   - Confirm consistency: `grep '^COMFYUI_API_BASE=' .env .env.example` must show the same value both lines; `grep 'DEFAULT_COMFYUI_API_BASE = ' src/comfyui/client.ts` must show the same string.

5. **Re-run live-smoke.**
   - From repo root: `RUN_LIVE_SMOKE=1 npx vitest run src/comfyui/__tests__/live-smoke.test.ts`
   - Expect 2 tests passed, 0 skipped, exit 0.
   - If live-smoke fails with `COMFYUI_ENDPOINT_DRIFT` → the healthcheck is catching drift that the probe missed; run the probe again and investigate the matrix.
   - If live-smoke fails with `COMFYUI_API_ERROR` → possibly a transient Cloud outage or the workflow's checkpoint name isn't available on the new tier. Override with `COMFYUI_SMOKE_CHECKPOINT=<available-ckpt>` and retry.

6. **Update memory and optionally commit docs.**
   - If the base or `HEALTHCHECK_PATH` changed, update `07-VERIFICATION.md §1` with the new probe matrix and run timestamps.
   - Per D-EP-15, also update `reference_env_comfyui_key.md` to reflect the new locked base (memory hygiene — Plan 08 pattern).

---
```

**§4 Fallback-If-Redirected and Memory Hygiene.**

```
## 4. Fallback-If-Redirected and Memory Hygiene

### Redirect allowlist (D-GEN-22)

`ComfyUIClient.download()` fetches the signed-URL via a two-hop redirect pattern:
1. `GET /api/view` → 302 with `Location` header.
2. `GET <signed-url>` → 200 with the file stream.

The redirect target hostname MUST match one of the patterns in `DEFAULT_ALLOWED_HOST_PATTERNS` at `src/comfyui/client.ts:113`:

<typescript code block verbatim from src/comfyui/client.ts lines 113-118>

If the probe or live-smoke surfaces a redirect to a host NOT on the allowlist, the download fails with `COMFYUI_API_ERROR: Unexpected redirect host: <host>`. Two remediation paths:

- **Runtime override:** set `COMFYUI_ALLOWED_REDIRECT_HOSTS=<host>` in `.env` (comma-separated, matched as EXACT or `.<host>` suffix per IS-01). No code change.
- **Code update:** add the new regex pattern to `DEFAULT_ALLOWED_HOST_PATTERNS` in `src/comfyui/client.ts:113`. Requires a code review because it widens the SSRF allowlist; prefer the runtime override for one-off probes.

The Phase 7 live-smoke observed signed-URL host (see §1) is inside the existing allowlist — no widening needed.

### Memory hygiene (D-EP-15)

Post-Phase-7 state of the two relevant project memories (updated by Plan 08):

- `~/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md` — marked `RESOLVED <date>` or removed entirely (criterion: live-smoke green for the second consecutive run per Pitfall #5). Plan 08 determines which.
- `~/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/reference_env_comfyui_key.md` — body updated to reflect the new locked `COMFYUI_API_BASE` value. `description` field unchanged.
- `~/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/MEMORY.md` — index entries updated to match the above.

If a future agent encounters endpoint-drift symptoms and the drift-memory is gone, this document is the canonical reference — start at §3 Rotation Procedure.

---

_Verified: <timestamp>_
_Verifier: Claude (Plan 07 executor)_
```

**STEP: Verify all placeholder values have been substituted.** `grep -n '<[A-Z_]*>' 07-VERIFICATION.md` should return zero matches. If any placeholder remains, the doc is incomplete.
  </action>

  <verify>
    <automated>test -f .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md &amp;&amp; grep -c '^## ' .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md | awk '{exit !($1 &gt;= 4)}' &amp;&amp; grep -q '^## 1\. Probe Matrix' .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md &amp;&amp; grep -q '^## 2\. Credential Layout' .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md &amp;&amp; grep -q '^## 3\. Rotation Procedure' .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md &amp;&amp; grep -q '^## 4\. Fallback-If-Redirected' .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md &amp;&amp; grep -q 'scripts/probe-comfy-endpoint.mts' .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md &amp;&amp; grep -q 'platform.comfy.org' .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md</automated>
  </verify>

  <done>
- `07-VERIFICATION.md` exists with all 4 required `## ` sections (1 Probe Matrix, 2 Credential Layout, 3 Rotation Procedure, 4 Fallback + Memory Hygiene)
- Probe matrix table is populated with ACTUAL values from `07-01-SUMMARY.md` (not placeholders)
- Rotation Procedure is numbered 1-6 and each step is executable without consulting other docs
- Signed-URL host observed in live-smoke is documented (§1) and confirmed inside `DEFAULT_ALLOWED_HOST_PATTERNS` (§4)
- No `<PLACEHOLDER>` brackets remain
- Frontmatter status is `passed`
  </done>
</task>

<task type="auto">
  <name>Task 2: Append cross-reference supplement to .planning/phases/02-comfyui-generation/02-VERIFICATION.md</name>
  <files>.planning/phases/02-comfyui-generation/02-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02-comfyui-generation/02-VERIFICATION.md (FULL — 156 lines) — identify the current tail (`_Verifier: Claude (gsd-verifier)_` at line 155). The new section appends AFTER that line.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md §D-EP-11 — locks the supplement shape: one H2 section, one paragraph + link.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md §".planning/phases/02-comfyui-generation/02-VERIFICATION.md (APPEND — cross-reference supplement)" — shows the header style + relative-link format.
  </read_first>

  <action>
Append a single new H2 section to the end of `02-VERIFICATION.md`, AFTER the existing `_Verifier: Claude (gsd-verifier)_` line (currently line 155). Content is one horizontal rule, one H2 header, one paragraph with a relative link. Exact insertion:

```
<existing file body ends here>

_Verified: 2026-04-21T10:35:00Z_
_Verifier: Claude (gsd-verifier)_

---

## Endpoint Reconciliation (Phase 7, <YYYY-MM-DD of Plan 07 commit>)

The Phase 2 live-smoke entry (see §"Behavioral Spot-Checks > Live-smoke gated") remained untested end-to-end until Phase 7 resolved the `COMFYUI_API_BASE` drift observed on 2026-04-22. As of <date>, the locked `COMFYUI_API_BASE` is `<WINNING_BASE>`, with `HEALTHCHECK_PATH=<HEALTHCHECK_PATH>` exported from `src/comfyui/client.ts` and a first-submit healthcheck wired into `ComfyUIClient.submit()` to catch future drift as `TypedError('COMFYUI_ENDPOINT_DRIFT')`. See [`07-VERIFICATION.md`](../07-comfyui-endpoint-reconciliation/07-VERIFICATION.md) for the probe matrix, credential layout, rotation procedure, and fallback-if-redirected behaviour.
```

**Key constraints:**
- Substitute `<YYYY-MM-DD>` with the actual Plan 07 commit date (e.g., `2026-04-24`).
- Substitute `<WINNING_BASE>` + `<HEALTHCHECK_PATH>` with the values from `07-01-SUMMARY.md`.
- Use a relative link (`../07-comfyui-endpoint-reconciliation/07-VERIFICATION.md`) — renders correctly from either raw markdown or a repo browser.
- The supplement is exactly ONE paragraph plus the link. Do NOT expand with sub-sections — the contract is minimal cross-reference.
- Do NOT modify anything above the `_Verifier:_` tail line; pure append only.
- Preserve the blank line + `---` horizontal rule to visually separate the supplement.
  </action>

  <verify>
    <automated>grep -q '^## Endpoint Reconciliation (Phase 7' .planning/phases/02-comfyui-generation/02-VERIFICATION.md &amp;&amp; grep -q '07-comfyui-endpoint-reconciliation/07-VERIFICATION.md' .planning/phases/02-comfyui-generation/02-VERIFICATION.md &amp;&amp; grep -q 'COMFYUI_ENDPOINT_DRIFT' .planning/phases/02-comfyui-generation/02-VERIFICATION.md &amp;&amp; grep -q '_Verifier: Claude (gsd-verifier)_' .planning/phases/02-comfyui-generation/02-VERIFICATION.md</automated>
  </verify>

  <done>
- `02-VERIFICATION.md` has a new `## Endpoint Reconciliation (Phase 7, YYYY-MM-DD)` section at the end
- Section contains exactly one paragraph plus a relative link to `07-VERIFICATION.md`
- Link path resolves (relative markdown link syntax)
- Nothing above the `_Verifier:_` tail line was changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 07-VERIFICATION.md § Rotation Procedure → future operator | Documentation that becomes an execution script during key rotation — accuracy matters |
| 02-VERIFICATION.md supplement → future agents | Discovery anchor — wrong link would strand a future reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DRIFT-01 | Information Disclosure | 07-VERIFICATION.md body | mitigate | The doc may paste probe-matrix response snippets (first 200 bytes). Plan 01 task already scrubs the key from snippets via `snippet.replaceAll(apiKey, '[redacted]')`. The executor writing this plan should visually scan the pasted matrix for any `****` or alphanumeric strings that look like API keys before committing — none should appear because Plan 01 scrubbed them, but defense in depth. |
| T-DRIFT-17 | Tampering | Rotation Procedure accuracy | mitigate | The procedure references specific files and commands. Task 1 verification includes grep assertions for `scripts/probe-comfy-endpoint.mts` and `platform.comfy.org` — proves those anchor strings survived the write. A future operator running the procedure against v7-baseline files will find them intact. |
| T-DRIFT-18 | Repudiation | Plan 08 memory hygiene dependency | accept | §4 references Plan 08's memory updates. If Plan 08 fails or is skipped, the memory-hygiene claim in §4 is inaccurate. Plan 08 depends on Plan 07, so a failed Plan 08 would surface as a Phase 7 close-out failure — acceptable chain. |
</threat_model>

<verification>
- `07-VERIFICATION.md` exists with 4 H2 sections (grep `^## [1-4]\.`)
- Frontmatter is valid YAML with `status: passed`
- Probe-matrix table is populated from `07-01-SUMMARY.md` (no `<...>` placeholders)
- Rotation Procedure is numbered 1-6 and executable standalone
- `02-VERIFICATION.md` supplement is appended; relative link resolves
- No code files touched by this plan
</verification>

<success_criteria>
- SC-3 partially satisfied (documentation leg complete; memory hygiene is Plan 08)
- Future operators have a single self-contained rotation runbook at `07-VERIFICATION.md §3`
- Phase 2 readers see a forward-pointer to Phase 7 resolution
- Zero placeholder brackets remain
</success_criteria>

<output>
After completion, create `.planning/phases/07-comfyui-endpoint-reconciliation/07-07-SUMMARY.md` capturing:
- Confirmation of `07-VERIFICATION.md` creation with 4 sections + frontmatter
- Confirmation of `02-VERIFICATION.md` append
- Word count or line count of `07-VERIFICATION.md` for rough audit
- Any deviations from the planned structure (e.g., if the probe matrix had fewer than 16 rows because docs-dynamic fetch failed — note the graceful-degradation outcome)
</output>
