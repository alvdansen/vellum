---
phase: 07
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .env
  - .env.example
autonomous: true
requirements: []
success_criteria:
  - "SC-1: .env and .env.example both point at the locked base (single source-of-truth contract)"
tags:
  - phase-07
  - config
  - endpoint-reconciliation
must_haves:
  truths:
    - ".env COMFYUI_API_BASE value matches the Plan 01 probe winner"
    - ".env.example COMFYUI_API_BASE value matches .env exactly (no drift between gitignored secret file and committed template)"
    - ".env.example has a rotation-procedure reference comment pointing at 07-VERIFICATION.md"
    - ".env remains gitignored and chmod 600 (never committed, never echoed)"
  artifacts:
    - path: ".env.example"
      provides: "Committed template with locked base + rotation comment for new contributors"
      contains: "COMFYUI_API_BASE="
      contains_also: "07-VERIFICATION.md"
    - path: ".env"
      provides: "Operator's live secrets file with locked base (gitignored)"
      contains: "COMFYUI_API_BASE="
  key_links:
    - from: ".env.example COMFYUI_API_BASE"
      to: "src/comfyui/client.ts DEFAULT_COMFYUI_API_BASE"
      via: "server boot reads .env.example-shaped variable; default is used when .env is absent or variable is empty"
      pattern: "COMFYUI_API_BASE="
---

<objective>
Update the two config files that mirror `DEFAULT_COMFYUI_API_BASE` — the gitignored `.env` (operator's live secrets) and the committed `.env.example` (contributor template) — to the Plan 01 probe winner. This plan runs in parallel with Plan 02 (Wave 2); both consume the Plan 01 probe output as shared dependency but touch disjoint files, so they never conflict.

Purpose: D-EP-06 locks the single-source-of-truth rule: the locked `COMFYUI_API_BASE` lives in exactly three places (`src/comfyui/client.ts`, `.env`, `.env.example`). Plan 02 owns the code constant; this plan owns the two env files. Pitfall #4 from 07-RESEARCH.md is real — forgetting to sync `.env.example` is a classic drift bug, so the plan treats both updates as one atomic unit even though the files are physically separate.

The `.env.example` file also gets a new rotation comment pointing operators at `07-VERIFICATION.md §3 Rotation Procedure` — the canonical rotation runbook created in Plan 07. This makes the env-file self-documenting for future contributors who never read the phase planning docs.

Output: two files with matching `COMFYUI_API_BASE=<winner>` lines. `.env.example` additionally has a one-line rotation comment. Nothing else in either file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-RESEARCH.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-VALIDATION.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- .env and .env.example are config files, no TypeScript interfaces. -->
<!-- Shape contract: one `COMFYUI_API_BASE=<url>` line per file. -->
<!-- .env is gitignored (confirmed in .gitignore line 1 per 02-CONTEXT.md D-GEN-09). -->
<!-- .env.example is committed — read it directly with the Read tool before editing. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .env.example with locked base + rotation comment</name>
  <files>.env.example</files>

  <read_first>
    - .env.example (FULL file — use the Read tool; file lives at repo root and IS committed to git, so reading is allowed). Identify the existing `COMFYUI_API_BASE=...` line and its surrounding comment convention (single-`#` preceding-line vs inline after value).
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-01-SUMMARY.md — provides `<WINNING_BASE>` value from Plan 01.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md §D-EP-06, D-EP-11, D-EP-12 — locks the update site and the rotation-comment reference target.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md §".env.example (MODIFY — value swap + comment)" — shows the suggested comment text and placement.
  </read_first>

  <action>
**STEP 1: Read `.env.example` via the Read tool** and identify:
- The exact `COMFYUI_API_BASE=` line.
- The file's existing comment convention (are other variables commented with a preceding `# ...` line, or inline `# ...` after the value?).
- Any `COMFYUI_API_BASE=` default value currently present.

Per 02-VERIFICATION.md line 51 (verified during phase 2), the file currently contains `COMFYUI_API_BASE=https://cloud.comfy.org`.

**STEP 2: Locate `<WINNING_BASE>` from `07-01-SUMMARY.md`.** If that file doesn't exist or doesn't contain a winning base, STOP and raise a dependency-missing error.

**STEP 3: Update the `COMFYUI_API_BASE=` line** to the locked value. Exact substitution:

Before:
```
COMFYUI_API_BASE=https://cloud.comfy.org
```

After:
```
# Locked by Phase 7 (Endpoint Reconciliation, 2026-04-24) — see .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md §3 Rotation Procedure
COMFYUI_API_BASE=<WINNING_BASE>
```

If the file's existing convention puts comments on a preceding line (most common pattern for `.env.example` files in the Node ecosystem), use the preceding-line form above. If the existing convention is inline `#` after values, convert to:

```
COMFYUI_API_BASE=<WINNING_BASE>  # Locked by Phase 7 — see .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md §3
```

**STEP 4: Do NOT change any other variables.** `COMFYUI_API_KEY` placeholder, `COMFYUI_ALLOWED_REDIRECT_HOSTS`, and any other lines stay byte-for-byte identical. This plan is surgical.

**STEP 5: Preserve blank lines and section headers.** If the file has `# === ComfyUI Cloud ===`-style section dividers, do not disturb them.

**STEP 6: Verify no secret material is introduced.** The `COMFYUI_API_KEY=` line in `.env.example` MUST remain a placeholder (empty, `your-key-here`, or similar). Never populate a real key into this file (it is committed to git).
  </action>

  <verify>
    <automated>grep -q "^COMFYUI_API_BASE=" .env.example &amp;&amp; grep -q "07-VERIFICATION.md" .env.example &amp;&amp; ( ! grep -E "^COMFYUI_API_KEY=[a-zA-Z0-9]{10,}" .env.example )</automated>
  </verify>

  <done>
- `.env.example` contains exactly one `COMFYUI_API_BASE=<winner>` line
- A comment (preceding-line or inline) references `07-VERIFICATION.md`
- `COMFYUI_API_KEY` value remains empty/placeholder (no real key leaked into committed file)
- No other variables in `.env.example` modified
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .env with locked base (value-only swap, no comments)</name>
  <files>.env</files>

  <read_first>
    - .env (use the Read tool — permission-denied paths may block this; if blocked, fall back to `grep '^COMFYUI_API_BASE=' .env` via Bash to confirm the current line shape without echoing the key). The file is gitignored, chmod 600, and its full contents MUST NOT be echoed in commit messages, summaries, or logs.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-01-SUMMARY.md — `<WINNING_BASE>` value from Plan 01.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md §".env (MODIFY, gitignored — value swap only)" — locks the "value only, no comment" contract for .env.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md §"Specific Values" — enumerates the three update sites for the locked base.
    - CLAUDE.md §"Never commit secrets, .env files, or credentials" — this task edits `.env` but never commits or echoes it.
  </read_first>

  <action>
**STEP 1: Read `.env`** using the Read tool to identify the current `COMFYUI_API_BASE=` line and its value. Do NOT print the `COMFYUI_API_KEY` value in any output, summary, or log. The drift memory (2026-04-22) indicates the current value is likely `https://api.comfy.org`.

**STEP 2: Locate `<WINNING_BASE>` from `07-01-SUMMARY.md`.**

**STEP 3: Replace the `COMFYUI_API_BASE=` line** with the locked value. Exact substitution:

Before (likely):
```
COMFYUI_API_BASE=https://api.comfy.org
```

After:
```
COMFYUI_API_BASE=<WINNING_BASE>
```

**STEP 4: Do NOT add any comment to `.env`.** Per 07-PATTERNS.md the operator's secrets file stays minimal (one key/value per line). The rotation-procedure reference lives in `.env.example` only.

**STEP 5: Preserve every other variable in `.env` byte-for-byte.** `COMFYUI_API_KEY`, `COMFYUI_ALLOWED_REDIRECT_HOSTS`, any debug flags — untouched.

**STEP 6: Verify file permissions remain chmod 600.** If they weren't, run `chmod 600 .env` to restore. Per CLAUDE.md + memory `reference_env_comfyui_key.md`, `.env` must stay non-world-readable.

**STEP 7: Do NOT print the file contents in any summary or commit message.** Verification commands in this task use `grep '^COMFYUI_API_BASE='` so the line (not the key) is visible for audit; the key line must never be grepped or printed.
  </action>

  <verify>
    <automated>grep -q "^COMFYUI_API_BASE=" .env &amp;&amp; test "$(stat -f '%Lp' .env 2>/dev/null || stat -c '%a' .env 2>/dev/null)" = "600"</automated>
  </verify>

  <done>
- `.env` contains exactly one `COMFYUI_API_BASE=<winner>` line matching the value in `.env.example`
- `.env` file permissions are `0600`
- `.env` is NOT staged for commit (git status shows it as ignored)
- No key material appears in task output, summary, or commit message
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify cross-file consistency between .env and .env.example</name>
  <files></files>

  <read_first>
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-RESEARCH.md §"Common Pitfalls > Pitfall 4: .env.example drift" (around line 576) — the anti-pattern this verification prevents.
  </read_first>

  <action>
Run a grep comparison to confirm `COMFYUI_API_BASE=` value is identical across the three source-of-truth sites. This task performs NO file modification — it's a sanity check that Tasks 1 + 2 landed cleanly and Plan 02 locked the same value.

**STEP 1: Extract the locked value from each site.**

```bash
grep '^COMFYUI_API_BASE=' .env | sed 's/^COMFYUI_API_BASE=//' > /tmp/phase7-env-base.txt
grep '^COMFYUI_API_BASE=' .env.example | sed 's/^COMFYUI_API_BASE=//' > /tmp/phase7-example-base.txt
grep 'export const DEFAULT_COMFYUI_API_BASE = ' src/comfyui/client.ts | sed "s/.*'\\(.*\\)'.*/\\1/" > /tmp/phase7-code-base.txt
```

**STEP 2: Compare the three values.**

All three files MUST contain the same base URL string (up to trailing whitespace). If they differ, the plan failed — Plan 02 and Plan 03 diverged. The discrepancy must be resolved before proceeding to Wave 3.

```bash
if ! diff -q /tmp/phase7-env-base.txt /tmp/phase7-example-base.txt > /dev/null; then
  echo "FAIL: .env and .env.example COMFYUI_API_BASE values differ"
  exit 1
fi
if ! diff -q /tmp/phase7-example-base.txt /tmp/phase7-code-base.txt > /dev/null; then
  echo "FAIL: .env.example and src/comfyui/client.ts DEFAULT_COMFYUI_API_BASE values differ"
  exit 1
fi
echo "OK: all three sites consistent"
rm /tmp/phase7-env-base.txt /tmp/phase7-example-base.txt /tmp/phase7-code-base.txt
```

**STEP 3: Record the verification outcome in the plan summary.** Note any discrepancies + how they were resolved.

This task does NOT touch the filesystem beyond the `/tmp` scratch files. It's a pure verification step that Plan 06 (live-smoke gate) depends on — a divergence here means the healthcheck will never match the probe winner and live-smoke will fail for an entirely mechanical reason.
  </action>

  <verify>
    <automated>test "$(grep '^COMFYUI_API_BASE=' .env.example | sed 's/^COMFYUI_API_BASE=//')" = "$(grep '^COMFYUI_API_BASE=' .env | sed 's/^COMFYUI_API_BASE=//')" &amp;&amp; test "$(grep '^COMFYUI_API_BASE=' .env.example | sed 's/^COMFYUI_API_BASE=//')" = "$(grep 'export const DEFAULT_COMFYUI_API_BASE = ' src/comfyui/client.ts | sed "s/.*'\\([^']*\\)'.*/\\1/")"</automated>
  </verify>

  <done>
- All three sites (`.env`, `.env.example`, `src/comfyui/client.ts`) report the same `COMFYUI_API_BASE` / `DEFAULT_COMFYUI_API_BASE` value
- No `/tmp` scratch files remain
- Summary records the verified-consistent base URL
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env (gitignored) → server process | Loaded via `dotenv/config` at server boot; contains secret key material |
| .env.example (committed) → git | Template for new contributors; must never contain real secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DRIFT-04 | Information Disclosure | .env.example | mitigate | Task 1 verification includes a grep check that `COMFYUI_API_KEY=` value is empty or placeholder. The executor follows a strict "value-swap only on COMFYUI_API_BASE line" discipline; no `COMFYUI_API_KEY` modifications. Grep-verifiable: `! grep -E "^COMFYUI_API_KEY=[a-zA-Z0-9]{10,}" .env.example`. |
| T-DRIFT-10 | Information Disclosure | .env contents in task output | mitigate | Task 2 instructions explicitly forbid printing `.env` contents. Verification commands use `grep '^COMFYUI_API_BASE='` scoped to the base line only — never touches the key line. File permissions assertion (chmod 600) confirms the file stays non-world-readable. |
| T-DRIFT-11 | Tampering | cross-file drift | mitigate | Task 3 is a verification task that fails loudly if `.env`, `.env.example`, and `client.ts` diverge. Plan 06 (live-smoke) would otherwise fail for mechanical reasons masking any real drift issue. |
</threat_model>

<verification>
- `.env.example` has the locked `COMFYUI_API_BASE` value + rotation comment referencing `07-VERIFICATION.md`
- `.env` has the locked `COMFYUI_API_BASE` value (chmod 600)
- Cross-file consistency check (Task 3) passes — all three sites report identical strings
- `git status` shows `.env.example` as modified; `.env` is not staged
</verification>

<success_criteria>
- SC-1 fully satisfied (code-side and env-side both locked to the probe winner)
- Single-source-of-truth contract (D-EP-06) holds: three files, one value
- Rotation-procedure pointer visible in `.env.example` for future contributors
- Zero secret material leaked to git or task output
</success_criteria>

<output>
After completion, create `.planning/phases/07-comfyui-endpoint-reconciliation/07-03-SUMMARY.md` capturing:
- Confirmation that `.env.example` was updated (with the new base URL redacted to `<locked>` if desired; the actual URL is non-sensitive but redact to match the audit convention)
- Confirmation that `.env` was updated AND chmod 600 preserved
- Cross-file consistency check result (PASS)
- Any deviations from the planned comment style (e.g., if `.env.example` used inline comments)
</output>
