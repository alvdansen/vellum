---
phase: 07
plan: 08
type: execute
wave: 5
depends_on: [07]
files_modified:
  - /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md
  - /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/reference_env_comfyui_key.md
  - /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/MEMORY.md
autonomous: true
requirements: []
success_criteria:
  - "SC-3: Project memories reflect resolved state — drift memory marked RESOLVED (or removed), reference memory updated to locked base, MEMORY.md index synchronized"
tags:
  - phase-07
  - memory-hygiene
  - cleanup
must_haves:
  truths:
    - "`project_comfy_api_endpoint_drift.md` is either marked `RESOLVED <date> → 07-VERIFICATION.md` OR removed entirely (per D-EP-15 second-consecutive-green-run criterion)"
    - "`reference_env_comfyui_key.md` body reflects the new locked `COMFYUI_API_BASE` (drift claim removed)"
    - "`MEMORY.md` index entries match the post-Phase-7 memory states (drift entry removed or updated; reference entry updated)"
    - "No information leakage — the new memories do NOT contain `COMFYUI_API_KEY` values or rotated-key details"
  artifacts:
    - path: "/Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md"
      provides: "RESOLVED marker or removed"
    - path: "/Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/reference_env_comfyui_key.md"
      provides: "Updated to reflect locked base"
    - path: "/Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/MEMORY.md"
      provides: "Index synchronized"
  key_links:
    - from: "memory/reference_env_comfyui_key.md"
      to: ".planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md"
      via: "body line references Phase 7 VERIFICATION doc as the canonical source for the locked base"
      pattern: "07-VERIFICATION"
---

<objective>
Update the three Claude-project memory files (`project_comfy_api_endpoint_drift.md`, `reference_env_comfyui_key.md`, `MEMORY.md`) to reflect the resolved state after Phase 7 live-smoke green-twice confirmation. This is the final closing task for Phase 7 — after this plan the drift memory either points at the Phase 7 resolution doc or is removed entirely, and the reference memory no longer makes claims that contradict the current `.env` contents.

Purpose: Memory files are Claude's project-scoped state. They load into every future session for this project. A stale drift memory saying "live-smoke fails on both hosts as of 2026-04-22" would confuse every future Claude session, even though the drift is resolved — agents might avoid touching live-smoke code, or repeat Phase 7's research. An accurate memory is the equivalent of a well-maintained comment: it saves future cognitive cost.

D-EP-15 lays out the exact memory operations:
1. **Drift memory** — mark `RESOLVED <date> → 07-VERIFICATION.md` OR remove entirely (criterion: live-smoke green for the SECOND CONSECUTIVE run; Plan 06 checkpoint delivered this).
2. **Reference memory** — update body line that currently asserts `COMFYUI_API_BASE=https://api.comfy.org` to match the Plan 02 / 03 locked base.
3. **MEMORY.md index** — reflect both of the above.

Output: three file edits (or two edits + one removal). No code changes. No leaked keys. No changes outside the `~/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-RESEARCH.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-06-SUMMARY.md
@.planning/phases/07-comfyui-endpoint-reconciliation/07-07-SUMMARY.md

<interfaces>
<!-- Memory file shapes (read these before editing). All three live in: -->
<!-- ~/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/ -->

Current contents of `project_comfy_api_endpoint_drift.md` (frontmatter + body):
```markdown
---
name: ComfyUI Cloud API endpoint drift
description: The ComfyUI Cloud public API has multiple hosts that don't overlap — `cloud.comfy.org` has `/api/prompt` but rejected our key as 401; `api.comfy.org` accepts our key but returns 404 on `/api/prompt`. Live-smoke tests have been effectively broken since at least 2026-04-22.
type: project
originSessionId: 93103d4e-7653-4b8a-9557-824c18157184
---
<body paragraphs about the drift observation, resolution paths, how-to-apply notes>
```

Current contents of `reference_env_comfyui_key.md`:
```markdown
---
name: ComfyUI Cloud API key location
description: Where the official ComfyUI Cloud API key and base URL are stored for this project
type: reference
originSessionId: 7412e4d7-f32d-4137-b135-b25cd469eb23
---
The ComfyUI Cloud API credentials live in `.env` at the project root:
- `COMFYUI_API_KEY` — official api.comfy.org key
- `COMFYUI_API_BASE=https://api.comfy.org`

`.env` is gitignored (`.gitignore:1`) and chmod 600. Never commit it, never echo the key in responses...
```

Current contents of `MEMORY.md`:
```markdown
- [Don't punt on tests](feedback_dont_punt_on_tests.md) — If a "human UAT" item is wire-level (tool calls, JSON-RPC, HTTP), drive it with MCP SDK client or curl before escalating
- [ComfyUI API key location](reference_env_comfyui_key.md) — COMFYUI_API_KEY + COMFYUI_API_BASE live in .env at repo root; gitignored, chmod 600, never echo
- [ComfyUI API endpoint drift](project_comfy_api_endpoint_drift.md) — `cloud.comfy.org` vs `api.comfy.org` diverged; live-smoke fails on both as of 2026-04-22; needs key/base reconciliation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve or remove project_comfy_api_endpoint_drift.md</name>
  <files>/Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md</files>

  <read_first>
    - /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md (FULL — 20 lines). Confirms current state: frontmatter includes `originSessionId: 93103d4e-...`, body describes the 401/404 drift. MUST be read before editing to preserve unmodified frontmatter fields.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-06-SUMMARY.md — confirms live-smoke was green on TWO consecutive runs (the D-EP-15 bar for removal-vs-marked-resolved).
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md §D-EP-15 — decision locks the two valid actions.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md §"project_comfy_api_endpoint_drift.md (MODIFY — mark RESOLVED or remove)" — shows both valid patterns.
  </read_first>

  <action>
**STEP 1: Decide between RESOLVED-mark and removal.**

Per D-EP-15, removal is preferred when "live-smoke green for the second consecutive run" — that bar was met in Plan 06. Remove the file entirely:

```bash
rm /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md
```

Rationale for preferring removal: the drift memory was a **point-in-time observation** that is no longer true. Marking it RESOLVED keeps stale-by-default content in Claude's memory loader (with a `RESOLVED` header, but still loaded into every session). Removing it cleans the memory deck. The cross-reference in `reference_env_comfyui_key.md` (updated by Task 2) + `07-VERIFICATION.md` (written by Plan 07) preserve the historical trail for anyone who needs it.

**STEP 2 (fallback — only if removal is declined by user policy):** mark RESOLVED in-place.

If the operator prefers to keep the memory file with a RESOLVED marker (e.g., to preserve the observation history), edit the file instead of removing. Use the Edit tool:

- Leave `frontmatter.originSessionId` intact (do not modify).
- Change `frontmatter.description` to start with `RESOLVED 2026-04-24 → see .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md.` followed by a brief past-tense recap.
- Insert at the top of the body: `**RESOLVED 2026-04-24** — see [07-VERIFICATION.md](../../../comfyui-vfx-mcp/.planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md).`
- Leave the rest of the body intact.

**STEP 3: Verify the resolution.**

Either the file no longer exists:
```bash
test ! -f /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md && echo "OK: removed"
```

Or the file exists with a RESOLVED marker:
```bash
grep -q "RESOLVED" /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md && echo "OK: marked"
```

Task 3 verification also depends on this outcome — if the file was removed, `MEMORY.md` must drop the matching index line entirely; if RESOLVED, the index line is rewritten.

**STEP 4: NEVER print the memory file contents in task output or summary.** The memory path is personal to this developer; the summary only records action taken ("removed" or "marked RESOLVED") — never body contents.
  </action>

  <verify>
    <automated>test ! -f /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md || grep -q "RESOLVED" /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/project_comfy_api_endpoint_drift.md</automated>
  </verify>

  <done>
- File is either absent (removed) OR present with `RESOLVED` in the frontmatter description + body header
- If marked RESOLVED, `originSessionId` field is preserved byte-for-byte
- No memory content printed to task output
- Decision (removed vs marked) recorded in `07-08-SUMMARY.md`
  </done>
</task>

<task type="auto">
  <name>Task 2: Update reference_env_comfyui_key.md body to reflect the locked base</name>
  <files>/Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/reference_env_comfyui_key.md</files>

  <read_first>
    - /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/reference_env_comfyui_key.md (FULL — 14 lines). Current body line 9 asserts `COMFYUI_API_BASE=https://api.comfy.org` — which is the stale claim that needs updating.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-01-SUMMARY.md — the `<WINNING_BASE>` value to substitute.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md §D-EP-15 — locks the memory update contract.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md §"reference_env_comfyui_key.md (MODIFY — body update)" — suggests body edits + sentence to append.
  </read_first>

  <action>
Use the Edit tool to update two lines in the body and append one sentence. Frontmatter stays intact.

**Target body (post-edit):**

```markdown
The ComfyUI Cloud API credentials live in `.env` at the project root:
- `COMFYUI_API_KEY` — official ComfyUI Cloud key issued at https://platform.comfy.org
- `COMFYUI_API_BASE=<WINNING_BASE>`

`.env` is gitignored (`.gitignore:1`) and chmod 600. Never commit it, never echo the key in responses, never write it into any file that gets committed (including CLAUDE.md, .planning/, or code comments).

Phase 2 (`comfyui-generation`) is the first phase that will read these — expect `dotenv` or similar to load `.env` when the server boots in generation mode. Locked as of Phase 7 (2026-04-24) — see `.planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md §2 Credential Layout` for the full source-of-truth contract and `§3 Rotation Procedure` for rotation steps.
```

**Specific edits (use Edit tool per change):**

1. **Line 8** (currently `- \`COMFYUI_API_KEY\` — official api.comfy.org key`):
   - Replace `api.comfy.org key` with `ComfyUI Cloud key issued at https://platform.comfy.org`.
   - This updates the stale "api.comfy.org" claim to the docs-canonical issuance URL.

2. **Line 9** (currently `- \`COMFYUI_API_BASE=https://api.comfy.org\``):
   - Replace `https://api.comfy.org` with `<WINNING_BASE>` from `07-01-SUMMARY.md`.

3. **Append at end of body** (after the existing final line):
   - Add the closing sentence: `Locked as of Phase 7 (2026-04-24) — see \`.planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md §2 Credential Layout\` for the full source-of-truth contract and \`§3 Rotation Procedure\` for rotation steps.`

**CRITICAL: Frontmatter stays intact.** Do NOT modify `name`, `description`, `type`, or `originSessionId`. Those four fields remain byte-for-byte.

**STEP: Do NOT print the file contents in summary output.** Verification confirms the edits landed via grep against the expected post-state — no key material is involved (the key's HOST is now documented as platform.comfy.org for issuance, which is public information).
  </action>

  <verify>
    <automated>grep -q 'platform.comfy.org' /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/reference_env_comfyui_key.md &amp;&amp; grep -q '07-VERIFICATION.md' /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/reference_env_comfyui_key.md &amp;&amp; ! grep -q 'COMFYUI_API_BASE=https://api.comfy.org' /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/reference_env_comfyui_key.md &amp;&amp; grep -q 'originSessionId: 7412e4d7' /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/reference_env_comfyui_key.md</automated>
  </verify>

  <done>
- Body line referencing `api.comfy.org` is gone
- New body line points at `<WINNING_BASE>` (Plan 01 winner)
- New closing sentence references `07-VERIFICATION.md §2` and `§3`
- `originSessionId: 7412e4d7-...` preserved verbatim in frontmatter
- Other frontmatter fields (name, description, type) unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Synchronize MEMORY.md index entries with the post-Phase-7 states</name>
  <files>/Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/MEMORY.md</files>

  <read_first>
    - /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/MEMORY.md (FULL — 3 lines). Current file has three index entries.
    - Task 1 outcome (file removed or marked RESOLVED) determines the action for line 3 of this index.
    - Task 2 outcome (body updated) determines the action for line 2 of this index.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-CONTEXT.md §D-EP-15 (last bullet) — locks the index-entries-update contract.
    - .planning/phases/07-comfyui-endpoint-reconciliation/07-PATTERNS.md §"MEMORY.md (MODIFY — index entries)" — shows the exact format + both valid post-states.
  </read_first>

  <action>
Use the Edit tool to update two lines in `MEMORY.md`. The file format is unambiguous: `- [Title](filename.md) — one-line summary` with a single dash, single space, square brackets, and em-dash separator.

**STEP 1: Update line 2** (the `reference_env_comfyui_key.md` entry).

Current:
```markdown
- [ComfyUI API key location](reference_env_comfyui_key.md) — COMFYUI_API_KEY + COMFYUI_API_BASE live in .env at repo root; gitignored, chmod 600, never echo
```

New (substitute `<WINNING_BASE>` from 07-01-SUMMARY.md):
```markdown
- [ComfyUI API key location](reference_env_comfyui_key.md) — COMFYUI_API_KEY + COMFYUI_API_BASE=<WINNING_BASE> live in .env at repo root; gitignored, chmod 600, never echo; locked Phase 7 (2026-04-24)
```

**STEP 2: Update line 3** (the drift memory entry) based on Task 1 outcome:

**If Task 1 REMOVED the file** (preferred per D-EP-15):
- DELETE line 3 entirely.
- The file ends at line 2 (`reference_env_comfyui_key.md`) after line 1 (`feedback_dont_punt_on_tests.md`).
- Three-entry index becomes two-entry.

**If Task 1 MARKED the file RESOLVED:**
- Replace line 3 summary with: `RESOLVED Phase 7 2026-04-24 → see .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md`.
- Full new line 3:
  ```markdown
  - [ComfyUI API endpoint drift](project_comfy_api_endpoint_drift.md) — RESOLVED Phase 7 2026-04-24 → see .planning/phases/07-comfyui-endpoint-reconciliation/07-VERIFICATION.md
  ```

**STEP 3: Do NOT modify line 1** (`feedback_dont_punt_on_tests.md`). That memory is still accurate — Phase 7 actually reinforced it (live-smoke was the wire-level gate; no human-UAT escalation was taken).

**STEP 4: Verify the file ends without trailing blank lines** (match the original file's shape — each entry on its own line, no double newlines between entries).
  </action>

  <verify>
    <automated>grep -q 'feedback_dont_punt_on_tests' /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/MEMORY.md &amp;&amp; grep -q 'reference_env_comfyui_key' /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/MEMORY.md &amp;&amp; grep -q 'Phase 7' /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/MEMORY.md &amp;&amp; ! grep -q 'live-smoke fails on both as of 2026-04-22' /Users/macapple/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/MEMORY.md</automated>
  </verify>

  <done>
- Line 1 (`feedback_dont_punt_on_tests.md`) unchanged
- Line 2 (`reference_env_comfyui_key.md`) mentions `<WINNING_BASE>` and `locked Phase 7`
- Line 3: either absent (if Task 1 removed file) OR rewritten with `RESOLVED Phase 7 2026-04-24 → ... 07-VERIFICATION.md`
- No trailing stale content about the drift
- File line count is 2 or 3 depending on Task 1 choice
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory/*.md → future Claude sessions | Memory files load at session start; stale content shapes agent reasoning |
| Task output → git diff / commit | Memory file paths are outside the repo but mentioned in plan artifacts; ensure no key material leaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DRIFT-01 | Information Disclosure | memory file edits | mitigate | The three memory edits touch only the base URL (public information) and cross-reference paths — no key material. Task instructions explicitly forbid printing file contents in summaries. `reference_env_comfyui_key.md` still reminds future agents "never commit it, never echo the key" — reinforcing the hygiene rule. |
| T-DRIFT-05 | Repudiation | memory audit trail | mitigate | If Task 1 removes the drift memory, the historical context is preserved via (a) `07-VERIFICATION.md` canonical doc, (b) the reference memory's closing sentence pointing at Phase 7, (c) the MEMORY.md line 2 note "locked Phase 7". Three independent forward-pointers survive the removal. |
| T-DRIFT-19 | Tampering | frontmatter preservation | mitigate | Task 2 verification includes `grep -q 'originSessionId: 7412e4d7'` — proves the frontmatter anchor survived. If Task 1 chose the RESOLVED path, its verify also includes the originSessionId check (applied on-file-exists branch). |
</threat_model>

<verification>
- Task 1: drift memory is either removed OR marked RESOLVED (both paths verified in the automated check)
- Task 2: reference memory body updated; `api.comfy.org` claim gone; `07-VERIFICATION.md` reference added; `originSessionId` preserved
- Task 3: MEMORY.md index matches post-state; line 1 untouched; drift index entry removed or rewritten
- No key material printed in any task output or summary
- All three files inside `~/.claude/projects/-Users-macapple-comfyui-vfx-mcp/memory/` — no changes outside that dir
</verification>

<success_criteria>
- SC-3 fully satisfied: canonical doc exists (Plan 07) + project memories accurately reflect resolved state
- Future Claude sessions load correct base URL from reference memory (no more stale `api.comfy.org` claim)
- Drift memory no longer misleads agents about the 2026-04-22 point-in-time observation
- MEMORY.md index is consistent with the actual memory files present
- Phase 7 is COMPLETE — SC-1 (locked base, probe, env) + SC-2 (live-smoke green twice) + SC-3 (documented + memory-hygienic) all met
</success_criteria>

<output>
After completion, create `.planning/phases/07-comfyui-endpoint-reconciliation/07-08-SUMMARY.md` capturing:
- Task 1 outcome (file REMOVED or MARKED RESOLVED) with rationale
- Task 2 confirmation (body updated, frontmatter preserved)
- Task 3 confirmation (index synchronized; final file line count: 2 or 3)
- Phase 7 close-out note: "SC-1 / SC-2 / SC-3 all met — ready for `/gsd-verify-phase 07`."
</output>
