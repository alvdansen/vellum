---
phase: 08-doc-attribution-backfill
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - .planning/phases/02-comfyui-generation/02-VERIFICATION.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements: []
requirements_addressed: []
tags: [verification, supplement, mcp-sdk-1.29, zod, envelope, audit, resolution-note, docs]

must_haves:
  truths:
    - "02-VERIFICATION.md contains a new H2 section heading 'MCP SDK 1.29 Zod inputSchema Envelope Caveat (Phase 8, 2026-04-24)' appended at end of file (after the existing Phase 7 supplement)"
    - "The supplement contains three concise paragraphs: runtime behavior, visible symptom (with verbatim JSON-RPC repro from INSPECTOR-SMOKE.md §3), and engine-layer contrast"
    - "The supplement does NOT contain a fix proposal or flattenZodError() helper code (forward-projection only)"
    - "v1.0-MILESTONE-AUDIT.md tech_debt.phase: 01-foundation-hierarchy.items entries reference 'Resolved by Phase 8 (2026-04-24)' (append-only; original prose preserved)"
    - "v1.0-MILESTONE-AUDIT.md frontmatter status remains 'tech_debt' (Phase 8 closes 3 of 9 items but does not change milestone status — that's /gsd-complete-milestone's job)"
    - "No mirroring of the SDK caveat into 03-VERIFICATION.md, 04-VERIFICATION.md, or 05-VERIFICATION.md (D-ATTR-11 single canonical home)"
  artifacts:
    - path: ".planning/phases/02-comfyui-generation/02-VERIFICATION.md"
      provides: "Phase 8 supplement: SDK 1.29 Zod inputSchema envelope caveat"
      contains: "MCP SDK 1.29 Zod inputSchema Envelope Caveat (Phase 8, 2026-04-24)"
    - path: ".planning/v1.0-MILESTONE-AUDIT.md"
      provides: "Phase 01 tech_debt items annotated as resolved by Phase 8"
      contains: "Resolved by Phase 8 (2026-04-24)"
  key_links:
    - from: ".planning/phases/02-comfyui-generation/02-VERIFICATION.md"
      to: ".planning/phases/01-foundation-hierarchy/INSPECTOR-SMOKE.md"
      via: "supplement §2 cite of live evidence (INSPECTOR-SMOKE.md §3 SH010 repro)"
      pattern: "INSPECTOR-SMOKE\\.md"
    - from: ".planning/phases/02-comfyui-generation/02-VERIFICATION.md"
      to: ".planning/phases/01-foundation-hierarchy/01-VERIFICATION.md"
      via: "supplement intro cite of frontmatter inspector_smoke_automation.notes"
      pattern: "01-VERIFICATION\\.md"
    - from: ".planning/phases/02-comfyui-generation/02-VERIFICATION.md"
      to: "src/tools/envelope.ts"
      via: "supplement §3 cite of toolError TypedError -> structuredContent.code mapping"
      pattern: "envelope\\.ts"
    - from: ".planning/v1.0-MILESTONE-AUDIT.md"
      to: ".planning/phases/08-doc-attribution-backfill/08-VERIFICATION.md"
      via: "forward-reference resolution note (08-VERIFICATION.md written at phase close, not here)"
      pattern: "08-VERIFICATION\\.md"
---

<objective>
Append two append-only annotations: (1) a Phase 8 supplement section to `02-VERIFICATION.md` documenting the MCP SDK 1.29 Zod `inputSchema` envelope caveat in three paragraphs (runtime behavior + visible symptom + engine-layer contrast); (2) "Resolved by Phase 8 (2026-04-24)" notes on the three Phase 01 tech_debt items in `v1.0-MILESTONE-AUDIT.md`. Both edits are append-only / suffix-only — no editing of historical prose. Mirrors Phase 7's supplement-in-upstream-VERIFICATION.md pattern (D-EP-11/D-EP-12).

Purpose: D-ATTR-09 + D-ATTR-10 capture the SDK caveat with a single canonical home (D-ATTR-11 — no mirroring); D-ATTR-03 marks the audit's tech_debt rows resolved; D-ATTR-04 enforces scope discipline (no REQUIREMENTS.md Traceability table refresh — that's `/gsd-complete-milestone`'s job).

Output:
- 1 modified VERIFICATION file (Phase 8 supplement section appended at end)
- 1 modified audit file (resolution notes appended to Phase 01 tech_debt items)
- 2 commits (one per task)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-doc-attribution-backfill/08-CONTEXT.md
@.planning/phases/08-doc-attribution-backfill/08-PATTERNS.md

@.planning/phases/02-comfyui-generation/02-VERIFICATION.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/01-foundation-hierarchy/01-VERIFICATION.md
@.planning/phases/01-foundation-hierarchy/INSPECTOR-SMOKE.md

<interfaces>
<!-- Phase 7's supplement is the literal template for Phase 8's supplement. -->
<!-- Append at end of 02-VERIFICATION.md, immediately after the Phase 7 block. -->

Phase 7 supplement (02-VERIFICATION.md lines 158-161, EXISTING):
```markdown
 ---

## Endpoint Reconciliation (Phase 7, 2026-04-24)

The Phase 2 live-smoke entry ... See [`07-VERIFICATION.md`](../07-comfyui-endpoint-reconciliation/07-VERIFICATION.md) for the probe matrix, credential layout, rotation procedure, and fallback-if-redirected behaviour.
```

Phase 8 supplement structure (to be appended after the Phase 7 block):
- `---` separator (Phase 7 used one; D-ATTR-09 + Claude's Discretion line 76: follow same shape)
- `## MCP SDK 1.29 Zod inputSchema Envelope Caveat (Phase 8, 2026-04-24)` (verbatim heading per CONTEXT line 194)
- 3 concise paragraphs (D-ATTR-10):
  1. Runtime behavior
  2. Visible symptom (with verbatim JSON-RPC repro from INSPECTOR-SMOKE.md §3 lines 97-118)
  3. Engine-layer contrast (cites src/tools/envelope.ts:32-60, src/store/hierarchy-repo.ts:55-63 + :95-101, src/engine/pipeline.ts:19,275-284)

Verbatim Zod intercept JSON-RPC response (from INSPECTOR-SMOKE.md §3 — must be embedded as fenced code block in §2):
```json
{
  "result": {
    "content": [{ "type": "text", "text": "MCP error -32602: Input validation error: Invalid arguments for tool shot: [{..., \"path\": [\"name\"], \"message\": \"INVALID_SHOT_FORMAT\"}]" }],
    "isError": true
  },
  "jsonrpc": "2.0",
  "id": 2
}
```

v1.0-MILESTONE-AUDIT.md frontmatter context (DO NOT MODIFY status field):
```yaml
 ---
milestone: v1.0
audited: 2026-04-23T23:00:00Z
status: tech_debt           # ← stays "tech_debt" — Phase 8 closes 3 of 9 items, not all 9
...
tech_debt:
  - phase: 01-foundation-hierarchy
    items:
      - "Inspector UI UX smoke checks ..."           # item 1 — append resolution suffix
      - "Zod inputSchema errors do not flow ..."     # item 2 — append resolution suffix
      - "5 requirement attributions ..."             # item 3 — append resolution suffix
  - phase: 02-comfyui-generation
    items: ...                                       # untouched (Phase 7 already resolved these)
  - phase: 05-web-dashboard
    items: ...                                       # untouched
```

Resolution suffix template (verbatim from CONTEXT line 195):
`Resolved by Phase 8 (2026-04-24) — see .planning/phases/08-doc-attribution-backfill/08-VERIFICATION.md.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append MCP SDK 1.29 Zod inputSchema Envelope Caveat supplement to 02-VERIFICATION.md (D-ATTR-09 + D-ATTR-10 + D-ATTR-11)</name>
  <files>.planning/phases/02-comfyui-generation/02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-comfyui-generation/02-VERIFICATION.md (lines 155-162 — end of file with Phase 7 supplement; supplement appends AFTER this)
    - .planning/phases/01-foundation-hierarchy/01-VERIFICATION.md (lines 9-17 — frontmatter `inspector_smoke_automation.notes` is the seed text; supplement expands this into 3 paragraphs)
    - .planning/phases/01-foundation-hierarchy/INSPECTOR-SMOKE.md (lines 95-118 — §3 SH010 repro with verbatim JSON-RPC response — embed in §2 of the supplement)
    - src/tools/envelope.ts (lines 32-60 — toolError TypedError → structuredContent.code mapping; cite in §3)
    - src/tools/shot-tool.ts (line 32 — Zod regex with sentinel message; lines 106-118 — handler ZodError catch + sentinel-detect-and-remap path; cite both in §1)
    - src/engine/pipeline.ts (line 19 imports SHOT_NAME_REGEX; lines 275-284 createShot block with regex test + INVALID_SHOT_FORMAT TypedError throw at line 279; cite in §3)
    - .planning/phases/08-doc-attribution-backfill/08-CONTEXT.md (lines 53-61, 194, 205-215 — D-ATTR-09/10/11 specifications + verbatim heading + verbatim JSON repro)
    - .planning/phases/08-doc-attribution-backfill/08-PATTERNS.md (lines 354-389 — full supplement target with citation requirements)
  </read_first>
  <action>
Use the Edit tool to APPEND a new H2 section at the END of `.planning/phases/02-comfyui-generation/02-VERIFICATION.md`. The supplement goes IMMEDIATELY AFTER the existing Phase 7 supplement (which ends at line 161 with the sentence ending "fallback-if-redirected behaviour.").

**Append-only constraint:** Do NOT modify any content above line 161. Phase 7's supplement (lines 158-161) stays exactly as it is. The frontmatter (lines 1-8) stays untouched — `overrides_applied: 0` does NOT change (D-ATTR-09: this is informational forward-projection, not an override).

**Verbatim text to append (use this exact content — incorporates D-ATTR-09 heading + D-ATTR-10's three paragraphs + D-ATTR-11 single canonical home):**

```markdown

 ---

## MCP SDK 1.29 Zod inputSchema Envelope Caveat (Phase 8, 2026-04-24)

**Runtime behavior.** MCP SDK 1.29 runs each tool's `inputSchema` validator before the tool handler is invoked. Any `z.ZodError` thrown by the schema surfaces at the SDK boundary, not inside the handler's `try/catch`. Concrete example: a `shot action=create` request with `name: "SH010"` triggers Zod's `^sh\d{3,}$` regex check at `src/tools/shot-tool.ts:32` and fails BEFORE the handler's catch block at `:106-118` ever runs. The handler's sentinel-detection path (which would emit `TypedError('INVALID_SHOT_FORMAT')` via `toolError`) is shadowed by the SDK's intercept on this code path.

**Visible symptom.** The wire-level response shape is `{ isError: true, content: [{ type: "text", text: "MCP error -32602: Input validation error: ..." }] }`. The sentinel message (`INVALID_SHOT_FORMAT`) is embedded inside `content[0].text` via the SDK's error message; `structuredContent.code` is **not populated** for SDK-intercepted Zod errors. Live decoded JSON-RPC response captured in `../01-foundation-hierarchy/INSPECTOR-SMOKE.md` §3:

```json
{
  "result": {
    "content": [{ "type": "text", "text": "MCP error -32602: Input validation error: Invalid arguments for tool shot: [{..., \"path\": [\"name\"], \"message\": \"INVALID_SHOT_FORMAT\"}]" }],
    "isError": true
  },
  "jsonrpc": "2.0",
  "id": 2
}
```

This diverges from the typed-envelope contract (`src/tools/envelope.ts:13-18` `toolOk` and `:32-60` `toolError`) where `structuredContent.code` IS populated for handler-thrown TypedErrors. See `01-VERIFICATION.md` `inspector_smoke_automation.notes[0]` for the original observation.

**Engine-layer contrast.** TypedErrors thrown inside the handler body — e.g. `DUPLICATE_NAME` from `src/store/hierarchy-repo.ts:55-63` (unique-violation wrapping) and `PARENT_NOT_FOUND` from `src/store/hierarchy-repo.ts:95-101` (parent pre-check) — DO populate `structuredContent.code` correctly. The defense-in-depth shot-regex enforcement at `src/engine/pipeline.ts:19,275-284` still fires for non-SDK callers (direct engine calls, test harnesses, alternative adapters), so `INVALID_SHOT_FORMAT` via the typed envelope is still **reachable** — just not via the MCP handler path on the current SDK version. The Phase 1 T2 pattern (Zod-at-tool + regex-at-engine, established in `01-02-SUMMARY.md` line 58) holds end-to-end for non-SDK callers; SDK-intercept is a single-layer regression on the MCP handler path only. Follow-up for Phase 2+ (non-blocking): wrap the SDK boundary so Zod errors flow through the typed envelope. Documented as future work in `01-VERIFICATION.md` `inspector_smoke_automation.notes[1]`; not in scope for Phase 8.
```

**Critical formatting requirements (D-ATTR-10 and Claude's Discretion line 76):**
- Begin the appended block with `\n---\n\n` (horizontal rule separator on its own line, blank line before the H2) — matches Phase 7's shape on line 158
- H2 heading is verbatim `## MCP SDK 1.29 Zod inputSchema Envelope Caveat (Phase 8, 2026-04-24)` — use exactly this text
- Three paragraphs separated by blank lines, NOT three subsections with `###` headers — Phase 7's supplement is one tight paragraph; Phase 8's spec calls for three but as a single H2 section with paragraph breaks
- The fenced JSON code block in §2 must be embedded as ` ```json ` (NOT ` ```javascript ` or ` ```text `) so syntax highlighters render it correctly
- File ends with a newline after the third paragraph

**No fix proposal (D-ATTR-10 final sentence):**
- NO code snippet for a `flattenZodError()` helper
- NO TODO scaffolding, NO `// TODO` markers
- NO timeline ("will be fixed by Phase X") — only "Follow-up for Phase 2+ (non-blocking)" as in the existing frontmatter note
- The supplement is descriptive, not prescriptive

**Single canonical home (D-ATTR-11):**
- Do NOT add similar supplement sections to `03-VERIFICATION.md`, `04-VERIFICATION.md`, or `05-VERIFICATION.md`
- Do NOT add cross-references TO this supplement from those other files
- Future readers grep for "Zod inputSchema" or "SDK 1.29 intercept" and land here once

Commit: `docs(02-verification): append MCP SDK 1.29 Zod inputSchema caveat (Phase 8 supplement) per D-ATTR-09 + D-ATTR-10 + D-ATTR-11`
  </action>
  <verify>
    <automated>grep -F "## MCP SDK 1.29 Zod inputSchema Envelope Caveat (Phase 8, 2026-04-24)" .planning/phases/02-comfyui-generation/02-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal heading `## MCP SDK 1.29 Zod inputSchema Envelope Caveat (Phase 8, 2026-04-24)`
    - File contains literal phrase `**Runtime behavior.**` (paragraph 1 marker)
    - File contains literal phrase `**Visible symptom.**` (paragraph 2 marker)
    - File contains literal phrase `**Engine-layer contrast.**` (paragraph 3 marker)
    - File contains literal string `INVALID_SHOT_FORMAT` (cited in symptom paragraph)
    - File contains literal string `structuredContent.code` is **not populated** (cited in symptom paragraph)
    - File contains literal string `src/tools/shot-tool.ts:32` (Zod regex line cited in §1; load-bearing for grep-discoverability per D-ATTR-11)
    - File contains literal string `:106-118` (handler ZodError catch + sentinel-detect-and-remap path cited in §1; load-bearing for grep-discoverability per D-ATTR-11)
    - File contains literal string `src/tools/envelope.ts:32-60` (cited in §3)
    - File contains literal string `src/store/hierarchy-repo.ts:55-63` (cited in §3)
    - File contains literal string `src/engine/pipeline.ts:19,275-284` (cited in §3)
    - File contains literal string `INSPECTOR-SMOKE.md` (citation for live evidence)
    - File still contains literal string `## Endpoint Reconciliation (Phase 7, 2026-04-24)` (Phase 7 supplement preserved)
    - File still contains literal string `_Verifier: Claude (gsd-verifier)_` (closing footer preserved)
    - File frontmatter line 1 is `---` (YAML start preserved)
    - File frontmatter line 5 contains `overrides_applied: 0` (NOT changed to 1 — D-ATTR-09 informational only)
    - File does NOT contain literal string `flattenZodError` (no fix proposal)
    - File does NOT contain literal string `// TODO` (no TODO scaffolding)
    - File does NOT contain literal string `function flatten` (no helper code)
  </acceptance_criteria>
  <done>02-VERIFICATION.md ends with the Phase 8 supplement section appended after the existing Phase 7 supplement; three paragraphs cite frontmatter notes, INSPECTOR-SMOKE.md §3 evidence, and source-tree files; no fix proposal; D-ATTR-11 single-canonical-home enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Append "Resolved by Phase 8" notes to v1.0-MILESTONE-AUDIT.md Phase 01 tech_debt items (D-ATTR-03 + D-ATTR-04)</name>
  <files>.planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v1.0-MILESTONE-AUDIT.md (lines 1-23 — frontmatter + tech_debt.phase: 01-foundation-hierarchy.items rows; 3 items in scope)
    - .planning/v1.0-MILESTONE-AUDIT.md (lines 24-34 — Phase 02 + Phase 05 tech_debt rows; UNTOUCHED — Phase 7 already handled Phase 02 in-place; Phase 8 only touches Phase 01 items)
    - .planning/phases/08-doc-attribution-backfill/08-CONTEXT.md (lines 35-43, 195 — D-ATTR-03 specification + verbatim resolution suffix template + scope discipline notes)
    - .planning/phases/08-doc-attribution-backfill/08-PATTERNS.md (lines 392-447 — D-ATTR-03 with Shape A vs Shape B options + things to preserve list)
  </read_first>
  <action>
Use the Edit tool to append a resolution suffix to each of the three Phase 01 tech_debt items in `v1.0-MILESTONE-AUDIT.md`. Use **Shape A (per-item append)** per CONTEXT line 77 — atomic resolution per item is more grep-discoverable than a closing line.

**Pre-edit state (lines 19-23):**
```yaml
tech_debt:
  - phase: 01-foundation-hierarchy
    items:
      - "Inspector UI UX smoke checks (items 1 & 2 of ROADMAP SCs) overridden — now automated via scripts/inspector-smoke.mjs (56/56 wire-level checks pass); browser-client UX layer not manually verified"
      - "Zod inputSchema errors do not flow through the typed structuredContent.code envelope — MCP SDK 1.29 intercepts them before the handler. Non-blocking follow-up for transport parity of error shape"
      - "5 requirement attributions (HIER-06, TOOL-02..05) are verified in 01-VERIFICATION.md but not attributed to any 01-XX-SUMMARY.md frontmatter — documentation-level only, not a functional gap"
```

**Post-edit state (lines 19-23 with resolution suffixes appended inline):**
```yaml
tech_debt:
  - phase: 01-foundation-hierarchy
    items:
      - "Inspector UI UX smoke checks (items 1 & 2 of ROADMAP SCs) overridden — now automated via scripts/inspector-smoke.mjs (56/56 wire-level checks pass); browser-client UX layer not manually verified. Resolved by Phase 8 (2026-04-24) — see .planning/phases/08-doc-attribution-backfill/08-VERIFICATION.md."
      - "Zod inputSchema errors do not flow through the typed structuredContent.code envelope — MCP SDK 1.29 intercepts them before the handler. Non-blocking follow-up for transport parity of error shape. Resolved by Phase 8 (2026-04-24) — see .planning/phases/08-doc-attribution-backfill/08-VERIFICATION.md."
      - "5 requirement attributions (HIER-06, TOOL-02..05) are verified in 01-VERIFICATION.md but not attributed to any 01-XX-SUMMARY.md frontmatter — documentation-level only, not a functional gap. Resolved by Phase 8 (2026-04-24) — see .planning/phases/08-doc-attribution-backfill/08-VERIFICATION.md."
```

**Verbatim resolution suffix (from CONTEXT line 195 — append to each of 3 items, after the existing closing period):**
` Resolved by Phase 8 (2026-04-24) — see .planning/phases/08-doc-attribution-backfill/08-VERIFICATION.md.`

(Note: the suffix begins with a single space, follows the existing trailing period of each item, and ends with a final period. The forward reference `08-VERIFICATION.md` is intentional — that file is created at Phase 8 close, not in this planning step. Per PATTERNS.md line 448: "The forward-reference is intentional; the link resolves once Phase 8 is verified.")

**Things to preserve:**
- The three original tech_debt strings — append-only, NO editing of historical text (CONTEXT line 153 + line 21: "Append-only; preserves audit history")
- Other phases' tech_debt entries (Phase 02 line 24-26, Phase 05 line 27-34) UNTOUCHED — Phase 8 only touches Phase 01 items
- YAML indentation (two-space indent under `items:`) preserved exactly
- Frontmatter `audited: 2026-04-23T23:00:00Z` UNCHANGED — Phase 8 does not re-audit, it resolves prior-audited items
- Frontmatter `status: tech_debt` UNCHANGED — Phase 8 closes 3 of 9 items but does not change milestone status (that's `/gsd-complete-milestone`'s job)
- Frontmatter `scores:` block UNCHANGED — no re-scoring
- The body of the audit (lines 41+) UNTOUCHED — only the frontmatter `tech_debt.phase: 01-foundation-hierarchy.items` array is modified

**D-ATTR-04 scope discipline:**
- Do NOT modify `REQUIREMENTS.md` Traceability table (lines 147-185 of REQUIREMENTS.md show "Pending" for all 38 v1 reqs) — that full sweep belongs to `/gsd-complete-milestone`, not a docs-only gap-closure phase
- Do NOT modify the audit body's `## Tech Debt` section (lines 168-189 of audit file) — it's a human-readable mirror of the YAML; updating both would be redundant and the YAML is the canonical source per CONTEXT D-ATTR-03

Commit: `docs(audit): append "Resolved by Phase 8 (2026-04-24)" notes to Phase 01 tech_debt items per D-ATTR-03`
  </action>
  <verify>
    <automated>grep -c "Resolved by Phase 8 (2026-04-24)" .planning/v1.0-MILESTONE-AUDIT.md | awk '{ if ($1 < 3) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal string `Resolved by Phase 8 (2026-04-24)` at least 3 times (one per Phase 01 tech_debt item)
    - File contains literal string `.planning/phases/08-doc-attribution-backfill/08-VERIFICATION.md` at least 3 times
    - File still contains literal string `"Inspector UI UX smoke checks (items 1 & 2 of ROADMAP SCs) overridden` (item 1 prose preserved)
    - File still contains literal string `"Zod inputSchema errors do not flow through` (item 2 prose preserved)
    - File still contains literal string `"5 requirement attributions (HIER-06, TOOL-02..05)` (item 3 prose preserved)
    - File frontmatter line still contains `status: tech_debt` (status field unchanged)
    - File frontmatter line still contains `audited: 2026-04-23T23:00:00Z` (audit timestamp unchanged)
    - File still contains literal string `- phase: 02-comfyui-generation` (Phase 02 block preserved untouched)
    - File still contains literal string `- phase: 05-web-dashboard` (Phase 05 block preserved untouched)
    - File still contains literal string `Live-smoke endpoint drift` (Phase 02 item 1 — UNTOUCHED, Phase 7 already resolved this in-place)
    - File still contains literal string `WR-04: pipeline.ts:676` (Phase 05 item 1 preserved untouched)
    - The string `Resolved by Phase 8` appears ONLY in the Phase 01 items block (NOT in Phase 02 or Phase 05 blocks) — verify by `awk` block extraction or by checking that the literal `Resolved by Phase 8 (2026-04-24)` does NOT appear within 3 lines of `Live-smoke endpoint drift` or `WR-04`
  </acceptance_criteria>
  <done>v1.0-MILESTONE-AUDIT.md Phase 01 tech_debt items each have the Phase 8 resolution suffix appended (Shape A); Phase 02 and Phase 05 items untouched; frontmatter unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning markdown surface | Append-only edits to `02-VERIFICATION.md` and `v1.0-MILESTONE-AUDIT.md`. No source code, no tests, no runtime config touched. Read-only consumers are humans, the phase-attribution test (Plan 08-01), and future audit/verification tooling. |
| External services | None. Zero env vars. Zero new SDK deps. Zero CLI/HTTP routes. |
| Forward-reference integrity | The audit notes point at `08-VERIFICATION.md` which doesn't exist yet at planning time. Per CONTEXT line 78 + PATTERNS.md line 448, this forward-reference is intentional — the file is created at phase close. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | append-only audit history | mitigate | Resolution suffixes are appended to existing tech_debt strings; original prose is preserved verbatim. The pattern matches Phase 7 prior-art (memory hygiene D-EP-15). The audit's `audited:` timestamp is unchanged so audit-trail integrity holds. |
| T-08-03-02 | Information Disclosure | supplement contents | accept | Cited file paths (`src/tools/envelope.ts:32-60`, `src/store/hierarchy-repo.ts:55-63`, etc.) are public source-tree references already visible in 01-VERIFICATION.md and other planning docs. The verbatim JSON-RPC response is from a test fixture (no real credentials). No PII surfaced. |
| T-08-03-03 | Repudiation | resolution attribution | mitigate | Each resolution suffix names the phase (8) and date (2026-04-24) and points at a specific verification doc. Triple-redundant trail (suffix + body cross-link + Phase 8 SUMMARYs that will be written at close). |
| T-08-03-04 | Spoofing | misleading "single canonical home" | mitigate | D-ATTR-11 forbids mirroring the SDK caveat into 03/04/05-VERIFICATION.md. The plan acceptance criteria explicitly check that those files are NOT modified. If a future maintainer wants to mirror, they re-open D-ATTR-11. |
| T-08-03-05 | Supply chain | new dependency intake | mitigate | Zero new dependencies. Zero `package.json` edits. Markdown-only edits. |

This phase introduces no new runtime code paths, no new dependencies, no new env vars, no new endpoints. Attack surface delta is zero.
</threat_model>

<verification>
**Plan-level verification (run after both tasks):**
1. `grep -F "## MCP SDK 1.29 Zod inputSchema Envelope Caveat (Phase 8, 2026-04-24)" .planning/phases/02-comfyui-generation/02-VERIFICATION.md` → 1 match (Task 1 done)
2. `grep -F "## Endpoint Reconciliation (Phase 7, 2026-04-24)" .planning/phases/02-comfyui-generation/02-VERIFICATION.md` → 1 match (Phase 7 supplement preserved)
3. `grep -c "Resolved by Phase 8 (2026-04-24)" .planning/v1.0-MILESTONE-AUDIT.md` → ≥3 matches (Task 2 done)
4. `grep -F "status: tech_debt" .planning/v1.0-MILESTONE-AUDIT.md` → 1 match (frontmatter status unchanged)
5. `! grep -F "flattenZodError" .planning/phases/02-comfyui-generation/02-VERIFICATION.md` → 0 matches (no fix proposal)
6. `! grep -F "Resolved by Phase 8" .planning/phases/03-provenance-versioning/03-VERIFICATION.md` → 0 matches (no mirroring; if 03-VERIFICATION.md doesn't exist, the grep simply returns 0 — defensive)
7. `! grep -F "Resolved by Phase 8" .planning/phases/04-asset-management/04-VERIFICATION.md` → 0 matches (no mirroring)
8. `! grep -F "Resolved by Phase 8" .planning/phases/05-web-dashboard/05-VERIFICATION.md` → 0 matches (no mirroring)
9. `npx vitest run` → full suite green (no test regression — phase-attribution.test.ts from Plan 08-01 still passes; no other tests touched)
10. `npx tsc --noEmit` → exit 0
</verification>

<success_criteria>
- 02-VERIFICATION.md contains Phase 8 supplement appended after Phase 7 supplement (D-ATTR-09 + D-ATTR-10)
- Supplement structured as 3 paragraphs (runtime / symptom / engine-contrast) with verbatim JSON-RPC repro embedded as fenced code block (D-ATTR-10)
- No fix proposal, no flattenZodError() helper, no TODO scaffolding (D-ATTR-10 final sentence)
- Single canonical home: zero mirroring into 03/04/05-VERIFICATION.md (D-ATTR-11)
- v1.0-MILESTONE-AUDIT.md Phase 01 tech_debt items each have "Resolved by Phase 8 (2026-04-24)" suffix (D-ATTR-03; Shape A)
- Phase 02 + Phase 05 tech_debt blocks untouched; frontmatter status: tech_debt unchanged (D-ATTR-04 scope discipline)
- SC-3 from ROADMAP closed: "A Phase 2+ follow-up note captures the Zod inputSchema → structuredContent.code intercept behavior (MCP SDK 1.29) so the divergence is easy to find later"
- All file edits preserve surrounding content; zero unrelated changes
</success_criteria>

<output>
After completion, create `.planning/phases/08-doc-attribution-backfill/08-03-SUMMARY.md` with:
- Frontmatter: `phase`, `plan: 03`, `requirements-completed: []` (gap-closure phase, no REQ-IDs to bind), `tags: [verification, supplement, mcp-sdk-1.29, zod, envelope, audit, resolution-note, docs]`
- Body: What landed (2 doc edits — supplement + audit notes), tasks executed, deviations, file diff summary
- Reference: D-ATTR-03, D-ATTR-04, D-ATTR-09, D-ATTR-10, D-ATTR-11
</output>
</output>
