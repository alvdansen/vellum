---
milestone: v1.1
milestone_name: Provenance Verification (C2PA)
audited: 2026-04-30
status: passed
scores:
  requirements: 10/10
  phases: 7/7
  integration: 18/18
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 10
    items:
      - "4 v1.1-audit ROADMAP-shape failures (phase-attribution.test.ts ×2 + validation-flags.test.ts ×2) — pre-date Phase 10 baseline (commit 04d5f60), v1.0-shaped audit tests vs v1.1 ROADMAP layout"
      - "Phase 10 deferred-items.md originally listed 5 such failures; the 5th closed naturally as phases filled SUMMARYs — root suite now reports 4 failures, not 5"
  - phase: 14
    items:
      - "PROV-V-05 partial — TIFF native-embed shipped (better than spec); EXR/PSD cryptographic sidecar deferred to v1.2 pending c2pa-node sidecar API"
      - "Sidecar HTTP route + dashboard download link deferred to v1.2 (no underlying mechanism yet)"
      - "HSM/Yubikey signing, multi-CA federated trust, streaming-friendly C2PA — all deferred to v1.2"
  - phase: 15
    items:
      - "Production cloud-mode componentOf hashes surface as vfx_familiar.unavailable_ingredient (control-image bytes not reachable from local outputRoot in cloud-only deploys); REVISION C3 fetch-from-cloud deferred to v1.2"
      - "IPAdapter pack node-variants (~12 forms) NOT in IMAGE_INPUT_CLASS_TYPES; v1.1 ships LoadImage/LoadImageMask/VAEEncode/VAEEncodeForInpaint/ControlNetApply/ControlNetApplyAdvanced (6 canonical core nodes); v1.2 audit deferred"
      - "Conditioning-graph traversal (ConditioningCombine/Concat) not followed by extractInputAssertion; direct CLIPTextEncode ancestors only in v1.1"
      - "VAEEncode upstream walks one-hop only (LoadImage → ImageScale → VAEEncode skips ImageScale); multi-hop deferred"
      - "T-15-07 disk I/O on every child sign for parent ingredient bytes; LRU cache deferred to v1.2"
      - "T-15-03 stale parent manifest_sha256 accepted; v1.2 export_manifest will re-derive on demand"
  - phase: 16
    items:
      - "deferred-ingredient-mirror — redacted manifest's active_manifest.ingredients[] carries only c2pa-rs's auto-promoted parent_relationship; full Phase-15 component graph NOT re-threaded through buildResult.ingredientSpecs (parent chain still verifiable via store.manifests JUMBF traversal); v1.2 will mirror"
      - "shared wire-UAT test util refactor (~600 lines duplicated across 5 test files; intentional for test isolation; v1.2 may consolidate)"
      - "redaction-of-redaction (multi-step) — engine supports redact-from-latest implicitly; tool layer doesn't surface from_original; full audit trail recoverable from SQLite"
      - "BUFFER_SIGNING_MAX_BYTES (500 MB) is enforced in pipeline.ts:1235 for Engine.signOutput but NOT in src/engine/c2pa/redaction.ts (redact path lacks symmetric size guard); not blocking ship — signed assets bounded at first sign — but flagged for v1.2 defense-in-depth"
---

# Milestone v1.1 — Provenance Verification (C2PA) — Audit Report

**Status:** ✅ PASSED — Cleared for milestone close.

**Audited:** 2026-04-30
**Auditor:** gsd-integration-checker (cross-phase wiring) + gsd-verifier (Phase 16 goal-backward)
**Driver:** EU AI Act Article 50 (effective Aug 2026), California SB 942 (effective Jan 2026), SEED-001

## Executive Summary

All 10 v1.1 requirements satisfied. All 7 phases verified individually. All 18 cross-phase integration wiring checks confirmed in source. All 5 E2E flows traced and complete. Test count delta exactly matches mandate: **760 (v1.0 close) → 1365 (post-Phase-16) = +605 net new tests**.

The architecture-purity invariants hold:
- c2pa-node imports restricted to `src/engine/c2pa/{signer,verifier,redaction}.ts` (3 actual importers in 4-element allowed-set including reserved exporter slot)
- `src/tools/version-tool.ts` zero c2pa-node imports
- Provenance table zero UPDATE/DELETE statements (append-only locked)
- Tool count stays at 7 of 12 cap
- Version action count grew 4 → 7 cleanly via discriminated union extension

## 1. Requirements Coverage (10/10)

| REQ-ID | Phase | Status | Verification Evidence |
|--------|-------|--------|-----------------------|
| **PROV-V-01** signed C2PA manifests embedded in PNG/JPEG/MP4/WebP/TIFF | 14 | ✅ | `c2pa-verification.test.ts` 17/17 across 5 formats; `c2pa-dual-transport-parity.test.ts` 9/9 byte-equal; Concern #8 `c2pa.hash.data` cryptographic-binding closed |
| **PROV-V-02** AI-origin disclosure (`c2pa.created` + ComfyUI softwareAgent + trainedAlgorithmicMedia) | 14 | ✅ | manifest-builder.ts:108 hardcodes `softwareAgent.name='ComfyUI'`; line 86 hardcodes digitalSourceType; Tests 11-13 lock contract |
| **PROV-V-03** SHA-256 fingerprints for every model in resolved prompt | 13 | ✅ | `model-fingerprint.ts` streams SHA-256; `getLatestFingerprints` flows into `derivePrimaryModel` (pipeline.ts:1190) → manifest builder; closes `model_hash: null` gap at provenance.ts:69 |
| **PROV-V-04** ingredient graph (`parentOf` + `componentOf` + vfx_familiar.input) | 15 | ✅ | `extractParentIngredient` + `extractComponentIngredients` + `extractInputAssertion`; `c2pa-ingredient-graph-e2e.test.ts` v3→v2→v1 traceback; dangling refs → `vfx_familiar.unavailable_ingredient` |
| **PROV-V-05** sidecar `.c2pa` for non-embed formats | 14 | ⚠ PARTIAL | TIFF promoted to native-embed (BETTER than spec); EXR/PSD deferred to v1.2 — c2pa-node v0.5.26 has no sidecar API; explicit scope reduction approved in REQUIREMENTS.md |
| **PROV-V-06** redaction primitive with `vfx_familiar.redacted` assertion | 16 | ✅ | `redaction.ts` (805 lines) bounded-resolver DSL + `applyRedactionPolicy` + integration helper re-signs with Phase 14 cert; original byte-identical (4-layer verified) |
| **PROV-V-07** `version.export_manifest` + `version.verify_manifest` (+ `redact_manifest`) | 16 | ✅ | exporter.ts + verifier.ts engine modules; D-PROV-08 envelope; 5-branch `signature_status`; tool count 7/12, version actions 4 → 7 |
| **DEMO-01** migrate-on-boot or refuse with `MIGRATION_PENDING` | 10 | ✅ | `runMigrations()` at store/migrate.ts:96-123; `openDb()` at store/db.ts:73 invokes BEFORE Engine construction at server.ts:232 |
| **DEMO-02** recovery poller surfaces ComfyUI Cloud `node_errors` | 11 | ✅ | `flattenComfyError` (format.ts:145) is single source of truth; both submit (client.ts:436) and recovery (generation.ts:214) delegate; 14/14 parity tests |
| **DEMO-03** divergence pill on reproduce-lineage with SHA-256 mismatch | 12 | ✅ | `reproduction_divergence` field on DiffResponse; `WarningPill` + side-by-side comparison in VersionDrawer; all automated checks green |

## 2. Phase Verification Summary (7/7)

| Phase | Plans | Status | Test Count | Score | Highlights |
|-------|-------|--------|------------|-------|------------|
| 10 Migrate-on-boot Hardening | 3/3 | passed | 766 (+10) | 4/4 | Boot bails before Engine via openDb→runMigrations→TypedError |
| 11 Recovery Poller Error Detail | 2/2 | passed | 797 (+31) | 4/4 | 14-case parity test across helper/submit/status arms |
| 12 Reproduce Divergence Transparency | 2/2 | human_needed* | 817 (+20) | 4/4 | All automated checks pass; visual/a11y smoke documented |
| 13 Model Fingerprinting | 3/3 | passed | 869 (+52) | 8/8 | Append-only via sibling `models_fingerprinted` event |
| 14 C2PA Signed Manifest Emission | 5/5 | passed | 1038 (+169) | 5/5 | 5 formats sign+verify; key-leak negatives across 5 channels |
| 15 Ingredient Graph | 4/4 | passed | 1175 (+137) | 5/5 | Per-version sign mutex; KSampler edge walk for inputTo |
| 16 Redaction & Agent Surface | 5/5 | passed | 1365 (+190) | 13/13 | Unified `assetWriterMutex`; 4-layer append-only verification; multi-encoding leak scan |

\* Phase 12 status `human_needed` is not a gap — automated coverage is complete; only visual/a11y verification was left for human eyes.

## 3. Cross-Phase Integration Findings (18/18 WIRED)

### Phase 10 → Phase 14 (migration boot path)
- ✅ `openDb()` runs `runMigrations()` BEFORE Engine construction
- ✅ Migration 0006 (`manifest_signed_json`) auto-applied; journal idx=6 in `_journal.json`
- ✅ Migration 0005 (Phase 12 `reproduction_warnings_json`) also applied
- ✅ Stale-DB boot fails before any provenance write — `engineConstructorSpy.not.toHaveBeenCalled()`

### Phase 11 → All later phases (single error helper)
- ✅ `flattenComfyError` is single source — only 2 production callers (submit + recovery)
- ✅ No drift: `error-extraction-parity.test.ts` 14/14 byte-equal
- ✅ Phase 14 cert-config + Phase 16 redact failures use `TypedError` codes through same envelope

### Phase 12 → Phase 14 (divergence + signing coexistence)
- ✅ Reproduce-lineage versions still get C2PA signed; signing path zero conditional check on lineage_type or warnings
- ✅ `reproduction_divergence` field flows through tool envelope independently of signing

### Phase 13 → Phase 14 + Phase 15 (model fingerprints)
- ✅ `getLatestFingerprints` flows into Phase 14 manifest as primaryModel (pipeline.ts:1189)
- ✅ Phase 15 ingredient graph and Phase 13 fingerprints are disjoint (no double-counting); IMAGE_INPUT_CLASS_TYPES disjoint from LOADER_CLASS_TYPES
- ✅ `appendModelsFingerprintedEvent` is append-only sibling event

### Phase 14 → Phase 16 (signer reuse + unified mutex)
- ✅ Phase 16 redaction calls `signEmbedBufferWithIngredients` / `signEmbedFileWithIngredients` verbatim
- ✅ Unified `assetWriterMutex` (FIFO) + `signMutex` (coalescing) layered correctly
- ✅ `BUFFER_SIGNING_MAX_BYTES` enforced on signOutput (pipeline.ts:1235)
- ⚠ `BUFFER_SIGNING_MAX_BYTES` NOT enforced on redaction path (redaction.ts:628 reads via `readFile` without size guard); flagged for v1.2 defense-in-depth — not blocking ship since signed assets are bounded at first sign

### Phase 15 → Phase 16 (manifest read shape compatibility)
- ✅ `getLatestManifestSignedEvent` shape consumed identically by Phase 16 readers
- ⚠ Redacted manifest preserves parent chain via JUMBF only; full Phase-15 component graph NOT re-threaded — documented as `deferred-ingredient-mirror` for v1.2

### Phase 16 internal cohesion
- ✅ 3 new tool actions delegate to engine facade
- ✅ Tool count stays at 7 of 12 cap (live `tools/list` confirms)
- ✅ Version action count grows 4 → 7 (discriminated union extension)
- ✅ c2pa-node imports limited to `src/engine/c2pa/{signer,verifier,redaction}.ts`
- ✅ Append-only invariant: original manifest_signed event byte-identical post-redact (UAT Test 12 SQLite full-row equality)

## 4. E2E Flow Status (5/5 COMPLETE)

| # | Flow | Status | Evidence |
|---|------|--------|----------|
| 1 | New version → signed manifest → export → verify | ✅ | `c2pa-verification.test.ts` 17/17; `c2pa-redaction-uat-mcp-tool.test.ts` Test 4; `verify-phase16-uat.mts` 5/5 against fresh server |
| 2 | Reproduce + divergence warning + signed manifest | ✅ | Phase 12 verification all 4 ROADMAP success criteria; signing path independent of divergence (verified by reading pipeline.ts:1080) |
| 3 | Redact sensitive prompt → verify redacted manifest | ✅ | `c2pa-redaction-e2e.test.ts` (10 tests / Scenarios A/B/C); multi-encoding scan UTF-8/UTF-16LE/UTF-16BE/base64 + emoji clean at 3 layers |
| 4 | Ingredient graph traceback v1 → v2 → v3 | ✅ | `c2pa-ingredient-graph-e2e.test.ts` (509 lines, 9 tests); independent `createC2pa()` instance — gold-standard verifier proof |
| 5 | Migration on boot → server starts | ✅ | `migrate-stale-db.test.ts` 7 assertions across 3 describe blocks; `migrate-no-op.test.ts` 4 assertions for clean-DB no-op |

## 5. Pre-existing Failures (NOT regressions)

| File | Failing Test | Root Cause |
|------|--------------|------------|
| `phase-attribution.test.ts` | `parses ROADMAP.md and finds at least 9 phase blocks` | v1.1 ROADMAP starts at Phase 10; v1.0 audit heuristic expects ≥9 |
| `phase-attribution.test.ts` | `every non-skipped phase declares at least one REQ-ID in ROADMAP` | v1.1 REQ-ID format (PROV-V-NN, DEMO-NN) differs from v1.0 expectation |
| `validation-flags.test.ts` | `parses ROADMAP.md body progress table and finds at least 9 phases` | Same ROADMAP-shape mismatch |
| `validation-flags.test.ts` | `detects [GAP CLOSURE] phases (6, 7, 8, 9) from ROADMAP top-level checklist` | v1.1 ROADMAP has no GAP CLOSURE checklist (v1.0-only construct) |

**Origin:** commit `04d5f60` (v1.1 ROADMAP landing), pre-Phase-10. Tracked at `.planning/phases/10-migrate-on-boot-hardening/deferred-items.md`. Out of milestone scope by definition.

**IT-20 timing flake** in generation-tool.test.ts is a tmpdir-cleanup race; passes in isolation; pre-existing per Phase 10 deferred-items.md. NOT seen in audit run.

## 6. Final Test Suite Snapshot

```
Test Files: 82 passed | 2 failed | 2 skipped (86 total)
Tests:      1365 passed | 4 failed | 3 skipped (1372 total)
Architecture-purity: 35/35 green
TypeScript: tsc --noEmit clean
Dashboard: 88/88 (separate; unchanged)
```

## 7. Verdict + Recommendation

**Milestone v1.1 (Provenance Verification — C2PA) cleared for ship.**

All audit gates green. v1.2 candidates captured in REQUIREMENTS.md "Deferred to v1.2" + per-phase deferred-items.md files for: cryptographic sidecar (EXR/PSD), HSM signing, multi-CA federated trust, IPAdapter pack audit, deferred-ingredient-mirror, multi-step redaction tool surface, parent-bytes LRU cache, redaction path size-guard symmetry.

**Adversarial review effectiveness (Phases 14/15/16 user mandate):**
- Phase 14: 11 issues caught at plan stage; 3 of blocker class (algorithm hardcoding, sidecar derivation, key-bytes residency) — all closed
- Phase 15: FATAL plan flaw caught (ingredients-in-assertions vs ingredients-as-separate-array); user selected heavy rewrite (Path A); plans corrected before execute
- Phase 16: 5 BLOCKERS + 6 CONCERNS caught (multi-encoding leak, append-only race, ManifestAssertion union shape, mutex semantics + atomic disk write, dev-cert verifier behavior); all 11 revised before execute, all 11 confirmed implemented in 16-VERIFICATION.md

**The gate works.** Crypto-correctness errors are caught at planning, not at run-time.

---

*Milestone v1.1 SHIPPED 2026-04-30. Audit complete 2026-04-30. Cleared for `/gsd-complete-milestone v1.1`.*
