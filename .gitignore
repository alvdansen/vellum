node_modules/
dist/
# Phase 5 (WEBUI-05): dashboard dist/ is a committed build artifact — server serves it at runtime, no build step required at install
!packages/dashboard/dist/
!packages/dashboard/dist/**
coverage/
*.db
*.db-wal
*.db-shm
*.log
.DS_Store
.env
.env.local
# Phase 5 (D-WEBUI-26): runtime ComfyUI output downloads — never committed
outputs/
# Phase 5: dashboard workspace node_modules (defensive — workspaces hoist to root, but cover both)
packages/dashboard/node_modules/
# Demo runner state — captures workspace/version ids between stage runs (local-only)
.demo-state.json
# Phase 14 (PROV-V-01): self-signed dev C2PA cert + key — generated by
# scripts/gen-dev-c2pa-cert.mts; NEVER commit. Production keys come from
# operator-managed paths (VFX_FAMILIAR_C2PA_CERT_PEM_PATH).
.c2pa-dev/
# Phase 14 Plan 14-02: per-test cert fixtures generated lazily by
# src/engine/c2pa/__tests__/signer.test.ts via openssl shell-out. Each
# fixture is a one-shot disposable cert + key — never committed.
tests/fixtures/c2pa/algorithms/
